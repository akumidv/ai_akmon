# D2 ledger — owner-verification points

> One entry per owner-verify point (guardrails/_common.md "Owner-verify any change to
> math, data shape, or architecture"). Pending entries await owner acceptance; Approved entries
> have that acceptance but no landing commit; Verified entries name the landed sha. Task lifecycle
> status is separate. No dates — the landing commit is the timeline (D5: the tool never runs git;
> the owner supplies the sha).

## Pending

> D2-23 owns the measured runtime evidence required only by C52 and remains awaiting owner
> action. D2-20 is Verified below.

| id | kind | what | anchor | draft | second_opinion |
|----|------|------|--------|-------|----------------|


| D2-23 | architecture | C52 runtime-envelope evidence lock. Verify: terminal F4 support or omission for every Codex entry with exact vendor/version/event/matcher evidence; F6 cap, unit, source and generated-entry mapping for every growing axis plus the combined supported corpus; then F5 entry-to-class mapping, timings, formula, literal and unit including aggregate event groups. Any unmeasured or unmapped entry keeps C52 blocked. This evidence populates the stable D2-20 protocol and does not block other stage-1 tasks. | meta/design/stage1-hardening-contracts.md §2 + meta/reviews/n5-f6-input-envelope-20260825.md (F6) + meta/reviews/n1-f4-codex-timeout-20260825.md (F4) + the N6 (F5) artifact |  |  |



## Approved

> Owner accepted the exact row, but no landing commit exists yet. Approved rows are clean for the
> landing commit; move them to Verified afterward with the landed sha.

| id | kind | what | anchor | draft | second_opinion |
|----|------|------|--------|-------|----------------|
| D2-41 | architecture | **C79 — skills reach every harness's selector: the stub carries the source frontmatter, the contract is the Agent Skills format, a description states the result, then the trigger.** Measured before deciding: `bin/sync.py` wrote each skill's `.claude/skills/<name>/SKILL.md` as a pointer without frontmatter and gave Codex no skills directory; Claude Code 2.1.270 lists such a stub under its bare name — all 7 in alphavar (M54) — and codex-cli 0.154.0 refuses to load one (M55), so no description, akmon's or a consumer's, reached a selector. The vendor guidance was read from each vendor's own documentation — Claude Code, Anthropic's authoring guide, Codex and its shipped `skill-creator`, the Agent Skills standard, Copilot in VS Code, Gemini CLI, Cursor — and is summarized with links in [the C79 review](reviews/c79-skill-guidance-20260913.md). **Choice (owner, this session):** (1) **Delivery.** `sync` writes each skill's stub into `.claude/skills/<name>/` and `.agents/skills/<name>/` (Codex; also read by Copilot, Gemini CLI and Cursor): the source's frontmatter copied verbatim, the generated banner as a YAML comment on its line 2, then the pointer body — for every skill `sync` finds, a project's own and future ones included; a source without frontmatter keeps the bare pointer, and `verify` reports that source. (2) **The trigger in `description`.** Only Claude Code reads `when_to_use`; every other host selects from `description` alone (M55), so the trigger goes in `description` and `when_to_use` is no longer required — the shipped skills drop it, which also ends the trigger Claude Code listed twice. (3) **`owner` under `metadata`.** The standard admits custom keys only under `metadata`; the contract requires `metadata.owner`. (4) **Text.** A description states in prose the expected result, then the trigger, within 1,024 characters, per the vendors' guidance; `release` and `stats-digest` are rewritten so. Result-first is akmon's own criterion: no vendor asks for it, all ask for "what it does, then when", and the result is that "what". (5) **Carrier.** `verify` checks the keys and the standard's limits — `name` of 1–64 lowercase letters, digits and single inner hyphens, matching its directory; `description` of 1–1,024 characters; `metadata.owner` present; no unquoted value containing ` #` or `: ` (`skills.frontmatter-yaml`) — Claude Code and Codex both cut a value at an unquoted ` #` without a word, and an unquoted `: ` loads on both but fails the standard's own validator (M56, M57); whether a description names its result and its trigger is judged on review. **Costs:** a breaking change for consumers — a skill with a top-level `owner` fails `verify` (`skills.required-fields`) until the key moves under `metadata:`, the alphavar pilot's five own skills among them; the first `sync` after the bump rewrites every stub and adds `.agents/skills/`; the frontmatter lives in the source and, generated, in two stubs, held together by `sync --check`; `verify` still reads frontmatter line by line, so YAML shapes beyond the two it flags are left to review. **Rejected:** a symlinked skill folder (both harnesses follow one, but in mode `package` the target is inside the venv, the path the pointer exists to avoid); keeping `when_to_use` beside the trigger (listed twice on Claude Code, ignored elsewhere); a per-model text for gpt-6-astra or Fable (Anthropic asks for testing on every model in use, not a text per model); a mechanical check that a description names a result (not decidable by a rule). **Carriers:** meta/tests/test_sync.py — `test_skill_stub_carries_the_source_frontmatter_in_both_harness_dirs`, `test_skill_stub_without_source_frontmatter_stays_a_bare_pointer`, `test_skill_stub_outside_the_repository_points_through_akmon_path`, `test_skill_frontmatter_block`, `test_apply_write_deletes_obsolete_generated_agents_skill_stub`; meta/tests/test_verify.py — `test_skill_contract_accepts_the_fixture_without_when_to_use`, `test_skill_owner_at_the_top_level_is_not_metadata_owner`, `test_skill_name_outside_the_standard_is_error`, `test_skill_name_of_64_characters_is_accepted`, `test_skill_description_limit`, `test_agents_md_should_not_link_generated_agents_skill_stubs`, `test_obsolete_generated_agents_skill_stub_is_error`, `test_skill_frontmatter_yaml_hazards`, `test_skill_frontmatter_yaml_hazard_in_a_nested_value`. A mutation probe turned each of 18 red: stubs only under `.claude/skills`, the frontmatter dropped, the banner moved after the frontmatter, the obsolete sweep or the pruning blind to `.agents/skills`, an unclosed frontmatter accepted, a package-mode source linked by a relative path, `owner` required at the top level, `when_to_use` still required, nested `metadata` unparsed, the name limit at 65, edge or double hyphens allowed, the description limit at 1,025, an AGENTS.md link to `.agents/skills` unwarned, an unquoted ` #`, a leading `#` or an unquoted `: ` let through, a quoted value flagged. Verify: (a) the delivery in (1), the YAML-comment banner included; (b) the trigger in `description` and `when_to_use` dropped (2); (c) `metadata.owner` (3); (d) the two rewritten descriptions (4); (e) the `verify` limits and the unquoted-YAML check, with "names a result" left to review (5); (f) the costs above, the consumer break first. | bin/sync.py::_skill_stubs/_skill_frontmatter_block/_obsolete_generated_files + bin/verify.py::_SKILL_REQUIRED_FRONTMATTER/check_skill_contract + skills/release/SKILL.md + skills/stats-digest/SKILL.md + meta/TASKS.md (C79) |  |  |
| D2-39 | architecture | C75 one-owner-per-.akmon.toml-reader fold. Three of the five narrow readers D2-26 counted now go through common/record.py: tools/release/release_check.py (its "does not import sync" premise had been stale since C74), tools/model_routing/init.py::_standard_tree_root (its materialized-copy premise went with C74/C77), and src/akmon/cli.py::_mounted_akmon_root (asks the embedded tree's copy, not the consumer's — the dependency it avoids is the consumer's tree). Two callers the original count missed now read through it too: src/akmon/_init.py::_recorded_mount, which borrowed the CLI's private reader (through the same embedded module), and hooks/hook_core.py::d2_sensitive_paths, which had kept its own bare tomllib call. Verify: (1) tools/d2_ledger/d2_ledger.py::_read_akmon_toml stays local and lenient-by-contract is the wrong default for it — it reads the sensitive_paths array and must fail rather than silently switch the D2 path check off on a malformed record; folding it into the shared lenient reader was rejected on that basis, not omitted by oversight. (2) common/record.py's shared reader's malformed-file contract — falls through to the stdlib line parser rather than returning {} or raising — is the right behaviour for every caller now routed through it — the three folded modules and the two missed callers. For hooks/hook_core.py::d2_sensitive_paths that means a malformed record yields no globs, so the PreToolUse D2 reminder goes quiet — measured, and not a change: its own reader already caught the parse error into [] — while `d2_ledger check` still fails loudly on the same record. **Carriers:** meta/tests/test_d2_ledger.py::test_read_akmon_toml_raises_tomldecodeerror_on_a_malformed_file (1); meta/tests/test_package_mode.py::test_a_malformed_record_falls_through_to_the_lenient_parser_rather_than_raising (2); meta/tests/test_hook_core.py::test_d2_sensitive_paths_is_empty_on_a_malformed_record (2, the quiet hook); meta/tests/test_cli.py::test_mounted_akmon_root_none_when_akmon_toml_says_package and its stale-mount-dir variant (the CLI through the embedded reader); meta/tests/test_record_owner.py (C83 — no function outside the listed three parses TOML, and no module outside the listed ones names the record's path). | common/record.py + hooks/hook_core.py::d2_sensitive_paths + meta/design/packaging/mount-resolution-owner.md (C75) + tools/d2_ledger/d2_ledger.py::_read_akmon_toml (kept local) |  |  |
| D2-40 | architecture | C70 implementation gate — the choices D2-27 and the TASKS.md C70 lock did not spell out, settled over four review rounds; landed in `898d8a2`, the fourth-round fixes in `766cdce`, owner-approved; the move to Verified with the landing sha is the owner's. (1) **Freshness gate before any parse.** `.codex/hooks.json` must be byte-identical to `bin/sync.py::_codex_hooks_text` (the one spelling `_planned_files` also writes), so a reformatted but equal file is stale exactly as `sync --check` counts it. Missing, unreadable, invalid, non-object, unknown-event and stale wiring make no host call and add no `codex.host-trust` finding of their own: the single `pointers.generated-freshness` error already reports them (the earlier duplicate `codex.host-trust` error for unreadable JSON is dropped). (2) **Strict protocol.** The matching project entry must carry `hooks`, `warnings` and `errors` as lists; non-empty `errors` is uninspectable; every `hooks` element, not only the expected ones, must have the measured HookMetadata shape — `enabled` a boolean, `trustStatus` one of the four schema values, a string `handlerType`, and for command handlers string `eventName`/`command` and a string-or-null `matcher`. Anything else is one warn and strict exit 1. Cost accepted: a future Codex that adds a fifth trust status or changes a field type warns until akmon maps it. Entries for other roots are ignored unvalidated. Exactly one `data` entry may answer the requested cwd: none, or two and more (identical ones included), is uninspectable — never the first, never a merge; the fourth review measured `[trusted, empty]` green and the reverse `missing`, and merging would be a delivery-semantics choice left to the owner. (3) **Ambiguity by delivery category.** Duplicate identities agree when they land in the same category (`trusted` beside `managed` is live), otherwise `ambiguous`, independent of order. (4) **No-leak boundary by construction.** Every diagnostic is a fixed category — problem vocabulary missing/disabled/untrusted/modified/unrecognized/ambiguous; a protocol failure is one text from the closed `common/codex_hooks.py::_PROTOCOL_FAILURES` table, because `CodexProtocolError` is built from a kind rather than a message, anything outside the table renders `unclassified`, and `query_hooks_list` rebuilds a runner's own `CodexProtocolError` from its kind alone (the fourth review saw a runner's `CodexProtocolError(<text>)` printed verbatim); any other runner exception is the fixed `runner-failed`. No response id, error code or message, trust value, cwd, exception text or exception type name reaches the output. (5) **No off switch.** The interim `AKMON_SKIP_CODEX_HOST_TRUST` bypass is removed rather than legitimized, so D2-27, TASKS.md C70 and design §7 stay unamended; akmon's self-CI legs and test suite hide the binary from PATH (`meta/self_ci.py::path_without`) and take the check's own absent-install skip. (6) **Pinned constants.** Query timeout 5.0 s; cleanup waits at most 2.0 s after terminate and 2.0 s after kill, then abandons the child; JSON-RPC ids 1/2 with the id echo required; code `codex.host-trust`; responses read by a thread plus queue, not select() on a buffered pipe. Each literal has a carrier that goes red when it changes: the runner seam receives `(root, 5.0)` through `Verifier.run()`, cleanup records exactly terminate, wait 2.0, kill, wait 2.0, and the stand-in `codex` logs ids 1 and 2. (7) **One owner, one caller.** A static carrier fails if anything shipped other than `bin/verify.py`, `common/codex_hooks.py` and `common/runtime.py` touches the route, or if the `app-server` argv is spelled outside `common/runtime.py` — the no-C59-side-query guard. (8) **Current host state, never cached.** Every check spawns a fresh query; a carrier flips the stand-in host green, inert, green, inert across runs, on fresh and on reused Verifiers, and requires each run to report the state current at that run. Verify: (a) byte-identical rather than parsed-equal as the freshness definition; (b) whole-list validation and its fifth-status cost; (c) removing the bypass in favour of PATH hiding in self-CI and tests only; (d) the constants in (6); (e) rejecting rather than merging a second answer for the project cwd; (f) the closed diagnostic table, which drops exception type names and the timeout value from protocol diagnostics. | common/codex_hooks.py + bin/verify.py::check_codex_host_trust + bin/sync.py::_codex_hooks_text + common/runtime.py::codex_hooks_list_command + meta/self_ci.py::path_without + meta/tests/test_codex_hooks.py + meta/tests/test_verify.py (C70) |  |  |
| D2-42 | architecture | **C80 — context pressure as a one-task context budget: info at 0.85, warn at 1.0, an env override.** First committed in `d0ed2b1` ahead of approval, then reworked in the working tree on the owner's review — two named levels replace the band list — and owner-approved as reworked. Amends the verified D2-38: its two bands (plan, then compact now) become the two levels in (3), its "compaction reset" becomes the episode reset, `warn_ratios` becomes `info_ratio`, and its display `… 200k max` reads `… 200k budget`; its basis — a share of a recommended maximum, not of the model's limit — is unchanged, and this row supersedes D2-38's bands (C23 says so). **Choice (owner, this session):** (1) Meaning: `recommended_max` is the recommended active-context budget for a session intended to carry one task — a session carries the task it is solving, not its history (fewer tokens, less drift from the task, less forgetting); the reminder says so. The hook knows neither which task a session carries nor what that task needs; it sees only the prompt fill. C80 is that pressure sensor and nothing more: the boundary between tasks is A21's, the Codex port C81's, and keeping large raw results out of the main context C29's. (2) `recommended_max` stays 200000 — a configurable owner policy, checked against measurement: it sits above where sessions get compacted in practice (the owner's manual Claude compactions at a median of 167,968; Codex main sessions at medians of 174,193 on gpt-5.5 and 123,180 on gpt-5.6-sol) and 58,400 tokens under the 258,400-token window Codex reports for every OpenAI model in its catalog, gpt-6-astra included (272,000 × 95%). The measurements bound the number; none measures quality. (3) Two levels. **Info** at `info_ratio` = 0.85: `ℹ context pressure: ~86% of the recommended 200k budget — /compact`. **Warn** at the budget itself, 1.0, fixed — reaching the budget is what the budget means, so moving it means moving `recommended_max`: `⚠ context pressure: ~100% of the recommended 200k budget — /compact; new task: /new`. Only the essence: the share and the command the user types — no token count, no advice addressed to the agent. Each level speaks once per pressure episode — warn also when the fill jumps straight past 100% — and nothing repeats past the budget: the model may allow more, and whether to go on, compact or start over is the developer's call. A fill below the info level ends the episode and resets the throttle, whatever lowered it. An `info_ratio` that is not a number strictly between 0 and 1 leaves only the warn level. **Delivery: the owner only** — `systemMessage`, at SessionStart too, without `additionalContext` — revising ADR 0006 decision 4 for this one notice: the model has nothing to act on, and the line would spend the context it warns about. Marked like the hook's other notices: `⚠` for the warning, `ℹ` for the info. **Commands:** `/compact` and `/new` exist under those names in Claude Code 2.1.270 (`/new` is an alias of `/clear`) and in codex-cli 0.154.0, so C81 can carry the texts over; the Codex side is read from its binary and docs, not yet run live. (4) ✅ owner-confirmed: `AKMON_CONTEXT_RECOMMENDED_MAX` — a positive integer overrides the budget for every model, ahead of `recommended_max_by_alias` and `recommended_max`; any other value is ignored. Per user: the shell or `env` in ~/.claude/settings.json; per project: `env` in .claude/settings.json. A whole-thousand budget shows as `200k`, any other value exactly (`1500`). (5) No technical limit is stored beside it. **Costs:** the registry hash moves, so every local routing config goes stale once and re-inits (the same Unreleased bump as D2-38); an overlay still carrying `warn_ratios` is ignored, like D2-38's legacy keys (alphavar has no `context_pressure` overlay); an env override lives outside the committed registry, so two developers on one project can see different levels; project-scope `env` reaching the hook rests on Claude Code's settings documentation, not a probe; the model no longer sees the reminder, so it will not raise the pressure on its own — acting is the owner's. **Rejected:** an info level at 0.67 (85 reminders against 46 on the same replay snapshot, 10 of 13 sessions) and a 150k budget (75) — both fire well before the owner's own median manual compaction; per-model entries from fill differences (a session's peak follows its length and its compactions, not the model); storing the model's limit (Models API: network, an API key and a cache; status-line bridge: couples the hook to personal configuration); repeating the warning past 100%, or any level above it (the nag §12.4 rejects); a free-form band list (it let an overlay stack bands past 100% or keep its top band under it, each case needing a rule of its own); promising that `/compact` keeps only the current task; advice addressed to the agent inside the reminder ("write decisions and task state to TASKS.md"), the token count and explanations of harness commands — the owner reads a fact and a command, and the model has no use for the line. **Carriers** (meta/tests/test_model_routing.py): `test_the_shipped_pressure_policy`; `test_context_pressure_levels_start_exactly_at_their_shares` (169,999 silent, 170,000 and 199,999 `ℹ`, 200,000 `⚠`); `test_context_pressure_levels_throttle_and_reset` (both texts, 95% silent between the levels, 130% silent after warn, a reset re-arms); `test_the_budget_warns_once_per_episode_also_on_a_jump`; `test_context_pressure_is_a_share_of_the_recommended_max_not_the_model_limit` (a first observation at 206% gets the warn); `test_without_a_valid_info_ratio_only_the_budget_warns` (absent, null, a bool, 0, 1.0, a string); `test_the_legacy_window_keys_are_ignored` (`warn_ratios` included); `test_the_recommended_max_env_overrides_the_registry_and_every_alias`; `test_a_recommended_max_env_that_is_not_a_positive_integer_is_ignored`; `test_the_recommended_max_is_shown_exactly_unless_a_whole_thousand`; `test_hook_user_prompt_submit_pressure_notice_reaches_the_owner_only`; `test_hook_session_start_shows_the_info_level_to_the_owner_only`. A mutation probe turned each of 17 red: info level moved to 0.9, env unread, env behind the alias, zero accepted, env renamed, the warn text without `/new`, warn repeating past the budget, warn only above 100%, the budget always in thousands, a silent first observation past 100%, the token count back in the text, `info_ratio` unvalidated, info marked `⚠`, SessionStart hiding the info level from the owner, `warn_ratios` standing in for `info_ratio`, the reminder back in the model's context at SessionStart and on a turn. **Evidence:** meta/reviews/c80-context-fill-20260912.md — one replay snapshot (13 sessions, 4,320 turns: 46 reminders) and the re-check of Codex windows and compaction points; it measures habits, windows and reminder frequency, not model quality or real savings. Verify: (a) the meaning in (1). (b) 200000 as an owner policy bounded by the measurements in (2). (c) the two levels in (3): the texts, warn fixed at 1.0, one reminder per level per episode, nothing past 100%, the episode reset, the `ℹ`/`⚠` marks, `/compact` and `/new`, delivery to the owner only. (d) ✅ confirmed by the owner — env precedence over the alias exceptions, and the display rule. (e) the costs above are acceptable. | tools/model_routing/registry.json (context_pressure) + tools/model_routing/routing.py::recommended_max_context/context_pressure_notice + hooks/model-routing.py + meta/design/model-routing.md §12.2 + meta/TASKS.md (C80) |  |  |

## Verified

| id | kind | what | anchor | commit |
|----|------|------|--------|--------|
| D2-36 | architecture | **`sync` is not a realign — installing the pin is not aligning to it.** As found, `sync` restamped `<AITNA_ROOT>/.akmon.toml`'s `akmon_version` with the installed version on every run (`bin/sync.py::_package_mode_akmon_toml`) and its own docstring called that sufficient — *"the installed package version is the pin (no separate realign step the way a submodule pin-bump needs one)"* — while `last_realign` moved only under `init`. So a package-mode bump advanced one key past the other and reported success. Measured on the alphavar pilot: `akmon_version = "0.4.0.dev0"` beside `last_realign = "v0.2.1"`, with `verify` green — two keys akmon itself writes, openly disagreeing, and no gate reading the difference. The boundary is narrower than "init rewrites everything": `sync` owns generated pointers, vendor wiring and imported guardrails; `init` runs sync, reinitializes the model-routing binding whose `registry_hash` a registry or overlay change invalidates (D2-6/D2-30), and advances `last_realign` only after both stages and — in mode `package` — the dev-pin completeness gate have all succeeded. Existing `AGENTS.md` blocks and existing CI workflows are hand-owned: `init` preserves them and emits checks or manual next steps, so the marker proves completion of the machine-owned sync+routing stages, not completion of manual edits. **Six changes.** (1) `verify` gains `attach.realign`: an **error** when the recorded pin and recorded realign name different versions, normalized through `common/versions.py::split_version` (C54) so mounted `git describe` and package-mode PEP 440 spellings compare and a mount N commits past its tag is not mistaken for a missing realign — that fact stays owned by `cli.py::_skew_notice`. Non-string version fields remain an `attach.record` error and do not enter this comparison. (2) `last_realign` becomes a transactional completion marker: a failed sync, a failed routing-init, or a package attach that ends without a dev-group pin (`init` exit 1) each preserves its previous value, and a fresh attach that fails any of the three does not gain one. (3) **Both** bump procedures in BOOTSTRAP §E run `akmon init` before the checks — the package block bare, the mounted block through `uvx` pinned at the target for a project that has no package installed, since `init` dispatches to the mount either way (ADR 0009 §5) — and §A stops instructing a hand-written `last_realign`, because a marker written ahead of the stages it certifies asserts an alignment no run performed; the manual path may write it only after running `sync.py` and the routing initializer itself. Bare init reuses a valid mount mode already recorded for the project rather than rediscovering a different default. (4) The delta-check window's `from` becomes `last_realign`, not `akmon_version`: in package mode `sync` moves `akmon_version` to the target the moment it runs, so it stops anchoring the window it is supposed to open. (5) The generated `k_*` delegates are **per-user and gitignored**, ending a contradiction the tree carried in both directions — `src/akmon/_init.py` gitignores them as "never committed" while `tools/model_routing/init.py` and `tools/README.md` called them "committed". The gitignore is right: the frontmatter pins each delegate to a model chosen for the current session's orchestrator and `routing.rebind_to` rewrites them when it changes, so committing one commits somebody else's session. This also settles the boundary `bin/verify.py::check_model_routing` already states — verify guards the committed surface, delegate freshness is the SessionStart hook's — so no delegate-freshness check is added. (6) **A realign of a recorded project makes no network request** (owner decision, found on the alphavar pilot): in mode `package` bare `init` called `git ls-remote` to GitHub on every run for the release tag it names in the AGENTS.md block and the pin instruction — a value a realign uses for neither, the block being preserved — so the first step of every bump failed offline with exit 2. When `<AITNA_ROOT>/.akmon.toml` already exists the ref comes from the installed version (`_tag_for_version`, the rule mounted modes already use); only a first attach without `--ref` still asks the remote. **Known limit, documented rather than closed (owner choice):** in the mounted modes nothing restamps `akmon_version` after `git submodule update --remote`, so a skipped `init` leaves both keys agreeing on the old version and `attach.realign` cannot see it; closing it would need `verify` to learn the mount's own version (a `git describe`, or a tree-version spelling that differs from the tag between releases). BOOTSTRAP and the CHANGELOG say so. **Verify:** (a) that a bump must run `init`, accepting one more command per bump and a regenerated `.codex/hooks.json` the Codex host asks the owner to re-trust; (b) that a consumer which bumped without realigning should **fail** CI rather than warn; (c) that `verify` compares the two recorded keys rather than the installed version, leaving "the pin was never synced" to `sync --check`, which plans this same file and is its sole owner; (d) that `last_realign` certifies successful sync+routing while hand-owned AGENTS/CI updates remain explicit owner work. **Rejected:** comparing `last_realign` against the installed version inside `verify` — a second owner of "what is installed" that ties the check to the environment; stamping `last_realign` in `sync` or before routing-init succeeds — both assert an alignment nothing completed; and rewriting existing hand-owned AGENTS/CI text during realign. Pinned by `meta/tests/test_verify.py` (matching pin, pin ahead of realign, both recorded spellings, describe distance, and non-string version fields) plus `meta/tests/test_init.py` (bare recorded-mode reuse; preservation of the completion marker across sync failure, routing-init failure and the missing-dev-pin gate (`test_package_attach_without_a_manifest_pin_is_incomplete`); no remote query on a realign, the installed version in its pin line, and exactly one query on a first attach). Exercised end to end on alphavar: each of the three failures left `last_realign` at `v0.2.1`; a bare `init` realigned it to `0.4.0.dev0` with `verify --strict` and `sync --check` at 0; a simulated bump showed `sync --check` catching the unsynced pin, `attach.realign` catching sync-without-init, and `init` clearing it. The pilot was restored byte-identical. | src/akmon/_init.py::_mark_realign_complete + bin/verify.py::_check_realign_freshness + bin/sync.py::_package_mode_akmon_toml + BOOTSTRAP.md §E + tools/model_routing/init.py | 920fe69 |
| D2-27 | architecture | C70 Codex hook-delivery diagnostic, owner-approved from N7 evidence. On the ordinary persisted, non-bypass path, `verify` asks the authoritative `codex app-server` `hooks/list` route after generated wiring has validated. The expected population is derived from that generated wiring, never from a second hand-maintained list. Any missing, disabled, `untrusted`, or `modified` expected entry produces one aggregate warning; strict mode keeps the warning severity and exits 1. An absent optional Codex installation is an explicit neutral skip. A resolved Codex installation that cannot be inspected because the bounded subprocess fails, times out, or returns unusable protocol data produces a warning and strict failure rather than a delivery claim. The check neither parses `~/.codex/config.toml` nor reimplements vendor hashing, never grants or approves trust, and directs owner remediation only through `/hooks`. Consumer-tree and host-config state remain read-only; bounded vendor-owned cache, log, or network side effects of app-server startup are accepted. C70 consumes C51 Findings and C57's sole vendor-command owner; C59 depends on C70 and receives its finding exactly once through `verify`, never through a third provider or its own host query. Rejected: config/hash reimplementation, attach-only advice, and documentation-only handling, because each misses or risks missing the previously working consumer after `sync`. ✅ owner-approved; landed with C70 in `898d8a2` and completed by the fourth-round fixes in `766cdce`. | bin/verify.py + meta/design/codex-runtime-contract.md + meta/design/stage1-hardening-contracts.md §7/§10 + meta/reviews/n7-codex-hook-delivery-20260825.md (C70) | 766cdce |
| D2-29 | architecture | **C57 amendment 1 of 2 — two named claims carry an unmeasured crash posture as a warn (owner choice: the allowlist).** ADR 0012 makes `matrix.unqualified-effect` an error whenever an `ask`/`deny` claim lacks a measured full route *or* crash posture. akmon's only two enforcement claims — the Claude commit guard (`deny`) and the Claude delegation nudge (`ask`) — have no crash measurement; those numbers are C52's campaign behind the D2-23 gate, and §7 forbids holding C57 behind an unrelated campaign. **Decision:** `meta/checks/capabilities.py::CRASH_POSTURE_EXEMPT` names exactly those two claim identities (their whole `###` titles in `CAPABILITIES.md`); on them an `unmeasured` crash posture under `ask`/`deny` is a **warn** — non-strict `self_ci` stays at zero, `--strict` fails, so the debt stays visible rather than silenced. Every other `ask`/`deny` claim with an `unmeasured` crash posture, and every `unmeasured` route coordinate, stays **error**, so the ADR rule stands as written for all future claims. **Expiry by test, not by calendar:** a mandatory F13 carrier on C52/D2-23 in `meta/tests/test_capabilities.py` fails while a listed claim records a measured crash posture, and fails when a listed title names no live claim. **Cost accepted:** two hardcoded identities in the checker — renaming either claim turns the tree red for a reason unrelated to the change, loudly (the renamed claim loses its warn and the carrier names the stale entry) — and C52 inherits a carrier about a checker it does not otherwise touch. **Rejected:** a global warn for the crash-posture branch, as first shipped in `c41eeb1`, which would pass every future `ask`/`deny` claim with no crash measurement in an ordinary run, permanently; blocking C57 on C52; dropping the two claims to `advisory`, false on the normal path; and a dated exemption list, which needs a date authority the tree does not have. **Texts stating it:** the ADR 0012 §matrix amendment note, the design §7 decision block and its F13 carrier, `CAPABILITIES.md`, the checker docstring, the CHANGELOG entry, and the C57 and C52 entries in `meta/TASKS.md`; the F19/A line keeps crash posture an `error` case with the named exception. Pinned by `meta/tests/test_capabilities.py`: a non-exempt claim is an error that fails a non-strict run; each exempt title is a warn with non-strict 0 and strict 1; an exempt claim's unmeasured route coordinate stays an error; a look-alike title is not exempt; and the two live-tree carriers. | [ADR 0012](decisions/0012-stage1-contracts-and-vocabulary.md) + meta/checks/capabilities.py::CRASH_POSTURE_EXEMPT | 920fe69 |
| D2-38 | architecture | **C23 context-pressure detection — landed in `7312972`; its basis, reworked on the owner's direction, landed in `920fe69` and is amended by D2-42 (approved).** The same transcript pass as C22 reads the last main-chain `usage`: fill = `input_tokens + cache_read_input_tokens + cache_creation_input_tokens` (output excluded) — and only when all three are non-negative integers (a bool is not a count); a partial or malformed record has no fill, so one valid component never carries a warning on its own. **Owner-directed choice, owner-approved as this row: the warn bands are shares of a recommended maximum context, not of the model's limit.** As landed, the block read `window_default: 200000` / `windows: {}` as the model's window — measured on this machine's akmon session transcripts, main-chain `claude-opus-5` turns reach 411,203 tokens with no compaction, so the assumed window was wrong by at least 2x and would go wrong again with each model release. Now: registry `context_pressure` = `{recommended_max: 200000, recommended_max_by_alias: {}, warn_ratios: [0.85, 0.95]}`; `recommended_max_context` takes the longest alias-substring key of `recommended_max_by_alias` found in the model id (e.g. a model whose hard limit sits below the default), else `recommended_max`; the warning reads `~86% of the recommended 200k max (172000 tokens)`, and a share above 100% is reported as such (411,203 → ~206%, critical). Only a band rise warns — a per-session marker `akmon-context-pressure-<session_id>` in the temp dir records the last announced band; a fill below the lowest band clears it (the compaction reset). The top band advises compacting now, lower bands checkpointing and planning; delivery is dual-channel; missing or malformed usage stays silent. The key rename changes the registry hash, so every local routing config goes stale once and asks for a re-init; an overlay still using `window_default`/`windows` is ignored (none found in alphavar). Evidence: meta/tests/test_model_routing.py carries each point below — the shipped policy (`test_the_shipped_pressure_policy`), exact band starts at 0.85 and 0.95 (`test_context_pressure_bands_start_exactly_at_their_ratios`), a non-default maximum through both the notice and `context_fill_ratio`, the legacy keys beside and instead of the new ones (`test_the_legacy_window_keys_are_ignored`), longest-match in both key orders, mixed/bool/negative usage staying silent, the default temp-dir marker (`test_the_pressure_marker_defaults_to_the_shared_temp_dir`), and the measured share past 100% (`test_context_pressure_is_a_share_of_the_recommended_max_not_the_model_limit`); a mutation probe confirmed each carrier fails on its defect. Verify: (1) 200000 as the recommended maximum, and 0.85/0.95 of it as the bands. (2) The old keys are dropped without a fallback reader. (3) Claude-only, as D2-37 (accepted Codex-pending). (4) The marker sits in the shared temp dir — the class already open as C36 (a). (5) No live warning is on file: akmon's own tree wires no hooks, and this machine holds no alphavar transcripts. | meta/decisions/0006-orchestrator-detection-corridor-context-pressure.md (decision 5, amended) + meta/design/model-routing.md §12 + tools/model_routing/registry.json (context_pressure) + tools/model_routing/routing.py::context_pressure_notice/recommended_max_context | 920fe69 |
| D2-37 | architecture | **C22 orchestrator detection, corridor warning, owner-visible delivery — landed in `7312972`.** The model-routing hook (SessionStart + UserPromptSubmit) reads the last main-chain assistant `message.model` from `transcript_path` (sidechain and synthetic turns skipped), maps it to the longest matching alias of the local `available` ladder, and when it differs from the recorded orchestrator recomputes the binding and regenerates the `k_*` defs (`rebind_to`) — refused, with the reason stated, while overlay briefs are unusable; an id matching no alias keeps the recorded orchestrator. `compute_binding` warns when the orchestrator leaves the corridor `floor ≤ orchestrator < top` (`orchestrator_floor` is a named alias — anthropic `opus`; openai keeps the relative `highest`): below the floor → `/model` up; on the top rung while the floor sits lower → `/model` down; silent when floor == top (the accepted degraded mode) or when the floor alias is not in the ladder. Everything owner-addressed (init instruction, corridor, rebind notice, context pressure) also goes out as `systemMessage`; the steady status line stays context-only. Evidence: the corridor, detection, rebind and owner-channel tests in meta/tests/test_model_routing.py (`test_binding_corridor_*`, `test_detect_orchestrator_*`, `test_rebind_*`, `test_hook_session_start_owner_sees_*`) pass at HEAD; no live-session trace is on file. Verify: (1) **Claude-only scope** — `bin/sync.py::_codex_hooks` does not wire model-routing and `hooks/codex_adapter.py::print_result` drops `system_message` (no documented Codex user channel), while ADR 0006 calls a warning only the model sees a defect. **Owner decision: accepted, with the gap recorded as Codex-pending** — C22 closes on Claude; the Codex half waits on Codex wiring the hook and offering a user-facing channel for `systemMessage`. (2) Detection by alias substring is the right mapping (longest match wins; a renamed or unknown id degrades to the recorded orchestrator, not to a guess). (3) **Pilot drift, measured on alphavar:** five `k-*.md` generated defs are tracked in git (since `5525011`), its `.gitignore` carries the pre-D2-15 `.claude/agents/k-*.md` line, and its local routing config is stale (recorded registry hash `140c3f6e46d2db23`, current `b8cf29dab445d5a6`), so every SessionStart shows the init instruction; `akmon init` adds the `k_*.md` line but never untracks committed files — untracking is an owner git step in the consumer. | meta/decisions/0006-orchestrator-detection-corridor-context-pressure.md (decisions 1-4) + meta/design/model-routing.md §3+§4.3 + tools/model_routing/routing.py::compute_binding/detect_orchestrator/rebind_to + hooks/model-routing.py | 7312972 |
| D2-30 | architecture | **C57 amendment 2 of 2 — one owner for the second-opinion command prefix.** The executable and operation prefix moved out of `tools/model_routing/registry.json` into `common/runtime.py`, the single harness-command map; the registry keeps policy — which harness, which operation, `model_flag`, `report_dir`. Retired keys `second_opinion.cli` and `second_opinion.invoke` are **refused**, in both the required and the optional lookup, because a stale project overlay deep-merges *over* the shipped registry: the new keys survive, the retired pair rides along beside them, and every presence check passes while the config states one command twice. `verify` reports the retired pair against the overlay file itself, since the merged result no longer shows which file is stale. Reason: two owners of one vendor command is the drift class C46 exists to remove, and `runtime.duplicate-query-owner` needs one owner to point at. Cost: `registry_hash` moves, so **every consumer must re-`init` its routing config**, and a consumer whose overlay carries the retired keys gets a hard failure rather than a degraded run — deliberate, following C50's rule that a broken overlay stops rather than silently weakens. Rejected: leaving the argv in the registry with a checker exception, which keeps the second owner and only stops naming it; and tolerating the retired keys as ignorable, which is the silent-acceptance defect this row exists to close. The C57 F19/A line in `meta/TASKS.md` now states that factual boundary and a task-local carrier rejects the old whole-argv overclaim; this does not approve D2-30. **On approval**, the owner accepts the prefix ownership and the hard migration behavior together. The map owns the executable and the operation prefix, while `routing.second_opinion_command` appends the registry's `model_flag` and the prompt, because a model pin is policy. That was already true of `model_flag` before C57; the implementation has not widened. | [design §7](design/stage1-hardening-contracts.md) | c41eeb1 |
| D2-11 | architecture | **C31 background/child-session posture: escalate ask→deny (owner decision).** Rather than isolate whether D2-10's silent-`ask` gap was specifically background-session-scoped or triggered by any non-default `permission_mode`, the owner chose to harden now and watch behavior: `hook_core.py::_escalate_unattended_ask` turns any `ask` from `git_commit_guard_result` or `delegation_nudge_result` into a hard `deny` whenever the PreToolUse payload's `permission_mode` is not the interactive `default` (missing field included — treated as the worst case, not the safe case). Both Claude wrappers (`git-commit-guard.py`, `delegation-nudge.py`) now read `permission_mode` from the payload and pass it through; the already-`deny` co-authored-by check is untouched. Codex is out of scope here — `.codex/hooks.json` does not wire either hook yet. Verify: (1) collapsing "background session" and "non-default permission_mode" into one escalation trigger is an acceptable simplification given D2-10 left them confounded; (2) `deny`-on-uncertainty is the right owner-chosen tradeoff vs. fatigue from any legitimate non-default-mode `ask` now hard-blocking instead of prompting. Pinned by test_hook_core.py (`test_ask_escalates_to_deny_outside_interactive_default`, `test_ask_stays_ask_in_interactive_default_mode`, `test_escalation_does_not_touch_deny_decisions`, `test_delegation_nudge_ask_escalates_to_deny_outside_interactive_default`); 342 passed, ruff clean; e2e-verified via both wrapper scripts on stdin JSON (missing field, `acceptEdits`, and `default` mode). | hooks/hook_core.py::_escalate_unattended_ask + git_commit_guard_result + delegation_nudge_result; hooks/git-commit-guard.py; hooks/delegation-nudge.py | ac54a5a |
| D2-10 | architecture | **C28a background/child-session ask-enforcement gap.** Live test (session `69bab2c3-...`, `CLAUDE_CODE_CHILD_SESSION=1`): cleared the ask-marker, then drove one real `Read` call and, separately, one real `Bash` call to the ask threshold. Both completed normally — no permission prompt, no block — even though the hook's stdout carried `permissionDecision: "ask"` for both (confirmed via the ask-marker being freshly written each time). Not a Read-vs-Bash asymmetry: neither tool's `ask` was enforced in this background session. Working hypothesis: Claude Code auto-resolves (or otherwise doesn't gate on) a hook-forced `ask` when there is no interactive owner to answer it — untested from a foreground/interactive session. Consequence if true: the delegation hard-ask (D2-8) **and** `git-commit-guard`'s D5 enforcement may both be no-ops in background jobs, not just this nudge. Verify: (1) reproduce from a foreground session to confirm the background/foreground split, not a general regression; (2) if confirmed, decide whether background sessions need a different guardrail posture (e.g. `deny` instead of `ask`, or refuse to run drift-prone work unattended) — this is a design decision, not a code fix, so route to C31 before touching the hook. Method note: instrumentation was added to `delegation-nudge.py` for the test and reverted (no diff left in the file). **Update (this session) — concrete lever found, not just a hypothesis:** the PreToolUse payload carries a `permission_mode` field (confirmed live: `'acceptEdits'` during the no-block test), enum `default\|acceptEdits\|plan\|dontAsk\|bypassPermissions`. Codex's own embedded JSON schema (see C28 in TASKS.md) documents the **identical** enum for its `pre-tool-use.command.input.permission_mode` — the vocabulary is shared across vendors, so this isn't Claude-specific. Actionable design option for C31: the hook can read `permission_mode` from the payload and, when it is not the strict interactive default (i.e. an `ask` cannot reach an owner), escalate to `deny` instead of emitting a now-know-to-be-inert `ask` — turning a silent no-op into either a real block or an honest downgrade to advisory. Still unresolved: whether `ask` is a no-op in *every* non-default mode or specifically tied to the background/child-session condition — the two were confounded in this test (background session + `acceptEdits` both present); needs a foreground `acceptEdits` session to separate them. **Superseded (this session):** the owner chose not to wait on that isolation — see **D2-11** for the ask→deny escalation actually implemented; this entry stays as the diagnostic record. | hooks/hook_core.py::delegation_nudge_result + hooks/delegation-nudge.py + meta/design/model-routing.md §13.2 | 7312972 |
| D2-9 | architecture | C28d subagent false-fire fix: `delegation_nudge_result` gains keyword-only `is_subagent` and returns early (before the counter/marker logic) when set; `hooks/delegation-nudge.py` derives it from `bool(payload.get("agent_id"))` — present only inside a subagent. Root cause: Claude Code gives a subagent's tool calls the **same `session_id`** as the main chain (contract confirmed), so subagent reads charged the orchestrator's counter and tripped the advisory/hard-ask *inside* a read-only `k-*` delegate that has no `Task` tool (demonstrated live at call #10). Design motivation enshrined in model-routing.md §13 (delegation drift → forcing function; axis-1 built, axis-2/C29 planned). Verify: (1) `agent_id`-presence is the right subagent discriminator (vs session_id parsing / transcript sidechain scan) and suppressing *before* the counter is correct (no double-charge, no reset-by-subagent); (2) §13 captures the motivation accurately for future verification. Pinned by test_hook_core.py (test_delegation_nudge_suppressed_in_subagent + test_delegation_nudge_subagent_calls_do_not_charge_counter); 338 passed, ruff clean. | hooks/hook_core.py::delegation_nudge_result + hooks/delegation-nudge.py::main + meta/design/model-routing.md §13 | 7312972 |
| D2-8 | architecture | Delegation nudge graduated from prose to enforcement (root cause of "orchestrator does everything"): `delegation_nudge_result` now counts `READ_TOOL` (Read/Grep/Glob) alongside edit/shell, so the orchestrator's read/sweep drift — the actual symptom — finally registers; the advisory (threshold 10) graduates to a hard PreToolUse `ask` on *sustained* drift (threshold 20, env `KEYSTONE_DELEGATION_ASK_THRESHOLD`, clamped ≥ advisory) via its own once-per-episode `ask-marker`, both markers cleared by any subagent delegation. Matcher widened to `Bash\|Edit\|Write\|MultiEdit\|Task\|Agent\|Read\|Grep\|Glob` in `bin/sync.py::_claude_hooks` (source of truth) + mirrored into live `.claude/settings.json`. Prose demoted in parallel (guardrail dup-bullet → clause on bullet 1; learn/release pointers dropped) restoring one-owner-per-fact. Verify: (1) graduated advisory→ask + reads-counted is the right mechanism to make delegation actually happen vs an 8th prose restatement; (2) thresholds 10/20 don't cause false-positive fatigue on legitimate orchestrator reads; (3) **runtime gate — C28a LIVE-VERIFIED (this session)**: a debug-instrumented live run confirmed the hook dispatches identically for a real `Read` and a real `Bash` call (same counter, same emitted `ask`) — no tool-category exemption. Surfaced a new, more consequential gap in the same test: see D2-10. Pinned by test_hook_core.py + test_adapters.py + test_sync.py (336 passed); ruff clean. | hooks/hook_core.py::delegation_nudge_result + bin/sync.py::_claude_hooks | 7312972 |
| D2-6 | architecture | C18 A7 (b) cross-cutting verification kinds: registry gains cross_cutting_kinds=[independent-review, audit] and drops both from the review/architect/engineer role_task_kinds rows; role_matrix_warning unions cross_cutting_kinds into every role's allowed set, so the C20 advisory no longer warns when any role routes k-auditor (audit) — dissolving the auditor seam (§9.4/§10.2). Docs half: MODEL.md §10 leverage principle + auditor/mid tiers + audit/plan-draft matrix rows + floor-warning demoted to weak prior; review-flow/design-flow auditor anchors (post-fan-out audit + pre-fan-out plan check) + loop-back; roles/*.md drafts-not-decides deltas; ADR 0005 addendum. Verify: making audit/independent-review role-agnostic (routable from any role, exempt from the matrix warning) is the right coherence fix vs a per-row copy. Pinned by test_model_routing.py (k-auditor silent under all roles; exemption driven by cross_cutting_kinds). Bumps registry_hash -> local model-routing config goes stale, re-init needed. | tools/model_routing/routing.py::role_matrix_warning + registry.json::cross_cutting_kinds | 7312972 |
| D2-35 | architecture | **C77 — mode `package` executes from the package, and stops materializing an executable surface.** As found, `sync` copied ~20 files into `<AITNA_ROOT>/.akmon/` (hooks, `common`, the model-routing library and `registry.json`, the D2-ledger tool) and rewrote all of them into the consumer's history on every bump. They were never a second implementation — the same stdlib-only files are installed beside the consumer inside the wheel — and they existed for exactly one reason: the generated wiring named a hook by a **path**, and the only path available in package mode carries the venv's Python version (`.venv/lib/python3.13/site-packages/…`), which breaks on the next interpreter bump, silently, because a hook command the harness cannot find prints nothing and fails nothing. **Supersession boundary. On owner acceptance, this row supersedes only D2-12's package-mode hooks materialization, D2-13's package runtime-materialization clause, D2-26's runtime-root choice, and D2-33's package-mode reachability/materialization premises (including its whole-package choice); every other decision in those rows remains in force.** **Six decisions to verify.** (1) **`akmon hook <name> [args...]` runs the hook in-process** (`runpy.run_path`, hooks dir pushed onto `sys.path`, argv tail verbatim so the Codex dispatcher keeps its shape) rather than as a subprocess: these hooks sit on the hottest tools of a session (the nudge matches `Read\|Grep\|Glob`), so a second interpreter start per tool call would be the only cost this indirection adds over the copies it replaces; before `runpy`, `common` is rebound to the same controlling tree as the hook and afterward the caller's modules are restored, so a mounted hook cannot pair with the CLI's already-cached embedded utilities. (2) **The wiring names the console script, `"<anchor>/.venv/bin/akmon" hook <name>`, and the launcher path is a property of the project, not of whoever ran `sync`**: `.venv`, then `venv`, then an interpreter-adjacent or `PATH` script **only if it resolves inside the project root**, else the convention. It never fails, because the first attach runs `sync` before the pin is installed by construction; the diagnostic is `verify`'s new `hooks.launcher`, which states that a missing or non-executable launcher fails *silently*. The consequence, recorded in the ADR: **mode `package` requires a venv inside the project root**; a venv outside stays an open point whose answer is a locator shim, not an absolute path in a committed file. (3) **The materialized set is the guardrails `AGENTS.md` imports, and nothing else** — the exception is the **path**, not containment: the import sits in a committed `AGENTS.md`, the only path from there into the package carries the venv's Python version, and an unresolved `@`-import reports nothing in any harness (importing straight from the package, by path or by gitignored symlink, is weighed and rejected in the ADR's alternatives). Names are read from the document with fenced blocks and inline code stripped first, because the akmon block *documents* the import as well as making it and a quoted example is otherwise indistinguishable; `_common.md` is unconditional because its import is the anchor `verify` requires; an import of a guardrail the standard does not ship is a **plan error**, not a silent skip. (4) **`akmon_runtime_root` answers with the executing tree** (`Path(__file__).parent.parent`), superseding the record-vetoes-the-directory rule (C69/D2-26) for that one function. It has to: `.akmon/` no longer holds a registry, so the old answer would have fired the routing hook's "older pin, stay silent" guard on every session — status line and delegation log gone, exit 0, empty stderr. What the veto defended against is not given up but dissolved: a stale mount is not a candidate because it is not executing. The guard itself is narrowed in the same breath — silence is right for a tree with no routing tools, wrong for a registry that has gone missing from one that has them. (5) **Under `<AITNA_ROOT>/.akmon/` everything unplanned is stale, banner or not**, unlike `.claude/skills/` where the project keeps its own files beside ours. Load-bearing rather than tidy: the materialized registry never carried a banner (it must stay parseable JSON), so a banner-only sweep would have left exactly that file behind forever. `__pycache__` is swept in the same run but never reported, because a project that merely *ran* a hook since the last sync is not drifted and `--check` is a CI gate. Also in scope, each for its own stated reason: skill stubs and the hooks' recovery instructions now spell `$(akmon path)` instead of a version-carrying path; `verify` stopped short-circuiting its hook check in package mode, since the scripts' presence in the tree is now what the wiring rests on; and `akmon.__version__` became lazy (PEP 562, cached in `globals()`, CLI reaching it through the module) because the wiring now invokes the console script on every hot tool call and `importlib.metadata.version()` walks every installed distribution. **(6) The one surviving copy announces its own staleness, in the session.** The guardrails are the consumer's *rules*, not its plumbing, and the failure they can have is quiet: a pin bump installs the new hooks the instant the dependency resolves — they run from the package — while `<AITNA_ROOT>/.akmon/guardrails/` still holds the previous release's text until someone runs `akmon sync`. `sync --check` and `verify` already catch that, but they run on a commit; the window between the bump and the commit is exactly where an agent works under rules the standard has retired. `session_start_result` now names any guardrail whose text is not what the *executing* tree ships and asks for `akmon sync` plus a **new session** — the restart is load-bearing, since the `@`-import is expanded once at session start and a mid-session sync changes nothing already in context. Four choices inside it: limited to package execution so an inactive leftover copy in mounted mode stays silent; compared by **content against the running tree** rather than a recorded version, so a release that leaves the guardrails alone stays silent and a hand-edited copy does not; carried by `session_start_result`, which **both** vendor wirings already call, so no new hook and no regenerated `.codex/hooks.json` (a changed wiring file invalidates the host's approval of every group in it) — on Codex it lands as context only, that runtime having no owner-facing channel; and the banner rule plus the comparison lifted into `common/materialization.py`, because `sync` writes the file and the hook judges it and two owners of one format is the split C69/D2-26 already paid for. Pinned by `meta/tests/test_cli.py` (the `hook` subcommand: mounted tree, embedded fallback, argv passthrough, `sys.path`/`sys.argv`/`common` restored, mounted/embedded `common` separation, `name.py`, four path-shaped refusals, unknown name listing what exists, exit-code propagation, `main` dispatch, and two subprocess carriers for version laziness), `meta/tests/test_package_mode.py` (materialization width, prose-quoted import, missing guardrail as a plan error, drift, the one-run sweep incl. the banner-less registry, emptied parents and unreported `__pycache__`, executable and non-executable launcher-resolution cases, the wiring in both modes, and entry recognition across every spelling the generator has emitted), `meta/tests/test_adapters.py` (the runtime-root table kept at all seven mount states — the point now being that none of them changes the answer — plus a direct regression that the package-mode hook reaches the routing registry), `meta/tests/test_hook_core.py` and `meta/tests/test_verify.py`. 1226 passed, ruff clean, validate exit 0, self-CI exit 0 — its installed-wheel smoke now builds the venv **inside** the fixture, asserts the materialization is one file, and runs **every generated hook command verbatim**, checking exit code, stderr and the *size of stdout*, the last because a hook that cannot find its tree exits 0 with an empty stderr. **Measured on the alphavar pilot** (a real package-mode consumer, Python 3.14 venv, medians over 30-40 runs), because decision 1 is only defensible if the residue is small: `akmon hook <name>` costs **+3.1 ms** over running the hook file directly, after three deferrals each larger than the dispatch itself — the lazy `__version__` (**89 ms**), `importlib.resources` reached only when the wheel data is not a plain directory beside the module (**~45 ms**; it pulls `inspect`, `typing`, `tempfile`), and `argparse` (~18 ms) + `subprocess` (~12 ms) moved to their use sites with `main` dispatching `hook` before it builds a parser. `import akmon.cli`: 88 ms -> 32 ms. The same pilot run is where **`runtime_root_display` was found wrong and fixed**: it asked "is the tree under the project root", and in a package-mode consumer the wheel's tree *is* — at `.venv/lib/python3.14/site-packages/akmon/_tree` — so the recovery command it printed carried the venv's Python version. It now asks "is the tree the mount", which is exact. One `sync` run on that consumer removed all 20 materialized files plus four `__pycache__` directories and left only the two imported guardrails, with `sync --check`, `verify` and `verify --strict` at 0 and the same empty non-OK remainder as before the change; every generated hook command was then run verbatim (rc 0, no stderr, and non-empty stdout from all three SessionStart hooks). Decision (6) was then exercised on the same consumer end to end: fresh copy -> silent; the installed package's `_common.md` edited to stand for a bump -> the notice on `systemMessage` **and** `additionalContext` through the Claude wiring and on `additionalContext` through `akmon hook codex-hook session-start`, rc 0 and empty stderr on both; `sync --check` and `verify` at rc 1 naming `_aitna/.akmon/guardrails/_common.md`; `akmon sync` -> `updated:` that one file -> silent again. The package file was restored and the consumer left byte-identical, with `sync --check`, `verify` and `verify --strict` back at 0. | [ADR 0009 amendment](decisions/0009-packaging-package-carrier-and-mount-modes.md) + src/akmon/cli.py::_cmd_hook + bin/sync.py::launcher_relative/_materialized_files + hooks/hook_core.py::akmon_runtime_root | db9ad7e |
| D2-34 | architecture | C68 supersedes only D2-12/ADR 0009's Python-floor clause: one Python >=3.11 floor governs package installation and every shipped venv-free entry point, including generated hooks' bare `python3`, across all carriers. Python 3.9 and 3.10 are unsupported; consumers must upgrade both the package interpreter and hook host before the Breaking pre-1.0 `x` release. Cost accepted: the measured 3.9 repair was only one runtime type spelling plus six guarded test expectations, but retaining it would preserve the continuing pre-3.11 compatibility obligation. Rejected: repair/retain 3.9, transitional 3.10, split package/hook floors, and metadata-only gating. Zero runtime dependencies, stdlib-only runtime and all other ADR 0009 decisions remain unchanged. Acceptance: exact joined declarations (`requires-python >=3.11`, Ruff `py311`, CI floor 3.11, lock metadata), wheel `Requires-Python >=3.11`, full gates and installed-wheel smoke; the 3.11 CI leg is the lower-bound runtime/import carrier. ✅ owner-approved; landing sha pending — the commit is the owner's. | pyproject.toml + .github/workflows/ci.yml + meta/design/packaging/python-39-floor.md + ADR 0009 amendment (C68) | db9ad7e |
| D2-26 | architecture | C69 tree-resolution owner: `hooks/hook_core.py::akmon_runtime_root` picks the hooks' tree by directory existence (`<AITNA_ROOT>/akmon` if present, else `.akmon`), while every other owner of that decision reads the recorded `mount` — `bin/sync.py::is_package_mode`/`standard_tree_root`, `src/akmon/cli.py::_mounted_akmon_root`, `tools/model_routing/init.py::_standard_tree_root`. Measured: a package-mode record beside a stale `_aitna/akmon` makes the hooks bind the stale registry and print stale tool paths, silently. The fork is not an engineer's call because `hook_core` must stay stdlib-only *and* run from the materialized `<AITNA_ROOT>/.akmon/hooks/` copy, so it cannot import sync: either one shared home for the `.akmon.toml` reader both carriers can reach, or deliberate duplication with each copy stating why it is not the others (the C46/C64 second-owner question). **Owner decision: the record is a veto over the directory check, not a replacement for it** — `records package` → the materialization; otherwise the mount if it exists; otherwise the materialization. Replacement was rejected on a measured state, not a preference: `read_mount_mode` defaults an absent record to `submodule`, so a record-only rule sends a project that predates the `mount` field — no record, no mount — to a mount that is not there. Seven states are pinned in `meta/tests/test_adapters.py`; today's answer was wrong in exactly two of them (a package record beside a stale mount, and the same record written in the documented `mount = "package"  # comment` form) and unchanged in the other five. The reader lands in `common/record.py` — the shared home D2-33 approved — lifted out of `bin/sync.py` unchanged rather than rewritten, with `sync` re-exporting it; the note's "third narrow reader" count was understated, there were **five** (`bin/sync.py:76`, `tools/release/release_check.py:496`, `tools/d2_ledger/d2_ledger.py:272`, `tools/model_routing/init.py:60`, `src/akmon/cli.py:144`) and the hooks would have been the sixth. Folding the remaining four is filed separately as C75, since each carries its own justification to re-examine. **Found while landing this:** `read_akmon_toml` documented "returns `{}` if absent or unreadable" but let `tomllib.TOMLDecodeError` propagate, so on 3.11+ a malformed record crashed `sync`/`verify` — and would have crashed a session once the hooks consulted it — while 3.9 parsed the same file to a partial dict. It now falls through to the stdlib parser on a decode error, which is both what it documents and what keeps the two hosts agreeing. **The pricing premise is withdrawn and the shared home now exists (C73 → C74):** this row and its note both said `bin/` does not exist in the materialized tree. It did (C57 put a module there), and the point is now moot — C74 gave the shared stdlib-only utilities their own package, `common/`, materialized whole into `<AITNA_ROOT>/.akmon/` beside `hooks/`, and `hook_core` already imports from it for the project-root walk. So the "one shared home" branch is built and proven by the wheel smoke's materialized-hook leg; adding the `.akmon.toml` reader to it is one module. What stays open and owner-owned is the behavioural question this row is actually about — whether the hooks should read the recorded `mount` at all, or keep resolving by directory existence. Only the cost argument against the shared home is gone. | hooks/hook_core.py::akmon_runtime_root + meta/design/packaging/mount-resolution-owner.md (C69) | 07f46ad |
| D2-31 | architecture | C54 version <-> changelog implementation gate (design §4 / owner choice F9). Eight choices the lock did not spell out. (1) **A new shipped module, `common/versions.py`**, is the sole owner of version spelling — C61 carried this forward explicitly ("lift this helper rather than write a second one"). It answers two questions: `split_version` (a leading `v` is a spelling, a `git describe` distance is a separate fact, a PEP 440 segment is neither) and `is_final`. `src/akmon/cli.py::_split_version` now delegates to it through the embedded tree, the same load path `_skew_notice` already used for `sync`. (2) **A third owner was found and folded in**: `src/akmon/_init.py::_tag_for_version` classified versions with a substring heuristic (`".dev", "a", "b", "rc", "+"`) that missed `.postN` entirely and would have pointed a consumer's links at `v0.4.0.post1`, a tag no repository carries. It now asks `is_final`, so the rule is exactly **final is `X.Y.Z`, everything else is `main`** — verified against that formulation over a 28-input corpus, where the only divergences are the input's *spelling*: a leading `v` is accepted and surrounding whitespace is trimmed. Accepting `v` is load-bearing rather than lenient: for a mounted mode this function receives `_describe(mount)`, i.e. `git describe --tags` output, which is `v`-prefixed by construction, so rejecting it would resolve every consumer standing exactly on a tag to `main`. This is a **runtime behaviour change in `akmon init` beyond C54's locked scope**: a version that is neither exactly `X.Y.Z` nor a recognized non-final form (e.g. `0.4`) now resolves to `main` instead of to an invented tag. (3) **The final/non-final classification is binary by construction** — final is exactly `X.Y.Z` with no distance past a tag, and every other spelling is non-final — so no version escapes both heading rules and no third branch exists to forget. (4) **A skip is a `warn`**, not `ok` and not `error`: `ok` disappears under `--quiet` (F9/4's "never silence") and `error` fails every non-Python consumer (the fork F9/4 closed). (5) **A rule that does not apply is not reported as skipped**: under a non-final version there is no re-release question, so git's absence skips only tag coverage. (6) **A released heading names its version first and may say more after it** — `## v0.3.0` here, `## [1.2.3] - 2024-01-01` in a consuming project — because the `package` subject reads a consumer's changelog and demanding a bare version would report "no released heading" over a file full of them; a pre-release heading (`## v1.2.3rc1`) is deliberately *not* released, since no `vX.Y.Z` tag matches it. (7) **The join is wired into `release_check --check` only**, not `self_ci` — it is a release-time gate, and the tree's steady mid-cycle state (non-final + `## Unreleased`) is exactly what it accepts. (8) **`vX.Y.Z` is the release tag's only admissible spelling** — owner-ruled during this task, closing the fork I had opened by reading tags in both spellings. A version-shaped tag without the prefix is therefore a *misspelled* tag, not a second convention: it is excluded from the re-release answer and from tag coverage, and reported as `release.tag-spelling` (warn) rather than filtered in silence — silence would hide it from both rules and say nothing, which is the F9/4 failure. Verify: (2) is the one that changes shipped behaviour; the rest are checker-internal. Pinned by 72 new carriers — 63 in meta/tests/test_release_check.py, 9 in meta/tests/test_init.py; the live tree was observed red (two errors) before the F9/1 repair and green after. A second, adversarial pass seeded twenty mutations of the checker, `common/versions.py`, `_tag_for_version` and the plan: all are now caught, and the three real defects it found — tag coverage returning silently with no `CHANGELOG.md`, the `v`-only tag filter, and a plan that never reopened the development cycle after the tag — are repaired here rather than filed. | tools/release/release_check.py::check_release_versions + common/versions.py | 07f46ad |
| D2-32 | architecture | C73 root-discovery owner: `common/project_root.py` becomes the single owner of "where is the project root" and "what is the dev-layer called", and the seven copies of the walk collapse to two. As found: three copies were package-aware and `AITNA_ROOT`-aware (`hooks/hook_core.py`, `bin/sync.py`, `tools/model_routing/init.py`), one was `AITNA_ROOT`-aware but required a mounted tree so it could never find a package-mode project (`second_opinion.py`), and three hard-coded `_aitna` past `AITNA_ROOT` (`gate_pack.py`, `coverage_map.py`, `release_check.py`) — and every one of them ended `return start`, so a command that had found nothing was indistinguishable from one that had found the root, and `gate_pack._report_path` then wrote the gate pack under the cwd. Three decisions to verify, **two of them superseded by C74/D2-33 before this row was accepted — read them there, not here.** (1) ~~The home is `bin/`~~ — the owner moved to the shared `common` package; the reachability argument is unchanged and is restated in D2-33. (2) ~~`hooks/hook_core.py` keeps its own copy~~ — it does not: the deferral rested on the belief that folding it in waited on C69/D2-26, which on re-check it does not (that fork is about which *tree* the hooks read, not where the root is), so the copy is gone and the drift carrier has no allow-list. (3) **A failed walk stops being silent at command-line entry points, and stays silent inside hooks.** `find_project_root` keeps the cwd fallback because a hook must not abort a session; `resolve_project_root` returns the same path plus one line naming the directory, both expected markers and `--project-root`, which every entry point prints to stderr — the fallback root is where the tool then reads its registry and writes its reports, so unstated it is a wrong answer wearing a right one's clothes. **Measured and routed out:** D2-26 and its design note both priced the shared home off `bin/` not existing in the materialized tree; it does exist, so that premise is withdrawn in both places and the fork is left to the owner on its remaining merits. **Also found late, after this row was first written:** four more sites still spelled `_aitna` inline past `AITNA_ROOT` — `coverage_map.py`'s default map path (the sibling of the `gate_pack.py` defect this row reports, missed in the same pass), and `release_check.py`'s akmon mount, pinned `[test].runner` record and dev-venv interpreter. All four are repaired here with their own carriers, and a scanner now fails on any inline `"_aitna"` outside the owner. Pinned by `meta/tests/test_project_root.py` (14 carriers incl. the package project under a custom `AITNA_ROOT` that no removed copy could reach, the notice contract, and three scanners — no second definition of the walk, of the dev-layer name, or of the `"_aitna"` literal — which since C74 carry no allow-list at all) plus carriers in `test_gate_pack.py`, `test_coverage_map.py` and `test_release_check.py` for the four report/record paths following `AITNA_ROOT`; 1142 passed, ruff clean, self-CI exit 0 incl. the installed-wheel smoke, validate exit 0, and the module runs on Python 3.9. | common/project_root.py + meta/tests/test_project_root.py (C73) | 07f46ad |
| D2-33 | architecture | C74 shared-utility package — `bin/` is the launcher directory again, and the shared stdlib-only utilities move to a package of their own: `common/{project_root,findings,runtime,versions}.py`. As found, `bin/` held two launchers and four libraries, and a library there is reachable only by putting `bin/` on `sys.path` and importing it under a bare top-level name, which puts `findings`, `runtime`, `versions` and `project_root` into `sys.modules` as generic names: `src/akmon/cli.py` had to register `versions` as `akmon_versions` because that name may already belong to a consumer's environment, and needed one `_load_embedded_*` helper per module because each bare name had to be pre-resolved individually. `hooks/hook_core.py` could not reach any of it and kept its own copy of the root walk. Four decisions to verify. (1) **A new top-level member of the standard tree, not `bin/_lib/`.** The cost difference is two lines (the wheel force-include and the materialization list) and consumers receive the directory by the same `git submodule update` / `akmon sync` either way, so the choice is what `bin/` means: launchers a project runs, nothing else. The name states the admission rule — shared by every carrier — rather than the contents; `utils` would have named a junk drawer and set no bar. Collision with a consumer's own `common` is not a live risk: the tree root reaches `sys.path` only inside processes that run akmon code and the stdlib (the two launchers, the hooks, the `tools/` CLIs), and the installed CLI registers the package in `sys.modules` after its own imports are done. (2) **`hooks/hook_core.py` folds in.** The package sits beside `hooks/` in the mounted tree and in the materialized `<AITNA_ROOT>/.akmon/` copy, both written by the same `sync` run, so they cannot arrive apart; `aitna_root_name` and `find_project_root` now have exactly one definition each, and `akmon_mount` joins them (it was a third one-line copy). (3) **`src/akmon/cli.py` asks the embedded tree instead of keeping its own copy.** Same precedent as C54's `_split_version`: the tree shipped inside the wheel is in lockstep with the CLI. The traded failure mode is stated rather than hidden — a wheel built without `common` would fail at `_dispatch` instead of degrading, which is why the force-include list, `TREE_MEMBERS` and the wheel smoke all assert it is there. (4) **The materialized set is the whole package, not a chosen subset**, so it never has to be re-decided when one more module grows an import. **The cost, stated because an earlier draft of this row got it backwards:** the number of `sys.path` inserts goes *up*, from eight to eleven — the two launchers and `hook_core` previously got their own directory for free as `sys.path[0]` and now add the tree root explicitly. What the change buys is not fewer inserts; it is one definition per fact, and a `sys.modules` that no longer carries four generic top-level names. Pinned by three scanners in `meta/tests/test_project_root.py` (no second definition of the walk, of the dev-layer name, or of the `"_aitna"` literal anywhere in `common/`, `bin/`, `tools/`, `hooks/`, `src/`) — none of them carrying an allow-list — plus the existing suite; 1142 passed, ruff clean, self-CI exit 0 including the installed-wheel smoke, whose package-mode leg runs the materialized `codex-hook.py` and therefore exercises the folded-in `hook_core` import from `.akmon/`, and validate exit 0. | common/ + meta/tests/test_project_root.py (C74) | 07f46ad |
| D2-25 | architecture | Packaging lifecycle boundary, owner-approved in the implementation request: C37 owns the implemented carrier and local/git-pin adoption; first PyPI publication is the separate release task V4. C68 is deferred and independent of C37, but V4 still requires either the 3.9 repair or a separately owner-locked floor change; V4 also retains the existing V1 and C69 gates. The living packaging concept grows into `meta/design/packaging/` with `README.md` as its hub and the C68/C69 notes as satellites; numbered ADRs remain flat. Verify after landing: the task split and gates preserve both early adoption and an honest published Python claim, and every old design path was migrated. | meta/TASKS.md::C37/C68/C69/V4 + meta/design/packaging/README.md | c41eeb1 |
| D2-13 | architecture | C39 Codex/package runtime contract for v0.4: keep AGENTS hand-owned but require a direct delegation-default anchor because Codex does not expand nested @ guardrail imports; repeat capability-neutral delegation in SessionStart; recognize package roots via .akmon.toml; materialize every stdlib runtime dependency of wired hooks; distinguish policy delivery from advisory/enforcement/model-routing capability in the vendor matrix. Verify: verify-only AGENTS ownership is preferable to a managed region, the expanded package runtime surface is acceptable, and unsupported Codex enforcement remains honestly gated on live probes. Pinned by package-root, Codex entrypoint, verifier-anchor, materialization, and installed-wheel smoke tests. **Pre-signature amendment (N7 evidence, no code change):** clause (c) is now signed against a matrix that states the delivery precondition it previously lacked — README's Codex column marks the host-delivered cells `†`, and the note under the table records the two measured host-side grants, the group-scoped `trusted_hash` that a matcher-only `sync` invalidates, and the silent `enabled: true` non-execution that follows (measured for `SessionStart` and, in the N7 addendum, for `PreToolUse`). BOOTSTRAP §F gains the package-mode re-approval step its bump path was missing; §A8 and §E already carried it. akmon still neither writes nor inspects that host state — that gap is C70, not this row. ✅ owner-approved (clauses a–c: verify-only `AGENTS.md` ownership, the package runtime surface, and the capability matrix as amended). Evidence at approval: 648 passed, ruff clean, self-CI exit 0 including the installed-wheel package-mode smoke that feeds the real `codex-hook.py session-start` a stdin JSON payload from a nested cwd. Landing sha pending — the amendment commit is the owner's. | meta/design/codex-runtime-contract.md + hooks/hook_core.py + bin/sync.py + bin/verify.py | 1a9ab0f |
| D2-1 | architecture | C16 (end-to-end): second-opinion provider+model now walks second_opinion_policy.diversity_ladder — other-vendor (model=CLI default) -> same-vendor-other-model (pinned via second_opinion_fallback_model, previously dead C15 config) -> skip on never:same-model; explicit same-vendor / --provider override honored; second_opinion_command injects registry model_flag. Consumed live by second_opinion.py (--gate-pack replaces --prompt-file; skip-branch when ladder exhausted). Verify: the routing policy + ladder ordering match intent. Pinned by test_second_opinion_ladder.py + test_second_opinion_cli.py; e2e dry-run verified (anthropic->openai codex exec). **Owner-approved, with the fourth choice settled and two repairs made at the gate.** (4) *Exhausting the ladder still skips* — no same-model rung was added. A rung that always succeeds would make `resolve_second_opinion` never return `None`, and the fact that this installation has no model-diverse reviewer would stop being observable: the silent-success class this repository has already closed four times. The owner's choice instead: state the impossibility and hand the weaker check to a person with its price attached. `second_opinion_unavailability` now walks the *configured* ladder step by step and reports the population each step had; the runner prints why, offers the same gate pack past a subagent on the same model in a fresh context, and prints inseparably from it that this removes anchoring on the author's own reasoning but not the shared model priors — a self-check, never filed as a second opinion. The explainer deliberately claims a *verdict* ("no rung differs") only where it derives one from `second_opinion_fallback_model`; a ladder shortened by an overlay reports the step as *not attempted*, because a first draft that always asserted the verdict was caught rendering a confident false one. **Repair 1, found while verifying this row:** `status_lines` named the provider through the pre-ladder `second_opinion_provider`, so an installation whose ladder is exhausted read `second-opinion=<harness>(on)` while every gate skipped. The display now asks the same ladder, reports `unavailable` with an owner-channel warning when the gate is on, and names the pinned model on the same-vendor rung. `second_opinion_provider` is **removed** rather than left beside the ladder as a second answer to one question. **Recorded, not closed:** a retired-key overlay (C57) reaches `status_lines` through `compute_binding`->`opposite_vendor`, so it costs the whole status block, and the hook catches it onto stderr whose owner visibility D2-19 leaves unverified — a stale overlay can therefore leave a session with no routing status at all. That is D2-30's boundary, not this row's. Pinned additionally by four `test_model_routing.py` carriers and three ladder/CLI carriers; 1129 passed, ruff clean, self-CI exit 0. | tools/model_routing/routing.py::resolve_second_opinion + second_opinion.py | 7312972 |
| D2-2 | architecture | C16 gate-pack contract: the structured markdown package every gate executor receives (k-auditor + second-opinion CLI). build_full_pack (yardstick + artifacts + decisions + coverage-map[C17 stub] + dep-graph) & build_plan_check_pack (yardstick + zone-plan, pre-fan-out). Verify: the pack sections + the "what to return" footer are the right auditor/second-opinion contract (design §9.4 / §9.3 item 4-5). **Owner-approved, with the stub wording corrected and one finding routed out.** The coverage-map slot is recorded above as a `[C17 stub]`; it is not one. `coverage_map.py` exists and assembles the map, and the pack only *embeds* what `--coverage-map` hands it — the correct boundary, since a pack builder that learned to read the delegation log itself would become a second owner of the map. What the slot does own is *absence*: with no map supplied the section still renders, carrying `_Coverage map not provided (assemble via coverage_map.py)._`, so the gap names its own repair instead of being a section that quietly is not there. Five embedded choices accepted: (1) **two pack kinds**, full and plan-check, over one parameterized builder — the pre-fan-out pack has no artifacts to carry and would render empty sections; (2) the **role** fixes the question and the **tier** only the model, so `review` and `architect` are the only roles with a pack question and an unknown role raises instead of defaulting — a silently-defaulted role files an answer to a question nobody asked; (3) the "What to return" footer is **part of the pack**, not the caller's prompt, so both executors get an identical return contract; (4) that footer asks for the precise gap over a diluted verdict; (5) the level verdict rides in the footer, one shape for both executors. **Routed out, not resolved here:** verifying this anchor found `_find_project_root` in seven copies with three behaviours — package-aware and `AITNA_ROOT`-aware, `AITNA_ROOT`-aware only, and neither with `_aitna` hardcoded — every one of them falling back to the cwd silently, so `gate_pack._report_path` can write a pack under a wrong root indistinguishably from a right one. That is one fact with several owners (the C46 class) and the sibling of C69's *which tree*; it is filed as C73 and locked in its own D2 row, because the gate-pack contract holds whichever way the root question is settled. Pinned by test_gate_pack.py; e2e rendered both kinds. | tools/model_routing/gate_pack.py::build_full_pack | 7312972 |
| D2-3 | architecture | C17 coverage-map assembler: delegation-log schema gains session_id + zone columns (zone parsed off a [zone:LABEL] marker in the description at log-write time; ts·session_id·subagent·model·zone·description; legacy 4-col still parsed by stats). coverage_map.py groups in-scope entries (scoped by --session, self-describing key; +optional --since/--until) by zone -> map table + uncovered-seam flags against the §10.3 zone plan; output feeds gate_pack.py --coverage-map. gate_id precision has no carrier and the row previously claimed one: it said the refinement rides on C20's session-state marker, but C20 reads the active role from the transcript instead (D2-4) and no such marker exists — the promise is §10.4 decision 1's, never reconciled with what shipped. session_id is therefore the finest key today (two rounds in one session assemble as one map; narrow with --since/--until), and a finer key whenever one is built changes the key, not the scope-by-key shape. Withdrawn in the row, in coverage_map.py's docstring and under §10.4. Verify: the schema reorder + scoping-by-key design + uncovered-seam semantics match §9.4/§10.3 intent. Pinned by test_coverage_map.py + test_model_routing.py (parse_zone/parse_delegation_entries); e2e verified (assemble -> embed in gate-pack). **Scope of this approval: the architecture only — the schema shape, scope-by-key design and uncovered-seam semantics. The implementation does not yet honour the scoping and legacy-parsing this row promises:** --session is optional so an unscoped run unions sessions and under-reports uncovered zones, --since/--until compare ISO strings so a bare-date bound silently drops a whole day, and the two default readers accept different minimum field counts. All three are C17-owned and registered as C76. **D2-3 and C17 cannot be Verified before C76 closes** — an approval sha here would pin a row the code contradicts. | tools/model_routing/coverage_map.py::build_coverage_map + routing.py::delegation_log_line | 7312972 |
| D2-4 | architecture | C20 conservative Claude role/agent advisory. The harness payload identifies the routed agent, not an authoritative invocation task kind. For a known active role and registered agent, warn iff agent_kinds(agent) ∩ effective_allowed(role) = ∅; silence proves only one allowed overlap, not that the intended kind or a phase-qualified use is allowed. Accepted blind spots include engineer k_reasoner carrying forbidden design-fork beside allowed kinds and architect k_mechanic carrying forbidden edit kinds beside allowed doc-sync. Active role is the transcript's last qualifying main-chain declaration: '🧭 agent: <name>' must be the first non-whitespace text of its assistant turn, the role name is case-normalized, and later inline or Markdown-prefixed examples cannot switch state; a first-position marker is a declaration by definition. Chosen over a written marker for consistency with C22/C23 scanning and one source. The advisory emits no permission decision; healthy and ordinary-Exception paths return 0, while crash/SystemExit posture remains C52/D2-23. D2-4 owns this transcript source and conservative predicate. D2-6 independently owns the cross_cutting_kinds population added to effective_allowed; accepting D2-4 does not accept that population. Rejected: an all-kinds-subset rule (systematic mixed-agent false positives), a description kind marker (new self-reported protocol), and per-kind agents (reopens grouped-by-brief design for advisory precision). No Codex runtime claim. Verify with test_active_role_requires_first_content_and_ignores_quoted_examples, test_active_role_case_normalizes_the_role_name, test_role_matrix_warning_d2_4_base_predicate_and_boundaries, test_role_matrix_warning_renders_effective_allowed_in_registry_order, and the delegation-log wrapper warning/silence/ordinary-exception carriers; test_role_matrix_warning_d2_6_cross_cutting_extension belongs to D2-6. One-shot 49/262 ms observations are not acceptance evidence or an F5 budget. | tools/model_routing/routing.py::role_matrix_warning + active_role; hooks/delegation-log.py | 7312972 |
| D2-5 | architecture | **Owner choice:** the delegation model column is the explicit call override, else the recorded generated-agent binding pin, else `-`. This is the declared selection captured at delegation time, not an independent observation of the model the harness actually launched; a later rebind must not rewrite it. When concrete local aliases exist, generated `k_*` frontmatter pins `model: <alias>` from the tier binding, but an Agent call rarely echoes that pin, so C21's model display was empty until `bound_model_for(config, subagent)` supplied it to `delegation_log_line`; an explicit call model still wins. Host built-ins and semantic-fallback bindings have no generated pin and remain `-` without an explicit override. **Cost:** those rows remain counted under the `-` requested-model bucket instead of being attributed to a named model, and Codex receives neither the record nor the UI display because it does not wire this hook and its adapter drops `systemMessage`. **Rejected:** payload-only attribution, because ordinary generated-agent calls omit the pin; displaying semantic tier labels as models, because that asserts a nonexistent pin; reconstructing old rows from the current binding, because a rebind would rewrite historical attribution. **Repair made at this gate:** the semantic-fallback guard had inverted polarity (`if available and model not in available`) even though fallback has no recorded ladder, so reachable fallback config reported `worker`/`strongest` as model names. The guard now requires a non-empty `list[str]` ladder containing the string binding alias; malformed or stale config also yields no pin. The carrier now builds fallback state through `compute_binding` + `local_config`, and the hook's own config→pin→console/TSV chain has bound and fallback carriers. The fallback and malformed-ladder wrapper carriers fail against the old guard; the bound-model wrapper carrier pins previously uncovered wiring. Pinned by test_model_routing.py (`bound_model_for`, malformed/stale/fallback boundaries, `delegation_log_line` precedence) + test_adapters.py (hook wiring, bound and fallback). Historical pre-A12 live evidence used the then-current names `k-mechanic→haiku` and `k-reasoner→opus`; it demonstrates the Claude display route, not the current underscore spelling. | tools/model_routing/routing.py::bound_model_for + delegation_log_line | 7312972 |
| D2-28 | architecture | C51 shared-finding implementation gate, owner-approved after D2-20. `fix` remains required for `ok`, `warn`, and `error`; an `ok.fix` states the invariant to keep. `sync` adopts the envelope only in `--check` and deliberately keeps 0 clean / 1 drift / 2 planning error, with no warn or strict stream. Every rendered field is one logical line; adopter boundaries escape every separator recognized by `str.splitlines()`, while raw subprocess detail may remain multi-line only on stderr. Sentence-shape enforcement is deliberately bounded: the mechanical carrier rejects a terminator followed by whitespace and the compact uppercase form, while residual natural-language sentence boundaries and imperative mood are review-owned so paths such as `meta/CONCEPT.md` remain valid. The current 57 stable code slugs — 42 verify, 7 devlayer, 4 sync and 4 selfci — and their target/fix semantics are accepted; renaming remains a later architecture change rather than implementation cleanup. Rejected: optional `ok.fix`, removing the ok stream, collapsing sync 2 into 1, LF/CR-only line protection, claiming a complete natural-language parser, and renaming the code vocabulary during adoption. Pinned by the C51 contract suite, its isolated sync carrier and the 34-seed mutation stand. ✅ owner-verified; landed at c41eeb1. | common/findings.py + bin/verify.py + bin/sync.py + meta/bin/validate.py + meta/self_ci.py + meta/tests/test_findings.py + meta/design/stage1-hardening-contracts.md §1 + meta/decisions/0012-stage1-contracts-and-vocabulary.md F1/F2 | c41eeb1 |
| D2-20 | architecture | Owner-verified A12 stage-1 architecture lock, landed at e89f3fe. It owns F1–F22, the stable F4–F6 measurement and runtime protocol, task split and dependencies, seeded-violation yardstick, capability vocabulary and top-level matrix home, generated-ownership modes, two-scope always-loaded ratchet and C60 boundary, runtime declaration, prose-to-A16 seam, the F20 version-stamped vendor tool/matcher inventory and drift posture, the F21 machine-written ledger ownership, exact path/schema and migration contract, and F22's exact verify→sync provider set, fresh read-only actual-state composition and transcript-only active-role residual. Concrete F4 support results, F6 caps and entry mappings, and F5 measurements, formulas and literals are excluded to D2-23 and block only C52. Verify: (a) every F1–F22 choice and cost, including the existing F8 seven-callable table, **F13**, the seeding unit the A17(g) audit forced (the independently violable rule; a rule whose seed needs unmeasured evidence is split, not waived), F20's neutral-owner, inventory, optional-detection, fallback and unknown-version choices, F21's sole writer/default-reader population, paths, v1 schema, migration and exception boundaries, and F22's provider order, once-only D2/caps ownership through verify, fresh/read-only/no-cache boundary, exact ordered equality, C51 rendering/exit matrix and active-role residual; (b) the two-ADR boundary as written — ADR 0012 (contracts and vocabulary, carrying the stage-wide yardstick, F14 retired-code record, F15 classification pin, F17 generated-ownership contract, F18 always-loaded ratchet, F19 capability-matrix home, F20 tool-name inventory/drift contract, F21 ledger ownership/path/schema/migration contract and F22 actual-state status composition contract) against ADR 0013 (survivability and crash posture), including the four register rows deliberately left in the design lock (F9, F10, F12, F16) and the ADRs' `Accepted` status after this verification; (c) the closed and rejected forks; (d) the complete dependency graph and seeded-violation contracts; (e) the stable F4–F6 protocol without approving unmeasured numbers. | meta/design/stage1-hardening-contracts.md + meta/decisions/0012-stage1-contracts-and-vocabulary.md + meta/decisions/0013-hook-survivability-and-crash-posture.md | e89f3fe56d25d6df6b1d763fe90dce24bf8aa6fe |
| D2-7 | architecture | V2 rename synthesizer→auditor across code+docs (owner-locked name trio). Registry: `selection_policy`/`semantic_fallback` key `synthesizer`→`auditor`, `task_kinds` `synthesis-verify`→`audit`, `cross_cutting_kinds`=[independent-review, audit]. routing.py: `Binding.auditor`, AGENT_SPECS `k-auditor` (tier `auditor`, kind `audit`), `local_config`/status/rebind labels, `second_opinion_fallback_model` param. Generated agent `k-synthesizer.md`→`k-auditor.md` (regen). Docs: MODEL/design §2+§9–11, review-flow/design-flow anchors, roles/*.md, TASKS, ADR 0005 V2 addendum. Generic *synthesize*/*synthesis* deliberately kept; ADR bodies + TASKS_ARCHIVE grandfathered. Verify: token-only rename, no semantics changed, no live reference to the old names remains. Pinned by full meta/tests green + ruff clean; `registry_hash` bumped → local config stale, re-init needed. ✅ owner-verified | tools/model_routing/routing.py + registry.json + meta/design/model-routing.md | 7312972 |
| D2-12 | architecture | A10 packaging lock + new mount mode `package` (ADR 0009 + design §mode-package). Locks: option C carrier (CLI + full tree incl. meta/ as package data, hatchling, zero runtime deps, floor 3.9); PyPI name `akmon` (both names checked free via pypi JSON 404); four mount modes — new `package` = consumer dev-group pin (git+https tag until first publish), no `<aitna>/akmon` tree, sync materializes the always-on surface (hooks incl. hook_core + adapters; guardrails) into `<aitna>/.akmon/` with generated banners; tree resolution mount-decoupled (mount else embedded via importlib.resources; root discovery accepts `.akmon.toml`); mount-aware hook templating (`_is_akmon_entry` recognises both markers); skew rule — mounted modes exec the mounted tree, package mode has no skew by construction; `akmon path` added to the CLI contract; init pin = latest tag + `--ref`; V1 sweep before first PyPI publish (pilot exempt). Pilot consumer: alphavar. Verify: (1) `package` as a fourth first-class mode vs "vendored-lite" is the right shape; (2) `.akmon/` as the materialization dir name/location; (3) dev-group pin (never runtime/extra) is the right dependency class; (4) the always-on surface = hooks+guardrails only (nothing else needs in-repo files). Not yet pinned by tests — C37 (unblocked by this lock) implements. ✅ owner-verified (ADR 0009 Accepted) | meta/decisions/0009-packaging-package-carrier-and-mount-modes.md + meta/design/packaging/README.md §Mount mode `package` | 556927e |
| D2-14 | architecture | A11 alternatives-adoption verdict set (ADR 0010): all 7 plan themes accepted with amendments (seam amendment P2.4-vs-reviewer, smallness boundary, links-only manifest, triviality boundary, provisional P2.6/caps); decisions closed — D1 mutability rejected, D2 probe-first, D3 parked to C42 measurement, D4 per-vendor by N2 data; normative change: the essence-first descriptive pattern codified in roles/architect.md + roles/review.md (truncated at the analysis seam). Verify: (1) direction-acceptance of all 7 themes matches the owner verdicts in the walkthrough record; (2) the D1–D4 closures are recorded as decided; (3) the roles/*.md pattern codification is the intended normative change. ✅ owner-verified | meta/decisions/0010-alternatives-adoption-a11-verdicts.md + roles/architect.md + roles/review.md | da51329 |
| D2-24 | architecture | D2 ledger lifecycle has three independent states: Pending awaits owner acceptance; Approved records acceptance without inventing a commit; Verified requires an Approved row and an existing landing sha. Task lifecycle remains separate, so completed-but-unlanded work stays blocked. The tool adds `approve`; `list` and session status expose Approved; `check` warns only on Pending; legacy two-section ledgers remain readable. Verify: this boundary and the required post-landing closure commit are intended. | meta/design/d2-ledger.md + tools/d2_ledger/d2_ledger.py | f6d181c |
| D2-22 | architecture | Clarification of D2-16 warning delivery while C36(a) remains open. Overlay validation and rebind refusal are deterministic, and SessionStart reports the current `BriefError`; the UserPromptSubmit warning at the moment a model switch is suppressed is best-effort because its content-keyed tempdir marker can survive a crashed/reused session id. Exactly-once-per-episode delivery is not claimed until C36 repairs marker lifecycle. Verify: accepting that a stale marker can hide the switch-time duplicate while SessionStart and direct `init.py` remain fail-visible, rather than blocking C50 on the broader marker sweep. **Amended at verification, three ways.** *The window is exact, not vague:* suppression needs **three** coincidences at once — a session id that survived a crash or was reused, the same detected model, and the same warning text (the marker's name is `sha256(session)[:20]`, its content the `model\0warning` digest). Anything else re-arms: a changed model or message writes a new digest, a cleared condition unlinks the marker, a missing session id repeats, and a failed read or write still emits. *The compensation is the reason, not a neighbouring fact:* this is the one place in the contract where akmon knowingly accepts **fail-quiet**, and it is tolerable only because the same condition is reported loudly on two other channels — SessionStart's `BriefError` and a direct `init.py` run. Read without that clause, the row licenses going quiet in general, which it must not. *The acceptance has an expiry:* it holds while C36(a) is open and ends when it lands — this marker migrates onto `hook_core.claim_diagnostic_marker` and gains an age-out, after which best-effort delivery is no longer the contract. ✅ owner-verified (single clause, amended three ways at verification) | tools/model_routing/routing.py::suppressed_rebind_warning + hooks/model-routing.py | f6d181c |
| D2-21 | architecture | C36 Codex malformed-edit diagnostic loudness. The three separately launched path-advisory handlers observe the same bad edit event, so the defect signal is event-scoped rather than handler- or session-scoped: one stderr line is emitted for a reliable `session_id` + `tool_use_id` pair, and a new tool-use id emits again. If either component is absent, or the atomic marker cannot be claimed, every handler repeats fail-visible. A once-per-session throttle is rejected because it can hide continuing payload drift; no throttle is rejected because it emits three indistinguishable lines for every event. Verify: (a) event-level visibility is the right boundary — **verified, and now half of one stated policy rather than a local choice**: D2-19(c) records the pair, route-level diagnostics throttle by session (what a route *can do*, said once), event-level ones by session/tool-use pair (a defect in one call, so the next stays visible). The boundary governs **three** diagnostics now, not one: `codex-unreadable-target` (this row), `codex-unmeasured-path` (D2-18, owner choice iii) and, on the route side, `shell-route` on both vendors. (b) hashing the composite identity into the tempdir marker does not expose vendor ids — **verified as narrowed (owner choice i): the property is claimed for this marker, not for the population.** What hashing actually buys is stated instead: a session id containing `/` would otherwise escape the tempdir, and a tool-use id in a filename is noise; secrecy is not the argument, because three sibling markers (`akmon-role-on-code-<sid>.marker`, `akmon-analysis-guard-…`, `akmon-d2-ledger-…`) still write the **literal** session id at `write_text` default permissions. That split is the convention gap C36(a) already owns and is not re-litigated here. (c) the existing C36 lifecycle repair remains required because these markers still outlive their sessions — **verified, with C36(a) resurveyed at this gate.** One half of it landed meanwhile: D2-19(e) lifted this row's claim idiom out of `codex-hook` into `hook_core.claim_diagnostic_marker`, so the exemplar C36(a) pointed at is now the shared helper, and the Claude `Bash` route creates markers too. What remains: migrate the three sibling markers onto the helper, unify the missing-identity policy — those three key on the literal string `nosession` and therefore **suppress across sessions**, the fail-quiet direction, while the helper repeats — and add the age-out neither has. Recorded so the audit does not rediscover it: those three also claim non-atomically (`exists()` then `write_text`), so two parallel edit calls in one session can emit one reminder twice; the direction is loud, and migration removes it. Pinned by `test_one_bad_edit_event_emits_one_defect_signal_across_handlers`, `test_a_later_bad_edit_event_is_not_hidden_by_the_first`, and `test_bad_edit_without_event_identity_repeats_fail_visible`. ✅ owner-verified (clauses a–c, all three extended; (b) narrowed per owner choice i) | hooks/codex_adapter.py::tool_use_id + hooks/hook_core.py::claim_diagnostic_marker + hooks/codex-hook.py::_report_unreadable_target | f6d181c |
| D2-19 | architecture | C49 Codex PreToolUse route contract. Measured on codex 0.146.0, `Edit`, `Write` and `apply_patch` are aliases for one patch route; the shell route matches as `Bash`. `bin/sync.py` therefore emits `Bash\|apply_patch`, one name per measured route and deliberately not `.*`. A Bash command is path-classified only when it contains both a recognized `apply_patch` invocation and a valid patch envelope; recognized means in *command position* — string start, after a `;`/`&&`/`\|\|`/`\|` separator, at the head of a subshell `(`/`$(` or a `{ ` group, or inside a `sh -c`/`bash -lc` wrapper, never after a bare newline, which is how a heredoc carries quoted data (anchoring on the string start alone let `cd sub && apply_patch …` fall back to unclassified: a bypass is spelled, not typed, and a false positive costs one advisory reminder while a false negative costs the route its only classification). The separator set is wide because the narrowing is carried by adjacency, not by it: the match must also sit on the first line, outside shell quoting, and open the patch heredoc directly, and a brace counts only when a space follows it (`{apply_patch` is one word naming another command). That exact exception depends on C48 path extraction and C47 path normalization. Every other Bash call remains unclassified and emits a generic hook-process stderr diagnostic that the route **may mutate files unseen** — no guessed paths and no guessed write detector. The three separately launched hooks share one atomic marker: with a reliable session id, exactly the first process to claim it emits one combined diagnostic for the session; a missing or unreliable id repeats. Stderr visibility to the owner is unverified and is not part of the claim. Verify: (a) `Bash\|apply_patch` is the right measured matcher — **verified, and the residual is now stated: the matcher is minimal by measurement and therefore silent on drift.** Every diagnostic in this contract lives inside the hook process, so if a later codex unfolds the three aliases into separate routes the matcher stops firing and nothing reports it — this task's own defect class, one level up. Fork closed: **keep the measured matcher** over padding it with `Edit\|Write` as drift insurance (free at runtime, but the config would then assert as routes two names never measured as routes) and over a version-stamped harness inventory that errors at `sync` on an unknown name — that third option is the C46/A12 §8 mechanism and belongs there, not here. (b) invocation plus envelope is the right narrow exception, and command position is the right width for "invocation" — **verified, with the property that makes the width affordable now stated: every unrecognized form degrades to the *reported* shell route, never to silence.** `apply_patch < p.txt`, `apply_patch "$(cat p.txt)"`, `env apply_patch <<EOF`, and an invocation on the second line of a script all stay Bash and draw the route diagnostic, so a false negative costs one line of precision in a message that still fires — not the signal. That is also why the prefix set stops here: chasing `env`/`nohup`/`stdbuf` would go back to enumerating spellings, which the `shlex` fork already closed. (c) the shared atomic marker, one combined reliable-session diagnostic, and repeat-without-id are the right degradation contract — **verified, and the two throttle domains are now recorded as a rule instead of being left to read as an inconsistency**: route-level diagnostics state what a *route* can do and throttle by session, so the second unclassified call in a session is deliberately silent; event-level diagnostics report a defect in one call and throttle by a session/tool-use pair, so the next defect stays visible (`hook_core.claim_diagnostic_marker`). (d) C47/D2-17 and C48/D2-18 are prerequisites for the exception — **both are Approved and the combined behaviour awaits owner landing.** (e) accepting the residual: this makes the measured heredoc route observable to advisories but does not close the hard-deny bypass or make arbitrary Bash effects classifiable. **Extended at verification (owner choice ii): the residual was stated too narrowly — the blind spot is not Codex's.** On Claude the advisories sit on `Edit\|Write\|MultiEdit`, so a write arriving through `Bash` (`python3 - <<EOF`, `cat > f <<EOF`) was not merely unclassified there but *unreported*, while Codex at least said the route may mutate files unseen. The diagnostic is therefore vendor-neutral now — one implementation and one wording in `hook_core.report_unclassified_shell_route`, emitted on Claude from the `Bash` guard process that was already wired, so the statement costs no extra process on the hottest tool. Two copies of a diagnostic are two things that can drift into disagreeing about what akmon can see. **Not** extended to Claude: the `apply_patch` classification itself, which is measured on codex only. Owner visibility of hook stderr remains unverified on both vendors and is not claimed. Pinned by `test_codex_pretooluse_matcher_names_routes_not_spellings`, `test_a_patch_carried_by_the_shell_is_an_edit_not_a_shell_call`, `test_patch_looking_text_without_apply_patch_invocation_stays_shell`, `test_an_apply_patch_invocation_is_recognized_in_any_command_position`, `test_role_advisory_fires_on_a_patch_the_shell_carried`, `test_role_advisory_fires_on_a_prefixed_patch_the_shell_carried`, `test_analysis_advisory_fires_on_a_patch_the_shell_carried`, `test_d2_advisory_fires_on_a_patch_the_shell_carried`, `test_every_nonpatch_shell_route_gets_one_generic_diagnostic_across_handlers`, `test_shell_diagnostic_without_session_id_repeats_fail_visible`, `test_shell_diagnostic_marker_is_atomic_across_handlers`, and `test_shell_diagnostic_marker_failure_repeats_fail_visible`; 504 passed, ruff clean, self-CI exit 0. The cross-vendor half added at verification is pinned by `test_the_shell_route_diagnostic_is_reported_on_claude_too`, `test_the_claude_shell_diagnostic_leaves_the_guard_decision_intact`, `test_a_non_shell_claude_call_gets_no_shell_route_diagnostic` and `test_both_vendors_emit_one_shell_route_wording`; 533 passed, ruff clean, self-CI exit 0. ✅ owner-verified (clauses a–e; (a), (b), (c) and (e) extended, (e) per owner choice ii) | bin/sync.py::_codex_hooks/_claude_hooks + hooks/codex_adapter.py::tool_kind/is_apply_patch_command + hooks/hook_core.py::report_unclassified_shell_route/claim_diagnostic_marker + hooks/git-commit-guard.py | f6d181c |
| D2-18 | architecture | C48 Codex payload contract: the advisory hooks read the key Codex actually sends, and a matcher that fires with no readable path is now a signal instead of silence. `codex_adapter.file_paths` sourced the patch body from invented keys (`tool_input.patch`, `payload.patch`) that no measured Codex version sends; codex 0.146.0 puts the whole patch in `tool_input.command` — the key the adapter's own `command()` already read. Result: `file_paths` returned [] and all three wired PreToolUse hooks (role-on-code, analysis-guard, d2-ledger-reminder) returned None on every Codex edit since they were wired. Three changes: (1) the patch body now comes from `command()`, with the invented `patch` keys removed rather than kept as insurance — a key list that looks thorough while missing the only real one is what hid the defect; `raw` stays because it is this module's own malformed-JSON fallback, not a guess; (2) `codex-hook.py`'s three near-identical advisory bodies collapse into one `_advisory(payload, decide)`, so a payload-reading defect can no longer hide in a copy; (3) when the normalized tool is the edit kind and no path can be extracted, the wrapper reports it on stderr with the payload's key names (never its values — a patch body carries file contents), advisory-only, never blocking. Verify: (a) dropping the speculative `patch` keys in favour of measured-only reading plus a loud unknown-shape signal is the right tolerance line — the alternative keeps dead keys that read as coverage. **Extended at verification (owner choice iii) to the *path* keys the change left untouched:** `file_paths` still tolerates nine guessed keys (`file_path`, `filePath`, `path`, `target`, `filename` and their list forms), none of which any measured Codex version sends, and a guess that *matches* is worse than one that misses — an empty list raises the no-path signal, while a wrong path is classified in silence. Ranking measured-first was rejected as insufficient for exactly that reason. The keys stay, because a probable target beats no target, but `codex_adapter.unmeasured_path_source` now separates patch-body provenance from guessed-key provenance and `codex-hook._report_unmeasured_path_source` states it on stderr once per session/tool-use identity while the advisories still run. Pinned by `test_a_path_from_an_unmeasured_key_still_advises_but_says_so`, `test_the_measured_patch_body_is_not_reported_as_unmeasured`, `test_unmeasured_path_source_is_false_when_the_patch_body_also_names_files`. **Related gap recorded, not closed:** `_PATCH_PATH_RE` reads `*** Add\|Update\|Delete File:` only; the apply_patch **rename** form (`*** Move to:`) is unmeasured and appears nowhere in the repository, so a rename would classify under its source path and its destination not at all — C48's own silent shape. Measurement assigned to N1 rather than a speculatively widened pattern, which this same clause forbids; (b) stderr is the right channel for that signal (owner/dev sees it, the model does not, no token cost, no block) rather than `additionalContext` or a nonzero exit; (c) the residual risk stated in the backlog is accepted, not closed: akmon still tracks a vendor payload shape by sample, and a pinned sample only catches drift when it is refreshed — the stderr signal is what makes the next drift visible. Tests are built on the payload captured live from codex 0.146.0, not an invented shape; the shell route is deliberately exempt from the signal (a command is not a patch — that is C49). Verified at process level too: all three hooks fire on the captured payload against a fixture repo, notes.txt stays silent, and an unrecognized `tool_input` key produces the stderr line. 480 passed, ruff clean, self-CI exit 0. ✅ owner-verified (clauses a–c, (a) extended per choice iii) | hooks/codex_adapter.py::file_paths/_paths_by_source/unmeasured_path_source + hooks/codex-hook.py::_advisory/_report_unreadable_target/_report_unmeasured_path_source | f6d181c |
| D2-17 | architecture | C47 path-classification normalization: one form for every predicate. `is_planning_doc` and `is_code_path` matched slash-delimited segments (`/_aitna/design/`, `/docs/`) against the raw payload string, so a **relative** path never matched and the hook returned None silently — "did not match" was indistinguishable from "matched and stayed quiet". Vendor-shaped exposure: Claude Code sends absolute `file_path`, Codex passes through the repo-relative paths its patch bodies carry. New `classify_target(file_path, root)` renders any input as project-relative POSIX, lowercased, leading-slash, and all three predicates (planning-doc, code, D2-sensitive) now share the single normalizer `_project_relative_posix`, which was previously used by the D2 branch alone. A relative input is anchored to the payload-derived project root and canonicalized there rather than against the hook process cwd; `.` and `..` collapse before matching, and a target that leaves the project matches no project-local predicate. **Narrowed at verification (owner):** that holds once a root is known. The root itself comes from the payload's `cwd`, and a payload carrying none falls back through `find_project_root(None)` to `Path.cwd()`; `classify_target(root=None)` likewise preserves the pre-C47 unstripped behaviour as a standalone API. The old style therefore survives behind a default argument, defended only by the convention that every wrapper passes a payload-derived root. Left as it is by choice: making `root` mandatory on the three predicates and renaming the standalone API would change signatures to fix a sentence, with no behavioural gain in any wired path. **That collapse is lexical (`os.path.normpath`), not `Path.resolve()`** — a distinction that is itself a verify point, because resolving follows symlinks: a symlinked subtree pointing outside the repo (an `_aitna` dev layer kept elsewhere, a monorepo's shared source, an `_aitna/akmon` linked at a developer's akmon checkout) landed outside the root and silenced all three predicates underneath it, reproducing C47's own silent-non-match failure inside the traversal guard added to harden it. Found in review and fixed before landing; not live in alphavar, whose tree carries no symlinks. The resolved comparison survives only as a fallback for the opposite case — root and payload naming one directory through different aliases (`/tmp` vs `/private/tmp`) — and a traversal escapes both forms, so the guard holds. Second defect closed by the same change: the project root is now stripped before segments are matched, so a repository living under a directory named `docs` no longer classifies every file in it as non-code. Root is threaded from the payload's `cwd` on both vendors (`codex-hook._payload_root`, new `claude_adapter.project_root`) instead of the process cwd, so `role_on_code_result` and `analysis_write_result` gained an optional `project_root` (D2's already had one). Verify: (a) treating a relative path as root-relative — not cwd-relative — is the right reading of the only source of relative paths (a Codex patch body); (b) the test suite now pins the **invariant** (both forms of a canonical path give the same answer across 3 predicates, with additional `.`/`..`/outside-root cases) rather than per-case answers, plus non-equality assertions so a uniformly-wrong answer cannot pass; (c) the deleted test `test_is_code_path_segment_match_requires_surrounding_slashes`, which pinned the defect as an accepted sharp edge, was documenting the bug and is gone by intent; (d) rejecting a path that *leaves* the project while accepting one that is merely *linked* is the right boundary, and lexical-first-with-resolved-fallback is the right way to hold both ends rather than picking one — pinned by `test_a_symlinked_subtree_is_still_a_project_target` and `test_a_root_named_through_a_symlinked_alias_still_matches`. **Recorded as an explicit non-property at verification (owner): this is an advisory-grade classification boundary, not a containment boundary.** It follows from the lexical-first rule itself — a link inside the tree pointing *out* of the repository (`<root>/link` → `/etc`) leaves `<root>/link/passwd` lexically inside the root, so it classifies as a project target. `..` is stopped; a link outward is not, and cannot be without resolving, which is the silencing failure this choice exists to avoid. Harmless while these predicates feed advisory reminders only — the worst outcome is one reminder too many — but it **must not be inherited as a security property** by later deny-class work (F3/C52), which owes its own boundary on its own evidence. Verified at process level in a scratchpad fixture, not only in unit tests: the exact G2 payload (relative `_aitna/design/probe-n2-hookfire.md`) now emits the analysis-before-mutation reminder, the absolute form still does, a relative `src/x.py` fires role-on-code, and `notes.txt` stays silent. Scope boundary: C47 governs classification after a path is extracted; C48 separately owns the measured `tool_input.command` extraction contract and is now code-complete with its owner gate Approved, awaiting landing. 480 passed, ruff clean, self-CI exit 0. ✅ owner-verified (clauses a–d) | hooks/hook_core.py::classify_target/_project_relative_posix + hooks/codex-hook.py + hooks/claude_adapter.py::project_root | f6d181c |
| D2-16 | architecture | C50 overlay-brief key contract (fixes the regression [D2-15] introduced). Overlay `briefs` keys are hand-authored in the consumer's repo and were matched against `spec.name` exactly, so the `k-*` → `k_*` rename orphaned alphavar's four live briefs and `remove_obsolete_agents` then deleted the `k-*.md` files still carrying them. Three behaviour changes, all owner-visible: (1) lookup is notation-insensitive (`agent_key` folds case and `-`/`_`), so an unmigrated overlay keeps working with no migration step in the consumer; (2) a key matching no agent — and two keys resolving to one agent, and a non-string/non-object shape — is now a hard `BriefError` instead of an empty brief; (3) on that error `init.py` writes nothing and exits 2, and the SessionStart/UserPromptSubmit hook **refuses the rebind** and reports the bad key on the owner channel, so delegates stay pinned to the previous model until the overlay is fixed. Verify: (a) refusing to regenerate is the right trade against rebinding-minus-briefs — it stops a lossy write, and it costs a stale model pin while the overlay is broken; (b) forgiving notation but not spelling is where the tolerance line belongs (a typo must fail, a rename must not) — **widened at verification (owner) to state what `agent_key` already does**: it also folds surrounding whitespace, so the line is *invisible and notational differences are forgiven; any difference in significant characters is not*. The `strip()` is kept rather than removed to match the narrower wording, because leading or trailing whitespace in a hand-authored key does not survive a diff and a lookup that fails on it costs far more to diagnose than the tolerance costs to hold; an **internal** space remains significant and still fails. Pinned by `test_overlay_brief_key_tolerance_covers_whitespace_but_not_spelling`; (c) accepting that overlay keys remain a second place where agent names are a public contract, to be folded into C46's neutral declaration later. Checked read-only against alphavar's live overlay: all four hyphenated keys resolve. Pinned by test_generated_agent_brief_survives_the_agent_name_notation_change, test_unmatched_overlay_brief_key_is_an_error_not_an_empty_brief, test_overlay_briefs_colliding_on_one_agent_are_an_error, test_overlay_briefs_reject_non_object_shapes_including_falsy_values, test_overlay_briefs_reject_non_string_values, test_brief_warning_names_the_bad_key_and_the_file_to_fix, test_hook_refuses_the_rebind_while_the_overlay_briefs_are_unusable, test_hook_user_prompt_submit_reports_the_suppressed_rebind_once_per_episode, test_hook_user_prompt_submit_stays_silent_on_a_bad_overlay_without_a_switch, test_init_writes_nothing_when_an_overlay_brief_key_matches_no_agent; 480 passed, ruff clean, self-CI exit 0. Re-checked read-only at verification: alphavar's live overlay carries four hyphenated keys (`k-mechanic`, `k-validator`, `k-implementer`, `k-reasoner`) and all four resolve, so without C50 every one of them would have been orphaned. ✅ owner-verified (clauses a–c) | tools/model_routing/routing.py::resolve_briefs/agent_key + hooks/model-routing.py + tools/model_routing/init.py | f6d181c |
| D2-15 | architecture | A12 agent-name notation lock (ADR 0011): all six generated delegates renamed `k-*` → `k_*`, one notation for every vendor. Forced by codex 0.146.0, whose `agent_name` accepts only lowercase letters, digits and underscores (read from the shipped binary; probe F2 was rejected outright) — the hyphen made all six names illegal there, while Claude's permissiveness had silently encoded one vendor's rule as a cross-vendor contract. Measured before deciding: Claude Code 2.1.221 offers a `k_`-named definition as a subagent type alongside hyphenated ones. Scope boundary: live code, tests and currently-true docs renamed; records of what was decided or observed at a time (ADRs 0004/0005/0006/0008/0010, `meta/reviews/**`, CHANGELOG, this ledger's earlier rows, V2's owner-locked name trio, and the hyphenated names quoted inside C46 as the defect) deliberately left hyphenated. Migration shipped as part of the change: `routing.obsolete_agent_files`/`remove_obsolete_agents` prune generated agent definitions the current `AGENT_SPECS` no longer plan, on every init and every hook rebind, gated on `GENERATED_BANNER` so hand-written agents are never deletion targets — without it the rename would duplicate rather than migrate, since `bin/sync.py`'s sweep only ever covered `.claude/skills/*/SKILL.md`. Verify: (1) one shared notation is preferred over a per-vendor name map (which would give one delegate two identities and presupposes C46's unbuilt mapping machinery); (2) the renamed-vs-preserved boundary is drawn where the owner wants it — in particular that historical records keep the old names; (3) accepting that delegation-log rows and subagent transcripts written before the rename no longer resolve to a tier via `agent_tier_map()`, so per-agent stats split at the rename boundary, rather than rewriting the log. **Fork closed (owner): the split is accepted; folding notation in the stats path is rejected.** The asymmetry with `resolve_briefs` — which folds case and `-`/`_` through `agent_key` (D2-16) while `stats.label_and_tier` matches exactly — is deliberate, not an unfinished migration: a brief key is a consumer's *address for a live agent* and must survive a rename, whereas a log row is a *record of what ran* under a name that no longer exists. Applying the same fold in `stats.py::label_and_tier` would silently join pre- and post-rename runs into one continuous per-agent series and erase the boundary the record exists to show — the same falsification, one layer up, that rewriting the log was rejected for. A reader who wants the joined view can state the mapping explicitly at read time. Pinned by `test_rebind_prunes_renamed_generated_agents_but_keeps_hand_written_ones`, `test_obsolete_agent_files_reports_without_deleting_in_dry_run`, `test_obsolete_agent_files_empty_when_agents_dir_missing`; 427 passed, ruff clean, self-CI exit 0. ✅ owner-verified (clauses 1–3) | meta/decisions/0011-agent-name-notation-k-underscore.md + tools/model_routing/routing.py::AGENT_SPECS/obsolete_agent_files + tools/model_routing/init.py + tools/model_routing/stats.py::agent_tier_map/label_and_tier | d4e5246 |
