# Archived tasks — akmon

Completed akmon work, one terse line each. Format: [`pipelines/tasks.md`](../pipelines/tasks.md).
Active backlog: [`TASKS.md`](TASKS.md).

## Done

- C84 · the package-mode pin is read from the manifest's TOML, not its lines · done · engineer · **Found:** the line scan under `package_pin_status` split every line at its first `=`, so an element of a multi-line array — `"akmon==0.4.0",`, the shape `uv add --dev` and formatters write and the pin the packaging design names once akmon is on PyPI — read as a key named `"akmon`, and `package_pin_status` answered `none`: `verify` warned a correctly pinned project, `init` exited 1, and a versioned runtime pin in a multi-line `dependencies` was not caught at all. A probe over 17 manifest forms misread 11: every versioned multi-line element (extras, a URL with `#sha256=`, a marker, single quotes, pdm) and the poetry inline-table and dotted-key spellings. The line scan's reason — no runtime dependency — had lapsed with the 3.11 floor (ADR 0009, C68 amendment). **Owner decisions (this session):** (1) the manifest is parsed with `tomllib`; `bin/sync.py::_read_manifest` joins the C83 `tomllib` list — the review point of this change; (2) a manifest that does not parse is a fourth answer, `unreadable`: `verify` warns `package.dev-pin` naming it (`--strict` fails) and `init` exits 1 naming it, rather than both reporting a missing pin. Also read now: poetry's pre-group `[tool.poetry.dev-dependencies]`. Carriers, red before the fix: meta/tests/test_sync.py — `test_package_pin_status_reads_a_versioned_element_of_a_multiline_array` (8 forms), `test_package_pin_status_reads_the_declaration_not_its_spelling` (5), `test_package_pin_status_reports_a_manifest_that_is_not_valid_toml`; meta/tests/test_init.py — `test_package_attach_names_an_unreadable_manifest_as_the_cause` and two rows of `test_package_pin_status_reads_the_manifest_not_the_word`; meta/tests/test_verify.py — `test_package_pin_check_reads_the_manifest_it_gates_on` · bin/sync.py::package_pin_status + bin/verify.py::check_package_pin + src/akmon/_init.py + meta/tests/test_record_owner.py · provenance: owner (this session)
- C23 · context-pressure detection · done · engineer · registry `context_pressure` block; the model-routing hook reads last main-chain `usage` in the same transcript pass, banded warnings (0.85/0.95 of a recommended maximum context, not the model's limit — owner-verified at D2-38, landed in `920fe69`) throttled per band via session marker, reset when the fill drops below the lowest band, dual-channel delivery; tested incl. live transcript (74% → silent) · [design §12](design/model-routing.md)
- C80 · context pressure — the active-context pressure sensor: evidence, info 0.85 / warn 1.0, env override · done · engineer · **Role:** C80 is only the pressure sensor of the active context; the boundary between tasks is A21's, the Codex port — with its own measured commands and delivery channel — is C81's, and prevention, keeping large raw results out of the main context, is C29's. **Owner decisions (this session):** `recommended_max` is the recommended active-context budget for a session intended to carry one task — a session carries the task it is solving, not its history (fewer tokens, less drift from the task, less forgetting), and the warning is that reminder; the code knows neither the task nor what it needs, only the prompt fill. 200000 stays — a configurable owner policy, checked against measurement: above where sessions get compacted in practice (Claude by hand at a median of 167,968; Codex main sessions at 123–174k) and below the 258,400-token window Codex reports for every OpenAI model, gpt-6-astra included. Two levels replace the bands: **info** at `info_ratio` 0.85 (`/compact`) and **warn** at the budget itself, 1.0 (`/compact`; new task `/new`) — the share and the command, no token count, no advice to the agent, shown to the owner only (`systemMessage`, not the model's context); warn fires once per pressure episode, also on a jump past 100%, and nothing repeats past it — going on is the developer's call; a fill below the info level resets the episode, whatever lowered it; `AKMON_CONTEXT_RECOMMENDED_MAX` overrides the budget per user or per project; no technical limit is stored beside it. C80 does not depend on C82: its documents cite its own evidence review, not measurement rows. Evidence: [replay + sources](reviews/c80-context-fill-20260912.md), [design §12.5](design/model-routing.md) · tools/model_routing/registry.json (context_pressure) + tools/model_routing/routing.py::recommended_max_context/context_pressure_notice + [design §12](design/model-routing.md) · provenance: owner (this session)
- C75 · one owner for the integration record · done · architect+engineer · The `.akmon.toml` readers were re-examined one at a time. Folded into `common/record.py`: `tools/release/release_check.py` (its "does not import sync" had been stale since C74), `tools/model_routing/init.py::_standard_tree_root` (its materialized-copy premise went with C74/C77), and `src/akmon/cli.py::_mounted_akmon_root`. That last one asks the *embedded* tree's copy: the dependency it has to avoid is the consumer's tree, not the one that ships with it. **Two callers the list missed:** `src/akmon/_init.py::_recorded_mount` borrowed the CLI's private reader and now reads through the same embedded module; `hooks/hook_core.py::d2_sensitive_paths` had kept its own bare `tomllib` call and now reads through the shared reader — on a malformed record it yields no globs, as it did before, while `d2_ledger check` fails loudly on the same record. **Kept local, for a reason the old justification did not give:** `tools/d2_ledger/d2_ledger.py::_read_akmon_toml` reads the `sensitive_paths` array and must fail on a malformed record, but the shared reader is lenient by contract — a line parser that yields only strings. Folding it would let a broken record switch the D2 path check off silently. Behaviour change: a malformed record no longer crashes `release_check`; it is read leniently, like every other caller reads it · [note](design/packaging/mount-resolution-owner.md) · provenance: C69 landing measurement
- C79 · skills reach every harness's selector — stub frontmatter, `.agents/skills`, the Agent Skills contract, result-first descriptions · done · engineer · **Found:** `sync` wrote each skill's `.claude/skills/<name>/SKILL.md` as a pointer without frontmatter and gave Codex no skills directory, so Claude Code listed every stub under its bare name (M54: 7 of 7 in alphavar) and Codex refused to load one (M55) — no description reached a selector, and both shipped descriptions led with the procedure. **Owner decisions (this session), D2-41:** the stub carries the source's frontmatter verbatim, banner as a YAML comment on line 2, in `.claude/skills/` and `.agents/skills/`, for every skill `sync` finds; the trigger goes in `description` and `when_to_use` is no longer required; `owner` moves to `metadata.owner`; a description states in prose the expected result, then the trigger, within 1,024 characters; `verify` checks the keys, the Agent Skills limits and an unquoted ` #` or `: ` in a value (the harnesses cut at ` #`, the standard's validator rejects `: `; M56, M57), and "names a result" is judged on review. The vendor guidance is read from the vendors' own documentation only. **Consumer break:** a top-level `owner` fails `verify` until moved under `metadata:` — CHANGELOG Breaking/Migration. Evidence: [vendor guidance + probes](reviews/c79-skill-guidance-20260913.md), M54, M55 · bin/sync.py::_skill_stubs + bin/verify.py::check_skill_contract + skills/*/SKILL.md · provenance: owner (this session)
- C83 · one reader of the integration record, enforced · done · engineer · C75 folded the narrow `.akmon.toml` readers into `common/record.py`, but nothing stopped a new one from appearing — that is how five grew. Two static carriers over shipped Python (`bin`, `common`, `hooks`, `src`, `tools`), docstrings excluded: (1) only three functions import `tomllib` — `common/record.py::read_akmon_toml`, the deliberately strict `tools/d2_ledger/d2_ledger.py::_read_akmon_toml` (D2-39), and `tools/release/release_check.py::_pyproject_version`, which reads `pyproject.toml`; (2) only the listed modules name the record's path, each with its use — through the shared reader, a writer, or an existence check. A new entry in either list is a review, not a fix. Not caught: a hand line parser inside an already-listed module · meta/tests/test_record_owner.py + common/record.py · provenance: D2-39 review (this session)
- C22 · orchestrator detection: corridor + owner-visible warnings · done · engineer · transcript detection + rebind + gitignored `k_*` + corridor warning in `compute_binding` (named floor `opus`, below-floor + reserved-top arms, silent collapse when `floor == top`, skip when floor not in ladder) + dual-channel owner-addressed output (`systemMessage`); e2e-verified on a live session · [design §3+§4.3](design/model-routing.md)
  - **Landed in `7312972`**, owner-verified at D2-37 — accepted on Claude, with the Codex half (hook wiring and a user-facing channel for `systemMessage`) recorded as Codex-pending.
- C18 · docs: leverage principle + role/pipeline deltas · done · engineer · name the leverage principle in MODEL.md; tier table + matrix rows update; review/design-flow auditor anchors (post-fan-out + the pre-fan-out plan check) + loop-back edges; roles/*.md one-paragraph deltas linking §10 incl. the drafts-vs-decides invariant reading; demote static floor warning to weak prior. **Also implement A7 (b): registry `cross_cutting_kinds` = [`independent-review`, `audit`] + a `role_matrix_warning` exemption so C20 no longer warns on cross-cutting verification kinds for any role + test + ADR 0005 addendum** — else C20 warns against the very audit routing C24/C25 enable (auditor seam 1); these anchors are the missing *process* half that says *when* to launch the auditor (seam 2) · [design §9.1–9.5+§10](design/model-routing.md) · provenance: verdict.md seams 1–2 (local, untracked)
  - **Landed in `7312972`**, owner-verified at D2-6; C24 and C25 unblock.
- C31 · background/child-session `ask` enforcement gap · done · engineer · owner decided the posture without a foreground repro first (skip diagnosis, harden and observe): `hook_core.py::_escalate_unattended_ask` escalates any `ask` from `git_commit_guard_result`/`delegation_nudge_result` to a hard `deny` whenever `permission_mode` isn't the interactive `default` (field missing = worst case, escalates too); both Claude wrappers thread `permission_mode` from the PreToolUse payload. Codex not wired to either hook yet, so out of scope. Owner now watches live behaviour (false-block fatigue vs. real gap closed) rather than gating on further isolation of background-vs-`acceptEdits` · [D2-11](D2_LEDGER.md) + model-routing.md §13.2 · provenance: D2-10 + owner decision (this session) · was **ranked #1 in [review-20260705](reviews/review-20260705.md)** (D5's commit guard rides the same channel; the audit session itself ran background, i.e. inside the gap)
  - **Landed in `ac54a5a`**, owner-verified at D2-11; its diagnostic record D2-10 landed in `7312972`.
- C16 · gate-pack builder · done · engineer · one structured package (artifacts + yardstick + coverage map) consumed by auditor subagent and second-opinion CLI, plus the minimal plan-check pack (yardstick + zone plan, pre-fan-out); replaces free-form `--prompt-file` in second_opinion.py; runnable as one deterministic script per §11. **Also wire the second-opinion diversity ladder** (currently dead registry data from C15): `second_opinion.py` selects provider *and model* by `second_opinion_policy.diversity_ladder` — other-vendor → same-vendor-other-model (via `second_opinion_fallback_model`, computed into config but unconsumed) → never same-model · [design §9.4+§9.3.5+§4.6](design/model-routing.md)
  - **Landed in `7312972`**, owner-verified at D2-1 and D2-2.
- C20 · role-matrix advisory check in delegation hook · done · engineer · conservative Claude agent-level signal: for a known active role and registered agent, warn iff the agent's carried kinds have no intersection with the role's effective allowed set. Silence proves one allowed overlap, not that a mixed agent's intended kind or a phase-qualified use is allowed. D2-4 owns this predicate and the active role read from the transcript's last qualifying main-chain declaration (first non-whitespace assistant text, role name case-normalized, later inline and Markdown-prefixed examples ignored); D2-6 separately owns the `cross_cutting_kinds` population added to the effective set. Advisory emits no permission decision; healthy and ordinary-Exception paths return 0, while C52/D2-23 owns crash/SystemExit posture. **Also (D2-5):** the delegation hook records the call's explicit model override, else the generated agent's recorded binding pin (`bound_model_for`), else `-`; this is the declared selection at delegation time, not an independently observed runtime identity. Host built-ins and semantic-fallback bindings therefore remain `-` without an explicit override; the semantic guard stood with inverted polarity and was repaired at the D2-5 gate, with malformed ladders rejected and the hook's config→pin wiring pinned for the first time · [design §4.4+§10.2+§10.4](design/model-routing.md)
  - **Landed in `7312972`**, owner-verified at D2-4 and D2-5; the `cross_cutting_kinds` population stays with C18/D2-6.
- V2 · rename synthesizer → auditor · done · architect+release · "synthesizer" doesn't convey the essence (a clean-context **audit** of a whole gate's material), and "review" is already the DEVELOP analysis role — owner picked "аудит". Name trio (owner-locked): agent `k-synthesizer`→`k-auditor`, task-kind `synthesis-verify`→`audit`, tier `synthesizer`→`auditor`. Scope: `registry.json` (agent def, `role_task_kinds` keys, tier→model binding, `gate_triggers`/policy refs), `routing.py`, `gate_pack.py` (ROLE + k-synthesizer refs), generated `agents/*.md`, design §9–11, `MODEL.md`, ADR 0004/0005, D2 ledger entries. Cheap now (alphavar sole consumer); sequence after the C16/C17/C20→C18 wave to avoid double doc-churn; fold in the C18 engineer-row fix
  - **Landed in `7312972`**, owner-verified at D2-7.
- V3 · restore the missing v0.3.0 changelog boundary · done · release+engineer · tag `v0.3.0` exists at `69ba3b0`, but both its snapshot and the live CHANGELOG jumped from `## Unreleased` to `## v0.2.1`; consumers therefore could not compute the released v0.3 delta from headings. **Delivered:** `## v0.3.0` now contains byte-for-byte the tag snapshot's former Unreleased payload, cross-checked against `v0.2.1..v0.3.0`; live Unreleased contains only post-tag work, and the post-tag C9 line was removed from the historical v0.2.1 section. `1a9ab0f..` was not used as the release range: `1a9ab0f` is a child after the tag and already carries next-cycle C39/package fixes. This historical repair precedes C54. **Both open ends are now closed by owner choice F9:** the current-state choice is **`0.4.0.dev0` + `## Unreleased`**, executed inside C54 so the live pair stays P1.3's acceptance evidence; and the history-completeness check **is in scope** — every tag `vX.Y.Z` should have a `## vX.Y.Z` heading — as a **warn**, because tags are immutable and a hard error over a gap that can never be repaired would simply be waived. A check of that shape would have caught what V3 repaired by hand · CHANGELOG.md + git tag/history · provenance: release-hygiene review,
  - **Landed in `f6d181c`**, closed on owner review; the tag-heading completeness warn it asked for shipped with C54 (`release.undocumented-tag`).
- C39 · Codex runtime contract + package hardening for v0.4 · done · architect→engineer · direct delegation anchor in the hand-owned AGENTS block + verifier gate; package-aware hook root/runtime dependencies; Codex SessionStart orchestration policy; self-hosted akmon AGENTS; honest capability matrix and package/release docs; installed-wheel/nested-cwd regression coverage; development version 0.4.0.dev0. Hard enforcement, named agents, and child-model routing stay unsupported until live Codex protocol probes prove them · [design](design/codex-runtime-contract.md) · D2-13
  - **Landed in `1a9ab0f`**, owner-verified at D2-13 (clauses a–c).
- C54 · version ↔ changelog cross-check (plan P1.3) · done · engineer · **Owner choice F9** ([§4](design/stage1-hardening-contracts.md)). The shape is not "one mismatched pair" — the tree carries **four independent version carriers** (`pyproject.toml` `version`, `src/akmon/__init__.py::_STATIC_VERSION`, the topmost CHANGELOG heading, the git tag) and nothing relates any two; the first two already disagree today. **The defect has already shipped:** the tree at tag `v0.3.0` carries `0.3.0.dev0`, so a wheel built from the reviewed state is misnamed, and `1a9ab0f` then wrote `0.4.0` — the literal `0.3.0` never existed. It reproduces because `run_plan` stages only `CHANGELOG.md` and never a version literal, while `__init__.py` claims in a comment that the pipeline "cuts the real version from the git tag", which is false under a static hatchling version. Consumer cost: package mode stamps `akmon_version` PEP 440-shaped (`bin/sync.py::_package_mode_akmon_toml`), mounted mode stamps `git describe` (`v`-shaped), and the bump procedure computes its window from CHANGELOG headings, which `0.3.0.dev0` matches none of. **The check:** non-final version (full PEP 440 set — `.devN`, `aN`/`bN`/`rcN`, `.postN`, `+local`, not `.devN` alone) requires `## Unreleased`; final (exactly `X.Y.Z`) requires the topmost released heading to equal it; final with a matching tag is a **warn** (re-release); `pyproject` version and `_STATIC_VERSION` must be **literally equal**; a tag with no `## vX.Y.Z` heading **warns**; no version source at all emits an explicit **skip finding**, never silence; comparisons normalize the leading `v` and read a `describe` suffix as "ahead of that tag". **Same task, outside the checker:** `run_plan` prints the version bump and stages `pyproject.toml` + `src/akmon/__init__.py` before `git add`, and the false `__init__.py` comment is corrected. **Current-state repair: `0.4.0.dev0` + `## Unreleased`, executed inside this task, not before it** — P1.3's acceptance is the live pair observed failing and then passing; repairing early leaves the check with a fixture where it could have had the tree. **Forks closed:** the git tag is **not** authoritative (a dev version legitimately runs ahead, so tag-equality would fail continuously and get waived); a single version literal is **impossible**, not merely unwanted (`pyproject.toml` is excluded from the packaged tree), and `hatch-vcs` fails the same dev-ahead-of-tag case plus mounted modes with no metadata; a missing version source must **not** error (it fails every non-Python consumer) and must not pass quietly either; a tag with no heading must **not** error (tags are immutable, so the gap can never go green and the error would be waived). The `v`-prefix defect this exposed is **C61**, not part of this task. **The two repairs outside the checker are seeded too (A17(g), per F13):** a `run_plan` that stages `CHANGELOG.md` without both version literals fails, and one that reaches `git add` before the bump step fails — their absence is what makes this shape's own defect reproducible, and a checker reporting a mismatch the procedure keeps re-creating is the theatre this task already rejects **Delivered:** `tools/release/release_check.py::check_release_versions` joins all four carriers and reports through the C51 envelope — `release.version-literals`, `release.changelog-window`, `release.retag` (warn), `release.undocumented-tag` (warn), `release.check-skipped` (warn) — wired into `--check`, which fails on an error and not on a warn. The live tree was observed **red** (`0.4.0` vs `0.4.0.dev0`, and final `0.4.0` over a topmost released `## v0.3.0`) before the F9/1 repair and green after it, in that order. Both repairs outside the checker landed: `--plan` now prints the bump first, names both literals and stages both, and `__init__.py` no longer claims the tag produces the built version. F9/6's normalizer was **lifted, not copied** (C61's carried-forward note): the new stdlib-only `common/versions.py` owns `split_version` + `is_final`, the CLI skew notice delegates to it, and the lift exposed a **third** owner — `_init._tag_for_version` classified by substring, missed `.postN`, and resolved an installed `0.4.0.post1` to the non-existent tag `v0.4.0.post1`; it now asks the same owner, which is a behaviour change in `akmon init` beyond this task's locked scope. 72 new carriers (1117 pass); ruff clean; `self_ci` exit 0; `validate` exit 0; all 30 shipped files compile on 3.9. **A second, adversarial pass** seeded twenty mutations of the checker, `common/versions.py`, `_tag_for_version` and the plan; all are caught now, and the three real defects it found are repaired here rather than filed: tag coverage returned **silently** when `CHANGELOG.md` was absent (the very silence F9/4 removes), the tag list was filtered to `vX.Y.Z` so a version-shaped tag without the prefix vanished from both git-dependent rules with nothing said (owner ruling: `vX.Y.Z` is the only admissible tag spelling, so it is now reported as `release.tag-spelling` and then excluded), and `--plan` stopped at the push — leaving a released version whose tag exists, which every later `--check` reports as a re-release, the procedure re-creating what the checker reports all over again. **One repair outside C54**, made because it blocked diagnosis during this pass: `meta/bin/validate.py` reported a failing `self_ci` by its *last* output line, which is routinely an unrelated `warn` — `self_ci` streams every finding and exits non-zero only for the error ones, so a reader was pointed at a rule that had not failed. It now names the first rendered `ERROR`, with the tail kept as a fallback and three carriers in `meta/tests/test_validate.py`. **D2-31 is Approved** — the owner accepted all eight implementation choices the lock left open (points (2) and (8) were ruled on directly during the review cycle, the rest accepted as recorded), so what remains is the landing commit, not a decision. The row records all eight: the `common/versions.py` lift, the `_tag_for_version` behaviour change, the binary final/non-final classification, the `warn` severity for a skip, "a rule that does not apply is not skipped", the tolerance for a released heading that says more than its version, the decision to gate at release time rather than in `self_ci`, and `vX.Y.Z` as the release tag's only admissible spelling (owner-ruled in this task)
  - **F13/C54 carriers:** the `release_check` contract suite uses separate negative fixtures for the two heading branches: every declared non-final spelling (`.devN`, `aN`, `bN`, `rcN`, `.postN`, `+local`) and accepted combinations of those segments without topmost `## Unreleased` fail, while final-version negatives are two separate fixture branches: no released heading at all fails, and a topmost released heading that differs fails; the latter keeps a matching older heading below the wrong topmost heading so accepting any heading fails. Both consistent states pass. Literal disagreement between `pyproject.toml` and `_STATIC_VERSION` fails. With no version source, `release_check` emits an explicit C51 skip `Finding`, never silence or a clean pass. The tag corpus contains multiple tags with the newest represented and an older tag missing from CHANGELOG, and must warn for the missing historical heading; with git unavailable, each git-dependent rule — matching-tag re-release detection and the all-tags heading check — emits its own explicit C51 skip `Finding`, never a silent pass. Normalization fixtures compare `v0.4.0`, `0.4.0`, and a `git describe` suffix; a final version with its matching tag asserts exactly **warn**, not error or merely non-clean. Process repairs have their own carriers: parameterized `run_plan` output mutations omitting either version literal or placing the bump after `git add` fail, and the `meta/tests/test_release_check.py` release-check contract suite fails a source mutation restoring the false claim that the tag produces the built version. Preserve the live red→green order: observe today's `0.4.0` + `## Unreleased` failure before changing either literal, then require `0.4.0.dev0` + `## Unreleased` to pass after the repair
  - **Landed in `07f46ad`**, owner-verified at D2-31.
- C67 · patch-path extraction misses the rename destination · done · engineer · `_PATCH_PATH_RE` (`hooks/codex_adapter.py`) read `Add|Update|Delete File:` only, so a renamed file was classified under its source and the destination was never seen — an advisory keyed on a file moved *into* a sensitive path was silently skipped; the C48 successor D2-18 named but never spawned, now measured rather than inferred. **Fixed:** the one extraction pattern covers all four measured forms, `*** Move to:` included, and a rename returns source and destination in patch order. Kept as one alternation rather than a second extractor — these lines are the single measured source of patch paths, and a second reader is a second place for the next form to be missing from. No new D2 point: D2-18(a) already locked the measured-only tolerance line and assigned this very shape to N1, so widening on the landed measurement executes that lock rather than deciding anything new — say so if you want it recorded anyway. Pinned by `test_patch_body_paths_include_the_rename_destination`, `test_the_rename_destination_is_a_measured_path_not_a_guessed_one`, `test_a_file_moved_into_a_sensitive_path_reaches_the_d2_advisory` and `test_a_rename_carried_by_the_shell_is_still_an_edit`; all four red against the old pattern first. 652 passed, ruff clean, self-CI exit 0 · [F4 evidence](reviews/n1-f4-codex-timeout-20260825.md)
  - **Landed in `c41eeb1`**; no D2 point of its own (it executes D2-18(a)).
- C68 · replace the broken Python 3.9 claim with one Python >=3.11 floor · done · engineer · package metadata, Ruff and CI agree on 3.11; the lock and wheel metadata carry the same floor, and a contract test joins those declarations. The floor also governs every shipped venv-free entry point, including generated hooks' bare `python3`; consumers must upgrade both that host interpreter and the package interpreter. The measured 3.9 repair is deliberately not applied, and harmless malformed-input fallbacks remain without constituting support below 3.11. Breaking pre-1.0 `x` bump before V4 · [note](design/packaging/python-39-floor.md) + D2-34
  - **Landed in `db9ad7e`**, owner-verified at D2-34.
- C69 · historical hook tree-resolution fork; C77 supersedes the runtime-root choice · done · preserve D2-26's shared record reader while pending D2-35 replaces only its runtime-root choice · [note](design/packaging/mount-resolution-owner.md)
  - **Historical D2-26 implementation, superseded in the current C77 worktree pending owner acceptance of D2-35:** the recorded `mount` vetoed the directory check — records `package` → the materialization, else the mount if present, else the materialization. C77 instead makes `akmon_runtime_root` return the executing tree; `common/record.py` and its other callers remain.
  - **Landed in `07f46ad`**, owner-verified at D2-26; its runtime-root choice is superseded by D2-35 (C77, landed in `db9ad7e`).
- C73 · one owner for "where is the project root" · done · architect+engineer · seven modules each define their own `_find_project_root` and answer one question three ways: package-aware **and** `AITNA_ROOT`-aware (`hooks/hook_core.py`, `bin/sync.py`, `tools/model_routing/init.py`); `AITNA_ROOT`-aware but blind to package mode (`tools/model_routing/second_opinion.py`, which requires `<root>/<aitna>/akmon` to exist — a package-mode consumer whose only marker is `<AITNA_ROOT>/.akmon.toml` (ADR 0009 §4) never matches); and neither, with `_aitna` hardcoded past `AITNA_ROOT` (`tools/model_routing/gate_pack.py`, `tools/model_routing/coverage_map.py`, `tools/release/release_check.py`). **Every copy ends `return start`** — the cwd — so a failed search is indistinguishable from a found root, and `gate_pack._report_path` then writes the gate pack under `<cwd>/_aitna/artifacts/gates/`: the silent-degradation class of C33's all-uncovered coverage map, on the artifact a gate is verified from. Two decisions the fix must make and record, hence the D2 row: (a) **one owner** reachable from both the mounted tree and the wheel's embedded tree, or deliberate duplication with each copy stating why it is not the others — the **same fork as C69/D2-26** one level out (which *root*, before which *tree*), so it should be settled once for both or the difference stated; (b) whether a failed search stays **silent** or becomes a stated signal. **Implemented under owner-approved D2-32; awaiting the landing commit:** the owner is `common/project_root.py`, reachable from the mounted tree, from `tools/`, from `tools/release/` and from the wheel's embedded tree, so seven copies became two; `hook_core`'s temporary copy stayed named until C74 folded it into `common/project_root.py`; and the walk keeps its cwd fallback for hooks while `resolve_project_root` makes every command-line entry point state the guess. The measured correction to C69's note — `bin/` *does* exist in the materialized tree — is recorded there and in D2-26, and does not decide that fork. Sibling: C69 · [note](design/packaging/mount-resolution-owner.md) · provenance: found while verifying D2-2's anchor
  - **Landed in `07f46ad`**, owner-verified at D2-32.
- C74 · one home for the shared utilities · done · architect+engineer · `bin/` held two launchers and four libraries, and a library there is reachable only by putting `bin/` on `sys.path` and importing it under a bare top-level name: eight call sites repeat the same insert, `src/akmon/cli.py` registers `versions` as `akmon_versions` because that name may already belong to a consumer's environment and needs one loader helper per module, and `hooks/hook_core.py` cannot reach any of it, which is why it kept its own copy of the root walk. The four libraries move to a package of their own — `common/{project_root,findings,runtime,versions}.py` — reachable at the same tree-relative path from the mounted tree and the wheel's embedded tree; `bin/` keeps only `sync.py` and `verify.py` · [decision](D2_LEDGER.md) · provenance: owner, on the C73 walkthrough
  - **Implemented under owner-approved D2-33; awaiting the landing commit:** one definition each of `aitna_root_name`, `find_project_root` and `akmon_mount`, with no allow-list left in the drift carriers; `hook_core` and `cli.py` both fold in. D2-35, pending owner acceptance, supersedes D2-33's package-mode reachability/materialization premise and whole-package materialization choice; the one-home decision remains.
  - **Landed in `07f46ad`**, owner-verified at D2-33; its package-mode materialization premise is superseded by D2-35 (C77, landed in `db9ad7e`).
- C77 · mode `package` executes from the package · done · engineer · `sync` copied the executable surface into `<AITNA_ROOT>/.akmon/` — the hooks, the `common` package they import, the model-routing library and its `registry.json`, the D2-ledger tool — ~20 files of the standard rewritten into the consumer's history on every version bump, never a second implementation (the same stdlib-only files are installed beside the consumer inside the wheel). They existed for one reason: the generated wiring named a hook by a **path**, and a path into the wheel carries the venv's Python version (`.venv/lib/python3.13/site-packages/…`), so it breaks on the next interpreter bump and on any differently laid out venv — silently, because a hook command the harness cannot find produces no output and no error. Delivered: a new CLI subcommand `akmon hook <name> [args...]` (in-process `runpy`, hooks dir on `sys.path`, tree-matched `common` binding with restoration, argv tail verbatim, a path where a name belongs refused); the wiring now spells `"<anchor>/.venv/bin/akmon" hook <name>` in package mode and is unchanged in mounted modes; materialization narrowed to the guardrails `AGENTS.md` actually `@`-imports, read from the document (fenced blocks and inline code stripped, since the block documents the import as well as making it) rather than derived from the recorded archetype; a one-run sweep of the previous materialization, banner or not, with `__pycache__` removed silently so `--check` does not fail a project for having run a hook; `akmon_runtime_root` answering with the executing tree; the routing hook's silence narrowed to a pin that has no routing tools at all; skill stubs and recovery instructions reaching the standard through `$(akmon path)`; `verify` gaining `hooks.launcher` for a regular executable file and no longer skipping its hook check in package mode; `akmon.__version__` made lazy (PEP 562) because the wiring now runs on the session's hottest tools; and the one surviving copy kept honest from both ends — `sync --check`/`verify` on a commit, plus a package-execution-only SessionStart line naming any guardrail the running package has moved past, since a pin bump installs the new hooks the instant the dependency resolves while the repository still holds the previous release's rules (`common/materialization.py` owns the format and the comparison, so writer and judge cannot drift apart) · [ADR 0009 amendment](decisions/0009-packaging-package-carrier-and-mount-modes.md) · provenance: owner
  - **Landed in `db9ad7e`**, owner-verified at D2-35.
- N7 · probe: what makes a consumer's Codex hooks load · done · terminal evidence complete; landed in c41eeb1 · [evidence](reviews/n7-codex-hook-delivery-20260825.md)
- C51 · shared finding envelope (plan P1.6) · done · engineer · one stdlib-only `bin/findings.py` with `severity · code · message · target · fix`, adopted by `verify.py`, `meta/bin/validate.py`, `sync --check` and `self_ci` (the last imports it directly rather than parsing subprocess output). `severity` carries the closed `ok/warn/error` vocabulary; `verify`, `validate` and `self_ci` share error → 1 and warn → 1 only under `--strict`, while `sync --check` keeps its exact 0-clean/1-drift/2-planning-error vocabulary with no warn/strict stream. `code` is a stable dotted slug (`boundary.missing-banner`, `caps.always-loaded`) never reused after a check is removed; it matches `[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)+`, rejecting whitespace, uppercase, empty segments, repeated hyphens and a slug with no dot. `target` semantics are review-owned. Every rendered field is one logical line; `fix` is required, non-empty and one sentence, and on `ok` states the invariant to keep; the bounded sentence-shape heuristic is mechanical, while residual natural-language boundaries and imperative mood are review-owned — a finding the reader cannot act on is a defect of the *check*. **Owner choice F1: B — rename `level`→`severity`, with no property, constructor, or serialization alias.** This accepts a breaking Python API change and requires one-step migration of every in-repository constructor, attribute read, fixture, and test, including the independent Finding in `meta/bin/validate.py`; rendered values and exit semantics stay unchanged. A contract test asserts that `severity` exists, `level` does not, and `level=` is rejected. **Owner choice F2: C — C51 owns and exactly tests one pure serializer (`Finding.to_dict()` or one shared function, not both), where pure means deterministic field mapping plus input non-mutation and makes no broader claim; C59 adds the first public JSON output.** Any adopter or later consumer that serializes a Finding calls that serializer and carries no local field mapping; the C51 adopters render text only. C51 adds no `--json` mode to its adopters. The JSON-safe schema contains only `severity · code · message · target · fix`, preserves Unicode, accepts an empty valid target, does not mutate the Finding, and passes `json.dumps`. **Text rendering *changes*, and C51 owns the change (A17(g)):** `verify.py` prints `[{level}] {message}` today (`bin/verify.py:690`), while the canonical greppable form is `SEVERITY code target: message → fix` — the draft said it "remains", describing a form the tree does not print, so every adopter's text output moves in the same commit as the `level`→`severity` rename and the tests pinning the old form move with it. Per **F13** it carries its own seed: a rendering that drops `code`, `target` or `fix`, reorders them, or admits a line separator in any rendered field fails — without it the whole text half could regress with every schema test green. **Out of scope: hooks** — they speak the harness JSON contract on stdout and prose on stderr, and a third output shape on a channel whose visibility is unverified helps nobody. **Known future adopter, recorded at D2-16 verification:** `routing.resolve_briefs` raises on the *first* bad overlay key, so an owner with two broken keys fixes one, re-runs and meets the next. Collecting every violation into one envelope is precisely what C51 buys, and the overlay path is its first non-check consumer. Out of C51's own scope — recorded so the one-error-at-a-time behaviour reads as a pending adoption rather than an oversight. **F14 carrier:** `findings.py` keeps the retired-code record; any live check whose code appears in that record fails. Appending a removed code is process-owned, with the accepted F14 cost that no checker forces an omitted append. **F13 triples run over all four adopters.** Seeded violations: a missing, empty, multi-line or multi-sentence `fix`; a missing, empty or multi-line `message`; a line separator in `code` or `target`; a duplicate live `code`; a live code restored from the retired record; an invalid severity such as `fatal`; an invalid dotted slug; a stale `.level` read, local `Finding` or old renderer in any adopter; serialization missing or changing a canonical field, adding `level`, mutating the Finding, returning different mappings on repeated calls, returning a value that fails direct `json.dumps`, importing a dependency outside the standard library in `findings.py`, or being reimplemented locally through either a literal or `dict(...)`; a second canonical serializer; public `--json` added before C59; and exact rendering with a field dropped or reordered. Each mutation fails the C51 contract suite; exact rendering covers all four adopters, the shared exit matrix covers the three strict-capable adopters, and sync carries its separate exact 0/1/2 carrier
  - **Delivered, owner-verified in D2-28, landed in c41eeb1:** `bin/findings.py` (stdlib-only, `severity · code · message · target · fix`, `RETIRED_CODES`, one `Finding.to_dict()`, `render`/`print_findings`/`exit_code`), adopted by `bin/verify.py` (72 emit sites, 42 codes), `meta/bin/validate.py` (7 codes), `bin/sync.py --check` (4 codes) and `meta/self_ci.py` (4 codes, now findings-based with `--strict`/`--quiet`). `level`→`severity` renamed with no alias; text output moved to `SEVERITY code target: message → fix` in the same change; `src/akmon/cli.py` pre-registers the embedded `findings` module so the package-mode launchers still import in-process. Contract suite `meta/tests/test_findings.py` (164 collected cases) — each declared seed was mutation-checked to fail, including every logical line separator across rendered fields, compact uppercase second sentences, literal/`dict(...)` serializer duplication, mounted-fixture launcher imports, invariant-specific successful `self_ci` fixes and quiet child execution. **Three build-time owner decisions are recorded in [§1](design/stage1-hardening-contracts.md):** `fix` required at every severity including `ok`; `sync` adopts `--check` only and keeps exit 2 for a planning error with no `--strict`; every rendered field is one logical line. The sync exit behavior is the explicit adopter exception recorded in §1 and ADR 0012. The paths above are as landed; the module moved to `common/findings.py` in C74, which gave the shared utilities their own package.
- A6 · roles & pipelines under the subagent model · done · architect · closed **in design**: recorded as §10 (role = orchestration contract; role→task-kinds matrix; zone plan from Decompose/Survey; loop-back edges; learn/release tiers); lock items folded into A5 (done), so nothing remained to implement under this id · [design §10](design/model-routing.md)
- A17 · A12 pre-lock closure — finish the walkthrough, split D2-20, repair the draft's internal contradictions · done · architect · one carrier for everything that must land before any stage-1 shape becomes runnable; A12 stayed awaiting-owner until every subitem closed. Ordered on purpose: (b) feeds (c), and (c) feeds (g); (f) runs before (g) too, because an audit over a document that contradicts itself in six places returns those six and proves nothing else. **The audit has run and its 24 findings are repaired; F13 came out of it and is decided; F14–F22 followed from the clause-(d) audits and owner walkthroughs and are decided; the two ADRs F12 fixes are written ([0012](decisions/0012-stage1-contracts-and-vocabulary.md), [0013](decisions/0013-hook-survivability-and-crash-posture.md)), both carried Status: Proposed until their gate ran. Closed by owner verification of D2-20 at `e89f3fe`**
 - **(a) current wave first** — owner-approve D2-15…D2-22 before the A12 coherence audit, then land the approved wave before the final clean-context audit. A pending point cannot carry a landing sha, and an audit run against a tree that can still move proves nothing. **D2-15 ✅ owner-verified** (clauses 1–3; sha pending on the wave commit) — clause 3 closed a fork the row had not stated: per-agent stats stay split at the rename boundary, and folding `-`/`_` in `stats.py::label_and_tier` the way `resolve_briefs` does is **rejected**, because a brief key addresses a live agent while a log label records what ran ([ADR 0011 Consequences](decisions/0011-agent-name-notation-k-underscore.md)). **D2-16 ✅ owner-verified** (clauses a–c) — clause (b) widened to the tolerance line the code already held: `agent_key` folds surrounding whitespace as well as case and `-`/`_`, an internal space stays significant, and the rule is now pinned by `test_overlay_brief_key_tolerance_covers_whitespace_but_not_spelling`. **D2-17 ✅ owner-verified** (clauses a–d) — two wordings tightened against what the code holds: the symlink boundary is recorded as an **advisory-grade classification boundary, not containment** (a link out of the repo stays lexically inside the root), explicitly not inheritable as a security property by deny-class work in F3/C52; and the "never against the hook process cwd" claim is narrowed to "once a root is known". **D2-18 ✅ owner-verified** (clauses a–c) — (a) extended per owner choice iii: the measured-only tolerance line now covers the nine guessed *path* keys too, which stay but no longer pass as measured fact (`unmeasured_path_source` + a stderr provenance line, advisories still run); the unmeasured `*** Move to:` rename form is recorded as an N1 probe item rather than guessed. **D2-19 ✅ owner-verified** (clauses a–e) — three statements the row was missing are now in it (matcher drift is silent and its fix belongs to C46; every unrecognized `apply_patch` form degrades to the *reported* shell route, never to silence; route-level diagnostics throttle by session while event-level ones throttle by event), and clause (e) was widened per owner choice ii: the unclassified-shell-route diagnostic is now vendor-neutral in `hook_core` and fires on **Claude** too, where a `Bash` write had been unreported entirely. **D2-21 ✅ owner-verified** (clauses a–c) — all three extended: (a) is now half of one stated policy with D2-19(c) and governs three diagnostics, not one; (b) narrowed per owner choice i to *this* marker, with the real purpose stated (a session id containing `/` must not escape the tempdir; secrecy is not the argument while three siblings carry literal ids) and the convention gap left with C36(a); (c) resurveyed C36(a) at the gate — the "lift the exemplar into a helper" half landed at D2-19(e), so migration, the `nosession` policy split and the age-out are what remain. **D2-22 ✅ owner-verified** (single clause, amended three ways) — the suppression window is now stated exactly (reused/surviving session id **and** same model **and** same warning text), the two loud channels are recorded as the *reason* fail-quiet is tolerable here rather than as a neighbouring fact, and the acceptance is given an expiry: it ends with C36(a). Task-status sweep (owner-approved): C47, C48, C50 and C49 first moved to **blocked** while their gates sat in Approved — a task whose gate has passed must not keep advertising itself as pending, or the audit in (g) reads the wave as unfinished — and moved again to **done** once the wave landed. **The wave landed at `f6d181c` and the ledger is closed behind it:** D2-16…D2-19, D2-21, D2-22 and D2-24 are Verified at that sha, D2-15 at `d4e5246` — where the rename it locks actually landed, not the wave commit that amended its ADR — and the three older Approved rows that had been carrying `landing sha pending` were closed at their own commits (D2-14 `da51329`, D2-12 `556927e`, D2-7 `7312972`). The `## Approved` section is now empty. C47, C48, C49 and C50 moved from `blocked` to `done` and are archived, their clause-level record kept in the Verified rows rather than duplicated in the task text. D2-20 is deliberately last: it remains a proposal rather than a lock; A17(c) has now separated its architecture boundary from D2-23. A17(b), A17(c), A17(d) and A17(f) are now closed. The date sweep that followed landed at `5e7b393`, and the audit in (g) ran against that tree — still, as its precondition requires
  - **(b) finish the register** ([stage1-hardening-contracts](design/stage1-hardening-contracts.md)) — **F8**: owner-lock the exhaustive seven-callable table (six exact policy IDs plus `session_start_result` as operational with a non-empty rationale) before C53. **F9**: version/changelog consistent-state repair, entangled with the live `pyproject 0.4.0` / `## Unreleased` / immutable `v0.3.0` state and therefore with V3. **F10+**: expand the placeholder row into named forks or declare it empty — a bucket whose contents cannot be counted cannot be closed. **F8 ✅ decided** ([§3](design/stage1-hardening-contracts.md)) — the seven-callable table is locked, and it forced four sub-decisions the fork had not named: `role.declaration` gets **new prose in `guardrails/_common.md`** rather than an operational exemption (the P0.3 orphan finally gets an owner, at an estimated +1…+2 always-loaded lines once three legacy `> **Enforced**` blocks are replaced by six markers — C56 confirms); `session_start_result` stays **operational**, but its rationale must record that on Codex it is the *only* delivery channel for the delegation rule, so the classification is not read as "incidental"; `verify.owner-verify-d2` **declares that its coverage is the consumer's** (`[d2_ledger] sensitive_paths`), with a future `init` default seeding it narrow, and erroring on the unconfigured state rejected against ADR 0007 §4's warn-first decision; and two mechanics are locked — the marker grammar (`Runtime check: <id>`, ID first token, ` — ` tail is human prose the parser ignores, needed because five of six markers claim a *subset*) and the statement that policy IDs and F1 finding codes are **separate namespaces** despite the identical shape. **F9 ✅ decided** ([§4](design/stage1-hardening-contracts.md)) — the packet found that the fork was mis-titled: not "pick a consistent pair" but "four version carriers with no join between any two of them", and that **the defect already shipped** — the tree at tag `v0.3.0` carries `version = "0.3.0.dev0"`, and `1a9ab0f` ("v0.3.0 package version") then wrote `0.4.0`, so the literal `0.3.0` never existed in the tree. Owner choices: **(1) `0.4.0.dev0` + `## Unreleased`** over cutting a `## v0.4.0` heading — a released heading with no tag names a release that does not exist, against the changelog's own "the git tag is the timeline" — **repaired inside C54, not before it**, because P1.3's acceptance is the live pair failing then passing and an early repair downgrades the tree to a fixture; **(2) two version literals joined by an equality check** (`pyproject.toml` ↔ `_STATIC_VERSION`) — a single literal is impossible, not merely unwanted, since `pyproject.toml` is excluded from the packaged tree, and `hatch-vcs` is rejected because a dev version legitimately runs ahead of the tag and mounted modes carry no metadata; **(3)** non-final is the full PEP 440 set, not `.devN` alone; **(4)** no version source → explicit skip finding, never silence; **(5)** tag-without-heading warns (tags are immutable — a hard error could never go green and would be waived), and `run_plan` gains the version-bump step whose absence produced the `v0.3.0` defect; **(6)** comparisons normalize the `v` prefix, and the `_skew_notice` defect this exposed is spun off as **C61** rather than voted on. **F10, F11, F12 ✅ decided** — the placeholder row was answered by first locking **what enters the register** (two or more defensible answers; the answer changes what a task implements or what the lock claims; it cannot be deferred into a task without the lock asserting something unverified), because a bucket without a counting rule cannot be shown to be empty. Sieving the draft with it leaves exactly three, and reclassifies the rest as repairs ((d), (f)), in-task decisions (C60 relocation-vs-deletion, C57's git trigger), parked items (D1, A16, N1, the public `akmon status` option name) and measurement gates (F4/F5/F6 literals — a number to obtain, not an answer to choose). **F10** — C58 is **dispatch-only**; the completion half becomes probe **N4**, with the drawback stated rather than hidden: the ledger cannot answer "what did I send that never came back". **F11** — the acceptance yardstick admits **no exemption class**; C59, the shape that adds no check of its own, is seeded against **fidelity to its sources**, at the cost of a test seam. **F12** — stage 1 produces **two ADRs**: contracts/vocabulary (F1, F2, F7, F8) apart from survivability/crash posture (F3–F6), so N1's measurements amend only the second. **(b) is closed — the register is walked in full**
  - **(c) split D2-20 — ✅ complete** ([ledger](D2_LEDGER.md)) — D2-20 owns the consolidated F1–F22 architecture and stable F4–F6 protocol (**F1–F12** when this row was written; **F13** was added at (g), **F14–F16** by the architect pass over clause (d), **F17** by the owner walkthrough of §5's C55 follow-up, **F18** by the owner walkthrough of §6's C56 follow-up, **F19** by the owner walkthrough of §7's C57 follow-up, **F20** by the owner walkthrough of §8's C46 follow-up, **F21** by the owner walkthrough of §9's C58 follow-up, and **F22** by the owner walkthrough of §10's C59 follow-up, with the ledger carrying the current spelling); D2-23 owns the measured vendor results, caps, entry mappings, budgets and literals and blocks only C52. The F8 table now exists and is part of D2-20 verification; the obsolete non-existence premise has been removed
  - **(d) missing seeded-violation contracts — ✅ complete** — C51, C53 and C56 already named checker, fixture mutation and expected failure; **C57** ([§7](design/stage1-hardening-contracts.md)) and the **C46 tool-name half** ([§8](design/stage1-hardening-contracts.md)) now do too. **C55 was counted here as complete and was not** — it named a mutation and an expected failure but no checker, found at (g)/G16 and closed there by naming `verify.py` (the boundary is a property of the tree `sync` wrote, which is the consumer's) with the same checker run over akmon's own tree inside `self_ci`. The miscount is left visible rather than quietly corrected, because it is the same defect class this item exists to remove. Neither of the two was the mechanical work this item expected. **C57 had nothing to check in the required form** — recorded here at the time as "every cell of the current matrix is a checkmark", which is **false and was disproved at (g)/G1**: the true premise is that **no current row carries the mandatory axes**, so today's table fails `matrix.missing-axis` throughout, and bare `✅`/`enforced` is a separate, narrower violation that would have caught only part of it. The false premise is kept in quotes as the record of what was believed; the corrected one is what the contract stands on. It forced `CAPABILITIES.md` as the axis-complete home with README reduced to a claim-free summary, an explicit scope boundary (vendor claims only; akmon's own checkers out, guardrail prose to C53), and a **join from the runtime declaration to the generator**, without which half of §7 stays unverified prose. **C46's version stamp had nothing to compare against** — nothing in the tree reads a harness version — which forced detection-when-present, a warn-and-stamp answer for an unknown version instead of an error that would brick consumers on the vendor's release schedule, and a seed against the **root cause**: a second owner naming vendor tools outside the map. Both are observed **red before green**, so neither `README.md:200` nor `routing.py:364`/`:470`/`:503` is repaired ahead of its task. Carried in the same pass: §2's seeded paragraph said "every executable wrapper" after (f5) had rescoped the rest of §2 to the nine spawned entry points. **C59 left this item via F11**: its case was not a missing contract but the yardstick's own hard case — a shape that adds no check — and the answer is that **no exemption class exists**; §10 carries a fidelity contract (mutate the source finding set, the aggregate must follow; no dropped or reordered finding; text and JSON identical in content and order; strict/exit semantics preserved). No shape ships without one — this stage's own acceptance criterion, not an extra
  - **Later §10 correction to (d).** C59 first left that item at shape level via F11. The §10 owner pass showed that conclusion was incomplete: F22 now pins the exact live provider population, read-only/actual-state boundary, active-role residual and full rendering/exit carriers in addition to F11's no-exemption equality rule.
  - **(e) C58 is underdefined** — **closed by F10: dispatch-only**, with the completion half spawned as **N4**. Completion event, ledger line schema and correlation key were all unknown on both vendors, and a ledger locked with half of it guessed is the failure class stage 1 exists to remove; a shape narrowed to what is measurable can be extended by evidence, while one locked on a guess must first be unlocked. Accepted drawback, recorded in §9: the stage-1 ledger cannot answer "what did I send that never came back"
  - **(f) dependency contradictions — ✅ complete** — five were listed, a sixth surfaced while repairing them, and none turned out to be purely editorial; each is recorded here with what it changed, because the repairs move the order graph and (g)'s audit reads that graph. **(1) F8 in the C53 row** ([order table](design/stage1-hardening-contracts.md)) — added, and marked as a *decision* C53 must hold before code rather than a task to wait for; the column already mixed tasks with evidence (`D2-23 evidence gate`, `P0.3 inventory`). **(2) C55's `P0.4 declaration (§6)`** — the row named a section belonging to C56, so a definition read as a task; it now names the always-loaded *definition* explicitly, and §6's `Declaration` block is annotated as the shared unit. Consequence, not cosmetics: **C55's only task dependency is C51** — it was never waiting for C56. Why C55 needs it at all: `AGENTS.md` is hand-owned by the consumer while carrying a generated akmon block, the single artifact with split ownership, and the boundary check cannot classify it without the definition. **(3) C60 in two lists** — the order-table row is **removed**, since the table's own caption limits it to dependencies internal to the proposed package and C60 is explicitly outside the lock; the `C56 → C60` edge moves into *Not in this proposed lock* so deleting the row loses nothing. Rejected: keeping the row under an "out of lock" marker, which would turn the table from the lock's contents into a mixed graph — and its completeness is what (g) audits. **(4) A16 `unblocked`** — now **`blocked (after D2-20)`**; the row's own qualifying sentence already said the schema cannot settle a vocabulary inheriting F3's axes before D2-20 locks F3, and its first job is consuming C57's cells, which sit behind the same gate, so the honest status costs nothing. Rejected: narrowing A16 to "schema from N2 data, no vocabulary commitment" to keep it running — it re-derives the vocabulary later and keeps a status arguing with its own text. Also rejected: a third status level for *decided but not locked*, which is what F3 is — that is D1, parked, and inventing it inside one task settles D1 by side effect. **(5) C52's "every executable wrapper"** — scoped to the **nine spawned entry points**, measured from the generator: eight Claude entries (`bin/sync.py:270`–`:306`) and one Codex entry, `codex-hook.py` with the route as an argument (`:203`, `:222`–`:244`); all nine carry a shebang and `__main__`, while `claude_adapter.py`, `codex_adapter.py` and `hook_core.py` carry neither. Adapters get **no guard of their own** — an imported module has no top level, and an exception inside one propagates into the wrapper's `main()`, which is also why F5 already excluded the adapter modules from timeout scopes; §2 and F5 now agree. The repair adds a contract rather than only deleting words: **an adapter writes its whole output in one operation**, because a guard cannot un-write truncated stdout and C52's seeded crash tests forbid partial output. Rejected: per-function `try/except` in the adapters — swallowing, not guarding. **(6) Found while repairing (1): C53 → C56 edge was missing** — C53 replaces three `> **Enforced**` blocks with six markers and writes the new `## Role declaration` section, all inside the surface C56 caps: +1…+2 lines against 13 lines of slack (~197 against the 210-line cap). They are compatible today, which is the reason to record the edge rather than to skip it — nothing else would show when they stop being
  - **(g) coherence audit — ✅ run, findings repaired; both ADRs written and audited in turn; D2-20 remains** — the audit was run **outside** the context that wrote the document, on the owner's own clean session, because an author checking a document against their memory of what they meant is not checking the document. Two independent passes; **24 findings**, 6 of them in both, none fabricated — every one re-verified against the tree before it was acted on. **Four landed on repairs made earlier in A17**, which is the return on running it elsewhere: G1 and G16 are (d)'s work, G11 and G15 are (f)'s, and G10 is the twin of the edge (f) did find. The two passes also **disagreed on one whole class** — one read the acceptance yardstick per shape and found two gaps, the other read it per rule and found nine — and that disagreement was not an error by either: it was the document's own ambiguity, which became **F13**. Verdicts below; the design doc carries each repair with its reasoning
    - **F13 (from G20–G24) — the seeding unit.** `## Acceptance` bound a seed to a **shape** ("every shape above ships with **a** test"), the Frame to a **check** ("what seeded violation makes *this check* fail"). Not theoretical: §2 states four numbered rules and verified two — `utf-8-sig` decoding and `timeout` in the generated wiring had no mutation, no expected failure and no test anywhere in 800 lines, and the word `BOM` occurred once, in the rule itself. Registered as a fork under F10's counting rule (two defensible answers; changes what five tasks implement; not deferrable without the lock claiming coverage it lacks). **Owner choice: the independently violable rule.** Rules that cannot fail separately share a seed; one that can be violated alone carries its own; a rule whose seed needs unmeasured evidence is **split**, not waived — §2's `timeout` presence is checked now by `sync --check`, its literal stays behind D2-23. Rejected: **per shape** as written (cheapest, and it locks §2 with half its rules unverified — the stage's target defect inside the stage's own criterion); **per rule with a declared exemption list** (visible holes beat hidden ones, but F11 already ruled that no exemption class exists, and a list of them is that class renamed). Accepted cost: +6…+10 tests across the stage, and a boundary that will be argued at implementation — argued in favour of the extra seed, since a redundant test is cheaper than an unnoticed rule. Closes **G20** (§2 BOM), **G21** (§2 `timeout`, split), **G22** (§4 `run_plan` order and staged set), **G23** (§9 read-old/write-new), **G24** (§8 no-binary disclosure) — all five now seeded in their sections
    - **G1 · §7's red-state premise was false, and the same paragraph disproved it.** It read "every cell of today's matrix (`README.md:82`–`:92`) is a checkmark, so the check fails on the whole table", then four lines later quoted `❌ subagent hook payload unverified` from that table. Twenty-plus cells are `❌`/`⚠️`/`❓`, and the seed catches "a bare `✅` or the word `enforced`", so those cells would not have failed it. **Repaired, and the conclusion survives on a true premise:** no row carries the mandatory axes, so the table still goes red in full — through a new `matrix.missing-axis` rule, not through bare-checkmark. *(d)'s work; the mechanism was right and the stated reason was not*
    - **G2 · §8 claimed a `hook_core` vocabulary that does not exist.** It named `read`/`search`/`shell`/`edit`; the live set is `edit`/`shell`/`read`/`subagent` (`hooks/hook_core.py:109`–`:115`), `search` appears nowhere, and `read` already folds Read/Grep/Glob by that module's own comment. Load-bearing, because the rejection of `AGENT_SPECS` rests on `hook_core` **already** owning the names — an invented token made the argument circular. **Repaired** to the real set, with the rule that a capability C46 needs and the set lacks is added *to* `hook_core`, never as a second list
    - **G3 · §1 asserted an output form the tree does not print.** "Text rendering **remains** `SEVERITY code target: message → fix`" against `bin/verify.py:690`, which prints `[{level}] {message}` — the spelling G3 itself got wrong, corrected at I8. *Remains* claims nothing needs doing; C51 planned nothing. **Repaired** as a **change** C51 owns, moving with the `level`→`severity` rename, and given its own exact-output seed — without it the entire text half could regress with every schema test green
    - **G4 · C46 stands on a fact ADR 0011 explicitly retracts.** The task says Codex "has no agent-definition file convention at all… subagents are spawned dynamically through `collaboration.spawn_agent`, where `agent_name` and `task_name` are separate parameters". ADR 0011 `:38`–`:46`: "**Both are wrong**" — the spawn tool exposes `task_name`, `fork_turns`, `model`, `message`, and codex 0.146.0 *does* carry `<repo>/.codex/agents/<name>.toml`, selected by an `agent_type` argument. **Repaired in the task text.** The carrier's existence is recorded and deliberately **does not enter this lock**: it belongs to the agent-*definition* half, closed by ADR 0011 and parked to D4, while C46 here owns tool names only. Flagged rather than acted on — whether akmon should emit Codex agent definitions is a scope question for D4, not a repair
    - **G5–G8 · four wrong coordinates.** `bin/sync.py:252` is a blank line; the `$CLAUDE_PROJECT_DIR` command is `:258` (design doc + this file). §2's `codex-hook.py (:203, :222–:244)` reads as coordinates inside a 168-line file; they are `bin/sync.py`'s, and the file is now named explicitly. `routing.py:363/:469/:502` here are the preceding `),` — the literals are `:364`/`:470`/`:503`, which the design doc had right, so this file carried both spellings. `bin/sync.py:569`/`:570` point inside `_package_mode_akmon_toml`; the `.codex` entries are `:580`–`:581`
    - **G9 · wrong probe id in the out-of-lock list.** "Codex dispatch observability (**N1**)" — N1 is the F4/F5/F6 measurement campaign, parked separately two lines above; the subject is **N4**, the completion probe F10 spawned
    - **G10 · C58 writes into the always-loaded surface and the graph omitted the edge.** §9 adds the "after compaction, trust the ledger and `git log`" clause to the orchestration guardrail — `guardrails/_common.md`, which §6 counts as always-loaded in full — so it draws on the same 13 lines of slack as C53. Exactly the edge (f) added for C53, with the reasoning "a graph that omits it cannot show when they stop being compatible"; the twin was missed by the same reading. **Recorded in the order table**
    - **G11 · the count claiming completeness was wrong.** "**Two** edges in the table are not task dependencies" — there are four, and the missed one points the *other* way: C46 emits the banner line C55 checks, an outgoing annotation, not an edge into C46. The sentence claims completeness over the column an audit reads as the lock's contents. *(f)'s sentence*
    - **G12 · C59's dependency range was one shape short.** Table: "all of the above" (rows 1–9, C46 included); this file: `C51–C58`. **Resolved in the table's favour** and both made explicit: C59 aggregates the finding stream `sync --check` surfaces and C46 emits into it (the degrade warn, the unknown-version warn), so C46 is inside the range
    - **G13 · three statuses outside the vocabulary.** `pipelines/tasks.md:39` admits exactly `active | blocked | deferred | done`; N4, C61 and C62 carried `open`. All three are ready and unstarted with nothing blocking them, so all three become **`active`**. N4 additionally declared the role `analyst`, which is not a role at all — roles are **derived** from the type letter, and `N` derives `review`
    - **G14 · C63 contradicted its own gate.** It read "awaiting owner landing, then `verify D2-24 --commit <sha>`" while D2-24 already sits under `## Verified` at `f6d181c` — recorded two entries above in A17(a). **Closed as done.** Found outside the audit's enumerated task set, reached through D2-24
    - **G15 · §2's single-write contract claimed a coverage it never had.** The text said a mid-render crash is "precisely what C52's seeded crash tests forbid"; the named mutation raises *inside the handler*, before anything is rendered, so the assertion "no partial stdout" passes whether or not the adapter honours the contract. Not a gap — a false statement about an existing test. **Repaired** with a second seed placing the failure mid-render. *(f5)'s contract*
    - **G16 · §5 named a mutation and an expected failure but no checker.** The one contract in the document with nobody assigned to run it, while §6 shows the choice is load-bearing (the same shape takes different severities in different checkers). **Repaired: `verify.py`** owns it — the boundary is a property of a tree `sync` has run in, and that tree is the consumer's — with `self_ci` running the same check over akmon's own tree so akmon is not exempt. Rejected: `self_ci` alone, which never sees a consumer tree. *(d) had recorded C55 as already carrying a full triple; it did not*
    - **G17 · §7 required evidence that nothing could hold.** The seed says an `unmeasured` cell replaced by "an assertion that cites no evidence" fails, but the declared axes were `documented`, `delivered`, route, effect, crash posture — **no evidence axis**, so `self_ci` had no way to tell a supported claim from an unsupported one. **Repaired:** `evidence` becomes a sixth axis; non-`unmeasured` requires it non-empty, `unmeasured` requires it empty. Its **encoding is C57's in-task decision**, beside C57's git trigger — the lock fixes that the axis exists, not how a citation is spelled. Rejected: a required prose citation, checkable only by pattern, which turns a missing citation into a formatting argument
    - **G18 · §9 pointed at an enforcer no task was building.** "The guardrail clause is covered by the §3 canary once its enforcer exists" — the canary joins a policy ID in prose to a `hook_core.*_result` docstring, and C58 adds no callable; it moves a TSV path and appends prose. **Repaired with a check that is actually mechanical here:** the clause names the ledger and C58 moves the ledger, so C58 checks that the path in the guardrail prose is the path the tool writes. Either side changed alone fails; deleting the clause fails from the same direction
    - **G19 · §10 forbade a source F8/3 requires C59 to read.** "No new check, no mutation, no new data source" against F8/3's "C59 reports `configured` / `not configured`", whose state lives in `.akmon.toml`'s `[d2_ledger] sensitive_paths`. **Repaired by assigning the emission:** the constraint binds **C59**, not the stage — a source may be extended by the task that owns it, and the coverage state is emitted as a finding by the existing `d2_ledger` check under **C53**, which writes the marker stating the dependency. C59 only aggregates it
    - **(g) tail — ✅ both ADRs written, then audited in successive clean-context passes, findings repaired**: [0012 · contracts and vocabulary](decisions/0012-stage1-contracts-and-vocabulary.md) (F1, F2, F7, F8 — the decisions other work cites — plus the stage-wide yardstick F11/F13, which is stable and governs how the contracts in *both* ADRs are written) and [0013 · survivability and crash posture](decisions/0013-hook-survivability-and-crash-posture.md) (F3–F6 — the protocol D2-23's measurements populate), so a timing number cannot formally disturb a vocabulary already built on; the boundary between them is the maintenance cost, accepted. **The ADRs were then audited the same way the design doc was**, and that pass returned **eleven distinct defects, ten of them introduced by the ADR writing itself** — the strongest available argument that the separation is not ceremony. Four had been found by the in-session check of the register mapping; seven had not, and they cluster on one axis that check did not have: **completeness of transfer inside an accepted fork**. None was fabricated; every one was re-verified against the tree before repair. Verdicts below. **Two things were decided rather than assumed.** (i) The **convention is amended, not excepted** — [decisions/README](decisions/README.md) now names the one legitimate `Proposed` case (an ADR whose own gate verifies its boundary must exist before the gate can run). The earlier call here was the opposite, and it was wrong for the reason H10 gives: an explanation next to a rule does not remove a categorical divergence from it, which is the exact figure this stage removes everywhere else. (ii) **F9, F10 and F12 are decided in the design lock and not re-decided in either ADR**; 0012 keeps a pointer only, and F12's rationale lives once, in 0012, with 0013 referring to it. **Closed in (g): D2-20 is owner-verified at `e89f3fe`.** **C51** is runnable and **C57** follows C51; C52 remains behind D2-23, and N1's F4 probe can run in parallel throughout
      - **H1 · 0013 inverted a seed.** The lock says a generated Claude entry emitted **without** a `timeout` key fails `sync --check`; the ADR said the **presence** of the key fails. Not a wording slip — in that form the test is red on a correct artifact. **Repaired.** The worst defect of the eleven, and it landed on F13's own worked example
      - **H2 · F9/F10/F12 crossed a boundary this task had just declared.** The lock promised three register rows "stay here rather than moving into either ADR", while 0012 restated F9's and F10's decisions and F12's rationale stood in full in both ADRs. Read literally the promise was absence, and it was broken three times. **Repaired on both sides:** the restatements are gone, F12's rationale has one home, and the lock's sentence now says what is true — decided here, not re-decided there, with a pointer so absence does not read as omission
      - **H3 · 0013 asserted a fact the tree disproves.** "No spawned entry has a top-level guard" — five of nine have one (`d2-ledger-reminder`, `delegation-log`, `delegation-nudge`, `session-start-agent`, `model-routing`), four do not (`git-commit-guard` — the deny-class hook — `role-on-code`, `analysis-guard`, `codex-hook.py`). **Repaired with measurement, and it changed C52's shape:** C52 **changes five and adds four**; the existing five are red against the contract rather than compliant with it, because they print `{type(exc).__name__}: {exc}` — the exception *message* the diagnostic rule forbids — and catch `Exception`, so an `argparse` `SystemExit` in the Codex entry escapes. The seed must be observed red on today's five, or it ships already-passing where a reader assumes the work was done. Recorded in §2 of the lock and in C52
      - **H4 · 0012 carried a claim sourced from a task, not from the lock.** The `routing.resolve_briefs` first-bad-key consequence comes from C51. True and useful, but the stated rule is that an ADR consolidates the design lock. **Cut** — C51 already holds it
      - **H5 · 0013 dropped a clause of F5.** The lock requires live validation of **both** individual entry expiry **and** aggregate latency where one event schedules a group of hook processes. Only the first survived. **Restored** — a per-entry budget correct alone can still be wrong in a group, and the group is what a consumer experiences
      - **H6 · 0013 dropped an axis of F6.** `**` complexity fell out of the D2 glob axis in an inventory the same paragraph calls exhaustive. **Restored.** A lost axis is a missing cap, not a shorter sentence
      - **H7 · 0012 transferred F8/3 incompletely.** The consumer-owned globs, the future narrow default and the rejection of erroring all made it; what the unconfigured state actually *costs* did not — one channel of three, with the pre-commit check over-warning and the SessionStart counter still running, plus the `tomllib` 3.11 limit that no option removes. **Restored** — without the cost the decision reads as a hole rather than a trade
      - **H8 · both carriers advertised the wrong status.** `blocked` = waiting on an external dependency ([tasks](../pipelines/tasks.md)), and A17/A12 wait on owner verification alone. The audit was right against the tree it read; the repairs above kept A17 genuinely active until they landed. **Both moved to `blocked (after D2-20)`** now that they are applied — the same doctrine A16 got at (f)
      - **H9 · (g)'s own header contradicted its body.** "ADRs and D2-20 remain" beside two lines saying the ADRs are written. Introduced by repairing one place and not the other. **Repaired**
      - **H10 · `Proposed` diverged from the ADR convention.** `decisions/README` says an ADR is written only for a locked decision; two `Proposed` files stood beside a note saying the convention would not be touched. **The convention is amended instead** — see (i) above. Holding the files until D2-20 was rejected: clause (b) of that row verifies the ADR *boundary*, which cannot be verified from a description of files that do not exist
      - **H11 · 0012 compressed the F8 table past its claims** (found in the in-session pass): the lock states explicitly that "tests pass before a commit is offered" and "branch for non-trivial work" are **not** machine-checked and the marker must not imply they are, and it states the substance of `privilege.no-escalation`. Both were reduced to the word "subset". **Restored.** The general rule in F7 covers it abstractly; the lock made it specific exactly where the temptation is to read wider. One neighbouring trim landed with it — the F2 rejection reasoning is back to the reason the owner recorded, without the half I had inferred
      - **Third pass over the repaired ADRs — five findings, all confirmed, all repaired.** **I1 · the convention contradicted itself across files:** `decisions/README` and this task recorded the amendment while the design lock still said "the habit is not amended"; the lock now says what the other two say, and the disagreement is recorded rather than quietly fixed. **I2 · (d) still carried a disproved premise about C55** — it counted C55 among the shapes that already had the full triple, which G16 disproved; rewritten as the miscount it was, with the checker G16 assigned. **I3 · (d) still carried the false matrix premise** ("every cell is a checkmark"), disproved by G1 and by (d)'s own next lines; the false form is kept in quotes as the record of what was believed and the true premise — no row carries the mandatory axes — is what the contract stands on. **I4 · 0012's F8 table transferred triggers but not effects:** `raises a reminder`, `raises a nudge, then an ask`, and the design→code switch the SessionStart reminder cannot catch were missing, which is the dangerous half — the trigger implemented right and the result wrong; restored verbatim, along with the Owner-verify bullet and `globs` in the D2 row. **I5 · F9/F10/F12 pointers still violated the criterion as originally stated** — see the criterion replacement below
      - **The F9/F10/F12 criterion is replaced, once and on the record.** It first read *must not appear in either ADR*; the pointers this task added did appear, so the audit was right twice under that reading. The strict form is now rejected on implementation grounds: an implementer who finds no trace of a neighbouring decision cannot tell deliberate absence from a lost requirement, and re-deciding it is the cheaper mistake. **New rule: not re-decided in an ADR — an identifier and a navigational pointer are allowed; a repetition of the choice, its mechanism or its consequences is not.** Recorded in the design lock and in 0012 because an unrecorded change of criterion is exactly how the next audit legitimately reports the same finding a third time. One neighbouring cut landed with it: 0012's "makes a second, later consumer cheap" was an unsourced promise with no carrier, and now names C59, the one consumer this lock has
      - **I6 · the replacement rule contradicted the lock three lines above it.** Written uniform over all three rows, it forbade repeating a decision's mechanism or consequences while the same paragraph permitted F12's rationale to live in 0012 — and 0013 was carrying its own argument for the split, not a pointer. **The rule is now split, because F12 is not like F9 and F10:** those are decisions about other work, while F12 is what gives each ADR its scope, and an ADR that cannot say why its scope is what it is fails at being an ADR. **F9/F10 — identifier and navigational pointer only. F12 — scope rationale once, in 0012; 0013 points at it and argues nothing.** 0013's Context paragraph became a pointer; stating plainly *what* an ADR covers stays allowed as self-description. Found by the pass that ran after the replacement landed, which is the case for writing a criterion down instead of holding it
      - **I7 · the same rule was still broken one line below the place I had just fixed.** 0013's "expected to move" consequence ended `— which is why the F12 boundary runs where [0012] says it does`, a clause that justifies the boundary instead of pointing at it; I6 had recorded that consequence as already reduced to a pointer, which it was not. The tail is deleted and the consequence now states only what it is for: N1's results amend F4's verdicts, F5's literals and F6's caps. **The pattern across I5–I7 is one thing — a rule about where an argument may live is not enforced by restating the rule, only by reading every site that argues.** 0013 now names F12 exactly once, as a pointer in its Context, and nowhere else
      - **I8 · stale coordinates survived the pass that existed to fix coordinates.** Two reported: C46's `bin/sync.py:262,:278` matchers are at `:273,:289`, and its `hooks/claude_adapter.py:12,14` tool sets are at `:13,15` (`:12,:14` are the comment lines above them). Both confirmed against the tree; the sync.py pair is a uniform **+11** drift, so a single edit moved the block after the citation was written. Two more found by sweeping every `path:line` citation in this file and in the lock, both ADRs and the ledger — mechanically, since that is what the class deserves: C46's Codex matcher `bin/sync.py:212` is at `:218`, and **`verify.py` prints `[{level}] {message}`, not `[{severity}]`** — the word `severity` occurs nowhere in `verify.py`, whose `Finding` field is `level` (`bin/verify.py:83`). That last one is not a coordinate but a **misquoted as-is**: three sites said the tree already prints the post-rename spelling while the same sentence promised the `level`→`severity` rename, and **G3 introduced it while repairing the neighbouring verb** — the round that fixed *remains* carried a wrong spelling into the lock, 0012 and this file. **The lesson is the one G5–G8 did not learn: a coordinate round that repairs the coordinates it was handed leaves the class open.** The sweep is now a script (extract every `path:line`, print the line it resolves to) rather than a habit; everything else it resolved is correct, including the deliberate wrong coordinates quoted inside G5–G8 as the record of what was wrong. Separately, (c) said D2-20 owns **F1–F12** while the ledger and the lock then said **F1–F13**; the row predates F13, so it now states F1–F13 and says so. That was the then-current extent; the later clause-(d) follow-ups and owner walkthroughs extended the owner register through F21.
      - **Architect pass over D2-20 clause (d) — the dependency graph is clean, the seeded contracts were not.** The graph reconciles exactly between the lock and this file: `D2-23 → C52`; `C51 → C53, C54, C55, C56, C57, C46`; `C57 → C46, C58`; all nine → `C59`; `C56 → C60` outside the table because the table's own caption limits it to the proposed package. No cycles, and the three collision edges plus C46's outgoing banner annotation are marked as not being order edges. The contracts are the other verdict: **twenty-one rules across nine of the ten sections are declared normative here and reachable by no named mutation.** Under the shape-level reading the document passes; under **F13**, which makes the independently violable rule the unit, it does not, and that reading is no longer available since F13 was accepted. **Six items were reported and resolve to thirteen findings here; seven more came from sweeping every section and both ADRs rather than the ones handed over — three of those in sections the report declared clean, and two in the ADRs, which that pass did not read.** One of the thirteen carried a defect the report did not name: read against the tree, §7's join does not merely under-check the modality, it fires a warn on the two binaries §8 depends on. That is I8's lesson in a second domain: a pass that checks what it was given leaves the class open. §9 (C58) is the one section that needed nothing under that pass's then-current coarse contract. F21's later owner walkthrough supersedes only that conclusion: it adds `C46 → C58` and exact §9 carriers; the earlier graph and result remain the historical outcome of the pass
      - **J1 · the bullet claiming the contracts were complete is falsified in all three sections it names.** (g) recorded "Seeded-violation contracts are completed — done" for §7 (C57) and §8 (C46), with §10 (C59) exempted earlier via F11; §7 carries three unseeded rules, §8 one, §10 one. **"Completed" was true of the paragraph each section lacked and false of its rule set.** The bullet keeps its wording and carries the correction rather than being narrowed after the fact — this is the third time in A17 that a repair round's own claim of coverage proved wider than the coverage (G3, I6, now this), and each time the claim, not the gap, is what made the next pass unnecessary-looking
      - **J2–J4 · §1/C51 declares three rules and seeds none of them.** (J2) `code` is "unique, never reused after a check is removed", which the duplicate-code seed structurally cannot see: no two live checks ever share the slug, so reuse is invisible to it. **Fork closed — `findings.py` carries a retired-code list**, and a live code appearing in it fails; rejected, dropping the never-reused clause, which is the half a consumer greps for. It is not the central registry F7 rejects in §3: that rule forbids a constant carrying the prose↔callable *join*, and a list of spent names carries none. (J3) `fix` is "one sentence in the imperative" and only its **presence** was seeded. **Split per F13** — non-empty, single sentence, no line break is mechanical now; the imperative mood is review-owned and the lock says so, the same fence F8's table puts around its non-checkable clauses. (J4) the exit contract ("errors exit 1; warnings exit 1 only under `--strict`") is declared preserved and preserved by intent alone — while F1's rename rewrites every adopter that implements it, which is the worst possible moment for an unseeded invariant
      - **J5–J6 · §2/C52.** (J5) the runtime rule says files are "preflighted and bounded-read to handle **growth races**" while the corpus is static cap−1/cap/cap+1 — a preflight-then-read implementation passes every cell of it and still reads a file that grew between the two calls. Seeded through a seam whose size probe returns under the cap and whose read yields over it; the oversize contract must hold. (J6) the oversize diagnostic's two rules — **its own stable code**, distinct from the no-path diagnostic it must never be mistaken for, and **exactly one per invocation** — were compressed into the words "safe diagnostics". Both are violable with every cap test green
      - **J7–J9 · §3/C53, where every named mutation is structural.** (J7) an F8 ID renamed consistently on **both** sides preserves the one-to-one join and passes, and "unknown ID" has nothing to be unknown against, because F7 rejects a central mapping constant. **Fork closed — a contract test pins the seven shipped callable classifications — six callable↔policy-ID pairs plus the operational assignment.** That is a regression pin, not a join mechanism: the join still runs prose→docstring at check time, and what F7 forbids is a constant standing *between* the two sides. (J8) F8/2 requires the operational rationale to say why the classification is not a demotion; the seed checks non-emptiness, so `n/a` passes. The dictated substance is pinned as shipped text, and for classifications added later only non-emptiness is mechanical — stated, not left to be inferred. (J9) F8/3's `configured` / `not configured` finding — the emission **G19 assigned to C53** — landed unseeded while §10 aggregates it as an existing source. A repair that moves an obligation between tasks does not carry the obligation's seed with it, and nothing noticed for two passes
      - **J10–J11 · §4/C54.** (J10) the then-current claim "**The two repairs outside the checker are seeded too**" was false for one of them: both named mutations are `run_plan` mutations, and the `__init__.py` comment asserting that the release pipeline cuts the real version from the git tag — half of why the defect reproduces — was covered by that sentence and by nothing else. Same shape as I6/I7: a sentence claiming coverage that the next line does not deliver. (J11) F9/6's normalization and the re-release **warn** had no mutation at that point. A raw-string comparison passed every then-listed fixture while failing on the `v`-prefixed and `git describe` forms F9/6 exists for; a checker that *errors* on a final version with a matching tag passes them too, because nothing in the list distinguished warn from error there. **Repaired in the operative carrier:** the false `__init__.py` comment has a named source-contract failure, normalization and matching-tag warning have exact fixtures, and the later D2-20(d) expansion separately seeds both heading branches, the complete non-final corpus, topmost-heading selection, the all-tags population, and git-unavailable degradation.
     - **J12 · §5/C55 — reported clean, and not.** Both mutations run in `verify.py`, so an implementation that never wires the check into `self_ci` passes them and leaves akmon exempt from its own rule — the exemption that same paragraph exists to refuse, and the reason `self_ci` alone was rejected in the other direction. The severity is unasserted too, in the one section whose neighbouring argument is that the same shape takes different severities in different checkers
      - **F17 · PlannedFile ownership mode (§5/C55) — owner choice A.** J12's missing self-CI/severity seeds exposed a deeper fork: treating every planned path as a banner-bearing whole-file output makes clean JSON and merged user configuration fail, while exempting them ad hoc leaves the generator's declaration incomplete. Accepted: every `PlannedFile` declares exactly one of three canonical modes — **bannered-file**, **structured-file**, or **field-owned** — and the exhaustive mode set is contract-pinned. The stale scanner is bounded to generator-declared output surfaces and banner syntax, never the mounted akmon source or arbitrary user files. Rejected: the universal banner rule, because JSON cannot carry a comment banner and merged files are not wholly generator-owned; rejected: filename/extension inference, which recreates a second ownership declaration outside the plan. Accepted cost: adding a planned artifact now requires an explicit mode and scanner-scope review.
      - **J13 · §6/C56 — reported clean, and not.** Both caps are two-dimensional (lines **and** bytes) and both seeds pad lines, so a check that never counts bytes passes them while 12 KB of long lines goes unmeasured. Separately, Acceptance requires these checks to **report the measured numbers** so C60's reduction can be verified against the same counter — and no seed asks them to print anything, which would leave C60 with a target and no instrument
      - **F18 · Always-loaded scope, units, and reporting (§6/C56) — owner choice A.** J13's missing byte and reporting seeds exposed that neither counted population nor byte unit was contract-pinned. Accepted: the akmon-shipped scope is the marked akmon block in `AGENTS.md` plus the complete guardrail chain it imports and the selected per-language guardrail; the consumer-total scope is the complete consumer `AGENTS.md` plus that same imported chain. Caps are inclusive and decimal: 210 lines / 12,000 bytes for akmon-shipped and 460 lines / 25,000 bytes for consumer-total. One counter supplies enforcement and dynamic reporting of scope, measured lines, measured bytes, and both caps. Rejected: counting all of `AGENTS.md` as akmon-shipped, sampling representative files, binary-KB interpretation, or maintaining a second reporting counter. Accepted cost: every change to an always-loaded population or counter rendering updates the exhaustive population fixtures and reviews headroom; actual reduction and target justification remain C60.
      - **J14–J16 · §7/C57, including a false finding this lock would have shipped.** (J14) the evidence rule is two-way — a measured value requires a citation, `unmeasured` requires none — and only the first direction was seeded, though the axis exists precisely to keep the two states distinguishable by a check rather than by a reader. (J15) **the declared runtime set has two populations and the join reads one.** `claude` and `codex` are invoked by `sync` itself for §8's inventory query and appear in no generated command, so "a declared binary no generated command uses **warns**" fires on the two optional binaries §8 depends on. **Fork closed:** the **population** (*generated wiring* / *akmon's own tooling*) and the **modality** (required, optional, or required on a named route — as `git` is on Codex) are declared with each binary and compared against their own source. Rejected: dropping the optional pair from the declaration, which restores the silence §7 exists to remove and leaves §8 pointing at a declaration that omits its own binaries. (J16) Windows-unsupported is a **scope declaration, not a checkable rule**, and is fenced as one, beside the two boundaries already fenced there, so the next audit reads the missing seed as a property of the claim
      - **F19 · Capability-matrix location and transition (§7/C57) — owner choice A.** The live tree must go red without pretending the legacy README table already has the new schema: `self_ci` independently emits `error matrix.missing-file` because top-level `CAPABILITIES.md` with its marked region is absent, and emits `error matrix.bare-claim` for the forbidden claims in the legacy README table and unqualified delegation prose. It does **not** parse the legacy table as a six-axis matrix and expects no current `matrix.missing-axis` population from it. After conversion, top-level `CAPABILITIES.md` with one marked region is the sole detailed matrix and README keeps only a claim-free delivery summary; a retained detailed README table is rejected through its forbidden claims. A16 later consumes these prose cells into owned data/schema and generation; C57 does not wait for A16. Rejected: a legacy-table parser, creating or hand-repairing `CAPABILITIES.md` before both live failures are observed, keeping two authoritative tables, hiding the only matrix under `meta/`, or compressing the six axes behind a legend. Accepted cost: prose/schema duplication lasts until A16; C57 carries no transitional parser.
      - **F20 · Exhaustive neutral/vendor tool ownership (§8/C46) — owner choice A.** `hook_core` owns exactly `edit` / `shell` / `read` / `subagent`; `k_explorer`, `k_reasoner` and `k_auditor` are exactly restricted to `{read, shell}`, while `k_mechanic`, `k_validator` and `k_implementer` use one explicit unrestricted sentinel. One version-stamped vendor tool-and-matcher inventory is the sole name owner consumed by generated agent frontmatter, generated Claude/Codex hook matchers and Claude/Codex adapter normalization. Detection selects an exact known row, absent binaries use and disclose the declared row, and an unknown detected version uses the newest known row with one warning and durable selected/detected provenance. Exact public findings are `error harness.invalid-inventory`, `error harness.unknown-name`, `warn harness.capability-degraded`, `warn harness.unknown-version`, `error harness.duplicate-name-owner` and `error harness.unknown-capability`; missing or wrong provenance is an exact artifact/contract-suite failure, not a seventh finding code. Errors exit 1 through `sync` and `sync --check`; warn-only operation exits 0 when `sync --check` has no independent drift. Rejected: frontmatter-only repair, an unpinned literal scan, declaration-only or mandatory detection, failing on a new vendor release, and degrading every capability to shell. Accepted cost: adapters and generators depend on the inventory, current populations are regression-pinned, and each capability, consumer class or harness version addition requires a data-and-test update.
      - **F21 · Exact dispatch-request ledger contract (§9/C58) — owner choice A.** Keep F10 dispatch-only, but define the event as the observed Claude `PreToolUse` dispatch request normalized through C46's `subagent` capability — never launch or completion. The sole writer emits versioned seven-field TSV to `<AITNA_ROOT>/model-routing.log`; stats and coverage-map are the exact default readers, consuming legacy `.claude/model-routing.log` first and the new file second with no deduplication, while `coverage_map.py --log` reads only its explicit path. Records are `v1` plus timestamp/session_id/subagent/model/zone/description; each data field uses `" ".join(str(value).split())`, empty→`-`, and timestamp is offset-bearing ISO. Successful in-envelope appends are serialized and intact; cap and exception behavior belongs to §2/C52 and its D2-23 evidence, with no C52/D2-23 task edge from C58, and C58 adds no rotation. Both paths stay gitignored. Rejected: PreToolUse-as-launch proof, a vendor-owned primary path, unversioned rows, a second/model writer, lock-free append, silent repair/deduplication/rotation, and authenticity claims over an unsigned local file. Accepted costs: duplicate-looking old/new rows both count, valid manual v1 rows are indistinguishable from hook rows, and operational failure leaves the request ledger incomplete. Exact self-CI errors are `ledger.wiring-drift`, `ledger.owner-drift`, `ledger.path-drift`, `ledger.schema-drift` and `ledger.ignore-drift`, each severity error and exit 1.
      - **F22 · Exact live status-source contract (§10/C59) — owner choice A.** C59 composes exactly the fresh consumer `verify` stream and then the fresh `sync --check` stream. C53's D2 state and C56's caps state occur exactly once through verify; C59 does not re-read their files, cache a snapshot, sort, deduplicate, synthesize findings or write consumer state. Both renderings expose exact ordered source equality through C51's sole serializer and full severity/strict exit matrix. Rejected: cached status, direct reparse/second ownership, status-only summaries and a persisted role marker created to make a standalone CLI claim transcript visibility. Accepted costs: source population/order become contract surface, fresh checks run on each invocation, and active role remains unavailable outside the harness transcript.
      - **J17 · §8/C46 — reported clean, and not.** The one-owner rule is seeded against **vendor** names only, so a second *neutral* vocabulary — a capability token C46 invents instead of adding to `hook_core` — passes all six seeds. Not hypothetical: this section's own prose named `search`, a token in no vocabulary anywhere in the tree, and **G2 found it by reading**, which is exactly the state a seed exists to replace. The check now runs from the other direction as well
      - **J18 · §10/C59 — the anti-exemption fork's own hole.** Every named mutation removes or disturbs a source finding, so an aggregator that copies its sources faithfully and **adds a finding of its own** to both renderings passes all of them while breaking the sentence §10 opens with. The aggregate is now asserted **equal** to the composed source set rather than merely containing it. F11 refused C59 the exemption it had a case for, and the seed it got instead was one-directional
      - **J19 · the misquoted as-is had a fourth site, and my own sweep could not have found it.** 0012's consequence bullet said every test pinning "today's `[{severity}] {message}` text" — the same claim I8 repaired in three places, in a fourth. It survived because **I8's sweep resolved `path:line` citations, and this sentence quotes the tree without citing it**: the population was wrong, not the method. The lesson from I8 stands and gains a clause — a sweep over citations does not cover quoted code, so the fragment itself (`[{…}] {message}`) is what has to be searched for. Repaired; the tree now carries no `{severity}` outside the post-rename target
      - **J20 · both ADRs carried the one-directional form of two rules the lock has now repaired.** 0012's **F11** bullet — the stage-wide yardstick, which travels with that ADR — stated fidelity as "dropped, reordered, or present in one rendering but not the other", the exact form J18 found insufficient, so the ADR that *governs* seeding carried the hole in its own statement of the rule. 0013's F6 half states the evidence rule correctly as two-way and then enumerates C57's check in one direction only, which is J14 on the ADR side. Both repaired, along with two transfers that F13 changed: 0012 now names the retired-name record F1's never-reused clause needs, and says that non-emptiness cannot see whether an operational rationale says what F8/2 requires. **Neither ADR was in the clause-(d) pass's scope**, and the yardstick living in an ADR while the contracts live in the lock is exactly the seam where a repair stops halfway
      - **What this changes for the gate.** No task boundary moves; F19 adds `C51 → C57` because C57 emits through the shared finding envelope, F20 expands the existing C46 carrier without changing its C51+C57 dependencies, while F21 adds C46 to C58's existing C57 dependency; every repair lands inside an existing carrier, whose row is updated with the added obligation. Eight repairs and owner follow-ups close a **fork** rather than add a test — the retired-code list (§1), the shipped-classification pin (§3), §7's two runtime populations, §5's PlannedFile ownership modes, §6's exact cap scopes, units, and reporting counter, §7's matrix location/transition, §8's exhaustive neutral/vendor ownership and populations, and §9's exact dispatch-ledger event/schema/ownership contract — so they were put to the owner rather than left for an implementer to settle. **All eight accepted**, and each clears the register's own counting rule (two defensible answers; a change to what C51, C53, C57, C55, C56, C46 or C58 implements; not deferrable without the lock claiming a coverage it lacks), so they enter the register as **F14–F21** rather than living in a working-record bullet — the register is the owner's fork decisions, and eight of them outside it is the inconsistency class these passes exposed. F14, F15, F17, F18, F19, F20 and F21 are carried into 0012 beside the contracts they amend; **F16 stays in the lock with F9, F10 and F12**, because neither ADR carries §7's runtime declaration, and clause (b) of D2-20 continues to name exactly four lock-only rows. The extent spelling is updated wherever the current register is stated — lock header, 0012's provenance line, the ledger row's ownership and clauses (a) and (b), and (c) above — because **I8 was exactly this drift left for later**. Clause (d) now has no open question in it: the graph reconciles, the contracts are seeded per F13, and the eight forks are decided
      - **Readiness pass before the gate — two carriers named less than they carried.** A pre-gate check of D2-20's five clauses against the tree found the artifacts themselves coherent and every clause covered, and found the *checklist* short in two places. **(1)** The ledger's clause (b) enumerates what ADR 0012 carries and named the yardstick and F17–F22 while omitting **F14** and **F15**, which that ADR does carry — so the one clause that verifies the ADR boundary would not have put the retired-code record or the classification pin in front of the owner, and both are amendments whose cost (a manual append, a second list of the same seven IDs) is exactly what a boundary check exists to surface. Clause (b) now names them. **(2)** `## Acceptance` enumerated suite pins for C57, C46 and the caps counters but not for **C55**, **C58** or **C59**, whose contracts F17, F21 and F22 had just created; the asymmetry was chronological — the older three entered during the red-before-green round, the newer three landed in their own sections and never reached the summary. Acceptance now carries all six **in section order** (§5, §6, §7, §8, §9, §10), so the list is read as a sequence rather than a sample. Neither is a fork and neither changes a rule: the obligations were already stated in the sections, and this is the enumeration drift the J-series keeps finding — a carrier claiming a completeness one of its readers does not get. C58's new line also draws the boundary the section draws: the malformed-row diagnostic is C58's, while the envelope and write-failure oracle stays with the §2/C52 corpus
      - **Later §10 extension to the gate paragraph above.** F22 is the ninth owner follow-up after F13 and extends the register from F14–F21 to F14–F22 without moving a task edge; it adds C59 to the implementation list and is carried into ADR 0012. The four lock-only rows remain F9, F10, F12 and F16.
- A12 · stage 1 design — deterministic hardening contracts (plan P1.1–P1.7 + P0.2/P0.4 decisions + C46 tool-name half) · done · the design was walked through fork by fork; the choices were recorded, consolidated into ADRs, audited, and owner-verified. The decision set is now consolidated into [ADR 0012](decisions/0012-stage1-contracts-and-vocabulary.md) and [ADR 0013](decisions/0013-hook-survivability-and-crash-posture.md), both Accepted after D2-20 was verified at `e89f3fe`. **A17 carried and closed that sequence.** The separately accepted agent-name slice remains locked by [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md) · [rationale: theme 6](reviews/alternatives/walkthrough-a11-20260717.md)
- C40 · stage 1 implementation · done · engineer · umbrella carrier only; the locked design split it into C51–C59 plus the remaining C46 tool-name half, with C60 as a later cap-reduction carrier. Final task boundaries and order are locked by the A12 walkthrough, coherence audit, ADRs, and D2-20 owner verification at `e89f3fe`
- C65 · TASKS.md status check reads the whole line, not the status field · done · engineer · `bin/verify.py`'s backlog check **could not fail on the defect it names**: `_TASK_STATUS_RE = \b(active|blocked|deferred|done)\b` was searched over the entire entry line, so an entry whose *prose* contained "blocked" passed as well-formed with any status text at all — which is how this backlog drifted to **seventeen** free-form statuses (`in-progress`, `code-complete & D2-pending`, `fixed`, `pending`, `owner-approved: build`) under a green check. The mirror defect sat one line below: `\bdone\b` over the same whole line reported any entry whose prose mentions "done" as needing archiving. A third fell out while fixing them — `line.lstrip().startswith("- ")` counted **indented notes under an entry** as entries, and `pipelines/tasks.md` allows exactly such a note, so the grammar was being enforced against text it does not govern. **Shipped:** `_entry_status` reads the third ` · ` field and compares its head word, so the house form `blocked (after C51)` and `**deferred**` stay valid while a free-form status fails; entries are top-level list items only; and both warnings name the offending ids via `_sample` rather than printing a count a reader cannot act on. Six tests, four of them **observed red on the pre-fix checker** — the free-form status produced no warning at all, the prose "done" produced a false one, the indented note produced a spurious malformed-entry warning, and the archive warning named nothing. **Consumer-visible**, hence a CHANGELOG line: a consumer with free-form statuses starts seeing a warning it did not see before. The seventeen statuses in this file were repaired in the same session, and the strict rule now reports 0 of 58 entries outside the vocabulary · `bin/verify.py` + `meta/tests/test_verify.py` · provenance: C62 session, the status sweep that followed it
- A7 · reconcile engineer role vs auditor on-signal (matrix coherence) · done · architect · **Locked (b) — recorded in design §9.4/§10.2/§10.4 and implemented in C18.** §9.4 permits an on-signal code audit but §10.2 omitted it for engineer (auditor **seam 1**). **Decided (b):** `audit` (+ `independent-review`) become **cross-cutting verification kinds** — routable from any role, exempt from the C20 role-matrix warning; *when* they apply is the structural trigger (§9.5), not the role. Dissolves the seam at source and simplifies C20 (no per-row copy to maintain); consequence — learn/release may route them too (advisory-only). Impl in **C18**: registry `cross_cutting_kinds` + `role_matrix_warning` exemption + test + ADR 0005 addendum. Sub-fork deferred (auto-trigger `plan-draft` §9.3 item 5 on un-decomposed high-leverage work — verdict contradiction 2's economics root); it carries its own id **A18** rather than living on in this archived line · [design §9.4+§10.2+§9.3](design/model-routing.md) · provenance: verdict.md seam 1 / contradiction 2 (local, untracked)
- C26 · self-CI drift: model-routing checks/fixture lag C15/ADR-0005 · done · engineer · **Promoted to major at the review linked below.** two checks pre-date the model-routing work and fail `meta/self_ci.py` on a clean tree (surfaced running self-CI during C18, not caused by it): (a) `bin/verify.py::_check_model_routing_registry` requires `selection_policy.reasoner=="highest"`, but ADR 0005 made the reasoner **dynamic** (`registry.reasoner:"orchestrator"`) → false "needs semantic selection_policy" error for anthropic+openai; (b) `meta/self_ci.py`'s fixture copy-list omits `tools/model_routing/second_opinion.py` (added C15/C16) so verify's must-exist check fails on the fixture. Fix: relax the reasoner assertion to the dynamic shape + add `second_opinion.py` (and any peer tool now required) to the fixture copy-list. **Confirmed live (review-20260705):** meta tests 338 green while `verify --strict` and `self_ci.py` both exit 1 — every verify-touching CI leg is deterministically red at this tree state; fix before reading anything into CI history. Root cause generalizes (checker hand-restates registry values — violates "derive from the single source"): prefer structural checks / derive expectations from the policy vocabulary, and add a test running the checker against the *live* registry · bin/verify.py + meta/self_ci.py · [review](reviews/review-20260705.md) finding 2. **Landed (this session):** (a) `_check_model_routing_registry` now accepts `reasoner` in `("orchestrator", "highest")`; (b) `second_opinion.py` added to `self_ci.py`'s fixture copy-list; (c) three new `test_verify.py` cases, including one asserting the checker passes against the *live* registry.json (not just a fixture). `self_ci.py` now exits 0; `verify.py --strict` still exits 1, but only on the separately-tracked **C35** USE-surface finding — unrelated to this item. 345 meta tests passed, ruff clean.
- C35 · USE-surface self-containment sweep (akmon `skills/`) · done · engineer · `skills/stats-digest/SKILL.md:11` cites `../../meta/design/model-routing.md` — a consumer-facing skill pointing into the dev tree, violating the ADR 0003 cordon (`verify --strict` errors on exactly this); `skills/release/SKILL.md` is the same surface and was checked by no audit zone. Replace dev-tree links with self-contained text or MODEL.md anchors across `skills/*`, re-run verify to green; also decide the cordon boundary for the new `examples/` dir (verify checks only `skills/` — `examples/gate-anatomy.md` carries one provenance link into `meta/reviews/`, same class as README's meta links: rule on whether that's in or out and teach verify accordingly) · [review](reviews/review-20260705.md) finding 7. **Landed (this session):** (a) rewrote stats-digest/SKILL.md:10-15 to drop the meta/design/ link, self-contained now; (b) `skills/release/SKILL.md` re-checked — already compliant, no change needed; (c) decided the `examples/` cordon boundary explicitly: same bridge class as README/CHANGELOG (maintainer-facing worked examples, not operative consumer guidance), documented in verify.py's USE-surface comment rather than left an accidental globs gap; (d) two new tests: one runs `check_use_surface_isolation()` against the live tree (the actual regression), one locks in the examples/ exemption. `verify --strict` still exits 1, but only on the separately-tracked V1 mixed keystone/akmon memory-index warning — unrelated to this item. 350 meta tests passed, ruff clean.
- C9 · release_check.py akmon-subject scoping · done · engineer · `--check --subject akmon` now runs upstream gates from the akmon source root (`meta/self_ci.py` plus meta tests) instead of applying consumer-only `bin/sync.py` and `bin/verify.py` to either a consumer or the upstream checkout; release-check runner resolution remains covered · tools/release/release_check.py + meta/tests/test_release_check.py
- N2 · stage 0 — vendor delivery probes & inventory (plan P0.1, P0.3 + the as-is half of P0.2/P0.4) · done · **Both arms complete.** prove real delivery paths (Claude → Codex, D4 built-ins arm); invariant + boundary/volume inventory; findings only · [rationale: themes 2, 6](reviews/alternatives/walkthrough-a11-20260717.md) · **findings: [n2-stage0-probes-inventory-20260807.md](reviews/alternatives/n2-stage0-probes-inventory-20260807.md)** — live probes on the attached consumer (alphavar, Claude Code 2.1.221, $0.688): guardrail delivery to built-ins is **type-dependent** (`Explore` gets zero akmon context, `general-purpose` gets the full chain); the `k-*` model pin holds against an opus session while built-ins carry **no pin** (they inherit the session model both directions) — so the tier floor is unenforceable on built-ins, which is the D4 economics answer; the delegation log **does** observe built-in dispatch but records no model for it (`bound_model_for` is registry-only), so coverage-map/stats undercount it; the SessionStart roster reaches **no** subagent, `k-explorer` included. P0.3: 5 invariant pairs (3 declared + 2 undeclared) + 1 orphan hook (`role_on_code_result`) — P1.2 must cover 5, not 3. P0.4: always-loaded chain measured at 433 lines / 23.6 KB (AGENTS.md 232 lines is hand-owned and the largest single item). **Spawns C46** (stale tool declarations). **Codex arm (codex-cli 0.146.0, [findings §1A](reviews/alternatives/n2-stage0-probes-inventory-20260807.md)):** Codex does **not** expand `@`-imports in `AGENTS.md` — the always-on guardrails (`_common.md`, `python.md`) never load in any Codex session, parent or child, so C39/D2-13 is now measured rather than assumed and the gap's shape is "the import mechanism is Claude-only", not "guardrails arrive weakened"; a delegate receives `AGENTS.md` through its own `world_state` but receives **nothing akmon injects at SessionStart** (`fork_turns="none"` → `[akmon]` ABSENT; the `fork_turns="all"` run only copied the parent's turns, same message ids), so akmon's dynamic channel is parent-only; the **model pin holds** (`thread_settings_applied {"model":"gpt-5.6-terra"}` + child `turn_context`, while the child self-reported "GPT-5" — enforced by the harness, invisible to the agent) and `agent_name` is a real addressable identity (`author:"/root"` → `recipient:"/root/k_explorer"`), which is what forced [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md); the advisory surface is inert for two independent reasons (**C48** payload key, **C47** relative paths); and a hard `deny` **is** enforced but only on the route its matcher names — the model re-issued the denied `apply_patch` through `exec_command` in the same turn, unprompted, and it went through, **spawning C49**. Method note carried into N1: agent self-reports about the harness are unreliable (two runs void on wrong workspace root, one wrong model self-report) — read codex's rollout files instead. Still unmeasured: `ask` as distinct from hard `deny`, TUI vs headless equivalence, and whether any subagent-start injection point exists on Codex (the Codex half of P4.1)
- C62 · `validate.py` picks its pytest runner from a fact that no longer holds · done · engineer · `meta/bin/validate.py::_pytest_command` justifies its runner choice with "akmon is stdlib-only and **ships no pyproject**, so a bare `uv run pytest` cannot provision pytest". Since C37 akmon ships `pyproject.toml` with `[dependency-groups] dev = ["pytest>=8", "ruff>=0.8"]`, which `uv run` installs by default — the stated reason is dead. The behaviour built on it is worse than the comment: the fallback fires only when **both** `uv` *and* `pytest` are on PATH, so it refuses `uv run pytest` in exactly the case `uv` now handles (no pytest present, resolvable manifest with a dev group) and accepts it only when pytest is already installed. Fix the comment to the real distinction — akmon's own root now resolves a manifest, a consumer dev layer may not — and re-derive the guard from *manifest resolvability* rather than from `which("pytest")`, keeping the probe-before-claiming shape so "pytest absent" stays a skip-warning and never a spurious failure. Surfaced beside F9 as a packaging-era leftover, deliberately kept out of C54: same era, different contract. **Shipped:** the guard is now `uv` on PATH **plus** a manifest that declares pytest in a group `uv run` installs by default (project dependencies + the default dependency-groups, honouring `[tool.uv] default-groups`); `which("pytest")` no longer participates at all. `_pytest_command` returns `(runner, reason)` so the skip-warning names which of the three ways it failed to resolve instead of the old undifferentiated "no pytest runner found" — the probe-before-claiming shape is unchanged and every unreadable state answers *no*, because a skip is the safe direction and a claimed runner is not. **Both of the old guard's answers were measured wrong on the pre-fix code**, not argued: a root whose manifest declares pytest with pytest absent returned `None` (refused exactly the case `uv` handles), and a root with **no manifest at all** but pytest on PATH returned `["uv", "run", "pytest"]` — a runner claimed where this root declares nothing to provision from, so the outcome then depended on a PATH binary rather than on the project. Eleven tests in a new `meta/tests/test_validate.py`, all observed red first. **Carried forward, not fixed here:** `tools/release/release_check.py::_pytest_command` is a **second owner** of runner selection with a richer discovery order (pinned `[test].runner` → `_aitna/.venv` → system pytest → `uv run --with pytest` → `python -m pytest`) and a contradictory claim about the same command — it states that a bare `uv run pytest` cannot work, which is the pre-C37 fact this task just retired. The two have genuinely different contracts (this one skips when nothing resolves, that one always returns a command and lets it fail), so unifying them is a design question rather than an engineer's call · `meta/bin/validate.py` + `meta/tests/test_validate.py` · provenance: F9 packet side finding,
- C61 · version-spelling normalization in the CLI skew notice · done · engineer · surfaced by **F9/6** and spun off deliberately: a defect with one right answer is not an owner fork. `src/akmon/cli.py::_skew_notice` compares the recorded pin to `__version__` with raw string equality and then renders `v{pinned}`, but the two sides use **different spellings by construction** — mounted mode records `git describe --tags` (`v0.3.0`, possibly with a `-N-gSHA` suffix, per BOOTSTRAP), package mode records installed metadata (`0.4.0.dev0`, PEP 440, no `v`, `bin/sync.py::_package_mode_akmon_toml`). So on any mounted consumer the notice fires on **every** command and prints "v v0.3.0". The existing test writes the pin as `f"{__version__}"` (`meta/tests/test_cli.py`), i.e. it only ever covers the exact-match case, which is why nothing caught this. Fix per F9's locked rule: normalize before comparing (strip a leading `v`; treat a `describe` suffix as "ahead of that tag", which is a *different* notice from a version mismatch, not the same one), and render the pin as recorded rather than re-prefixing it. Tests must cover both spellings and the describe-suffix form. Not part of C54 — C54 owns the release-time invariant, this owns the runtime notice. **Shipped:** `cli._split_version` returns `(base, commits_ahead)` and is applied to *both* sides, so one normalizer governs the comparison; a differing base keeps the mismatch notice, an equal base with a `describe` distance gets its own sentence naming the distance, and an equal base with no distance is silent — the case that fired on every command in a mounted consumer. Both notices render each version **as recorded**. A PEP 440 pre/post/dev segment is deliberately *not* normalized: `0.4.0` and `0.4.0.dev0` are different versions, not two spellings, which is F9/3 held to at runtime. Four tests, **observed red on the pre-fix code** before green (two assertion failures, one double-prefix, one missing callable). **One correction to this row:** it said the notice prints `v v0.3.0`; the live rendering is `vv0.3.0` — no space, since the old form interpolated `v{pinned}` into an already-prefixed pin. **Carried forward:** C54 needs the same F9/6 rule at release time and should lift this helper rather than write a second one — the second-owner pattern C46 exists to remove · `src/akmon/cli.py` + `meta/tests/test_cli.py` · provenance: F9 packet,
- C63 · D2 approval state · done · engineer · D2-24 is Verified at `f6d181c`; the status had kept advertising "awaiting owner landing" after the transition it names had already been recorded, found at A17(g) · [design](design/d2-ledger.md)
- C47 · path predicates must agree on relative and absolute forms · done · engineer · `is_planning_doc` matched raw path strings against slash-delimited segments, so a **relative** path never matched and the hook returned `None` — silently, with "did not match" indistinguishable from "matched and stayed quiet"; the one predicate guarding the owner's analysis-before-mutation rule was the one that failed, probe-verified live in alphavar (N2 G2). Fixed as a collapse, not a patch of the broken predicate: `classify_target(file_path, root)` renders any input project-relative POSIX and all three predicates now share the single normalizer the D2 branch used alone. The canonicalization is lexical (`normpath`), **not** `resolve()` — resolving follows symlinks, so a linked subtree pointing out of the repo silenced every predicate underneath it: C47's own defect class re-entered through the guard added to harden it, caught in review before landing. Root threaded from the payload `cwd` on both vendors; tests pin the invariant *and* the answers, so a uniformly-wrong result cannot pass as agreement. Owner gate [D2-17](D2_LEDGER.md) — an advisory-grade classification boundary, explicitly not containment · landed f6d181c · hooks/hook_core.py::classify_target + hooks/claude_adapter.py::project_root · provenance: N2 stage-0 probe G2
- C48 · codex adapter reads the wrong payload key: every advisory hook on Codex is inert · done · engineer · `codex_adapter.file_paths` sourced the patch body from invented keys (`tool_input.patch`, `payload.patch`) that no measured Codex version sends, while codex 0.146.0 puts the whole patch in `tool_input.command` — the key the adapter's own `command()` already read. All three wired PreToolUse advisories therefore returned `None` on every Codex edit since they were wired. The invented keys were **removed** rather than kept as insurance: a key list that looks thorough while missing the only real one is what hid the defect. Three near-identical advisory bodies collapsed into one `_advisory`, and an edit-kind call with no extractable path now reports on stderr with the payload's key *names* — never its values, since a patch body carries file contents. Owner gate [D2-18](D2_LEDGER.md), extended at verification to the nine guessed *path* keys, which stay but no longer pass as measured fact · landed f6d181c · hooks/codex_adapter.py::file_paths/unmeasured_path_source + hooks/codex-hook.py::_advisory · provenance: payload captured live from codex 0.146.0
- C49 · the Codex PreToolUse matcher covers the patch route three times and the shell route not at all · done · engineer · measured on codex 0.146.0: `Edit`, `Write` and `apply_patch` are three aliases for one patch route, while the shell route matches only as `Bash` — so the generated matcher named one route three times and the other never. `bin/sync.py` now emits `Bash|apply_patch`, one name per measured route and deliberately not `.*`. A shell command is path-classified only when a recognized `apply_patch` invocation sits in **command position** and opens a patch envelope; every other Bash call stays unclassified and draws one diagnostic saying the route may mutate files unseen — no guessed paths, no guessed write detector. Owner gate [D2-19](D2_LEDGER.md), widened at verification: the diagnostic is vendor-neutral and fires on **Claude** too, where a `Bash` write had been unreported entirely. Residual recorded, not closed: the matcher is minimal by measurement and therefore silent on drift — that fix belongs to C46's version-stamped inventory · landed f6d181c · bin/sync.py::_codex_hooks + hooks/codex_adapter.py::is_apply_patch_command + hooks/hook_core.py::report_unclassified_shell_route
- C50 · consumer briefs are keyed by agent name; the `k_*` rename orphans them silently · done · engineer · a regression introduced by [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md), not a pre-existing defect: overlay `briefs` keys are hand-authored in the consumer's repo and were matched against `spec.name` exactly, so the rename orphaned alphavar's four live briefs and `remove_obsolete_agents` then deleted the `k-*.md` files still carrying them. Lookup is now notation-insensitive (`agent_key` folds case, `-`/`_` and surrounding whitespace — an internal space stays significant), an unmatched or colliding key is a hard `BriefError` instead of an empty brief, `init.py` writes nothing and exits 2, and the hook **refuses the rebind** so delegates stay pinned to the previous model until the overlay is fixed. Owner gate [D2-16](D2_LEDGER.md): forgiving notation but not spelling is where the tolerance line belongs — a typo must fail, a rename must not · landed f6d181c · tools/model_routing/routing.py::resolve_briefs/agent_key + hooks/model-routing.py · re-checked read-only against alphavar's live overlay: all four keys resolve
- C45 · `d2_ledger.py add` duplicate-id bug · done · engineer · `table_bounds` now skips blank lines within a table's data range instead of stopping at the first one, trimming `end` back to just past the last real row; 3 new regression tests (blank-gap bounds/next_id/add_entry), 424 meta tests green, ruff clean · tools/d2_ledger/d2_ledger.py::table_bounds · provenance: manual D2-14 fix, this session
- C38 · privilege-escalation guardrail (`sudo` deny) · done · engineer · unconditional Bash `sudo` deny wired into the PreToolUse entrypoint (Claude + Codex parity); a permission boundary is information for the owner, not an obstacle to route around · hooks/hook_core.py + guardrails/_common.md § Privilege escalation · provenance: owner instruction, not review-20260705
- A11 · alternatives-plan walkthrough: accept/reject per theme · done · architect+owner · all 7 themes accepted, decisions D1–D4 closed (D1 mutability rejected, D2 probe-first, D3 parked to C42, D4 per-vendor by N2 data); verdict set folded into [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md), owner-verify D2-14 · per-theme rationale, amendments, pilot criteria and revisit conditions: [walkthrough record](reviews/alternatives/walkthrough-a11-20260717.md)
- A10 · packaging: `uvx akmon init` design lock · done · architect · [ADR 0009](decisions/0009-packaging-package-carrier-and-mount-modes.md) Accepted, owner-verified (D2-12): option-C scope, PyPI name `akmon` (checked free), floor 3.9, four mount modes incl. new `package` (dev-group pin + `<aitna>/.akmon/` always-on materialization; pilot alphavar), skew rule, init pin = latest tag; consumer-facing docs in BOOTSTRAP §F + README (BOOTSTRAP consumer-only — co-dev workflow lives in README) · [design](design/packaging/README.md)
- C11 · D2 ledger · done · architect/engineer · 5 phases shipped ([ADR 0007](decisions/0007-d2-ledger.md), [design](design/d2-ledger.md)): `d2_ledger.py` add/list/verify/check (stdlib-only, never git) + PreToolUse reminder hook + SessionStart `D2: N pending` counter + warn-first pre-commit step; akmon mechanism, project `.akmon.toml` `[d2_ledger] sensitive_paths`; ledger entry = unit of verify gate for reasoner/second-opinion attachments (feeds C16 gate-pack, per A5); ADR wording reconciled to the build
- A5 · synthesizer + roles design: owner lock · done · architect · §9.7 + §10.4 locked by owner → [ADR 0005](decisions/0005-synthesizer-gate-audit-and-role-routing.md) (extends 0004); unblocks C15–C20 · [design §9.7+§10.4](design/model-routing.md)
- C24 · tasks-archive tool · done · engineer · `tools/tasks/archive.py` moves `· done ·` entries (bullet + indented continuation) from TASKS.md into TASKS_ARCHIVE.md `## Done`; `--check` dry-run (exit 1, CI-usable) / `--apply`; verbatim move, never git; 13 tests. Mechanizes the tasks-pipeline archive step (was hand-editing) · [pipelines/tasks.md](../pipelines/tasks.md)
- C21 · delegation indication in the host UI · done · engineer · delegation-log hook emits a one-line `systemMessage` per subagent call (`[akmon] → <agent> (<model>): <description>`) — UI-visible at zero token cost, TSV log kept as the record; `_format_system_message` + 3 tests (test_adapters.py) · owner request 2026-07
- C15 · registry/binding: synthesizer tier · done · engineer · pinned-max `synthesizer` policy + `synthesis-verify`/`plan-draft` rows + dynamic reasoner (orchestrator rung + per-kind floors) + `role_task_kinds` + diversity-ladder registry data; compute_binding, init, generated `k-synthesizer` (read-only, pinned max) — all binding-side, reality-verified; downstream consumers: ladder+`second_opinion_fallback_model`→C16, `role_task_kinds`→C20, `gate_triggers`→C16 · [ADR 0005](decisions/0005-synthesizer-gate-audit-and-role-routing.md)
- T0 · tooling test coverage · done · engineer · `tests/` for `hook_core`/`sync`/`verify`; +sync drops stale hook entries on rename; +session-start logs swallowed errors to stderr
- T1 · cross-agent contract · done · architect · AGENTS.md remains hand-reviewed source; verify checks vendor pointers/imports and source skill-root linkage
- T2 · skill contract (minimal) · done · architect · `name/description/when_to_use/owner` frontmatter + verify check
- T5 · audit attached project (alphavar) for drift · done · architect · `sync --check` + `verify --strict` pass
- T7 · prune orphaned generated files · done · engineer · sync deletes stale banner-marked stubs; verify flags them
- T8 · akmon self-CI · done · engineer · own `tests/` + fixture `verify --strict` run in CI
- T3 · release/versioning standard · done · architect · design consolidated + locked in ADR 0001; resolves ROADMAP O2; impl phased T10–T15
- T10 · extract `learn` role · done · architect · roles/learn.md + README row; learning/memory-distill pipelines wired to the role
- T11 · `release` role + pipeline · done · architect · roles/release.md + pipelines/release.md (lightweight cut vs cadence, subject gate, anti-super-role routing)
- T12 · versioning + CHANGELOG · done · architect · v0.x.y convention + CHANGELOG.md (Unreleased); BOOTSTRAP pull-bump references release notes + downstream record
- T13 · verify CHANGELOG Unreleased warn · done · engineer · verify.check_changelog warns when CHANGELOG lacks `Unreleased`; +3 tests
- T15 · subject-relativity in layer model · done · architect · README §2: DEVELOP/USAGE/OPERATE relative to a subject; USAGE-vs-OPERATE by consequence
- T14 · release skill/tool (propose/prepare) · done · engineer · skills/release/SKILL.md + tools/release/release_check.py (--state/--check/--plan, --subject akmon|package, runner-resilient, never executes); +verify check_akmon_gitignore; pin-bump split to T18
- A5 · review role + review-flow pipeline · done · roles/review.md + pipelines/review-flow.md; analysis (decompose→measure→report), criteria as hand-off · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- A6 · narrow architect to synthesis; engineer escalation; roles/README routing · done · triad + discriminator + "why separate mode" in role docs · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- A7 · review task-id letter `N` · done · pipelines/tasks.md table + ADR 0002 amendment + interim memory note · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- A8 · README→develop/CONCEPT + thin USE MODEL.md · done · vision out of consumer USE context; AGENTS.md retargeted; thin root README bridge · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- A9 · decouple USE docs from develop artifacts · done · 11 ADR/ROADMAP citations stripped; USE→MODEL repointed; boundary check green · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C3 · create develop/, move dev artifacts · done · decisions/design/ROADMAP/TASKS/tests/self_ci/CONCEPT cordoned; all links fixed · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C4 · verify.py develop-boundary check + path updates · done · forbids USE→develop links; tooling paths + fixtures updated; self_ci + 96 tests green · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C5 · triad-aware role hooks · done · review/architect/engineer nudges in role-on-code + analysis-guard; per-direction markers; `OPERATE` label fixed · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C7 · develop→meta rename + USE/dev verify split · done · folder `develop/`→`meta/`; `bin/verify.py` USE-only; new `meta/bin/validate.py` runs dev-layer self-checks + self_ci + tests; strict USE-surface isolation (no `meta/` path, no numbered-ADR/ROADMAP/dev-path/bare-name citations); ADR 0003 §5–§6a + CHANGELOG; 102 tests green · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C8 · BOOTSTRAP: dev-layer venv on non-Python projects · done · attach step 5 provisions `_aitna/.venv` (uv-first, caveated stdlib fallback) + installs pytest when language≠python/mixed; §D gitignores it; validate.py docstring points at it; CHANGELOG Unreleased
- A4 · `_aitna/` path is configurable, not fixed · done · `AITNA_ROOT` env (default `_aitna`) configures the dev-layer root; sync.py/verify.py/hook_core derive every path (pointers, hook commands, banner, classifiers) from it; MODEL.md §2 + BOOTSTRAP §A; +13 tests
- C10 · model routing — capability tiers · done · engineer · registry (semantic selection policy + task-kind matrix) + idempotent init tool + SessionStart/delegation-log hooks + second-opinion runner; MODEL.md §10/guardrail/role/pipeline edits; alphavar consumed (init, overlay, gitignore); live-verified end-to-end (k-explorer delegation, log line, staleness→refresh) · owner-verified (D2) · [ADR 0004](decisions/0004-model-routing-capability-tiers.md) · [design](design/model-routing.md)
- A10 · SessionStart discriminator routing-hint · done · session hint carries the DEVELOP discriminator (decompose→review · construct→architect · realize→engineer) up front when dev agents exist; OPERATE-only keeps generic line; +2 tests · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C14 · delegation-nudge hook · done · engineer · `hooks/delegation-nudge.py` + `hook_core.delegation_nudge_result` (counter/marker in tempdir per session; `Task`/`Agent` resets the counter and re-arms the reminder — once per drift episode; threshold 10, env `KEYSTONE_DELEGATION_NUDGE_THRESHOLD`; advisory, never blocks); wired by sync as one combined-matcher entry — fixed an entry-level dedup bug in `_merge_hook_entries` wiring that swallowed matcher groups sharing a command; +6 tests; fired live in-session · owner-verified
- C13 · statistics digest · done · architect/engineer · `tools/model_routing/stats.py` (delegation-log + session-transcript + per-subagent token parsing; budget via Claude OAuth usage API `api/oauth/usage`, verified live; full report to `.claude/stats/`, digest to stdout) + `skills/stats-digest/SKILL.md` (chat trigger; learn-loop pass delegated to k-explorer, persistence owner-gated); +27 tests; live-verified on the building session · known limit: delegation log is cross-session (no session id in TSV) · pending owner verify
- C12 · Codex hook payload contract · done · engineer · `codex-hook.py` now uses Codex SessionStart `cwd` payload and serializes `HookResult` as `hookSpecificOutput` JSON; live-verified `codex exec` on codex-cli 0.142.5 reaches `hook: SessionStart Completed`
