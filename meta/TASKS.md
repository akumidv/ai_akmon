# TASKS — akmon development backlog

The single backlog for developing the akmon standard itself. Format:
[`pipelines/tasks.md`](../pipelines/tasks.md). Directions / model gaps: [`ROADMAP.md`](ROADMAP.md).

> This is akmon's *own* backlog (evolving the standard), distinct from a consuming
> project's `_aitna/TASKS.md`.

## Status

Tooling spine (`bin/sync.py`, `bin/verify.py`, vendor-neutral `hooks/hook_core.py`) exists and
is **tested** (`tests/`). Remaining work is contract specification + scale hardening, not new
mechanism.

## Active / blocked / deferred

Model routing ([ADR 0004](decisions/0004-model-routing-capability-tiers.md); design in
[`meta/design/model-routing.md`](design/model-routing.md)):

Auditor / gate-pack extension (design in [`model-routing.md` §9–11](design/model-routing.md)) —
**architecture** (lock, then ADR):

- A6 · roles & pipelines under the subagent model · done in design · architect · recorded as §10 (role = orchestration contract; role→task-kinds matrix; zone plan from Decompose/Survey; loop-back edges; learn/release tiers); lock items folded into A5 (done) · [design §10](design/model-routing.md)
- A7 · reconcile engineer role vs auditor on-signal (matrix coherence) · **locked (b) → recorded §9.4/§10.2/§10.4; implemented in C18** · architect · §9.4 permits an on-signal code audit but §10.2 omitted it for engineer (auditor **seam 1**). **Decided (b):** `audit` (+ `independent-review`) become **cross-cutting verification kinds** — routable from any role, exempt from the C20 role-matrix warning; *when* they apply is the structural trigger (§9.5), not the role. Dissolves the seam at source and simplifies C20 (no per-row copy to maintain); consequence — learn/release may route them too (advisory-only). Impl in **C18**: registry `cross_cutting_kinds` + `role_matrix_warning` exemption + test + ADR 0005 addendum. Sub-fork deferred (auto-trigger `plan-draft` §9.3 item 5 on un-decomposed high-leverage work — verdict contradiction 2's economics root) · [design §9.4+§10.2+§9.3](design/model-routing.md) · provenance: verdict.md seam 1 / contradiction 2 (local, untracked)

**implementation** (A5 locked → ADR 0005; C15 done → C16/C17/C20 unblocked, then C18):

- C16 · gate-pack builder · code-complete & D2-pending (D2-1/D2-2) · engineer · one structured package (artifacts + yardstick + coverage map) consumed by auditor subagent and second-opinion CLI, plus the minimal plan-check pack (yardstick + zone plan, pre-fan-out); replaces free-form `--prompt-file` in second_opinion.py; runnable as one deterministic script per §11. **Also wire the second-opinion diversity ladder** (currently dead registry data from C15): `second_opinion.py` selects provider *and model* by `second_opinion_policy.diversity_ladder` — other-vendor → same-vendor-other-model (via `second_opinion_fallback_model`, computed into config but unconsumed) → never same-model · [design §9.4+§9.3.5+§4.6](design/model-routing.md)
- C17 · coverage map from delegation log · code-complete & D2-pending (D2-3) · engineer · zone label on fan-out delegations (from the §10.3 zone plan) + assembler tool — map at code cost, not tokens. Delivered: log schema `ts·session_id·subagent·model·zone·description` (zone off a `[zone:LABEL]` marker), `coverage_map.py` (scope by `--session`, +opt `--since/--until`; uncovered-seam flags vs zone plan) feeding `gate_pack.py --coverage-map`; `gate_id` precision rides on C20's session marker without changing the scope-by-key shape · [design §9.4+§10.3](design/model-routing.md)
- C18 · docs: leverage principle + role/pipeline deltas · code+docs complete & D2-pending (D2-6) · engineer · name the leverage principle in MODEL.md; tier table + matrix rows update; review/design-flow auditor anchors (post-fan-out + the pre-fan-out plan check) + loop-back edges; roles/*.md one-paragraph deltas linking §10 incl. the drafts-vs-decides invariant reading; demote static floor warning to weak prior. **Also implement A7 (b): registry `cross_cutting_kinds` = [`independent-review`, `audit`] + a `role_matrix_warning` exemption so C20 no longer warns on cross-cutting verification kinds for any role + test + ADR 0005 addendum** — else C20 warns against the very audit routing C24/C25 enable (auditor seam 1); these anchors are the missing *process* half that says *when* to launch the auditor (seam 2) · [design §9.1–9.5+§10](design/model-routing.md) · provenance: verdict.md seams 1–2 (local, untracked)
- C19 · owner-attention metrics in stats digest · deferred · extends C13: D2 pending/verified, decisions per session, next to token spend · [design §9.6](design/model-routing.md)
- C20 · role-matrix advisory check in delegation hook · code-complete & D2-pending (D2-4) · engineer · warn when a routed subagent has no task kind in the active role's `role_task_kinds` row. Active role read from the transcript's last main-chain `🧭 agent:` declaration (chosen over a written marker — consistent with C22/C23 transcript scanning); subagent-granularity rule (warn iff kinds ∩ allowed = ∅); advisory, never blocks. **Also (D2-5):** the delegation hook now derives each agent's pinned model from the binding (`bound_model_for`), so the console line + record name the real model — completing C21's always-empty `(<model>)` · [design §10.2+§10.4](design/model-routing.md)
- C24 · deterministic gate-runner (one command) · queued (after C18) · engineer · chain `coverage_map.py` → `gate_pack.py` → `second_opinion.py` into one deterministic gate run (§11): assemble the pack from the log, get the second-opinion verdict, write the bundle. The fan-out + the k-auditor **spawn stay orchestrator-side** — a script cannot call the Task tool (§11 "vehicle, not contract"); this covers pack-assembly + second-opinion CLI + verdict only. Builds on C16 (`second_opinion.py --gate-pack` is half of it). Scopes the auditor-usage analysis's "fix #1" honestly · [design §9.4+§11](design/model-routing.md) · provenance: `_aitna/artifacts/gates/20260704-185954-subagent-usage/verdict.md` (local, untracked)
- C25 · missed-gate forcing function (revive dead `gate_triggers`) · queued (after C18) · engineer · make `gate_triggers` (`review_min_findings:3` / `architect_min_options:2` — findings/options **counts**, no zone key, currently unenforced dead registry data) an actual advisory at the review/architect gate: when a role emits ≥threshold findings/options, nudge to run the auditor. **NOT fan-out-conditioned** (zero fan-outs ever ⇒ would fire zero times — strictly weaker than the dead data it revives); independent of coverage/zones. Fixes the auditor-usage analysis's misspecified "fix #2" · [design §9.4](design/model-routing.md) · provenance: verdict.md contradiction 3 / re-rank delta 1 (local, untracked)

- C26 · self-CI drift: model-routing checks/fixture lag C15/ADR-0005 · queued · engineer · two checks pre-date the model-routing work and fail `meta/self_ci.py` on a clean tree (surfaced running self-CI during C18, not caused by it): (a) `bin/verify.py::_check_model_routing_registry` requires `selection_policy.reasoner=="highest"`, but ADR 0005 made the reasoner **dynamic** (`registry.reasoner:"orchestrator"`) → false "needs semantic selection_policy" error for anthropic+openai; (b) `meta/self_ci.py`'s fixture copy-list omits `tools/model_routing/second_opinion.py` (added C15/C16) so verify's must-exist check fails on the fixture. Fix: relax the reasoner assertion to the dynamic shape + add `second_opinion.py` (and any peer tool now required) to the fixture copy-list · bin/verify.py + meta/self_ci.py

Orchestrator detection + runtime weakness ([ADR 0006](decisions/0006-orchestrator-detection-corridor-context-pressure.md); design [§3/§4.3/§12](design/model-routing.md)):

- C22 · orchestrator detection: corridor + owner-visible warnings · implemented, awaiting owner verify (D2) · engineer · transcript detection + rebind + gitignored `k-*` + corridor warning in `compute_binding` (named floor `opus`, below-floor + reserved-top arms, silent collapse when `floor == top`, skip when floor not in ladder) + dual-channel owner-addressed output (`systemMessage`); e2e-verified on a live session · [design §3+§4.3](design/model-routing.md)
- C23 · context-pressure detection · implemented, awaiting owner verify (D2) · engineer · registry `context_pressure` block; the model-routing hook reads last main-chain `usage` in the same transcript pass, banded warnings (0.85/0.95) throttled per band via session marker, reset on compaction, dual-channel delivery; tested incl. live transcript (74% → silent) · [design §12](design/model-routing.md)
- C27 · plan-gate: delegation named up front in design-flow plan-check · queued · engineer · complement to the delegation-nudge enforcement (D2-8): extend the existing `design-flow.md` pre-fan-out plan-check gate yardstick so a realization/decomposition plan must **name a delegate per delegable sub-step** before work starts — makes "delegation up front" auditable at a gate, not only advisory at runtime (the hook catches drift *during* work; the gate catches an under-delegated *plan*). The always-loaded guardrail keeps only a pointer clause, not a second rule owner · [design §9.4+§10](design/model-routing.md) · provenance: D2-8 delegation-enforcement audit (this session)
- C28 · verify `ask`/`deny` PreToolUse contract per vendor + count-axis tuning · in-progress · engineer · **(d) LANDED** (subagent exemption via `agent_id`; hook_core+wrapper+tests, 338 passed; D2-9; motivation in model-routing.md §13). **(a) LIVE-VERIFIED** (this session, D2-10): debug-instrumented live test confirmed the hook dispatches identically for a real `Read` and a real `Bash` call — no tool-category exemption. Surfaced an unplanned, bigger gap — the emitted `ask` was not enforced/blocked for *either* tool in this background/child session; spun off as **C31**. Remaining: (b) codex runtime-enforcement check (wire shape now schema-verified — see below), (c) count-axis tuning (defer to C29 output-weight axis). · the graduated delegation nudge (D2-8) emits a hard `ask` on sustained drift; gates before the ask arm is trusted: (b) **codex vendor — wire shape SCHEMA-VERIFIED (this session)**: the installed `codex` binary (0.142.5) embeds its own JSON schema for hook I/O (extracted via `strings`/binary search, not docs); `pre-tool-use.command.output.hookSpecificOutput.permissionDecision` enum is exactly `allow|deny|ask`, matching what `codex_adapter.py`/`hook_core.py` already emit — no adapter change needed for the shape. Codex also exposes a separate `PermissionRequest` hook event (own `behavior: allow|deny` contract, `interrupt`/`updatedInput`/`updatedPermissions` explicitly reserved/fail-closed) we don't touch yet — noted, not required. **Still open:** runtime enforcement (does codex actually block on `ask`/`deny`, especially under non-interactive `codex exec`, mirroring the C31 question) — untested, owner deferred the live `codex exec` run (cost/time); do that before claiming codex enforcement is "known-good". (c) **count-axis false-positive tuning (D2-8 item 2), on the right axis** — the residual noise is **not** read-vs-write: a read-only `git diff`/`git log` across many files is the *most* expensive thing to pull inline and **should** trip the counter (delegating it to k-explorer keeps the dump out of orchestrator context); only *tiny-output* commands (`git status`, `rev-parse`, `ls`) are genuine inline-justified noise where a subagent round-trip costs more than the command. Do **not** discount read-only shell wholesale (brittle: `git status && rm -rf` isn't read-only); if the count axis over-fires, prefer the **output-weight axis (C29)** over heuristic bash-string classification; (d) **subagent false-fire — correctness defect, fix FIRST (owner: keep-and-harden the ask)**: `delegation_nudge_result` is session-keyed with no main-chain/subagent discrimination, so the nudge fires on read-only leaf subagents (k-auditor/k-explorer/k-reasoner) that have no `Task` tool — advisory wastes their context, the hard `ask` is un-actionable or blocks legit reads (demonstrated live: the C29-design audit tripped it at call #10). Suppress on subagent sessions (or count main-chain turns only); resolve whether a subagent shares the parent `session_id` (double-charge risk). **Owner decision: keep the hard ask, but only after (a) runtime-verify + (d) subagent fix land — accept fatigue risk & monitor.** Verify (b), fix pending; encode the vendor contract in the adapter · hooks/claude_adapter.py + hooks/codex_adapter.py · provenance: D2-8 + auditor "could not verify" + live subagent misfire (this session)
- C31 · background/child-session `ask` enforcement gap · queued · engineer · a live C28a test showed a hook-forced `ask` permissionDecision was not enforced (no block, no prompt) for either a `Read` or a `Bash` call inside a Claude Code background/child session (`CLAUDE_CODE_CHILD_SESSION=1`); working hypothesis is that Claude Code auto-resolves hook `ask` without an interactive owner to answer it. If confirmed, this is not scoped to the delegation nudge — `git-commit-guard`'s D5 enforcement rides the same `ask`/`deny` contract and may be equally non-blocking in background jobs. Needs: (1) reproduce/refute from a foreground/interactive session to isolate the split; (2) if confirmed, a design decision on background-session guardrail posture (e.g. `deny` instead of `ask`, or refusing unattended drift-prone work) before any hook code changes · [D2-10](D2_LEDGER.md) + model-routing.md §13.2 · provenance: C28a live test (this session)
- C29 · second axis: inline-output-weight nudge (token-proxy), advisory · **owner-approved: build (advisory)** · engineer · axis 1 (D2-8) counts delegable *calls* without a delegation (behavioural drift); add **axis 2** counting *cumulative raw tool-output pulled into orchestrator context* without a delegation — the metric that maps to **context cost in both senses**: token budget (measurable, load-bearing) *and* reasoning quality (raw dumps are low-signal noise that dilutes attention / buries detail as the window grows — context-rot / lost-in-the-middle; a subagent returns distilled signal, not the dump; one 5k-tok `git diff` inline ≫ fifteen tiny `git status`). Measure output as a **byte/char proxy** (no tokenizer dep, akmon pure-stdlib) of `tool_response` for `{Read,Grep,Glob,Bash}`, accumulate per session, **reset on `Task`/`Agent`**. **Measurement needs `PostToolUse`** (output doesn't exist at PreToolUse) → new PostToolUse entrypoint wired in `bin/sync.py::_claude_hooks` + `.claude/settings.json`. **Delivery — advisory, dual-channel (owner resolved the fork → advisory, NOT armed-ask):** `additional_context` nudges the model to route heavy inspection to k-explorer; a parallel **owner-visible `system_message`** reports that a quality/token recommendation fired **and states the owner may opt to escalate it to a hard `ask`** — teeth stay owner-controlled, off the `git-commit-guard` modal channel (auditor: armed-ask contradicts design §12.4 "nag learned to ignore", misfires on the innocent next call, and shares the security-ask channel → dropped). **Context-pressure coefficient — ≥4 steps (owner):** weight the axis-2 increment by a rising step-multiplier over `fill_ratio` with **at least 4 steps** (not C23's 2 warning-bands) — e.g. ~0.5/0.7/0.85/0.95 → ×1/×2/×4/×8 — so the same inline dump costs more as the window fills (token headroom shrinks near the limit; low-signal dumps also plausibly dilute attention as context grows — qualitative reinforcement, not a second quantified ground; drop "convex on two grounds"). Reuse **C23**'s `fill_ratio` source (last main-chain `usage` in transcript); axis-2 owns its own ≥4-step multiplier ladder. **NB — auditor's "problem in remission" rebutted (owner):** the healthy delegation log is owner-driven (audits/delegations are the owner's manual calls), so autonomous self-delegation is still weak → axis-2 is warranted, not premature. Watch (advisory-only keeps these nudges, not blocks): byte-count conflates big-with-noisy — a legit large Read of a doc the orchestrator must adopt trips identically; byte→token ratio not stable across Read/Grep/Bash; per-call transcript re-parse for `fill_ratio` is I/O cost ("code cost, not token cost") · hooks/hook_core.py + new PostToolUse entrypoint · provenance: owner proposal + audit (this session), extends D2-8
- C30 · reasoner model laddering (one rung above orchestrator, capped below auditor) · queued · engineer · replace the current dynamic `reasoner = orchestrator` (ADR 0005) with: **reasoner = one rung above the orchestrator, floored at the orchestrator (never below) and capped strictly below the auditor's rung**; **auditor = highest available** (fable if available, else opus). Ladder high→low: fable · opus · sonnet · haiku. Rule: `reasoner = max(orchestrator, min(one_above(orchestrator), second_highest_available))`; `auditor = highest_available`. Cases: orch=opus → reasoner=opus (one-above=fable is the auditor-max → capped to opus), auditor=fable(else opus); orch=sonnet → reasoner=opus, auditor=fable(else opus); orch=fable → reasoner=fable, auditor=fable; orch=haiku → reasoner=sonnet. Touches `registry.json` `selection_policy` (reasoner+auditor) + `routing.py` resolution + regenerated agent frontmatter + `bin/verify.py` reasoner assertion (currently expects the C26 dynamic shape — update together) + `MODEL.md` tier table. **Verify `fable` is in the vendor's `available` set before pinning auditor to it** (else fall back to opus) · registry.json + routing.py · provenance: owner spec (this session)
- A8 · routing defaults: reasoner re-checks ordinary tasks more often, auditor on every design · queued · architect · two owner-wanted shifts to *when* verification fires (not just *which* model, C30): (1) **reasoner more often** — an `independent-review` re-check pass becomes a near-default for ordinary implement/analysis tasks, not only an on-signal escalation (raise the default; keep it advisory/draft — reasoner never decides); (2) **auditor on all designs** — every design-flow / architecture task routes a clean-context `audit` at its gate, making it **mandatory-for-designs** rather than only ≥2-zone-triggered. Encode via `registry.json` `gate_triggers` + role/pipeline anchors (design-flow, review-flow) + MODEL.md routing defaults; relate to **C25** (missed-gate forcing function) — this widens *which* gates always fire. Watch cost/fatigue: more reasoner/auditor passes = more spend + the alarm-fatigue risk the D2-8 audit flagged; scope "ordinary task" so the reasoner default doesn't fire on trivial edits · registry.json gate_triggers + pipelines · provenance: owner request (this session)

Keystone identity (rename):

- V1 · mythological rename `keystone`→`akmon`, `_forge`→`_aitna`, `k-`=Kyklōpes · pending (owner-timed; **names locked**) · release+architect · one myth cluster (Hephaestus's forge beneath Etna) replaces the mixed smithy/masonry metaphor — the *akmon* (anvil / standard) mounted inside each project's *aitna* (volcano-forge / dev workspace), worked by the *Kyklōpes* subagents; supersedes the earlier English `ai_anvil`; **`k-` prefix does NOT migrate** (pure re-reading, orthogonal to V2's suffix change). Two subjects, one migration release — **keystone:** repo `ai_keystone`→`ai_akmon`, mount `<AITNA_ROOT>/keystone`→`<AITNA_ROOT>/akmon`, `.keystone.toml`→`.akmon.toml`, hooks/tools path derivation, doc sweep (older ADRs grandfathered), consumer pin bump; **alphavar (parent consumer):** `git mv _forge→_aitna` (moves `_forge/keystone`→`_aitna/akmon`), rename the `_forge` Python package + `_forge.*` imports, sweep `README`/`AGENTS`/`CLAUDE`/`_forge/README` + `_forge/**` refs + `.gitignore`/hook path derivations + the "read `_forge/memory/`" prime-directive path. Cheapest now (alphavar sole consumer); sequence after the C15–C18 wave · [ADR 0008](decisions/0008-mythological-naming-aitna-akmon-kyklopes.md)
- V2 · rename synthesizer → auditor · code+docs complete & D2-pending (D2-7) · architect+release · "synthesizer" doesn't convey the essence (a clean-context **audit** of a whole gate's material), and "review" is already the DEVELOP analysis role — owner picked "аудит". Name trio (owner-locked): agent `k-synthesizer`→`k-auditor`, task-kind `synthesis-verify`→`audit`, tier `synthesizer`→`auditor`. Scope: `registry.json` (agent def, `role_task_kinds` keys, tier→model binding, `gate_triggers`/policy refs), `routing.py`, `gate_pack.py` (ROLE + k-synthesizer refs), generated `agents/*.md`, design §9–11, `MODEL.md`, ADR 0004/0005, D2 ledger entries. Cheap now (alphavar sole consumer); sequence after the C16/C17/C20→C18 wave to avoid double doc-churn; fold in the C18 engineer-row fix

Role-triad + develop/use split ([ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)):

- C6 · UserPromptSubmit role-confirm for sub-agents · deferred · verify Claude Code hook behaviour first · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C9 · release_check.py akmon-subject scoping · active · `--check --subject akmon` from a non-Python consumer: (a) ✅ pytest-runner resolution fixed in v0.2.1 (dev-venv `python -m pytest`, pinned `[test].runner` from `.akmon.toml`, `uv run --with pytest` fallback); (b) ⬜ `verify --strict` still runs against the consumer root, so the consumer's own TASKS/CI warnings block a akmon release — scope akmon-subject verify to the submodule tree (or reuse `meta/bin/validate.py`). Surfaced cutting v0.2.0; (a) closed cutting v0.2.1
- C2 · release tool: pin-bump subject · deferred · the 3rd release subject (akmon pin bump recorded in a consuming project); only after akmon+package subjects settle · [design](design/release-versioning.md)
- A3 · cross-agent contract v2 · deferred · skill/role inventory beyond thin pointers · [design](design/cross-agent-contract-v2.md)
- C1 · harden git-commit-guard parsing · deferred · close regex bypasses only if a real one bites
- A1 · orchestration / role handoffs · deferred · until role routing causes real friction (4 roles now; release routes) · [ROADMAP O4](ROADMAP.md)
- A2 · OPERATE mode · deferred · keep as design note until a runtime actor exists · [ROADMAP O1](ROADMAP.md)

Ids use the typed scheme ([ADR 0002](decisions/0002-task-id-convention.md)): **A** architecture/
design · **C** code · **L** learning · **V** release · **N** analysis/review; the role is derived. Archived `T#` are
grandfathered (frozen), so the historical references below keep their `T#`.

Trim rationale (A1/A2/C1/A3/C2 "deferred") is recorded in ROADMAP O# / the release design; do not
re-expand without a reason. The T3 release design (ADR 0001) is implemented (T10–T15); C2 is the
deferred pin-bump follow-up, A3 the deferred O3 v2 design, A4 surfaced while building the release
tool.

## Done

See [`TASKS_ARCHIVE.md`](TASKS_ARCHIVE.md).
