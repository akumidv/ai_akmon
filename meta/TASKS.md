# TASKS — akmon development backlog

The single backlog for developing the akmon standard itself. Format:
[`pipelines/tasks.md`](../pipelines/tasks.md). Directions / model gaps: [`ROADMAP.md`](ROADMAP.md).

> This is akmon's *own* backlog (evolving the standard), distinct from a consuming
> project's `_aitna/TASKS.md`.

## Status

Tooling spine (`bin/sync.py`, `bin/verify.py`, vendor-neutral `hooks/hook_core.py`) exists and
is **tested** (`tests/`). Remaining work is contract specification + scale hardening, not new
mechanism.

## Active / blocked / deferred

Model routing ([ADR 0004](decisions/0004-model-routing-capability-tiers.md); design in
[`meta/design/model-routing.md`](design/model-routing.md)):

Auditor / gate-pack extension (design in [`model-routing.md` §9–11](design/model-routing.md)) —
**architecture** (lock, then ADR):

- A6 · roles & pipelines under the subagent model · done in design · architect · recorded as §10 (role = orchestration contract; role→task-kinds matrix; zone plan from Decompose/Survey; loop-back edges; learn/release tiers); lock items folded into A5 (done) · [design §10](design/model-routing.md)

**implementation** (A5 locked → ADR 0005; C15 done → C16/C17/C20 unblocked, then C18):

- C16 · gate-pack builder · blocked (code complete; owner D2-1/D2-2 verification pending) · engineer · one structured package (artifacts + yardstick + coverage map) consumed by auditor subagent and second-opinion CLI, plus the minimal plan-check pack (yardstick + zone plan, pre-fan-out); replaces free-form `--prompt-file` in second_opinion.py; runnable as one deterministic script per §11. **Also wire the second-opinion diversity ladder** (currently dead registry data from C15): `second_opinion.py` selects provider *and model* by `second_opinion_policy.diversity_ladder` — other-vendor → same-vendor-other-model (via `second_opinion_fallback_model`, computed into config but unconsumed) → never same-model · [design §9.4+§9.3.5+§4.6](design/model-routing.md)
- C17 · coverage map from delegation log · blocked (code complete; owner D2-3 verification pending) · engineer · zone label on fan-out delegations (from the §10.3 zone plan) + assembler tool — map at code cost, not tokens. Delivered: log schema `ts·session_id·subagent·model·zone·description` (zone off a `[zone:LABEL]` marker), `coverage_map.py` (scope by `--session`, +opt `--since/--until`; uncovered-seam flags vs zone plan) feeding `gate_pack.py --coverage-map`; `gate_id` precision rides on C20's session marker without changing the scope-by-key shape · [design §9.4+§10.3](design/model-routing.md)
- C18 · docs: leverage principle + role/pipeline deltas · blocked (code+docs complete; owner D2-6 verification pending) · engineer · name the leverage principle in MODEL.md; tier table + matrix rows update; review/design-flow auditor anchors (post-fan-out + the pre-fan-out plan check) + loop-back edges; roles/*.md one-paragraph deltas linking §10 incl. the drafts-vs-decides invariant reading; demote static floor warning to weak prior. **Also implement A7 (b): registry `cross_cutting_kinds` = [`independent-review`, `audit`] + a `role_matrix_warning` exemption so C20 no longer warns on cross-cutting verification kinds for any role + test + ADR 0005 addendum** — else C20 warns against the very audit routing C24/C25 enable (auditor seam 1); these anchors are the missing *process* half that says *when* to launch the auditor (seam 2) · [design §9.1–9.5+§10](design/model-routing.md) · provenance: verdict.md seams 1–2 (local, untracked)
- C19 · owner-attention metrics in stats digest · deferred · extends C13: D2 pending/approved/verified, decisions per session, next to token spend · [design §9.6](design/model-routing.md)
- C20 · role-matrix advisory check in delegation hook · blocked (code complete; owner D2-4 verification pending) · engineer · warn when a routed subagent has no task kind in the active role's `role_task_kinds` row. Active role read from the transcript's last main-chain `🧭 agent:` declaration (chosen over a written marker — consistent with C22/C23 transcript scanning); subagent-granularity rule (warn iff kinds ∩ allowed = ∅); advisory, never blocks. **Also (D2-5):** the delegation hook now derives each agent's pinned model from the binding (`bound_model_for`), so the console line + record name the real model — completing C21's always-empty `(<model>)` · [design §10.2+§10.4](design/model-routing.md)
- C24 · deterministic gate-runner (one command) · blocked (after C18) · engineer · chain `coverage_map.py` → `gate_pack.py` → `second_opinion.py` into one deterministic gate run (§11): assemble the pack from the log, get the second-opinion verdict, write the bundle. The fan-out + the k_auditor **spawn stay orchestrator-side** — a script cannot call the Task tool (§11 "vehicle, not contract"); this covers pack-assembly + second-opinion CLI + verdict only. Builds on C16 (`second_opinion.py --gate-pack` is half of it). Scopes the auditor-usage analysis's "fix #1" honestly · [design §9.4+§11](design/model-routing.md) · provenance: `_aitna/artifacts/gates/20260704-185954-subagent-usage/verdict.md` (local, untracked)
- C25 · missed-gate forcing function (revive dead `gate_triggers`) · blocked (after C18) · engineer · make `gate_triggers` (`review_min_findings:3` / `architect_min_options:2` — findings/options **counts**, no zone key, currently unenforced dead registry data) an actual advisory at the review/architect gate: when a role emits ≥threshold findings/options, nudge to run the auditor. **NOT fan-out-conditioned** (zero fan-outs ever ⇒ would fire zero times — strictly weaker than the dead data it revives); independent of coverage/zones. Fixes the auditor-usage analysis's misspecified "fix #2" · [design §9.4](design/model-routing.md) · provenance: verdict.md contradiction 3 / re-rank delta 1 (local, untracked)
- C32 · enforce or reclassify dead routing data (`task_kind_floors` + `binding.escalation`) · active · engineer · both are computed and written to local config (routing.py:194–212, :952) but **no hook reads either** (auditor-verified: grep over `hooks/*.py` empty) — that slice of the routing contract is silently advisory, same "dead registry data" class as C25. Fix: an advisory floor check in the delegation-log hook (`bound_model_for` already resolves the routed agent's model at that exact point) — warn when a routed kind's floor exceeds the delegate's rung; surface or drop the escalation path likewise. Alternative (honest-contract option): reclassify the fields as documentation · [review](reviews/review-20260705.md) finding 3
- C33 · zone-marker robustness + convention surfacing · active · engineer · `parse_zone` (routing.py:1043–1058) accepts only the literal `[zone:` prefix, and the syntax lives only in dev-meta docs/docstrings — not in MODEL.md, pipelines, role docs, k_* briefs, or the status line. Observed live (review-20260705 gate): unbracketed `zone:X …` fan-out descriptions → an all-uncovered coverage map, hand-assembled fallback — a silently degrading map defeats the uncovered-seam mechanism the gate leans on. Fix: tolerant parsing (`zone:X` with/without brackets) + state the convention in the generated k_* briefs and the status-line delegation guidance (routing.py:988–990) + delegation-log warn on an unlabelled fan-out delegation while a zone plan is active · [review](reviews/review-20260705.md) finding 5
- C34 · role docs ↔ `role_task_kinds`: restore single owner · active · engineer · triad role tier sections partially restate the registry row (review.md:57–59 names 2 of 4 kinds; architect.md:66–68 3 of 6; engineer.md:54–55 4 of 10) — partial duplication is primed drift, worse than link-only or full generation. Pick one: link-only (registry is the owner) or generate the tier lines from the registry (the `doc-sync` kind exists for exactly this); sweep the three triad role docs · [review](reviews/review-20260705.md) finding 6

- C46 · vendor-specific names in cross-vendor contracts: declare neutral, map per vendor (tool names + agent names) · blocked (tool-name half: after C51+C57; agent-name half complete via ADR 0011) · engineer · `AGENT_SPECS` hardcodes `tools="Read, Grep, Glob, Bash"` (routing.py:364/:470/:503 — corrected at A17(g), which found this entry carrying both this spelling and the off-by-one `:363/:469/:502`) into generated agent frontmatter, but **Claude Code 2.1.221 has no `Grep`/`Glob`/`MultiEdit` tool at all** — probe-verified live in alphavar (k-explorer attempted both calls → `UNAVAILABLE`; actual set `Read, Bash`), so akmon's read-only **sweep** delegate runs without its sweep tools and nothing warns. Same staleness in `bin/sync.py:273,:289` matchers and `hooks/claude_adapter.py:13,15` tool sets — dead alternatives, so the delegation nudge's read/sweep axis (D2-8) now sees only `Read`. Root cause is a **second owner**: `hook_core.py:107–:115` already models neutral tool *kinds* normalized by each vendor adapter, and `AGENT_SPECS` bypasses that pattern to name Claude's tools directly. Fix (owner-chosen): declare **capabilities** in `hook_core`'s set (`read`/`shell`/`edit`/`subagent`) + a per-vendor name map, degrade a missing capability to its Bash equivalent **explicitly**, and add a deterministic check that every declared name is in a known harness inventory — so the next harness drift fails loudly instead of silently. Record the Bash-degradation contract as a contract: Bash is strictly wider than Grep/Glob, so the read-only boundary then rests on the agent body's prose, not on the tool set. **Rejected here:** shipping akmon's own tools over MCP — that is P4.3, gated behind A15/C42 by [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md). **Second instance, same root cause — agent *names*:** Codex 0.146.0 rejects akmon's delegate names outright. Its `agent_name` validation, read from the shipped binary, is `must not be empty` / `must use only lowercase letters, digits, and underscores` / `must not contain "/"` / `"root" is reserved` — so the hyphen in `k-explorer`, `k-mechanic`, `k-auditor` makes all three illegal Codex agent names; probe F2 died on exactly this (`Rejected: agent_name must use only lowercase letters, digits, and underscores`). Claude accepts the hyphen, which is why the split never surfaced. **No shipped artifact breaks today:** `bin/sync.py` emits only `.codex/README.md` (:580) and `.codex/hooks.json` (:581) for Codex — no agent definitions. **Corrected at A17(g):** this entry went on to say codex 0.146.0 "has no agent-definition file convention at all" and that `collaboration.spawn_agent` takes `agent_name` and `task_name` as separate parameters — [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md) retracts **both** from measurement (the spawn tool exposes `task_name`, `fork_turns`, `model`, `message`, and `<repo>/.codex/agents/<name>.toml` does exist, selected by an `agent_type` argument). A live task standing on a fact its own cited ADR withdraws is this stage's target defect, so it is struck here rather than left as background. **The carrier's existence does not enter this lock:** it belongs to the agent-*definition* half, closed by ADR 0011 and parked to **D4** — whether akmon should emit Codex agent definitions is a scope question for D4, not a repair, and C46 here owns tool names only. The exposure is to the **plan**: D4 and any step that gives Codex delegates the `k-*` identity. **A12 chose the global rename** (owner, this session) over a per-vendor name map: all six agents are now `k_*` in live code, tests and current docs, verified against Claude Code 2.1.221 (an underscore-named agent is offered as a subagent type alongside the hyphenated ones) — [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md). The hyphenated names above are kept verbatim as the record of the defect and must not be rewritten. What stays open **here** is the tool-name half: C46's mechanism (one neutral declaration, per-vendor name map, loud failure on an unknown name) is still unbuilt, and the rename bought a shared *notation*, not the mechanism. **Not an instance — hook matcher names:** first read as a third occurrence, then disproved by measurement — codex 0.146.0 accepts Claude's matcher vocabulary (`Edit`, `Write` and `apply_patch` all fire on its patch tool; its shell tool matches as `Bash`), so `bin/sync.py:218` carries no dead names. That gap is a *missing* alternative, not a wrong one, and is carried separately as C49. **What C46 does own here (recorded at D2-19 a):** the emitted matcher is minimal by measurement and therefore **silent on drift** — nothing distinguishes "correct for codex 0.146.0" from "written a year ago", and if a later version unfolds the three aliases into separate routes the hooks stop firing with no signal. Matcher names are vendor names in a cross-vendor contract, so they belong in the same **version-stamped inventory** as tool names: mapped at generation time, unknown name = error at `sync`. D2-19 deliberately did **not** solve it locally (padding the matcher with `Edit|Write` would assert as routes two names never measured as routes). **A12's lock defines the shape of the remaining tool-name half** ([stage1-hardening-contracts §8](design/stage1-hardening-contracts.md)), so this task remains blocked on C51 and C57 before implementation: declare neutral capabilities in `hook_core`'s own vocabulary — `edit`/`shell`/`read`/`subagent` (`hooks/hook_core.py:109`–`:115`), where `read` already folds Read/Grep/Glob by that module's own comment; **corrected at A17(g)**, which found this entry and §8 both naming a `search` token that exists in no vocabulary, and the correction matters because the whole rejection of `AGENT_SPECS` rests on `hook_core` *already* owning these names — map **neutral → vendor at generation time**, keep the harness inventory as **version-stamped data** (harness id + version → available names); a mapped name outside the known inventory is an **error at `sync`**, and a capability the harness has no name for **degrades to `shell` explicitly plus a warn finding** (C51's envelope) naming the widened boundary — because Bash is strictly wider than Grep/Glob, and after the degrade the read-only property rests on the agent body's prose, not on the tool set. **Fork closed: the map is data, not code** (as `AGENT_SPECS` has it today) — a code map carries no version stamp, so nothing distinguishes "correct for 2.1.221" from "written a year ago", and the stamp is the entire point of moving it. **Seeded-violation contract added at A17(d)**, and writing it exposed that the stamp had nothing to compare against: **no code in `bin/`, `tools/` or `hooks/` reads a harness version** — 2.1.221 and 0.146.0 were obtained by hand in N2 — so a written stamp makes staleness *printed*, not *visible*. (i) **Detect independently per harness:** `sync` queries each of `claude` and `codex` when that binary is on PATH and compares against its inventory; for each absent harness independently, it proceeds against that harness's declared version and names the one it assumed. Rejected: declaration only (honest but toothless, and §8's visibility claim would have to go) and mandatory detection (`sync` runs where no harness is installed). This is what puts the two optional binaries into C57's runtime declaration. (ii) **An unknown harness version warns and stamps, it does not fail:** generate from the newest known inventory, emit a warn finding, and write the assumption into the **generated artifact's banner** (`generated against claude-code 2.1.221 inventory; detected 2.2.0`) rather than only into a warning that scrolls away. Erroring would brick a consumer on the vendor's release schedule, which akmon cannot preempt — it cannot ship data for a version that does not exist yet; degrading everything to `shell` would dismantle the read-only boundary to preserve a formality. The banner line is C55's surface, recorded as an edge in the order table. (iii) **Seeded against the root cause, not only the symptom:** the defect was never a wrong name but a *second owner*, so a literal vendor tool name in generator code outside the inventory data is a static failure — the analogue of C51 rejecting a local field mapping in an adopter. Seeded violations: a map emitting `Grep` against a 2.1.221 inventory fails at `sync` and `sync --check`; a capability with no vendor name yields the shell name **plus** a warn naming the widened boundary, and **the same degrade without the finding fails the test** — the silent degrade is the seed; an inventory entry with no version stamp fails; a detected version outside the inventory warns and stamps, while generating with no stamp fails; restoring `tools="Read, Grep, Glob, Bash"` into `routing.py` fails the second-owner check; and with each binary absent separately, then both absent — the fallback (i) introduces — generation must state the declared version assumed for every absent harness while retaining the detected row for a present harness; omitting either statement or replacing the present harness's detected row with its fallback fails (added at A17(g) per **F13**: the disclosure sat with five seeds around it and none on it, which is the state where a silent fallback and an honest one look identical). **Red before green:** `routing.py:364`/`:470`/`:503` are **not** repaired ahead of C46 — C46's own red-before-green rule, so the check meets the tree rather than a fixture. With D2-20 Verified, runs after C51 and C57 · tools/model_routing/routing.py::AGENT_SPECS + bin/sync.py::_claude_hooks/_codex_hooks + hooks/claude_adapter.py + hooks/codex_adapter.py · provenance: [N2 findings §1.5](reviews/alternatives/n2-stage0-probes-inventory-20260807.md); agent-name instance: N2 stage-0 probe F2 + codex 0.146.0 binary string inspection

  - **F20/C46 carriers:** one pure inventory/resolution checker runs through `sync` and `sync --check`; `self_ci` runs the source-owner scanner and exact neutral/agent population contract. The vocabulary is pinned as exactly `edit`/`shell`/`read`/`subagent`; each restricted agent (`k_explorer`, `k_reasoner`, `k_auditor`) is pinned to `{read, shell}` and each unrestricted agent (`k_mechanic`, `k_validator`, `k_implementer`) to the explicit sentinel. Separate mutations add/delete a neutral token, change each agent assignment and cross each restricted/unrestricted boundary; consumer-only tokens emit exactly one `error harness.unknown-capability`, and every other population mutation fails the exact contract. The vendor-owner population is pinned separately for agent frontmatter, Claude hook generation, Codex hook generation, Claude adapter normalization and Codex adapter normalization; adding a second name set/map in each produces exactly one `error harness.duplicate-name-owner`, while removing a consumer from the scan fails before the literal oracle. Unstamped, duplicate or malformed inventory rows and a missing required map each emit exactly one `error harness.invalid-inventory`; emitting `Grep` against Claude 2.1.221 emits exactly one `error harness.unknown-name`. Each missing capability mapping yields the inventory's shell name plus exactly one `warn harness.capability-degraded` naming capability, harness/version and widened boundary; silent warn, wrong fallback and wrong disclosure mutations fail. Known-version fixtures select each exact row; a two-version unknown-version fixture must select the newest row, emit exactly one `warn harness.unknown-version` and stamp selected+detected versions in every affected banner; an older selection, duplicate warn, missing stamp or failing exit fails. PATH fixtures remove each binary separately and then both together: every absent harness uses and discloses its declared version without warning, while any present harness still selects its detected row; missing, cross-assigned or wrong provenance fails the artifact oracle. Every `harness.*` error exits 1 through both callers; warn-only exits 0 when `sync --check` has no independent drift. Changing code, severity, count, target, selection, provenance, fallback or caller wiring fails. The live `routing.py:364`/:470/:503 defect is observed red before green under C46's own transition; the earlier attribution to F9 is withdrawn because F9 belongs only to C54. Dependencies remain C51+C57; the C55 banner relation remains outgoing.

Orchestrator detection + runtime weakness ([ADR 0006](decisions/0006-orchestrator-detection-corridor-context-pressure.md); design [§3/§4.3/§12](design/model-routing.md)):

- C22 · orchestrator detection: corridor + owner-visible warnings · blocked (implemented; owner verification pending — no ledger row yet) · engineer · transcript detection + rebind + gitignored `k_*` + corridor warning in `compute_binding` (named floor `opus`, below-floor + reserved-top arms, silent collapse when `floor == top`, skip when floor not in ladder) + dual-channel owner-addressed output (`systemMessage`); e2e-verified on a live session · [design §3+§4.3](design/model-routing.md)
- C23 · context-pressure detection · blocked (implemented; owner verification pending — no ledger row yet) · engineer · registry `context_pressure` block; the model-routing hook reads last main-chain `usage` in the same transcript pass, banded warnings (0.85/0.95) throttled per band via session marker, reset on compaction, dual-channel delivery; tested incl. live transcript (74% → silent) · [design §12](design/model-routing.md)
- C27 · plan-gate: delegation named up front in design-flow plan-check · active · engineer · complement to the delegation-nudge enforcement (D2-8): extend the existing `design-flow.md` pre-fan-out plan-check gate yardstick so a realization/decomposition plan must **name a delegate per delegable sub-step** before work starts — makes "delegation up front" auditable at a gate, not only advisory at runtime (the hook catches drift *during* work; the gate catches an under-delegated *plan*). The always-loaded guardrail keeps only a pointer clause, not a second rule owner · [design §9.4+§10](design/model-routing.md) · provenance: D2-8 delegation-enforcement audit (this session)
- C28 · verify `ask`/`deny` PreToolUse contract per vendor + count-axis tuning · active · engineer · **(d) LANDED** (subagent exemption via `agent_id`; hook_core+wrapper+tests, 338 passed; D2-9; motivation in model-routing.md §13). **(a) LIVE-VERIFIED** (this session, D2-10): debug-instrumented live test confirmed the hook dispatches identically for a real `Read` and a real `Bash` call — no tool-category exemption. Surfaced an unplanned, bigger gap — the emitted `ask` was not enforced/blocked for *either* tool in this background/child session; spun off as **C31**. Remaining: (b) codex runtime-enforcement check (wire shape now schema-verified — see below), (c) count-axis tuning (defer to C29 output-weight axis). · the graduated delegation nudge (D2-8) emits a hard `ask` on sustained drift; gates before the ask arm is trusted: (b) **codex vendor — wire shape SCHEMA-VERIFIED (this session)**: the installed `codex` binary (0.142.5) embeds its own JSON schema for hook I/O (extracted via `strings`/binary search, not docs); `pre-tool-use.command.output.hookSpecificOutput.permissionDecision` enum is exactly `allow|deny|ask`, matching what `codex_adapter.py`/`hook_core.py` already emit — no adapter change needed for the shape. Codex also exposes a separate `PermissionRequest` hook event (own `behavior: allow|deny` contract, `interrupt`/`updatedInput`/`updatedPermissions` explicitly reserved/fail-closed) we don't touch yet — noted, not required. **Measured but still incomplete:** N2 proved hard `deny` only on the headless `apply_patch` route, while C49 separately proved that the shell matcher is `Bash`; that does not yet verify the exact git-guard behavior. Before wiring `codex-git-commit-guard`, live-probe one real `Bash` payload for each normal result class (`deny`, interactive `ask`, unattended ask→deny), record whether Codex supplies `permission_mode`, and compare headless with TUI. Keep the mode available for fixtures until that exact payload/enforcement matrix is measured; no separate C61 duplicates this owner. (c) **count-axis false-positive tuning (D2-8 item 2), on the right axis** — the residual noise is **not** read-vs-write: a read-only `git diff`/`git log` across many files is the *most* expensive thing to pull inline and **should** trip the counter (delegating it to k_explorer keeps the dump out of orchestrator context); only *tiny-output* commands (`git status`, `rev-parse`, `ls`) are genuine inline-justified noise where a subagent round-trip costs more than the command. Do **not** discount read-only shell wholesale (brittle: `git status && rm -rf` isn't read-only); if the count axis over-fires, prefer the **output-weight axis (C29)** over heuristic bash-string classification; (d) **subagent false-fire — correctness defect, fix FIRST (owner: keep-and-harden the ask)**: `delegation_nudge_result` is session-keyed with no main-chain/subagent discrimination, so the nudge fires on read-only leaf subagents (k_auditor/k_explorer/k_reasoner) that have no `Task` tool — advisory wastes their context, the hard `ask` is un-actionable or blocks legit reads (demonstrated live: the C29-design audit tripped it at call #10). Suppress on subagent sessions (or count main-chain turns only); resolve whether a subagent shares the parent `session_id` (double-charge risk). **Owner decision: keep the hard ask, but only after (a) runtime-verify + (d) subagent fix land — accept fatigue risk & monitor.** Verify (b), fix pending; encode the vendor contract in the adapter · hooks/claude_adapter.py + hooks/codex_adapter.py · provenance: D2-8 + auditor "could not verify" + live subagent misfire (this session)
- C31 · background/child-session `ask` enforcement gap · blocked (code+tests complete; owner D2-11 verification pending) · engineer · owner decided the posture without a foreground repro first (skip diagnosis, harden and observe): `hook_core.py::_escalate_unattended_ask` escalates any `ask` from `git_commit_guard_result`/`delegation_nudge_result` to a hard `deny` whenever `permission_mode` isn't the interactive `default` (field missing = worst case, escalates too); both Claude wrappers thread `permission_mode` from the PreToolUse payload. Codex not wired to either hook yet, so out of scope. Owner now watches live behaviour (false-block fatigue vs. real gap closed) rather than gating on further isolation of background-vs-`acceptEdits` · [D2-11](D2_LEDGER.md) + model-routing.md §13.2 · provenance: D2-10 + owner decision (this session) · was **ranked #1 in [review-20260705](reviews/review-20260705.md)** (D5's commit guard rides the same channel; the audit session itself ran background, i.e. inside the gap)
- C29 · second axis: inline-output-weight nudge (token-proxy), advisory · active · engineer · **Owner-approved: build, advisory.** axis 1 (D2-8) counts delegable *calls* without a delegation (behavioural drift); add **axis 2** counting *cumulative raw tool-output pulled into orchestrator context* without a delegation — the metric that maps to **context cost in both senses**: token budget (measurable, load-bearing) *and* reasoning quality (raw dumps are low-signal noise that dilutes attention / buries detail as the window grows — context-rot / lost-in-the-middle; a subagent returns distilled signal, not the dump; one 5k-tok `git diff` inline ≫ fifteen tiny `git status`). Measure output as a **byte/char proxy** (no tokenizer dep, akmon pure-stdlib) of `tool_response` for `{Read,Grep,Glob,Bash}`, accumulate per session, **reset on `Task`/`Agent`**. **Measurement needs `PostToolUse`** (output doesn't exist at PreToolUse) → new PostToolUse entrypoint wired in `bin/sync.py::_claude_hooks` + `.claude/settings.json`. **Delivery — advisory, dual-channel (owner resolved the fork → advisory, NOT armed-ask):** `additional_context` nudges the model to route heavy inspection to k_explorer; a parallel **owner-visible `system_message`** reports that a quality/token recommendation fired **and states the owner may opt to escalate it to a hard `ask`** — teeth stay owner-controlled, off the `git-commit-guard` modal channel (auditor: armed-ask contradicts design §12.4 "nag learned to ignore", misfires on the innocent next call, and shares the security-ask channel → dropped). **Context-pressure coefficient — ≥4 steps (owner):** weight the axis-2 increment by a rising step-multiplier over `fill_ratio` with **at least 4 steps** (not C23's 2 warning-bands) — e.g. ~0.5/0.7/0.85/0.95 → ×1/×2/×4/×8 — so the same inline dump costs more as the window fills (token headroom shrinks near the limit; low-signal dumps also plausibly dilute attention as context grows — qualitative reinforcement, not a second quantified ground; drop "convex on two grounds"). Reuse **C23**'s `fill_ratio` source (last main-chain `usage` in transcript); axis-2 owns its own ≥4-step multiplier ladder. **NB — auditor's "problem in remission" rebutted (owner):** the healthy delegation log is owner-driven (audits/delegations are the owner's manual calls), so autonomous self-delegation is still weak → axis-2 is warranted, not premature. Watch (advisory-only keeps these nudges, not blocks): byte-count conflates big-with-noisy — a legit large Read of a doc the orchestrator must adopt trips identically; byte→token ratio not stable across Read/Grep/Bash; per-call transcript re-parse for `fill_ratio` is I/O cost ("code cost, not token cost") · hooks/hook_core.py + new PostToolUse entrypoint · provenance: owner proposal + audit (this session), extends D2-8
- C30 · reasoner model laddering (one rung above orchestrator, capped below auditor) · active · engineer · replace the current dynamic `reasoner = orchestrator` (ADR 0005) with: **reasoner = one rung above the orchestrator, floored at the orchestrator (never below) and capped strictly below the auditor's rung**; **auditor = highest available** (fable if available, else opus). Ladder high→low: fable · opus · sonnet · haiku. Rule: `reasoner = max(orchestrator, min(one_above(orchestrator), second_highest_available))`; `auditor = highest_available`. Cases: orch=opus → reasoner=opus (one-above=fable is the auditor-max → capped to opus), auditor=fable(else opus); orch=sonnet → reasoner=opus, auditor=fable(else opus); orch=fable → reasoner=fable, auditor=fable; orch=haiku → reasoner=sonnet. Touches `registry.json` `selection_policy` (reasoner+auditor) + `routing.py` resolution + regenerated agent frontmatter + `bin/verify.py` reasoner assertion (currently expects the C26 dynamic shape — update together) + `MODEL.md` tier table. **Verify `fable` is in the vendor's `available` set before pinning auditor to it** (else fall back to opus) · registry.json + routing.py · provenance: owner spec (this session)
- A8 · routing defaults: reasoner re-checks ordinary tasks more often, auditor on every design · active · architect · two owner-wanted shifts to *when* verification fires (not just *which* model, C30): (1) **reasoner more often** — an `independent-review` re-check pass becomes a near-default for ordinary implement/analysis tasks, not only an on-signal escalation (raise the default; keep it advisory/draft — reasoner never decides); (2) **auditor on all designs** — every design-flow / architecture task routes a clean-context `audit` at its gate, making it **mandatory-for-designs** rather than only ≥2-zone-triggered. Encode via `registry.json` `gate_triggers` + role/pipeline anchors (design-flow, review-flow) + MODEL.md routing defaults; relate to **C25** (missed-gate forcing function) — this widens *which* gates always fire. Watch cost/fatigue: more reasoner/auditor passes = more spend + the alarm-fatigue risk the D2-8 audit flagged; scope "ordinary task" so the reasoner default doesn't fire on trivial edits · registry.json gate_triggers + pipelines · provenance: owner request (this session)
- C36 · hook runtime hardening · active · engineer · (a) `/tmp` session markers race (TOCTOU on create) and **outlive sessions** — a stale counter from a crashed/reused session_id false-fires or wrongly suppresses the nudge; add lifecycle handling (age-out or session-start reset). **The marker population grew with C50's suppressed-rebind throttle** (`routing.suppressed_rebind_warning`, `akmon-suppressed-rebind-<sha256(session)[:20]>` in the tempdir): it is content-keyed, so a stale one suppresses a *real* refused-rebind warning rather than merely miscounting, and it is the one member of the class with no `.marker` suffix and a hashed rather than literal session id — the hashing is the defensible half (a session id containing `/` would otherwise escape the directory) and should become the convention for all of them, not stay the exception. **C49's marker is the exemplar to copy, not another copy** (`akmon-codex-shell-route-<sha256(session)[:20]>`, written by `codex-hook._first_time_for_identity`): after the C49 review it claims the marker with `os.open(O_CREAT|O_EXCL, 0o600)` — the TOCTOU half of (a) already solved in one member of the population, and solved where it matters most, since three separately launched hook processes race on the *same* file per Bash call — and it fails **visible**: an unwritable tempdir repeats the diagnostic instead of swallowing it. The sweep is therefore not "collapse four copies of one 8-line idiom onto a helper" but **lift that member into the helper and migrate the other three onto it**, then add the lifecycle it still lacks (the one part of (a) C49 did not touch). **The lift happened at D2-19(e)**, as a side effect of making the shell-route diagnostic vendor-neutral: the idiom now lives in `hook_core.claim_diagnostic_marker` (atomic, `0o600`, hashed name, repeat-when-unidentified), the Codex diagnostics call it with `codex-`-prefixed kinds, and Claude's `Bash` route creates a marker of its own through `git-commit-guard.py`. So the exemplar is already the helper, and **what is left here is the migration and the lifecycle**, resurveyed at D2-21(c): move `akmon-role-on-code-*`, `akmon-analysis-guard-*`, `akmon-d2-ledger-*` and `akmon-suppressed-rebind-*` onto it; drop the literal-session-id names and `0644`; make the missing-identity policy one policy (those three still key on the literal `nosession` and therefore **suppress across sessions** — fail-quiet, the wrong direction); add the age-out. Two consequences to keep in view: the three advisory markers also claim **non-atomically** (`exists()` then `write_text`), so two parallel edit calls in one session can emit one reminder twice — loud direction, removed by the migration; and **D2-22's acceptance of best-effort switch-time delivery expires when this lands** — after the migration and age-out, exactly-once per episode becomes the contract rather than a residual. **The population also now disagrees on a missing session id**: `codex-hook` and `routing.suppressed_rebind_warning` *repeat* rather than share a `nosession` marker (a global one would let a single id-less session silence every other one), while hook_core's three (`akmon-role-on-code-<sid|nosession>.marker`, `akmon-analysis-guard-…`, `akmon-d2-ledger-…`) still key on the literal string `nosession` and therefore *suppress* across sessions. One policy has to win — repeat-when-unidentified is the fail-visible one — and the same edit closes the leftover naming split: literal session id in the filename (the `/`-escape the hashing exists to prevent), the `.marker` suffix, and 0644 vs 0600; (b) untested load-bearing paths: `model-routing.py` `main()` (exception fallback, malformed-transcript JSONL), the delegation-log write path — add tests; (c) **owner-locked diagnostic loudness:** malformed edit payloads repeat per bad tool event but deduplicate across the three separately launched path-advisory handlers by a reliable `tool_use_id`; with no id or a marker failure they repeat fail-visible. A once-per-session alarm is rejected because it could hide continuing protocol drift. **Review follow-up:** the transitional `_first_time_this_session` alias over `_first_time_for_identity` is removed, so the claim helper has one name again — two names for the same eight lines would have left (a)'s sweep with two migration targets and a pointer in this task that resolved to a wrapper rather than to the atomic claim it names as the exemplar. Commit-guard `$(git push)` regex bypass stays C1 (deferred, unchanged) · [review](reviews/review-20260705.md) finding 9 + C49/C50 follow-up review (this session)
  **Substatus:** C36(c) is code-complete and D2-21-pending; C36(a)/(b) remain active.
- A9 · tool-layer LLM-agnosticism scope · active · architect · the agnosticism claim holds for the data layer (semantic policies, no committed model names) but not operations: hardcoded `.claude/` paths (routing.py:28–30, stats.py), Anthropic-only usage endpoint (stats.py:32), Claude transcript/`agent-*.jsonl` assumptions, inconsistent vendor defaults (init.py: anthropic vs second_opinion.py:73: openai), report_dir vendor-flip, **Gemini absent from the registry** despite the GEMINI.md pointer. Decide the operational multi-vendor contract in an ADR: what "LLM-agnostic" promises at the tool layer (full path/endpoint abstraction vs explicit first-class-Anthropic + documented degradation), then align code; relates C28 (b) codex runtime enforcement · [review](reviews/review-20260705.md) finding 8

Keystone identity (rename):

- V1 · mythological rename `keystone`→`akmon`, `_forge`→`_aitna`, `k-`=Kyklōpes · deferred (owner-timed; **names locked**) · release+architect · one myth cluster (Hephaestus's forge beneath Etna) replaces the mixed smithy/masonry metaphor — the *akmon* (anvil / standard) mounted inside each project's *aitna* (volcano-forge / dev workspace), worked by the *Kyklōpes* subagents; supersedes the earlier English `ai_anvil`; **`k-` prefix does NOT migrate** (pure re-reading, orthogonal to V2's suffix change). Two subjects, one migration release — **keystone:** repo `ai_keystone`→`ai_akmon`, mount `<AITNA_ROOT>/keystone`→`<AITNA_ROOT>/akmon`, `.keystone.toml`→`.akmon.toml`, hooks/tools path derivation, doc sweep (older ADRs grandfathered), consumer pin bump; **alphavar (parent consumer):** `git mv _forge→_aitna` (moves `_forge/keystone`→`_aitna/akmon`), rename the `_forge` Python package + `_forge.*` imports, sweep `README`/`AGENTS`/`CLAUDE`/`_forge/README` + `_forge/**` refs + `.gitignore`/hook path derivations + the "read `_forge/memory/`" prime-directive path. Cheapest now (alphavar sole consumer); sequence after the C15–C18 wave · [ADR 0008](decisions/0008-mythological-naming-aitna-akmon-kyklopes.md) · **propagation incomplete — priority raised ([review-20260705](reviews/review-20260705.md) finding 4):** mixed naming already breaks user-visible state — consumer memory index links `akmon-*` while files on disk are `keystone-*` (broken links `_aitna/memory/README.md:23,25`; 5 files missing from index per `verify --strict`), akmon README `develop/`→`meta/` links (fixed in the onboarding-docs pass — CHANGELOG Unreleased/Fixed), `ROADMAP.md:21` says "Two roles" vs five, `KEYSTONE_*` env prefixes (hook_core.py:467,478); 77 remnant lines across 26 files (exact count); every interim doc edit propagates remnants — fold these into the sweep checklist
- V2 · rename synthesizer → auditor · blocked (code+docs complete; owner D2-7 verification pending) · architect+release · "synthesizer" doesn't convey the essence (a clean-context **audit** of a whole gate's material), and "review" is already the DEVELOP analysis role — owner picked "аудит". Name trio (owner-locked): agent `k-synthesizer`→`k-auditor`, task-kind `synthesis-verify`→`audit`, tier `synthesizer`→`auditor`. Scope: `registry.json` (agent def, `role_task_kinds` keys, tier→model binding, `gate_triggers`/policy refs), `routing.py`, `gate_pack.py` (ROLE + k-synthesizer refs), generated `agents/*.md`, design §9–11, `MODEL.md`, ADR 0004/0005, D2 ledger entries. Cheap now (alphavar sole consumer); sequence after the C16/C17/C20→C18 wave to avoid double doc-churn; fold in the C18 engineer-row fix
- V3 · restore the missing v0.3.0 changelog boundary · blocked (code complete; owner review pending) · release+engineer · tag `v0.3.0` exists at `69ba3b0`, but both its snapshot and the live CHANGELOG jumped from `## Unreleased` to `## v0.2.1`; consumers therefore could not compute the released v0.3 delta from headings. **Delivered:** `## v0.3.0` now contains byte-for-byte the tag snapshot's former Unreleased payload, cross-checked against `v0.2.1..v0.3.0`; live Unreleased contains only post-tag work, and the post-tag C9 line was removed from the historical v0.2.1 section. `1a9ab0f..` was not used as the release range: `1a9ab0f` is a child after the tag and already carries next-cycle C39/package fixes. This historical repair precedes C54. **Both open ends are now closed by owner choice F9:** the current-state choice is **`0.4.0.dev0` + `## Unreleased`**, executed inside C54 so the live pair stays P1.3's acceptance evidence; and the history-completeness check **is in scope** — every tag `vX.Y.Z` should have a `## vX.Y.Z` heading — as a **warn**, because tags are immutable and a hard error over a gap that can never be repaired would simply be waived. A check of that shape would have caught what V3 repaired by hand · CHANGELOG.md + git tag/history · provenance: release-hygiene review,

Role-triad + develop/use split ([ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)):

- C6 · UserPromptSubmit role-confirm for sub-agents · deferred · verify Claude Code hook behaviour first · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C2 · release tool: pin-bump subject · deferred · the 3rd release subject (akmon pin bump recorded in a consuming project); only after akmon+package subjects settle · [design](design/release-versioning.md)
- A3 · cross-agent contract v2 · deferred · skill/role inventory beyond thin pointers · [design](design/cross-agent-contract-v2.md)
- C1 · harden git-commit-guard parsing · deferred · close regex bypasses only if a real one bites
- A1 · orchestration / role handoffs · deferred · until role routing causes real friction (4 roles now; release routes) · [ROADMAP O4](ROADMAP.md)
- A2 · OPERATE mode · deferred · keep as design note until a runtime actor exists · [ROADMAP O1](ROADMAP.md)

Distribution / adoption:

- C37 · `akmon` pip package: init/sync/verify console CLI · active (A10 locked) · engineer · pyproject + src/akmon thin CLI; `init` mechanizes BOOTSTRAP's mechanical steps (mount + layout + `.akmon.toml` + sync + routing init); launchers exec the mounted tree per the skew rule; **mode `package`**: mount-decoupled tree resolution, `<aitna>/.akmon/` hooks+guardrails materialization, `akmon path`, `mount` key in `.akmon.toml` — pilot consumer alphavar (git+https dev-group pin); self_ci package leg; README quick-start cut-over; publish step in the release pipeline (owner runs publish, D5). **Sequencing: first publish only after the V1 rename sweep** (the git+https pilot is not a publish) · [design](design/packaging-uvx-init.md) · [ADR 0009](decisions/0009-packaging-package-carrier-and-mount-modes.md)
- C39 · Codex runtime contract + package hardening for v0.4 · blocked (code complete; owner D2-13 verification pending) · architect→engineer · direct delegation anchor in the hand-owned AGENTS block + verifier gate; package-aware hook root/runtime dependencies; Codex SessionStart orchestration policy; self-hosted akmon AGENTS; honest capability matrix and package/release docs; installed-wheel/nested-cwd regression coverage; development version 0.4.0.dev0. Hard enforcement, named agents, and child-model routing stay unsupported until live Codex protocol probes prove them · [design](design/codex-runtime-contract.md) · D2-13
- N1 · vendor support matrix — verify & fill the Claude/Codex cells · active · every unknown Claude/Codex cell checked by experiment against the real harness, not vendor docs. **F4 timeout mechanics:** use disposable scratch wiring and a sentinel literal to probe every candidate Codex command entry; record exact build/version, invocation/config source, event/matcher/entry, literal/units, no-timeout control, before/after markers, expiry elapsed/tolerance, termination exit/signal, captured stdout/stderr and tool-call outcome. `Supported` requires observed timely termination, not parse acceptance; `unsupported` requires observed rejection or ignored field; `unmeasured` is not terminal. **F5 production budgets, after F6 bounds inputs:** map every generated Claude and F4-supported Codex command entry to exactly one dominant-workload class (payload/marker, project/config scan, git subprocess, transcript/routing I/O); run actual processes repeatedly on the largest F6-supported fixture corpus; record raw cold/warm timings, environment, fixture sizes, class rationale, formula/factor/rounding/floor, literal/units, false-timeout risk and hung-hook upper bound. Live-test individual entries and aggregate event groups at proposed literals. The F4 sentinel is not a production budget. Owner verifies the exact class/literal table before C52; later vendor versions or material runtime changes invalidate affected evidence · [hooks/README](../hooks/README.md) caveat inherited by Codex warning rows; stale evidence is an honesty bug
  - **F6 evidence gate (owner choice C):** inventory every consumer-growing axis per generated command entry — raw stdin bytes plus JSON depth/items/string bytes; command/description bytes; patch path count and per-path bytes/depth; transcript total/line bytes and matching records; D2 ledger bytes/rows; D2 config bytes and glob count/length/depth/`**` complexity; registry/overlay/local config/settings bytes/depth/items; briefs and generated output; agent roster/name/output; generated-agent directory/per-file/total bytes and rebind/prune population; marker/counter bytes. Propose exact cap/unit and combined supported corpus; owner verifies every cap and mapping before F5 timing or C52. The complete F4 → F6 → F5 evidence packet closes D2-23, which gates only C52. Over-cap is a separate measured `input envelope exceeded → fail-open` outcome, not crash or timeout. Normal deny claims are valid only inside the supported envelope; D2 owner verification explicitly accepts the payload-padding bypass
  - **Payload gap assigned at D2-18 verification:** the apply_patch **rename** form is unmeasured. `codex_adapter._PATCH_PATH_RE` reads `*** Add|Update|Delete File:` only, and `*** Move to:` occurs nowhere in this repository, so a renamed file would be classified under its source path and its destination not at all — C48's own silent shape, one form over. Probe codex 0.146.0 with a rename patch: capture the literal body, the `tool_input` keys, and whether the destination path arrives at all. Until it is measured no pattern is added, because guessing the form is precisely what D2-18(a) rejects. Gates nothing; feeds the C48 successor work and C52's payload envelope
- N4 · probe: is dispatch **completion** observable, per vendor · active · review · spawned by owner choice **F10** when C58 was narrowed to dispatch-only; the completion half is not deferred, it is unmeasured, and this is where it gets measured. Three unknowns, in order, because the second and third are meaningless without the first: (1) **is there a completion signal at all** on each vendor — an event, an exit payload, anything a hook can observe when a dispatched agent finishes, on the exact route (vendor/version/event/matcher) rather than in principle; on Codex the starting position is N2's finding that the SessionStart channel is parent-only, so `delivered: no` is a permitted and likely outcome; (2) **what a completion line may claim** — only fields the signal actually carries, no inferred success/failure; (3) **the correlation key** back to the dispatch line, and whether it survives a subagent that shares the parent `session_id` (the same ambiguity C28(d) hit). Output is a matrix cell per vendor plus, if and only if a signal exists, the line schema C58 would extend. **Not a design item:** nothing here is decided by choosing — it is measured or it is declared absent · provenance: F10, [§9](design/stage1-hardening-contracts.md)
- N3 · vendor support matrix — Gemini/Copilot cells · deferred · demand-driven: fill by experiment when a consumer asks; declared-only until then (gates nothing — A15 gates on the N1 Claude/Codex set)

Alternatives adoption ([plan](reviews/alternatives/plan-akmon-from-alternatives-20260718.md) —
weaknesses W1–W24 mined from ponytail, Superpowers, OpenSpec, wshobson/agents; reviews in
[`meta/reviews/alternatives/`](reviews/alternatives/)). Direction accepted: the A11 walkthrough
closed all 7 themes and decisions D1–D4 — [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md);
per-theme rationale, amendments and pilot acceptance criteria live in the
[walkthrough record](reviews/alternatives/walkthrough-a11-20260717.md). Process rule: **every
stage is design-first** — the A-task locks the contract, only then its C-task implements.
A-task design sessions run in the host's plan mode where available (Claude Code: the
owner-approved plan file is promoted into `meta/design/`, not left vendor-side); in agent-team
("tech lead") execution the lead's approval of each teammate's plan enforces the same rule per
dispatch (plan P2.8 / W24). The plan file stays the why-record (weakness→source mapping);
execution state lives here:

> **A12 gate override:** A12 is an awaiting-owner design draft, not a lock. D2-20 remains
> pending; C40, the remaining C46 tool-name work, and C51–C60 are not runnable. Inline
> `locked`, `runnable now`, and `Fork closed` wording below describes the proposal being
> walked through and has no gate effect until the owner choices are recorded and D2-20 is verified.
> The closure sequence is carried by **A17**.
- A13 · stage 2 design — pipeline & delegation contracts (plan P2.1–P2.8) · blocked (after C40) · one lock per contract, amendments per rationale; a lock ready early may spawn its own C-id · [rationale: themes 3–6](reviews/alternatives/walkthrough-a11-20260717.md)
- C41 · stage 2 implementation · blocked (after A13; may land per lock) · guardrail/pipeline text + defer-marker harvest + routing policy fields + manifest tooling
- A14 · stage 3 design — behavioral evaluation contract (plan P3.1+P3.3) · blocked (after C41) · layered eval with a control arm; baseline-must-fail; evidence classes in PROMOTE · extends [ROADMAP O5](ROADMAP.md) · [rationale: theme 1](reviews/alternatives/walkthrough-a11-20260717.md)
- C42 · stage 3 implementation — harness + first behavior gates (plan P3.2) · blocked (after A14) · `meta/benchmarks/` harness; first two gates on the cheapest claims; D3 arm + W18 outcome data
- A16 · capability matrix as data — schema/ownership/generation contract (plan P3.4) · **blocked (after C57)** · lock the matrix data schema, ownership, README generation, `sync.py` consumption. The Codex cells N2 measured (no `@`-import expansion, no SessionStart injection into a delegate, model pin observed on its exact route, `deny` observed per route not per intended effect) are the first real data the schema has to express. Inherit exactly six independent axes from the C57 lock: documented, delivered, exact vendor/version/event/matcher route, observed normal-path effect, crash posture, and evidence; bare `enforced` and checkmarks are invalid. **Status repaired at A17(f):** the row read `unblocked — N2 landed` while its own text said the schema cannot settle a vocabulary inheriting F3's axes before D2-20 locks F3, and its first job — consuming C57's prose cells — sits behind that same gate. A status that contradicts its own qualifying sentence is the defect this stage exists to remove, and the honest status costs nothing here. N2's landed data is what A16 will express, not a reason to start. **Rejected: a third status level** for *decided but not locked*, which is what F3 actually is — more precise, but that is D1 (mutable status levels), parked, and inventing it inside one task would settle D1 by side effect · [plan P3.4](reviews/alternatives/plan-akmon-from-alternatives-20260718.md)
- C44 · matrix-as-data implementation · blocked (after A16) · data file + README generator + round-trip smoke checks; live cell evidence comes from N1
- A15 · stage 4 designs — new surface, one ADR each (plan P4.1–P4.3) · blocked (gated on C42 + N2 + the N1 Claude/Codex rerun, plan R2; ADRs may land one at a time, each spawning its C-id) · SubagentStart injection only if N2 reproduced the drift; plugin carrier; MCP transport; P4.4 rejected — D1, [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md)
- C43 · stage 4 implementation · blocked (after A15; may split per ADR) · per the accepted ADRs, effects verified by the C42 harness; stage 5 polish stays opportunistic (plan §6 Phase 5), no task yet

**Stage 1 locked shapes** — the C40 split in the
[locked A12 design](design/stage1-hardening-contracts.md). Each
entry carries the locked decision and the fork it closed, so the alternative is retrievable
without re-reading the lock. Acceptance is identical everywhere: **a seeded violation makes the check fail**,
with the test named in the task.

- C51 · shared finding envelope (plan P1.6) · active · engineer · one stdlib-only `bin/findings.py` with `severity · code · message · target · fix`, adopted by `verify.py`, `meta/bin/validate.py`, `sync --check` and `self_ci` (the last imports it directly rather than parsing subprocess output). `severity` carries the closed `ok/warn/error` vocabulary and preserves the current exit-code contract (error → 1; warn → 1 only under `--strict`); `code` is a stable dotted slug (`boundary.missing-banner`, `caps.always-loaded`) never reused after a check is removed; it matches `[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)+`, rejecting whitespace, uppercase, empty segments, repeated hyphens and a slug with no dot. `target` semantics are review-owned. `fix` is required, non-empty, one sentence and one line; imperative mood is review-owned — a finding the reader cannot act on is a defect of the *check*. **Owner choice F1: B — rename `level`→`severity`, with no property, constructor, or serialization alias.** This accepts a breaking Python API change and requires one-step migration of every in-repository constructor, attribute read, fixture, and test, including the independent Finding in `meta/bin/validate.py`; rendered values and exit semantics stay unchanged. A contract test asserts that `severity` exists, `level` does not, and `level=` is rejected. **Owner choice F2: C — C51 owns and exactly tests one pure serializer (`Finding.to_dict()` or one shared function, not both), where pure means deterministic field mapping plus input non-mutation and makes no broader claim; C59 adds the first public JSON output.** Every adopter calls that serializer and carries no local field mapping. C51 adds no `--json` mode to its adopters. The JSON-safe schema contains only `severity · code · message · target · fix`, preserves Unicode, accepts an empty valid target, does not mutate the Finding, and passes `json.dumps`. **Text rendering *changes*, and C51 owns the change (A17(g)):** `verify.py` prints `[{level}] {message}` today (`bin/verify.py:690`), while the canonical greppable form is `SEVERITY code target: message → fix` — the draft said it "remains", describing a form the tree does not print, so every adopter's text output moves in the same commit as the `level`→`severity` rename and the tests pinning the old form move with it. Per **F13** it carries its own seed: a rendering that drops `code`, `target` or `fix`, or reorders them, fails an exact-output test — without it the whole text half could regress with every schema test green. **Out of scope: hooks** — they speak the harness JSON contract on stdout and prose on stderr, and a third output shape on a channel whose visibility is unverified helps nobody. **Known future adopter, recorded at D2-16 verification:** `routing.resolve_briefs` raises on the *first* bad overlay key, so an owner with two broken keys fixes one, re-runs and meets the next. Collecting every violation into one envelope is precisely what C51 buys, and the overlay path is its first non-check consumer. Out of C51's own scope — recorded so the one-error-at-a-time behaviour reads as a pending adoption rather than an oversight. **F14 carrier:** `findings.py` keeps the retired-code record; any live check whose code appears in that record fails. Appending a removed code is process-owned, with the accepted F14 cost that no checker forces an omitted append. **F13 triples run over all four adopters.** Seeded violations: a missing, empty, multi-line or multi-sentence `fix`; a duplicate live `code`; a live code restored from the retired record; an invalid severity such as `fatal`; an invalid dotted slug; a stale `.level` read, local `Finding` or old renderer in any adopter; serialization missing or changing a canonical field, adding `level`, mutating the Finding, returning different mappings on repeated calls, returning a value that fails direct `json.dumps`, importing a dependency outside the standard library in `findings.py`, or being reimplemented locally; a second canonical serializer; public `--json` added before C59; exact rendering with a field dropped or reordered; and any wrong cell in the severity × strict-state exit matrix. Each mutation fails the C51 contract suite; exact-render and exit tests are parameterized over every adopter
- C52 · hook survivability contract (plan P1.1) · blocked (after D2-23) · engineer · **Gate:** D2-23; no partial implementation, then parallel-safe with C51. Bounded stdin at the measured F6 literal (cap+1 detection before decode), `utf-8-sig`, a top-level exception guard in every **spawned entry point** — nine, enumerated at A17(f): the eight generated Claude entries (`bin/sync.py:270`–`:306`) plus `codex-hook.py`, which carries its route as an argument (emitted at `bin/sync.py:203` and `:222`–`:244`; the file is named explicitly since A17(g), where the bare form read as coordinates inside the 168-line `codex-hook.py`) — measured timeout literals in generated Claude wiring and F4-supported Codex command entries, and `hooks/README.md` contract. **F3/C:** every unexpected hook crash is fail-open with exactly one safe hook-process stderr diagnostic, including deny-class hooks; healthy explicit deny remains deny; matrix separates exact-route normal effect from crash posture. Fail-closed remains a separate measured owner/D2 gate with tested recovery. **F4/C:** disposable sentinel probes establish Codex timeout mechanics per entry; support requires observed termination, unsupported pins omission, evidence is version-scoped, and timeout kills inherit no F3 labels. **F5/C:** after F6 bounds valid inputs, N1 derives budgets from actual-process measurements by dominant-workload class using an owner-verified formula and exact entry→class→literal table; include the git class's existing 5-second inner timeout and aggregate event-group latency. The sentinel is not the production budget. CI pins exhaustive class mapping, exact generated literals/units, omission on unsupported/unprobed Codex scopes, and failure for a new unmapped entry — not wall-clock timing. Evidence harness proves largest-supported fixtures finish below budget and controlled hangs expire; a regression in measured runtime re-blocks rather than silently raises a literal. **Five of the nine entry points already carry a top-level guard and four do not** (measured at the ADR audit): `d2-ledger-reminder`, `delegation-log`, `delegation-nudge`, `session-start-agent` and `model-routing` wrap the whole of `main()`; `git-commit-guard` (deny-class), `role-on-code`, `analysis-guard` and `codex-hook.py` do not. So C52 **changes five and adds four**, and the existing five are **red** against this contract, not compliant with it: they print `{type(exc).__name__}: {exc}` — the exception *message* the diagnostic rule forbids — and they catch `Exception`, so an `argparse` `SystemExit` in the Codex entry escapes. The seeded crash test must be observed red on today's five too, or the contract ships already-passing in the five places a reader would assume were done. **Adapters and `hook_core` get no guard of their own** — imported, never spawned, so they have no top level; an exception inside them propagates into the wrapper's `main()` and is caught there, which is the same reason F5 excludes the adapter modules from timeout scopes. That coverage carries a precondition, so C52 owns it as a contract: **an adapter assembles its whole output and writes it once**, or a crash mid-render leaves truncated stdout that the guard exits 0 behind. **Rejected: per-function `try/except` inside the adapters** — that is not a guard but swallowing; the function returns something plausible and the wrapper never learns its decision ran on empty data. The four top-level additions decompose per F13 into the independently violable triples below; no representative seed stands for the whole shape
  - **F6/C oversize acceptance:** each declared growing axis has an exact owner-verified cap/unit and generated-entry mapping. Raw stdin uses cap+1 before decode; files use stat plus bounded read; collections enumerate cap+1 and discard on overflow; structural JSON caps are checked immediately after bounded parse. Preflight all applicable sources before stdout or any marker/log/config/artifact write, rebind, prune or delete. At cap+1 call no handler, retain no prefix/side effect, emit exactly one unthrottled safe stderr line per invocation with hook/dimension/cap/remediation, and exit 0 with empty stdout and no ask/deny/context/system message. Other processes on the event keep independent envelopes. Tests pin cap−1/cap/cap+1 for every source, exact declarations, handler-not-called and zero-side-effects at overflow, safe output, healthy deny at cap/fail-open above, and failure for an unmapped synchronous source or command entry. Raising a cap requires a separate owner/D2 gate with bounded streaming/index/cache equivalence, atomic update/invalidation/corruption recovery, live boundary probes and rederived F5 budgets; a numeric increase alone is insufficient
  - **F13/C52 triples:** the crash matrix injects ordinary failures at every applicable wrapper, adapter and handler layer across all nine spawned entries, plus a separate Codex-boundary `SystemExit`; each must exit 0 with one safe diagnostic and no blocking or partial stdout, while a paired healthy deny remains deny. A mid-render failure proves single-write; a BOM-prefixed valid payload reaches the handler. Removing Claude `timeout` fails `sync --check`; deleting the `hooks/README.md` contract, omitting its table, evidence environment/scope or operational-bound-not-SLA statement, or drifting its entry→class→literal mapping fails `self_ci`; an outer git-class literal able to pre-empt the healthy 5-second inner fallback fails a relational check, while literal correctness remains D2-23-owned. Instrumented stdin/file readers fail on reads beyond cap+1 or decode after overflow; instrumented collections fail on requesting cap+2 and require full discard. A below-cap preflight followed by an above-cap read seeds the growth race. Handler spies and side-effect sentinels prove pre-dispatch rejection. Oversize uses its own stable code, not the no-path code; multiple oversized sources still emit exactly one line; repeated invocations each emit one unthrottled line; an exact oracle admits only hook/dimension/cap/remediation and rejects sentinel path/payload/command/session/secret values. An oversized entry cannot cancel or contaminate a healthy sibling on the same event. Cap raises and measured-performance reblocking are explicitly process/D2-owned: CI pins approved literals, but review owns proof that a later raise used the architecture/evidence gate, and D2-23's reproducible corpus—not wall-clock CI—owns performance regression
- C53 · invariant canary (plan P1.2) · blocked (after C51; **the F8 table is now locked**) · engineer · **Owner choice F8 — the table C53 checks, and the tree changes C53 performs** ([§3](design/stage1-hardening-contracts.md)): six policy IDs — `privilege.no-escalation`, `commits.owner-owned`, `analysis.before-mutation`, `verify.owner-verify-d2`, `delegation.tier-floor`, `role.declaration` — plus `session_start_result` as operational. Five of the six claim a **subset**, not a section, and the marker wording must say so: the commit guard covers only the AI `Co-Authored-By` deny and `push`/`tag`/`merge` or a commit on the default branch or detached/unresolved HEAD, asking in interactive default mode and denying otherwise; the analysis guard covers only the first planning/design edit reminder; the D2 reminder sees only paths matching the consumer's configured globs; the delegation guard covers only uninterrupted read/shell/edit volume, producing a nudge and then an ask in interactive default mode or a deny otherwise; and the role guard covers only the first code edit design→code switch. **Added at A17(g): C53 also owns emitting that coverage state** as a `configured` / `not configured` finding from the existing `d2_ledger` check — F8/3 requires C59 to *report* it while C59's own contract forbids it a new data source, and nothing had been assigned to emit it, so the requirement and the constraint could not both hold. C53's one mechanical pass: convert the three live `> **Enforced** (not just documented) by …` blocks (Privilege escalation, Commits & ownership, Analysis before mutation) into `Runtime check:` markers, add the two missing markers, **write the new `## Role declaration` section** in `guardrails/_common.md` (the P0.3 orphan's prose owner), and annotate the seven docstrings. Deliberately **not** done at design time: prose asserting a join before the checker exists is the stage-1 defect itself. **Owner choice F7/B:** stable dotted policy IDs form a one-to-one join between exact runtime-checkable rule prose and callable-local AST metadata. Each runtime-backed guardrail subset owns one unique `Runtime check: <policy-id>` marker; never bare `Enforced by` or a claim over the whole surrounding section. Every public `hook_core.*_result` docstring declares exactly one of `Policy ID: <id>` or `Runtime classification: operational` plus non-empty `Rationale:`. Every policy ID occurs exactly once on each side; operational results have no prose marker. `self_ci` parses source/docs without importing hooks and reports the policy ID, callable, and prose location, and rejects malformed/duplicate/orphan IDs, missing or dual classifications, empty operational rationale and legacy live enforcement markers. No `INVARIANTS.md`, central mapping, decorator registry or generated annotations; function names/paths are not identity. F8 owner-locks the initial seven-function table before code: six scoped policies, `session_start_result` operational. `role_on_code_result` gains concise Role-declaration prose rather than an exemption. C56 remeasures actual annotation cost. **Edge added at A17(f):** C53's tree changes land *inside* the always-loaded surface C56 caps — an estimated +1…+2 lines against a measured slack of 13 (~197 shipped against the 210-line cap), so the two are compatible today; the edge is recorded precisely because nothing else would show when they stop being. **F15 carrier:** a contract fixture pins all seven shipped callable classifications — the six callable↔policy-ID pairs plus `session_start_result` as operational — so a coordinated policy-ID rename on both prose and docstring sides fails even though the structural join still balances. Seeded tests cover every rejected relation and delete on either side
  - **F13/C53 carriers:** pin the six callable↔policy-ID assignments — `privilege_escalation_guard_result`↔`privilege.no-escalation`, `git_commit_guard_result`↔`commits.owner-owned`, `analysis_write_result`↔`analysis.before-mutation`, `d2_ledger_reminder_result`↔`verify.owner-verify-d2`, `delegation_nudge_result`↔`delegation.tier-floor`, and `role_on_code_result`↔`role.declaration` — plus `session_start_result`↔`Runtime classification: operational`. Five marker tails are subset oracles: commit covers only the AI `Co-Authored-By` deny and `push`/`tag`/`merge` or a commit on the default branch or detached/unresolved HEAD, asking in interactive default mode and denying otherwise; analysis covers only the first planning/design edit reminder; D2 covers only paths matching the consumer's configured `sensitive_paths`; delegation covers only uninterrupted read/shell/edit volume producing a nudge and then an ask in interactive default mode or a deny otherwise; role covers only the first code edit design→code switch. The contract suite fails on: a coordinated two-sided policy-ID rename despite a balanced structural join (F15); a non-empty `session_start_result` rationale that omits either delivery-not-ownership or Codex's only-delivery-channel substance; `d2_ledger.py check` failing to emit, before its no-pending early return and through the C51 `Finding`, `configured` with `sensitive_paths` or `not configured` without them, while a fixture that emits neither also fails; any subset tail omitted or broadened to the surrounding rule; or any `self_ci` structural diagnostic omitting the policy ID, callable, or prose location, or failing to name the absent side explicitly when a violated relation has no coordinate. Each is a separate seeded mutation with its expected relational or exact-output failure
- C54 · version ↔ changelog cross-check (plan P1.3) · blocked (after C51) · engineer · **Owner choice F9** ([§4](design/stage1-hardening-contracts.md)). The shape is not "one mismatched pair" — the tree carries **four independent version carriers** (`pyproject.toml` `version`, `src/akmon/__init__.py::_STATIC_VERSION`, the topmost CHANGELOG heading, the git tag) and nothing relates any two; the first two already disagree today. **The defect has already shipped:** the tree at tag `v0.3.0` carries `0.3.0.dev0`, so a wheel built from the reviewed state is misnamed, and `1a9ab0f` then wrote `0.4.0` — the literal `0.3.0` never existed. It reproduces because `run_plan` stages only `CHANGELOG.md` and never a version literal, while `__init__.py` claims in a comment that the pipeline "cuts the real version from the git tag", which is false under a static hatchling version. Consumer cost: package mode stamps `akmon_version` PEP 440-shaped (`bin/sync.py::_package_mode_akmon_toml`), mounted mode stamps `git describe` (`v`-shaped), and the bump procedure computes its window from CHANGELOG headings, which `0.3.0.dev0` matches none of. **The check:** non-final version (full PEP 440 set — `.devN`, `aN`/`bN`/`rcN`, `.postN`, `+local`, not `.devN` alone) requires `## Unreleased`; final (exactly `X.Y.Z`) requires the topmost released heading to equal it; final with a matching tag is a **warn** (re-release); `pyproject` version and `_STATIC_VERSION` must be **literally equal**; a tag with no `## vX.Y.Z` heading **warns**; no version source at all emits an explicit **skip finding**, never silence; comparisons normalize the leading `v` and read a `describe` suffix as "ahead of that tag". **Same task, outside the checker:** `run_plan` prints the version bump and stages `pyproject.toml` + `src/akmon/__init__.py` before `git add`, and the false `__init__.py` comment is corrected. **Current-state repair: `0.4.0.dev0` + `## Unreleased`, executed inside this task, not before it** — P1.3's acceptance is the live pair observed failing and then passing; repairing early leaves the check with a fixture where it could have had the tree. **Forks closed:** the git tag is **not** authoritative (a dev version legitimately runs ahead, so tag-equality would fail continuously and get waived); a single version literal is **impossible**, not merely unwanted (`pyproject.toml` is excluded from the packaged tree), and `hatch-vcs` fails the same dev-ahead-of-tag case plus mounted modes with no metadata; a missing version source must **not** error (it fails every non-Python consumer) and must not pass quietly either; a tag with no heading must **not** error (tags are immutable, so the gap can never go green and the error would be waived). The `v`-prefix defect this exposed is **C61**, not part of this task. **The two repairs outside the checker are seeded too (A17(g), per F13):** a `run_plan` that stages `CHANGELOG.md` without both version literals fails, and one that reaches `git add` before the bump step fails — their absence is what makes this shape's own defect reproducible, and a checker reporting a mismatch the procedure keeps re-creating is the theatre this task already rejects
  - **F13/C54 carriers:** the `release_check` contract suite uses separate negative fixtures for the two heading branches: every declared non-final spelling (`.devN`, `aN`, `bN`, `rcN`, `.postN`, `+local`) and accepted combinations of those segments without topmost `## Unreleased` fail, while final-version negatives are two separate fixture branches: no released heading at all fails, and a topmost released heading that differs fails; the latter keeps a matching older heading below the wrong topmost heading so accepting any heading fails. Both consistent states pass. Literal disagreement between `pyproject.toml` and `_STATIC_VERSION` fails. With no version source, `release_check` emits an explicit C51 skip `Finding`, never silence or a clean pass. The tag corpus contains multiple tags with the newest represented and an older tag missing from CHANGELOG, and must warn for the missing historical heading; with git unavailable, each git-dependent rule — matching-tag re-release detection and the all-tags heading check — emits its own explicit C51 skip `Finding`, never a silent pass. Normalization fixtures compare `v0.4.0`, `0.4.0`, and a `git describe` suffix; a final version with its matching tag asserts exactly **warn**, not error or merely non-clean. Process repairs have their own carriers: parameterized `run_plan` output mutations omitting either version literal or placing the bump after `git add` fail, and the `meta/tests/test_release_check.py` release-check contract suite fails a source mutation restoring the false claim that the tag produces the built version. Preserve the live red→green order: observe today's `0.4.0` + `## Unreleased` failure before changing either literal, then require `0.4.0.dev0` + `## Unreleased` to pass after the repair
- C55 · source/generated boundary check (plan P1.7) · blocked (after C51) · engineer · **Owner choice F17/A — explicit PlannedFile ownership modes** ([§5](design/stage1-hardening-contracts.md)). The generator remains the declaration, but a planned path is not automatically a banner-bearing whole-file output. Every `PlannedFile` declares exactly one exhaustive mode: **bannered-file** (generator owns the whole file and the planned content carries the generated banner), **structured-file** (generator owns the whole file but its syntax, including JSON, cannot carry the comment banner; content drift remains `sync --check`'s job), or **field-owned** (sync owns only declared keys/region and must declare the owned selectors without claiming the whole file). Adding a fourth mode, a `PlannedFile` with no mode or more than one mode, or a field-owned plan with no selectors fails the mode contract; filename or extension inference is forbidden. `AGENTS.md` stays hand-owned with its marked akmon block and is not promoted to whole-file ownership; merged `.claude/settings.json` and the preserved-key package `.akmon.toml` are field-owned. The stale-banner scanner is limited to generator-declared **bannered-file surfaces** and exact banner positions: root vendor pointer paths, generated `.claude/skills` stubs, and package materialization under `<AITNA_ROOT>/.akmon`. It considers only regular UTF-8 files, does not follow symlinks, and recognizes a banner only in lines 1–4, including a banner that names an old source root. It excludes the mounted/source akmon tree, `AGENTS.md`, structured-file and field-owned paths, arbitrary user paths, VCS metadata, and cache trees; structured-file and field-owned modes never enter the reverse stale-banner scan. One pure boundary checker is called by consumer `verify.py` and by `self_ci`; both return C51 `Finding`s with exact **error** severity. A banner-required planned output without its banner emits `boundary.missing-banner`; an in-scope bannered artifact absent from the current plan emits `boundary.stale-generated`. Content-hash drift remains out of C55. The always-loaded definition in §6 clarifies field ownership but is not a task edge: C55's only task dependency is C51; it does not depend on C56, and C46's provenance banner is an outgoing annotation.
  - **F13/C55 carriers:** a contract fixture pins the complete current `PlannedFile` path→mode population. Separate mutations fail for a path with no mode, a fourth mode, two modes, or missing field-owned selectors; for every pinned path, each incorrect concrete mode assignment fails rather than being accepted by filename or extension inference. The banner-required seed changes the planned expected content itself to omit its banner, materializes that exact content, and first proves ordinary `sync --check` is clean; the shared pure checker must then emit exactly one `error boundary.missing-banner` Finding. The stale-artifact seed uses a non-skill bannered pointer: materialize the old plan, rename its planned target, materialize the new plan so the new target is clean while the old target survives outside `_obsolete_generated_files`' skill-stub scan, prove ordinary drift/obsolete-skill checks are clean, then require exactly one `error boundary.stale-generated` Finding. Separate exact scanner fixtures place an otherwise-stale bannered file on each included surface — a root vendor pointer path, a generated `.claude/skills` stub, and package materialization under `<AITNA_ROOT>/.akmon` — and each must emit exactly one `error boundary.stale-generated`; an in-surface symlink to a bannered file is not followed and produces no boundary finding; separate non-regular and non-UTF-8 fixtures each produce no boundary finding; a banner on line 4 emits exactly one `error boundary.stale-generated` while the same banner moved to line 5 produces no boundary finding; and a banner naming an old source root emits exactly one `error boundary.stale-generated`. Separate exclusion fixtures put the same banner-looking content in mounted akmon source, `AGENTS.md`, an arbitrary user path, VCS metadata, and a cache tree, and each produces no boundary finding. Representative structured-file and field-owned paths produce no reverse-scan finding. The same parameterized pure-checker cases run through `verify.py` and `self_ci`; deleting either caller's wiring fails, and warn severity, a different code, duplicate findings, or a non-isolated fixture fails the exact oracle.
- C56 · always-loaded caps (plan P0.4) · blocked (after C51) · engineer · **Owner choice F18/A — exact scopes, decimal units, one enforcement/reporting counter.** Always-loaded means every artifact reaching the model without it asking. The exact **akmon-shipped** scope is the marked akmon block in `AGENTS.md`, the complete guardrail chain that block imports, and the selected per-language guardrail; the hand-owned remainder of `AGENTS.md` and on-demand material are excluded. The exact **consumer-total** scope is the complete consumer `AGENTS.md` plus that same imported guardrail chain and selected per-language guardrail. The inclusive caps are **210 lines / 12,000 bytes** for akmon-shipped and **460 lines / 25,000 bytes** for consumer-total; bytes are decimal, not KiB. One pure counter owns both enforcement and reporting. An akmon-shipped overage emits exactly one C51 **error** `caps.always-loaded` Finding through `self_ci`; a consumer-total overage emits exactly one **warn** `caps.always-loaded` Finding through `verify.py`, remains warn under `--strict`, and makes strict verification exit 1. Every run dynamically reports the scope, measured line count, measured byte count, line cap, and byte cap from that same counter. The measured N2 baseline remains 433 lines / 23,613 bytes consumer-total and approximately 197 lines / 10.8 KB akmon-shipped; it is evidence, not a hard-coded result. Rejected: treating all of `AGENTS.md` as akmon-shipped, sampling representative files, binary-KB interpretation, or a separate reporting counter. Cost accepted: the exhaustive population and exact rendering fixtures move whenever an always-loaded surface legitimately changes. **C60 boundary:** C56 ships the ratchet and instrument only; justifying and lowering the target remains C60, so the borrowed 150-line reference is not adopted here. C56's only task dependency is C51. C53 and C58 write into the measured always-loaded surface and must exercise this counter against their landed additions, but those collision annotations are not dependencies on either task.
  - **F13/C56 carriers:** population contract fixtures pin both exact scopes. For akmon-shipped, separately omit the marked block, an imported guardrail, or the selected language guardrail, include the hand-owned `AGENTS.md` remainder, or include an on-demand artifact; each wrong population fails before a cap oracle. For consumer-total, separately omit the complete `AGENTS.md`, an imported guardrail, or the selected language guardrail, or add an on-demand artifact; each wrong population fails before a cap oracle. A parameterized boundary corpus runs independently for each scope and each dimension: line count at cap−1 and cap passes, cap+1 produces the scope's exact `caps.always-loaded` severity/exit behavior while bytes remain below their cap; byte count at cap−1 and cap passes, cap+1 produces the same exact scope behavior while lines remain below their cap. Thus all six boundary cases per scope — twelve in total — are isolated from the other dimension, and equality is accepted. Reporting fixtures assert exact dynamic scope, measured lines, measured bytes, line cap, and byte cap on every case; an isolated line-only mutation makes an equal-byte replacement of a one-byte character with a newline so the line report changes while the byte report stays fixed, an isolated byte-only mutation inserts one byte inside an existing logical line so the byte report changes while the line report stays fixed, and separate line-cap and byte-cap mutations change only their corresponding cap while both actuals and the other cap remain fixed. A missing, stale, hard-coded, wrong-scope, wrong-cap, or separately computed report fails even when enforcement remains correct. The same pure counter cases run through `self_ci` for akmon-shipped and `verify.py` with and without `--strict` for consumer-total; a second counter, wrong code, duplicate finding, error instead of warn in consumer mode, warn instead of error in self-CI, strict-mode severity escalation, or strict exit 0 fails the exact oracle.
- C57 · OS contract + capability-matrix vocabulary (plan P0.2) · **blocked (after C51)** · engineer · **Owner choice F19/A — explicit matrix transition and one six-axis vocabulary.** Runtime declaration is contract-pinned as `generated-wiring: POSIX-shell|required, python3|required, git|required-on:codex` and `akmon-own-tooling: claude|optional, codex|optional`. Generated command strings are the source for the first population; one declarative query map is the sole owner and constructor of `sync`'s harness-version queries for the second. `self_ci`, through C51, joins each population to its own source. An invoked but undeclared binary is one **error** `runtime.undeclared-binary`; a declared but unused binary is one **warn** `runtime.unused-binary` and leaves non-strict `self_ci` at exit 0; a wrong population is one **error** `runtime.wrong-population`; a wrong modality is one **error** `runtime.wrong-modality`; a direct or second query owner outside the map is one **error** `runtime.duplicate-query-owner`. Every runtime error exits 1. Windows unsupported is explicitly a scope declaration, not a checkable rule. The git-removal trigger remains a consumer report or P4.2 changing the wiring; C57 declares the dependency rather than removing it. A capability has exactly six independently required axes: `documented`, `delivered`, exact `vendor/version/event/matcher` route, observed normal-path `effect` (`none/advisory/ask/deny`), `crash-posture` (`fail-open/fail-closed/unmeasured`), and `evidence`. A missing axis or route coordinate is **error** `matrix.missing-axis`; an invalid effect/crash value is **error** `matrix.invalid-value`; an ask/deny claim that retains every axis and route coordinate but sets one route coordinate or crash posture explicitly to `unmeasured` is **error** `matrix.unqualified-effect`; a measured claim with empty evidence or an unmeasured claim with non-empty evidence is **error** `matrix.uncited-claim`; a forbidden `enforced`/`enforces` or `✅`/`⚠️` claim adjacent to a vendor or harness event outside marked regions is **error** `matrix.bare-claim`; and an absent top-level `CAPABILITIES.md` or absent marked region is **error** `matrix.missing-file`. `self_ci` checks vendor-harness claims only: akmon's own in-process checker claims and C53-owned guardrail prose are excluded. **F19 transition:** on the unchanged tree, `self_ci` independently emits `error matrix.missing-file` for the absent top-level file/marked region and `error matrix.bare-claim` for forbidden legacy README claims; it does not parse the legacy table as matrix rows. After both failures are observed, C57 creates top-level `CAPABILITIES.md` with one marked region, converts all nine capabilities to the six axes, removes the detailed README table, and leaves a short claim-free delivery summary. The top-level marked matrix is then the sole detailed matrix; a retained README table fails through its forbidden claims. Rejected: conversion before both live failures, dual authority, a meta-only matrix, compact legend encoding, a legacy-table parser, or waiting for A16. Cost accepted: prose/schema duplication lasts until A16 consumes the cells into its owned schema/generation contract; C57 carries no fallback parser. A16 remains the declared later seam, not a C57 predecessor. C46 and C58 remain downstream of C57; C57 has no edge to C52.
  - **F13/C57 carriers:** the runtime contract suite pins all five shipped runtime-entry→population→modality assignments. Separate mutations add an undeclared binary to generated wiring and to an own-tool query, remove each used declaration, add an unused declaration in each population, move each binary to the other population, and assign each alternative wrong modality; every case asserts its exact `runtime.*` code, severity, finding count, and exit. A direct query of an already-declared `claude` or `codex` outside the declarative map and a second map both produce exactly one `error runtime.duplicate-query-owner`, so the one-owner rule cannot pass through the undeclared-binary seed. The matrix suite uses a complete non-enforcement row and separately deletes each of the six axes, then separately deletes `vendor`, `version`, `event`, or `matcher`; every isolated deletion requires exactly one `error matrix.missing-axis` and cannot overlap the ask/deny rule. Separate complete ask and deny rows retain every axis and coordinate but set, one at a time, `vendor`, `version`, `event`, `matcher`, or crash posture explicitly to `unmeasured`; each yields exactly one `error matrix.unqualified-effect`. Invalid effect and crash-posture tokens each yield exactly one `error matrix.invalid-value`. A measured row with empty evidence and an unmeasured row with a citation separately yield exactly one `error matrix.uncited-claim`. Outside marked regions only, `enforced`, `enforces`, `✅`, and `⚠️` adjacent to a vendor or harness event are parameterized separately and each yields exactly one `error matrix.bare-claim`; the vocabulary checker has no inside-region bare-word or glyph oracle. Negative scope fixtures keep the accurate `pipelines/tasks.md` in-process “enforced by verify.py” claim, each C53 guardrail marker/prose case, and an ordinary non-vendor use of the word `enforced` clean with no matrix finding. Location fixtures separately cover an absent top-level `CAPABILITIES.md`, a top-level file with no marked region, and a matrix placed only under `meta/`; each yields exactly one `error matrix.missing-file`. Separate retained-README fixtures keep the old detailed table after a valid top-level matrix and require its forbidden claims to emit their exact `error matrix.bare-claim` findings. The live transition test runs `self_ci` first on the unchanged tree and records both `error matrix.missing-file` and the expected `error matrix.bare-claim` findings without any legacy-row `matrix.missing-axis`; it then runs the same checker against the converted top-level marked matrix and claim-free README and requires clean green. A fixture that creates or repairs CAPABILITIES before both independent red observations fails the red-before-green oracle.
- C58 · execution ledger — dispatch half (plan P1.5) · blocked (after C46 and C57) · engineer · **Owner choice F10: dispatch-only.** The completion half leaves this task for probe **N4** — which event marks a dispatch finished, what a completion line holds, and what key correlates it back are unknown on both vendors, and a ledger locked with half of it guessed is the failure class stage 1 exists to remove. Narrowed to the measurable, it can be extended by evidence; locked on a guess, it would have to be unlocked first. **Accepted drawback, recorded in [§9](design/stage1-hardening-contracts.md) rather than hidden:** a dispatch-only ledger cannot answer "what did I send that never came back", which is much of what "after compaction, trust the ledger" was meant to buy — stage 1 ships the smaller promise and says so. **Owner choice F21/A — one bounded dispatch-request ledger:** the observed Claude `PreToolUse` request is normalized through C46's `subagent` capability and writes v1 TSV only to `<AITNA_ROOT>/model-routing.log`; it proves neither launch nor completion. Stats and coverage-map read legacy `.claude/model-routing.log` first and the new path second without deduplication; `coverage_map.py --log` reads only its explicit path. Both default paths remain gitignored, and the orchestration guardrail names the new symbolic path and request-only scope. No migration command. **Fork closed: not a model-written ledger** (the plan's line reads as prose appended per dispatch) — a ledger the model maintains degrades exactly when the thing it guards against happens, under compaction and context pressure; if it is worth having *after* compaction it cannot depend on the compacted party to write it. **Codex gap declared, not papered over:** the delegation-log hook does not fire on Codex and whether Codex dispatch is observable at all is unmeasured (N2: the SessionStart channel is parent-only) → a matrix cell `delivered: no`, not a cross-vendor claim. **Edge added at A17(g):** the guardrail clause lands in `guardrails/_common.md`, which §6 counts as always-loaded in full, so C58 draws on the same 13 lines of slack as C53 — one line, same budget; the twin of the C53↔C56 edge found at A17(f) and missed by the same reading. Seeded violations: a dispatch with no ledger line; **migration** — a line at the old path must still be read, a write must land only at the new path, and a fixture writing the old path fails (the rule was two-way and the test only asked whether *some* line existed); **the guardrail clause gets its own check, not the §3 canary** — the canary joins a policy ID in prose to a `hook_core.*_result` docstring and C58 adds no callable, so "covered by the §3 canary once its enforcer exists" named an enforcer no task in this lock was building. What is mechanical here is the **path**: the clause names the ledger and C58 moves the ledger, so C58 checks that the path in the guardrail prose is the path the tool writes — either side changed alone fails, and deleting the clause fails from the same direction, because the prose then names no path at all
  - **F13/F21/C58 carriers:** one pure contract checker runs through `self_ci`. Its exact error findings are `ledger.wiring-drift`, `ledger.owner-drift`, `ledger.path-drift`, `ledger.schema-drift` and `ledger.ignore-drift`; every isolated mutation produces exactly one finding and exit 1. Wiring fixtures separately remove the C46-normalized Claude subagent matcher, move it away from `PreToolUse`, wire another entry or add a non-dispatch event; a healthy request writes one row and non-dispatch calls write none. Ownership fixtures add/remove the sole writer, add/remove either exact reader, or introduce rename/truncate/delete/rotation ownership. Path fixtures change each legacy/new constant, write legacy, reverse old→new order, add deduplication, omit either reader path, make `--log` fallback/merge, or change/delete either side of the exact guardrail↔writer join. Old-only/new-only/both states run through stats and coverage; both retains duplicate-looking old/new lines in order. The exact new schema is seven fields — literal `v1` plus offset-bearing timestamp/session_id/subagent/model/zone/description — with each data field normalized by `" ".join(str(value).split())` and empty→`-`; fixtures delete/add/reorder every field, change the marker/timestamp/normalizer/sentinel, inject tab/CR/LF into every data field, accept legacy on the new path or reject four-/six-field legacy on the old path. Malformed or unknown-version rows cause a safe visible reader failure naming only path, line and structural reason, exit 1, with no derived artifact and no rewrite. Ignore fixtures remove either path or hard-code `_aitna` under relocated AITNA_ROOT. Serialized parallel fixtures require N successful in-envelope requests→N intact parseable rows. §2/C52 owns the cap-boundary and injected lock/open/short-write/durability/rollback failure carriers under D2-23; C58 neither duplicates them nor gains a C52/D2-23 task edge. Deleting self-CI wiring, changing code/severity/count/exit, combining mutations or passing through an unrelated failure fails. Manual structurally valid v1 rows are not authenticated and are deliberately indistinguishable; completion remains N4. C58 depends on C46+C57; its C56 cap collision remains a non-edge.
- C59 · `akmon status` (plan P1.4) · blocked (after C51–C58 **and C46**) · engineer · **Owner choice F11 — exact fidelity, no exemption and no source of its own.** C59 has exactly two providers in exact order: the consumer `verify` finding provider, then the consumer `sync --check` finding provider. C53-owned D2 coverage/pending state and C56-owned caps/headroom each enter exactly once through `verify`; C59 never calls either checker directly, and `sync --check` never repeats them. Every invocation runs both providers read-only against fresh actual consumer state at the requested root — current pin/carrier, materialized hook wiring and files — with no cache, saved snapshot or declaration substituted for the tree. The aggregate is exactly the ordered concatenation `verify findings + sync findings`: no sorting, deduplication, synthesis, dropped finding or C59-invented finding. Text and JSON expose identical findings in identical order. C59 is a C51 adopter and uses the sole canonical C51 serializer/rendering/exit owner; it carries no local field map or serializer. The full severity behavior remains C51's: ok-only exits 0 in strict and non-strict modes; warn-only exits 0 non-strict and 1 strict; any error exits 1 in both, identically in text and JSON. **Active-role residual:** the active role exists only in the live harness transcript; ordinary status has no transcript provider and therefore makes no active-role claim and persists no role marker or cache. Adding one would violate the no-new-source/read-only contract. Sequenced last so its source population is exactly C51–C58 plus C46 and every source owner has landed. Accepted cost: a pure aggregator gains a provider seam and cannot report the transcript-only active role. Public JSON framing and option name remain a later status fork; **mutable status levels remain parked as D1** in [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md).
  - **F13/C59 carriers:** one pure aggregation seam accepts provider callables and proves exact fidelity before the CLI adapters render it. A provider-population contract pins exactly `verify` then `sync --check`; removing either, adding a third, reversing them, invoking either twice or deleting any dependency from the exact C51–C58+C46 source population fails. Separate provider fixtures place C53's D2 coverage/pending finding and C56's caps/headroom finding exactly once in `verify`; emitting either directly from C59, repeating either through `sync`, dropping either or changing its provider fails. Every current family is mutated independently — ordinary verify contract/pin/carrier/hook findings, C53 D2 state, C56 caps/headroom, ordinary sync drift and C46's sync warnings — by adding, deleting and changing one source finding; the aggregate must change by exactly that ordered element and nothing else. Provider reorder remains visible: there is no sort or dedup seam. A source finding omitted, duplicated, reordered, rewritten or synthesized fails; an invented finding present in both renderings but in neither provider fails exact equality. Text and JSON fixtures assert the same canonical fields and values in the same order; a finding present in only one rendering, a rendering-only reorder or a local JSON/text field map fails. C59 is pinned in C51's complete adopter-owner population and calls the sole canonical C51 serializer; removing that adopter entry, adding a C59 serializer/helper or bypassing the canonical renderer fails even when output happens to match. The full parameterized `ok`/`warn`/`error` × strict/non-strict × text/JSON matrix has twelve cases: ok-only is 0 everywhere; warn-only is 0 non-strict and 1 strict; error is 1 everywhere; findings and order remain identical across each text/JSON pair. Read-only/freshness fixtures snapshot the whole consumer tree before and after every mode and fail on any write, marker, cache or materialization change. Separate actual-state seeds mutate the current package pin and generated hook wiring while leaving declarations and a stale cached provider result unchanged; both text and JSON must follow the actual consumer state, while an implementation that reports the declaration/cache fails. Conversely, changing only a declaration or cache without changing the actual pin/wiring cannot fabricate an actual-state finding. An active-role fixture supplies no transcript provider and asserts no role claim, no transcript read and no persisted role marker/cache; adding any of those fails the declared residual rather than creating a new provider. A fixture that makes only one mode fresh/read-only, combines source families, or passes because another provider is already red fails isolation.
- C60 · reduce the always-loaded chain toward a justified cap · blocked (after C56) · engineer · the work C56's ratchet deliberately did not do. Two obligations, in order: (1) **justify the target** — ≤150 lines is wshobson's number for a differently-shaped artifact, so akmon must argue its own budget (what a model must hold *before* it asks for anything) rather than inherit a borrowed one; (2) then move content out of the always-loaded chain and lower the caps in the same commit, so the ratchet actually moves down instead of standing as permanent permission for 197 lines. **Fork to decide in-task, not now: shrink by relocation or by deletion** — moving rules to on-demand includes keeps them at the cost of them not being read (which for a guardrail may equal deletion with extra steps), while deleting rules is honest but forfeits them; the measurement C56 ships is what makes either arm checkable. Carries the doc-gardening sweep (dead links, staleness) that C55 deliberately declined to fold in
- C64 · one owner for pytest-runner selection · active · architect+engineer · two modules decide how to run pytest here and **disagree about the same command**. `meta/bin/validate.py::_pytest_command` (rewritten in C62) claims a runner only when it resolves — importable pytest, else `uv` plus a manifest declaring pytest in a default group — and returns `None` with a reason otherwise, because its contract is *skip rather than fail*. `tools/release/release_check.py::_pytest_command` has the richer discovery — pinned `[test].runner` from the attach record, then `_aitna/.venv`, a system pytest, `uv run --with pytest`, and finally `python -m pytest` — and always returns a command, because its contract is *run something and let it fail*. Its docstring still states that a bare `uv run pytest` runs in an ephemeral env without pytest and fails, which is the pre-C37 fact C62 retired on the other side: akmon's root has declared a `dev` group since C37, so the claim is now era-specific rather than general, and two modules asserting opposite things about one command is the second-owner pattern C46 exists to remove. **The fork, and why it is not an engineer's call:** unifying means splitting *discovery* (where could pytest come from, in priority order) from *policy* (what to do when nothing resolves) so both contracts can be served by one owner — versus keeping two deliberately and making each say why it is not the other. Neither is free: one owner costs a dependency from `meta/bin/` into `tools/release/` (or a third home for the shared half), two owners cost the drift that produced this entry. **Do not lose in either direction:** the pinned `[test].runner` is decided once at attach and must keep winning over discovery (BOOTSTRAP §A5); the dev venv must keep being invoked as `<venv>/bin/python -m pytest` and never through its `bin/pytest` console script, whose baked shebang breaks on a relocated venv; and validate.py's skip must never become a failure · `meta/bin/validate.py` + `tools/release/release_check.py` · provenance: C62 side finding
- A18 · auto-trigger `plan-draft` on un-decomposed high-leverage work · deferred · architect · the sub-fork [**A7 · reconcile engineer role vs auditor on-signal**](TASKS_ARCHIVE.md) deferred when it locked (b) — named in full because that id was reused and the archive holds two A7s. Carried here because a deliberate not-now was living in an archived entry with no id of its own. **The asymmetry:** §9.3 item 5 gives the plan *check* a mechanical trigger — the zone plan names ≥2 zones, the pre-fan-out form of §9.5's structural trigger, owner-decided as always-for-gate-qualifying-work — while the plan *draft* fires only when the orchestrator recognizes a leverage signal (cross-zone task, D2-dense territory, an expected gate-qualifying fan-out). **That is the economics root of verdict contradiction 2:** un-decomposed high-leverage work is exactly the state in which the orchestrator does not notice, which is the same failure the delegation nudge exists for (§13.1 — "the standard said delegate seven times and the orchestrator still didn't", D2-8). **Why it is not a small fix:** the check's criterion reads the zone plan, and the draft is what produces one, so an auto-trigger has to fire on a signal that exists *before* any decomposition — the task's own scope, path D2-sensitivity, expected fan-out — none of which the tree measures today. **Options, none chosen:** (a) a structural pre-plan trigger on such a signal, which needs the signal defined and measured first; (b) leave it to judgement and rely on the plan check, cheap but blind in exactly item 5's own way — a check reads the plan against the goal and cannot see a zone nobody conceived; (c) a **nudge** rather than a trigger, reusing the graduated advisory→ask mechanism already built and measured for delegation (§13.2, C29 axis 2), which fits the house doctrine that an unmeasured hard gate gets routed around. Sequencing note: (c) is the only option whose mechanism exists today · [design §9.3 item 5 + §9.5 + §13](design/model-routing.md) · provenance: A7 sub-fork / verdict contradiction 2

Ids use the typed scheme ([ADR 0002](decisions/0002-task-id-convention.md)): **A** architecture/
design · **C** code · **L** learning · **V** release · **N** analysis/review; the role is derived. Archived `T#` are
grandfathered (frozen), so the historical references below keep their `T#`.

Trim rationale (A1/A2/C1/A3/C2 "deferred") is recorded in ROADMAP O# / the release design; do not
re-expand without a reason. The T3 release design (ADR 0001) is implemented (T10–T15); C2 is the
deferred pin-bump follow-up, A3 the deferred O3 v2 design, A4 surfaced while building the release
tool.

## Done

See [`TASKS_ARCHIVE.md`](TASKS_ARCHIVE.md).
