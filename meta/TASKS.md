# TASKS — akmon development backlog

The single backlog for developing the akmon standard itself. Format:
[`pipelines/tasks.md`](../pipelines/tasks.md). Directions / model gaps: [`ROADMAP.md`](ROADMAP.md).

> This is akmon's *own* backlog (evolving the standard), distinct from a consuming
> project's `_aitna/TASKS.md`.

## Status

Tooling spine (`bin/sync.py`, `bin/verify.py`, vendor-neutral `hooks/hook_core.py`) exists and
is **tested** (`tests/`). Remaining work is contract specification + scale hardening, not new
mechanism.

## Active / blocked / deferred

Model routing ([ADR 0004](decisions/0004-model-routing-capability-tiers.md); design in
[`meta/design/model-routing.md`](design/model-routing.md)):

Auditor / gate-pack extension (design in [`model-routing.md` §9–11](design/model-routing.md)) —
**architecture** (lock, then ADR):

- A6 · roles & pipelines under the subagent model · done in design · architect · recorded as §10 (role = orchestration contract; role→task-kinds matrix; zone plan from Decompose/Survey; loop-back edges; learn/release tiers); lock items folded into A5 (done) · [design §10](design/model-routing.md)
- A7 · reconcile engineer role vs auditor on-signal (matrix coherence) · **locked (b) → recorded §9.4/§10.2/§10.4; implemented in C18** · architect · §9.4 permits an on-signal code audit but §10.2 omitted it for engineer (auditor **seam 1**). **Decided (b):** `audit` (+ `independent-review`) become **cross-cutting verification kinds** — routable from any role, exempt from the C20 role-matrix warning; *when* they apply is the structural trigger (§9.5), not the role. Dissolves the seam at source and simplifies C20 (no per-row copy to maintain); consequence — learn/release may route them too (advisory-only). Impl in **C18**: registry `cross_cutting_kinds` + `role_matrix_warning` exemption + test + ADR 0005 addendum. Sub-fork deferred (auto-trigger `plan-draft` §9.3 item 5 on un-decomposed high-leverage work — verdict contradiction 2's economics root) · [design §9.4+§10.2+§9.3](design/model-routing.md) · provenance: verdict.md seam 1 / contradiction 2 (local, untracked)

**implementation** (A5 locked → ADR 0005; C15 done → C16/C17/C20 unblocked, then C18):

- C16 · gate-pack builder · code-complete & D2-pending (D2-1/D2-2) · engineer · one structured package (artifacts + yardstick + coverage map) consumed by auditor subagent and second-opinion CLI, plus the minimal plan-check pack (yardstick + zone plan, pre-fan-out); replaces free-form `--prompt-file` in second_opinion.py; runnable as one deterministic script per §11. **Also wire the second-opinion diversity ladder** (currently dead registry data from C15): `second_opinion.py` selects provider *and model* by `second_opinion_policy.diversity_ladder` — other-vendor → same-vendor-other-model (via `second_opinion_fallback_model`, computed into config but unconsumed) → never same-model · [design §9.4+§9.3.5+§4.6](design/model-routing.md)
- C17 · coverage map from delegation log · code-complete & D2-pending (D2-3) · engineer · zone label on fan-out delegations (from the §10.3 zone plan) + assembler tool — map at code cost, not tokens. Delivered: log schema `ts·session_id·subagent·model·zone·description` (zone off a `[zone:LABEL]` marker), `coverage_map.py` (scope by `--session`, +opt `--since/--until`; uncovered-seam flags vs zone plan) feeding `gate_pack.py --coverage-map`; `gate_id` precision rides on C20's session marker without changing the scope-by-key shape · [design §9.4+§10.3](design/model-routing.md)
- C18 · docs: leverage principle + role/pipeline deltas · code+docs complete & D2-pending (D2-6) · engineer · name the leverage principle in MODEL.md; tier table + matrix rows update; review/design-flow auditor anchors (post-fan-out + the pre-fan-out plan check) + loop-back edges; roles/*.md one-paragraph deltas linking §10 incl. the drafts-vs-decides invariant reading; demote static floor warning to weak prior. **Also implement A7 (b): registry `cross_cutting_kinds` = [`independent-review`, `audit`] + a `role_matrix_warning` exemption so C20 no longer warns on cross-cutting verification kinds for any role + test + ADR 0005 addendum** — else C20 warns against the very audit routing C24/C25 enable (auditor seam 1); these anchors are the missing *process* half that says *when* to launch the auditor (seam 2) · [design §9.1–9.5+§10](design/model-routing.md) · provenance: verdict.md seams 1–2 (local, untracked)
- C19 · owner-attention metrics in stats digest · deferred · extends C13: D2 pending/approved/verified, decisions per session, next to token spend · [design §9.6](design/model-routing.md)
- C20 · role-matrix advisory check in delegation hook · code-complete & D2-pending (D2-4) · engineer · warn when a routed subagent has no task kind in the active role's `role_task_kinds` row. Active role read from the transcript's last main-chain `🧭 agent:` declaration (chosen over a written marker — consistent with C22/C23 transcript scanning); subagent-granularity rule (warn iff kinds ∩ allowed = ∅); advisory, never blocks. **Also (D2-5):** the delegation hook now derives each agent's pinned model from the binding (`bound_model_for`), so the console line + record name the real model — completing C21's always-empty `(<model>)` · [design §10.2+§10.4](design/model-routing.md)
- C24 · deterministic gate-runner (one command) · blocked (after C18) · engineer · chain `coverage_map.py` → `gate_pack.py` → `second_opinion.py` into one deterministic gate run (§11): assemble the pack from the log, get the second-opinion verdict, write the bundle. The fan-out + the k_auditor **spawn stay orchestrator-side** — a script cannot call the Task tool (§11 "vehicle, not contract"); this covers pack-assembly + second-opinion CLI + verdict only. Builds on C16 (`second_opinion.py --gate-pack` is half of it). Scopes the auditor-usage analysis's "fix #1" honestly · [design §9.4+§11](design/model-routing.md) · provenance: `_aitna/artifacts/gates/20260704-185954-subagent-usage/verdict.md` (local, untracked)
- C25 · missed-gate forcing function (revive dead `gate_triggers`) · blocked (after C18) · engineer · make `gate_triggers` (`review_min_findings:3` / `architect_min_options:2` — findings/options **counts**, no zone key, currently unenforced dead registry data) an actual advisory at the review/architect gate: when a role emits ≥threshold findings/options, nudge to run the auditor. **NOT fan-out-conditioned** (zero fan-outs ever ⇒ would fire zero times — strictly weaker than the dead data it revives); independent of coverage/zones. Fixes the auditor-usage analysis's misspecified "fix #2" · [design §9.4](design/model-routing.md) · provenance: verdict.md contradiction 3 / re-rank delta 1 (local, untracked)
- C32 · enforce or reclassify dead routing data (`task_kind_floors` + `binding.escalation`) · active · engineer · both are computed and written to local config (routing.py:194–212, :952) but **no hook reads either** (auditor-verified: grep over `hooks/*.py` empty) — that slice of the routing contract is silently advisory, same "dead registry data" class as C25. Fix: an advisory floor check in the delegation-log hook (`bound_model_for` already resolves the routed agent's model at that exact point) — warn when a routed kind's floor exceeds the delegate's rung; surface or drop the escalation path likewise. Alternative (honest-contract option): reclassify the fields as documentation · [review](reviews/review-20260705.md) finding 3
- C33 · zone-marker robustness + convention surfacing · active · engineer · `parse_zone` (routing.py:1043–1058) accepts only the literal `[zone:` prefix, and the syntax lives only in dev-meta docs/docstrings — not in MODEL.md, pipelines, role docs, k_* briefs, or the status line. Observed live (review-20260705 gate): unbracketed `zone:X …` fan-out descriptions → an all-uncovered coverage map, hand-assembled fallback — a silently degrading map defeats the uncovered-seam mechanism the gate leans on. Fix: tolerant parsing (`zone:X` with/without brackets) + state the convention in the generated k_* briefs and the status-line delegation guidance (routing.py:988–990) + delegation-log warn on an unlabelled fan-out delegation while a zone plan is active · [review](reviews/review-20260705.md) finding 5
- C34 · role docs ↔ `role_task_kinds`: restore single owner · active · engineer · triad role tier sections partially restate the registry row (review.md:57–59 names 2 of 4 kinds; architect.md:66–68 3 of 6; engineer.md:54–55 4 of 10) — partial duplication is primed drift, worse than link-only or full generation. Pick one: link-only (registry is the owner) or generate the tier lines from the registry (the `doc-sync` kind exists for exactly this); sweep the three triad role docs · [review](reviews/review-20260705.md) finding 6

- C46 · vendor-specific names in cross-vendor contracts: declare neutral, map per vendor (tool names + agent names) · blocked (tool-name half: after A12/D2-20, then C51+C57; agent-name half complete via ADR 0011) · engineer · `AGENT_SPECS` hardcodes `tools="Read, Grep, Glob, Bash"` (routing.py:363/:469/:502) into generated agent frontmatter, but **Claude Code 2.1.221 has no `Grep`/`Glob`/`MultiEdit` tool at all** — probe-verified live in alphavar (k-explorer attempted both calls → `UNAVAILABLE`; actual set `Read, Bash`), so akmon's read-only **sweep** delegate runs without its sweep tools and nothing warns. Same staleness in `bin/sync.py:262,:278` matchers and `hooks/claude_adapter.py:12,14` tool sets — dead alternatives, so the delegation nudge's read/sweep axis (D2-8) now sees only `Read`. Root cause is a **second owner**: `hook_core.py:107–113` already models neutral tool *kinds* normalized by each vendor adapter, and `AGENT_SPECS` bypasses that pattern to name Claude's tools directly. Fix (owner-chosen): declare **capabilities** (`read`/`search`/`shell`) + a per-vendor name map, degrade a missing capability to its Bash equivalent **explicitly**, and add a deterministic check that every declared name is in a known harness inventory — so the next harness drift fails loudly instead of silently. Record the Bash-degradation contract as a contract: Bash is strictly wider than Grep/Glob, so the read-only boundary then rests on the agent body's prose, not on the tool set. **Rejected here:** shipping akmon's own tools over MCP — that is P4.3, gated behind A15/C42 by [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md). **Second instance, same root cause — agent *names*:** Codex 0.146.0 rejects akmon's delegate names outright. Its `agent_name` validation, read from the shipped binary, is `must not be empty` / `must use only lowercase letters, digits, and underscores` / `must not contain "/"` / `"root" is reserved` — so the hyphen in `k-explorer`, `k-mechanic`, `k-auditor` makes all three illegal Codex agent names; probe F2 died on exactly this (`Rejected: agent_name must use only lowercase letters, digits, and underscores`). Claude accepts the hyphen, which is why the split never surfaced. **No shipped artifact breaks today:** `bin/sync.py` emits only `.codex/README.md` (:569) and `.codex/hooks.json` (:570) for Codex — no agent definitions — and codex 0.146.0 has no agent-definition file convention at all (no `.codex/agents/`; subagents are spawned dynamically through `collaboration.spawn_agent`, where `agent_name` and `task_name` are separate parameters). The exposure is to the **plan**: D4 and any step that gives Codex delegates the `k-*` identity. **A12 chose the global rename** (owner, this session) over a per-vendor name map: all six agents are now `k_*` in live code, tests and current docs, verified against Claude Code 2.1.221 (an underscore-named agent is offered as a subagent type alongside the hyphenated ones) — [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md). The hyphenated names above are kept verbatim as the record of the defect and must not be rewritten. What stays open **here** is the tool-name half: C46's mechanism (one neutral declaration, per-vendor name map, loud failure on an unknown name) is still unbuilt, and the rename bought a shared *notation*, not the mechanism. **Not an instance — hook matcher names:** first read as a third occurrence, then disproved by measurement — codex 0.146.0 accepts Claude's matcher vocabulary (`Edit`, `Write` and `apply_patch` all fire on its patch tool; its shell tool matches as `Bash`), so `bin/sync.py:212` carries no dead names. That gap is a *missing* alternative, not a wrong one, and is carried separately as C49. **What C46 does own here (recorded at D2-19 a):** the emitted matcher is minimal by measurement and therefore **silent on drift** — nothing distinguishes "correct for codex 0.146.0" from "written a year ago", and if a later version unfolds the three aliases into separate routes the hooks stop firing with no signal. Matcher names are vendor names in a cross-vendor contract, so they belong in the same **version-stamped inventory** as tool names: mapped at generation time, unknown name = error at `sync`. D2-19 deliberately did **not** solve it locally (padding the matcher with `Edit|Write` would assert as routes two names never measured as routes). **A12's draft proposes the shape of the remaining tool-name half** ([stage1-hardening-contracts §8](design/stage1-hardening-contracts.md)), so this task remains awaiting owner verification rather than implementation-ready: declare neutral capabilities in `hook_core`'s own vocabulary (`read`/`search`/`shell`/`edit`), map **neutral → vendor at generation time**, keep the harness inventory as **version-stamped data** (harness id + version → available names); a mapped name outside the known inventory is an **error at `sync`**, and a capability the harness has no name for **degrades to `shell` explicitly plus a warn finding** (C51's envelope) naming the widened boundary — because Bash is strictly wider than Grep/Glob, and after the degrade the read-only property rests on the agent body's prose, not on the tool set. **Fork closed: the map is data, not code** (as `AGENT_SPECS` has it today) — a code map carries no version stamp, so nothing distinguishes "correct for 2.1.221" from "written a year ago", and the stamp is the entire point of moving it. **Seeded-violation contract added at A17(d)**, and writing it exposed that the stamp had nothing to compare against: **no code in `bin/`, `tools/` or `hooks/` reads a harness version** — 2.1.221 and 0.146.0 were obtained by hand in N2 — so a written stamp makes staleness *printed*, not *visible*. (i) **Detect when the binary is present:** `sync` queries `claude`/`codex` if either is on PATH and compares against the inventory; if neither is, it proceeds against the declared version and names the one it assumed. Rejected: declaration only (honest but toothless, and §8's visibility claim would have to go) and mandatory detection (`sync` runs where no harness is installed). This is what puts the two optional binaries into C57's runtime declaration. (ii) **An unknown harness version warns and stamps, it does not fail:** generate from the newest known inventory, emit a warn finding, and write the assumption into the **generated artifact's banner** (`generated against claude-code 2.1.221 inventory; detected 2.2.0`) rather than only into a warning that scrolls away. Erroring would brick a consumer on the vendor's release schedule, which akmon cannot preempt — it cannot ship data for a version that does not exist yet; degrading everything to `shell` would dismantle the read-only boundary to preserve a formality. The banner line is C55's surface, recorded as an edge in the order table. (iii) **Seeded against the root cause, not only the symptom:** the defect was never a wrong name but a *second owner*, so a literal vendor tool name in generator code outside the inventory data is a static failure — the analogue of C51 rejecting a local field mapping in an adopter. Seeded violations: a map emitting `Grep` against a 2.1.221 inventory fails at `sync` and `sync --check`; a capability with no vendor name yields the shell name **plus** a warn naming the widened boundary, and **the same degrade without the finding fails the test** — the silent degrade is the seed; an inventory entry with no version stamp fails; a detected version outside the inventory warns and stamps, while generating with no stamp fails; restoring `tools="Read, Grep, Glob, Bash"` into `routing.py` fails the second-owner check. **Red before green:** `routing.py:364`/`:470`/`:503` are **not** repaired ahead of C46 — F9's rule for C54, so the check meets the tree rather than a fixture. After D2-20, runs after C51 and C57 · tools/model_routing/routing.py::AGENT_SPECS + bin/sync.py::_claude_hooks + hooks/claude_adapter.py · provenance: [N2 findings §1.5](reviews/alternatives/n2-stage0-probes-inventory-20260807.md); agent-name instance: N2 stage-0 probe F2 + codex 0.146.0 binary string inspection

- C26 · self-CI drift: model-routing checks/fixture lag C15/ADR-0005 · **fixed** (was promoted to major, review-20260705) · engineer · two checks pre-date the model-routing work and fail `meta/self_ci.py` on a clean tree (surfaced running self-CI during C18, not caused by it): (a) `bin/verify.py::_check_model_routing_registry` requires `selection_policy.reasoner=="highest"`, but ADR 0005 made the reasoner **dynamic** (`registry.reasoner:"orchestrator"`) → false "needs semantic selection_policy" error for anthropic+openai; (b) `meta/self_ci.py`'s fixture copy-list omits `tools/model_routing/second_opinion.py` (added C15/C16) so verify's must-exist check fails on the fixture. Fix: relax the reasoner assertion to the dynamic shape + add `second_opinion.py` (and any peer tool now required) to the fixture copy-list. **Confirmed live (review-20260705):** meta tests 338 green while `verify --strict` and `self_ci.py` both exit 1 — every verify-touching CI leg is deterministically red at this tree state; fix before reading anything into CI history. Root cause generalizes (checker hand-restates registry values — violates "derive from the single source"): prefer structural checks / derive expectations from the policy vocabulary, and add a test running the checker against the *live* registry · bin/verify.py + meta/self_ci.py · [review](reviews/review-20260705.md) finding 2. **Landed (this session):** (a) `_check_model_routing_registry` now accepts `reasoner` in `("orchestrator", "highest")`; (b) `second_opinion.py` added to `self_ci.py`'s fixture copy-list; (c) three new `test_verify.py` cases, including one asserting the checker passes against the *live* registry.json (not just a fixture). `self_ci.py` now exits 0; `verify.py --strict` still exits 1, but only on the separately-tracked **C35** USE-surface finding — unrelated to this item. 345 meta tests passed, ruff clean.

Orchestrator detection + runtime weakness ([ADR 0006](decisions/0006-orchestrator-detection-corridor-context-pressure.md); design [§3/§4.3/§12](design/model-routing.md)):

- C22 · orchestrator detection: corridor + owner-visible warnings · implemented, awaiting owner verify (D2) · engineer · transcript detection + rebind + gitignored `k_*` + corridor warning in `compute_binding` (named floor `opus`, below-floor + reserved-top arms, silent collapse when `floor == top`, skip when floor not in ladder) + dual-channel owner-addressed output (`systemMessage`); e2e-verified on a live session · [design §3+§4.3](design/model-routing.md)
- C23 · context-pressure detection · implemented, awaiting owner verify (D2) · engineer · registry `context_pressure` block; the model-routing hook reads last main-chain `usage` in the same transcript pass, banded warnings (0.85/0.95) throttled per band via session marker, reset on compaction, dual-channel delivery; tested incl. live transcript (74% → silent) · [design §12](design/model-routing.md)
- C27 · plan-gate: delegation named up front in design-flow plan-check · active · engineer · complement to the delegation-nudge enforcement (D2-8): extend the existing `design-flow.md` pre-fan-out plan-check gate yardstick so a realization/decomposition plan must **name a delegate per delegable sub-step** before work starts — makes "delegation up front" auditable at a gate, not only advisory at runtime (the hook catches drift *during* work; the gate catches an under-delegated *plan*). The always-loaded guardrail keeps only a pointer clause, not a second rule owner · [design §9.4+§10](design/model-routing.md) · provenance: D2-8 delegation-enforcement audit (this session)
- C28 · verify `ask`/`deny` PreToolUse contract per vendor + count-axis tuning · in-progress · engineer · **(d) LANDED** (subagent exemption via `agent_id`; hook_core+wrapper+tests, 338 passed; D2-9; motivation in model-routing.md §13). **(a) LIVE-VERIFIED** (this session, D2-10): debug-instrumented live test confirmed the hook dispatches identically for a real `Read` and a real `Bash` call — no tool-category exemption. Surfaced an unplanned, bigger gap — the emitted `ask` was not enforced/blocked for *either* tool in this background/child session; spun off as **C31**. Remaining: (b) codex runtime-enforcement check (wire shape now schema-verified — see below), (c) count-axis tuning (defer to C29 output-weight axis). · the graduated delegation nudge (D2-8) emits a hard `ask` on sustained drift; gates before the ask arm is trusted: (b) **codex vendor — wire shape SCHEMA-VERIFIED (this session)**: the installed `codex` binary (0.142.5) embeds its own JSON schema for hook I/O (extracted via `strings`/binary search, not docs); `pre-tool-use.command.output.hookSpecificOutput.permissionDecision` enum is exactly `allow|deny|ask`, matching what `codex_adapter.py`/`hook_core.py` already emit — no adapter change needed for the shape. Codex also exposes a separate `PermissionRequest` hook event (own `behavior: allow|deny` contract, `interrupt`/`updatedInput`/`updatedPermissions` explicitly reserved/fail-closed) we don't touch yet — noted, not required. **Measured but still incomplete:** N2 proved hard `deny` only on the headless `apply_patch` route, while C49 separately proved that the shell matcher is `Bash`; that does not yet verify the exact git-guard behavior. Before wiring `codex-git-commit-guard`, live-probe one real `Bash` payload for each normal result class (`deny`, interactive `ask`, unattended ask→deny), record whether Codex supplies `permission_mode`, and compare headless with TUI. Keep the mode available for fixtures until that exact payload/enforcement matrix is measured; no separate C61 duplicates this owner. (c) **count-axis false-positive tuning (D2-8 item 2), on the right axis** — the residual noise is **not** read-vs-write: a read-only `git diff`/`git log` across many files is the *most* expensive thing to pull inline and **should** trip the counter (delegating it to k_explorer keeps the dump out of orchestrator context); only *tiny-output* commands (`git status`, `rev-parse`, `ls`) are genuine inline-justified noise where a subagent round-trip costs more than the command. Do **not** discount read-only shell wholesale (brittle: `git status && rm -rf` isn't read-only); if the count axis over-fires, prefer the **output-weight axis (C29)** over heuristic bash-string classification; (d) **subagent false-fire — correctness defect, fix FIRST (owner: keep-and-harden the ask)**: `delegation_nudge_result` is session-keyed with no main-chain/subagent discrimination, so the nudge fires on read-only leaf subagents (k_auditor/k_explorer/k_reasoner) that have no `Task` tool — advisory wastes their context, the hard `ask` is un-actionable or blocks legit reads (demonstrated live: the C29-design audit tripped it at call #10). Suppress on subagent sessions (or count main-chain turns only); resolve whether a subagent shares the parent `session_id` (double-charge risk). **Owner decision: keep the hard ask, but only after (a) runtime-verify + (d) subagent fix land — accept fatigue risk & monitor.** Verify (b), fix pending; encode the vendor contract in the adapter · hooks/claude_adapter.py + hooks/codex_adapter.py · provenance: D2-8 + auditor "could not verify" + live subagent misfire (this session)
- C31 · background/child-session `ask` enforcement gap · **code+tests complete & D2-pending (D2-11)** · engineer · owner decided the posture without a foreground repro first (skip diagnosis, harden and observe): `hook_core.py::_escalate_unattended_ask` escalates any `ask` from `git_commit_guard_result`/`delegation_nudge_result` to a hard `deny` whenever `permission_mode` isn't the interactive `default` (field missing = worst case, escalates too); both Claude wrappers thread `permission_mode` from the PreToolUse payload. Codex not wired to either hook yet, so out of scope. Owner now watches live behaviour (false-block fatigue vs. real gap closed) rather than gating on further isolation of background-vs-`acceptEdits` · [D2-11](D2_LEDGER.md) + model-routing.md §13.2 · provenance: D2-10 + owner decision (this session) · was **ranked #1 in [review-20260705](reviews/review-20260705.md)** (D5's commit guard rides the same channel; the audit session itself ran background, i.e. inside the gap)
- C29 · second axis: inline-output-weight nudge (token-proxy), advisory · **owner-approved: build (advisory)** · engineer · axis 1 (D2-8) counts delegable *calls* without a delegation (behavioural drift); add **axis 2** counting *cumulative raw tool-output pulled into orchestrator context* without a delegation — the metric that maps to **context cost in both senses**: token budget (measurable, load-bearing) *and* reasoning quality (raw dumps are low-signal noise that dilutes attention / buries detail as the window grows — context-rot / lost-in-the-middle; a subagent returns distilled signal, not the dump; one 5k-tok `git diff` inline ≫ fifteen tiny `git status`). Measure output as a **byte/char proxy** (no tokenizer dep, akmon pure-stdlib) of `tool_response` for `{Read,Grep,Glob,Bash}`, accumulate per session, **reset on `Task`/`Agent`**. **Measurement needs `PostToolUse`** (output doesn't exist at PreToolUse) → new PostToolUse entrypoint wired in `bin/sync.py::_claude_hooks` + `.claude/settings.json`. **Delivery — advisory, dual-channel (owner resolved the fork → advisory, NOT armed-ask):** `additional_context` nudges the model to route heavy inspection to k_explorer; a parallel **owner-visible `system_message`** reports that a quality/token recommendation fired **and states the owner may opt to escalate it to a hard `ask`** — teeth stay owner-controlled, off the `git-commit-guard` modal channel (auditor: armed-ask contradicts design §12.4 "nag learned to ignore", misfires on the innocent next call, and shares the security-ask channel → dropped). **Context-pressure coefficient — ≥4 steps (owner):** weight the axis-2 increment by a rising step-multiplier over `fill_ratio` with **at least 4 steps** (not C23's 2 warning-bands) — e.g. ~0.5/0.7/0.85/0.95 → ×1/×2/×4/×8 — so the same inline dump costs more as the window fills (token headroom shrinks near the limit; low-signal dumps also plausibly dilute attention as context grows — qualitative reinforcement, not a second quantified ground; drop "convex on two grounds"). Reuse **C23**'s `fill_ratio` source (last main-chain `usage` in transcript); axis-2 owns its own ≥4-step multiplier ladder. **NB — auditor's "problem in remission" rebutted (owner):** the healthy delegation log is owner-driven (audits/delegations are the owner's manual calls), so autonomous self-delegation is still weak → axis-2 is warranted, not premature. Watch (advisory-only keeps these nudges, not blocks): byte-count conflates big-with-noisy — a legit large Read of a doc the orchestrator must adopt trips identically; byte→token ratio not stable across Read/Grep/Bash; per-call transcript re-parse for `fill_ratio` is I/O cost ("code cost, not token cost") · hooks/hook_core.py + new PostToolUse entrypoint · provenance: owner proposal + audit (this session), extends D2-8
- C30 · reasoner model laddering (one rung above orchestrator, capped below auditor) · active · engineer · replace the current dynamic `reasoner = orchestrator` (ADR 0005) with: **reasoner = one rung above the orchestrator, floored at the orchestrator (never below) and capped strictly below the auditor's rung**; **auditor = highest available** (fable if available, else opus). Ladder high→low: fable · opus · sonnet · haiku. Rule: `reasoner = max(orchestrator, min(one_above(orchestrator), second_highest_available))`; `auditor = highest_available`. Cases: orch=opus → reasoner=opus (one-above=fable is the auditor-max → capped to opus), auditor=fable(else opus); orch=sonnet → reasoner=opus, auditor=fable(else opus); orch=fable → reasoner=fable, auditor=fable; orch=haiku → reasoner=sonnet. Touches `registry.json` `selection_policy` (reasoner+auditor) + `routing.py` resolution + regenerated agent frontmatter + `bin/verify.py` reasoner assertion (currently expects the C26 dynamic shape — update together) + `MODEL.md` tier table. **Verify `fable` is in the vendor's `available` set before pinning auditor to it** (else fall back to opus) · registry.json + routing.py · provenance: owner spec (this session)
- A8 · routing defaults: reasoner re-checks ordinary tasks more often, auditor on every design · active · architect · two owner-wanted shifts to *when* verification fires (not just *which* model, C30): (1) **reasoner more often** — an `independent-review` re-check pass becomes a near-default for ordinary implement/analysis tasks, not only an on-signal escalation (raise the default; keep it advisory/draft — reasoner never decides); (2) **auditor on all designs** — every design-flow / architecture task routes a clean-context `audit` at its gate, making it **mandatory-for-designs** rather than only ≥2-zone-triggered. Encode via `registry.json` `gate_triggers` + role/pipeline anchors (design-flow, review-flow) + MODEL.md routing defaults; relate to **C25** (missed-gate forcing function) — this widens *which* gates always fire. Watch cost/fatigue: more reasoner/auditor passes = more spend + the alarm-fatigue risk the D2-8 audit flagged; scope "ordinary task" so the reasoner default doesn't fire on trivial edits · registry.json gate_triggers + pipelines · provenance: owner request (this session)
- C36 · hook runtime hardening · active · engineer · (a) `/tmp` session markers race (TOCTOU on create) and **outlive sessions** — a stale counter from a crashed/reused session_id false-fires or wrongly suppresses the nudge; add lifecycle handling (age-out or session-start reset). **The marker population grew with C50's suppressed-rebind throttle** (`routing.suppressed_rebind_warning`, `akmon-suppressed-rebind-<sha256(session)[:20]>` in the tempdir): it is content-keyed, so a stale one suppresses a *real* refused-rebind warning rather than merely miscounting, and it is the one member of the class with no `.marker` suffix and a hashed rather than literal session id — the hashing is the defensible half (a session id containing `/` would otherwise escape the directory) and should become the convention for all of them, not stay the exception. **C49's marker is the exemplar to copy, not another copy** (`akmon-codex-shell-route-<sha256(session)[:20]>`, written by `codex-hook._first_time_for_identity`): after the C49 review it claims the marker with `os.open(O_CREAT|O_EXCL, 0o600)` — the TOCTOU half of (a) already solved in one member of the population, and solved where it matters most, since three separately launched hook processes race on the *same* file per Bash call — and it fails **visible**: an unwritable tempdir repeats the diagnostic instead of swallowing it. The sweep is therefore not "collapse four copies of one 8-line idiom onto a helper" but **lift that member into the helper and migrate the other three onto it**, then add the lifecycle it still lacks (the one part of (a) C49 did not touch). **The lift happened at D2-19(e)**, as a side effect of making the shell-route diagnostic vendor-neutral: the idiom now lives in `hook_core.claim_diagnostic_marker` (atomic, `0o600`, hashed name, repeat-when-unidentified), the Codex diagnostics call it with `codex-`-prefixed kinds, and Claude's `Bash` route creates a marker of its own through `git-commit-guard.py`. So the exemplar is already the helper, and **what is left here is the migration and the lifecycle**, resurveyed at D2-21(c): move `akmon-role-on-code-*`, `akmon-analysis-guard-*`, `akmon-d2-ledger-*` and `akmon-suppressed-rebind-*` onto it; drop the literal-session-id names and `0644`; make the missing-identity policy one policy (those three still key on the literal `nosession` and therefore **suppress across sessions** — fail-quiet, the wrong direction); add the age-out. Two consequences to keep in view: the three advisory markers also claim **non-atomically** (`exists()` then `write_text`), so two parallel edit calls in one session can emit one reminder twice — loud direction, removed by the migration; and **D2-22's acceptance of best-effort switch-time delivery expires when this lands** — after the migration and age-out, exactly-once per episode becomes the contract rather than a residual. **The population also now disagrees on a missing session id**: `codex-hook` and `routing.suppressed_rebind_warning` *repeat* rather than share a `nosession` marker (a global one would let a single id-less session silence every other one), while hook_core's three (`akmon-role-on-code-<sid|nosession>.marker`, `akmon-analysis-guard-…`, `akmon-d2-ledger-…`) still key on the literal string `nosession` and therefore *suppress* across sessions. One policy has to win — repeat-when-unidentified is the fail-visible one — and the same edit closes the leftover naming split: literal session id in the filename (the `/`-escape the hashing exists to prevent), the `.marker` suffix, and 0644 vs 0600; (b) untested load-bearing paths: `model-routing.py` `main()` (exception fallback, malformed-transcript JSONL), the delegation-log write path — add tests; (c) **owner-locked diagnostic loudness:** malformed edit payloads repeat per bad tool event but deduplicate across the three separately launched path-advisory handlers by a reliable `tool_use_id`; with no id or a marker failure they repeat fail-visible. A once-per-session alarm is rejected because it could hide continuing protocol drift. **Review follow-up:** the transitional `_first_time_this_session` alias over `_first_time_for_identity` is removed, so the claim helper has one name again — two names for the same eight lines would have left (a)'s sweep with two migration targets and a pointer in this task that resolved to a wrapper rather than to the atomic claim it names as the exemplar. Commit-guard `$(git push)` regex bypass stays C1 (deferred, unchanged) · [review](reviews/review-20260705.md) finding 9 + C49/C50 follow-up review (this session)
  **Substatus:** C36(c) is code-complete and D2-21-pending; C36(a)/(b) remain active.
- A9 · tool-layer LLM-agnosticism scope · active · architect · the agnosticism claim holds for the data layer (semantic policies, no committed model names) but not operations: hardcoded `.claude/` paths (routing.py:28–30, stats.py), Anthropic-only usage endpoint (stats.py:32), Claude transcript/`agent-*.jsonl` assumptions, inconsistent vendor defaults (init.py: anthropic vs second_opinion.py:73: openai), report_dir vendor-flip, **Gemini absent from the registry** despite the GEMINI.md pointer. Decide the operational multi-vendor contract in an ADR: what "LLM-agnostic" promises at the tool layer (full path/endpoint abstraction vs explicit first-class-Anthropic + documented degradation), then align code; relates C28 (b) codex runtime enforcement · [review](reviews/review-20260705.md) finding 8

Keystone identity (rename):

- V1 · mythological rename `keystone`→`akmon`, `_forge`→`_aitna`, `k-`=Kyklōpes · pending (owner-timed; **names locked**) · release+architect · one myth cluster (Hephaestus's forge beneath Etna) replaces the mixed smithy/masonry metaphor — the *akmon* (anvil / standard) mounted inside each project's *aitna* (volcano-forge / dev workspace), worked by the *Kyklōpes* subagents; supersedes the earlier English `ai_anvil`; **`k-` prefix does NOT migrate** (pure re-reading, orthogonal to V2's suffix change). Two subjects, one migration release — **keystone:** repo `ai_keystone`→`ai_akmon`, mount `<AITNA_ROOT>/keystone`→`<AITNA_ROOT>/akmon`, `.keystone.toml`→`.akmon.toml`, hooks/tools path derivation, doc sweep (older ADRs grandfathered), consumer pin bump; **alphavar (parent consumer):** `git mv _forge→_aitna` (moves `_forge/keystone`→`_aitna/akmon`), rename the `_forge` Python package + `_forge.*` imports, sweep `README`/`AGENTS`/`CLAUDE`/`_forge/README` + `_forge/**` refs + `.gitignore`/hook path derivations + the "read `_forge/memory/`" prime-directive path. Cheapest now (alphavar sole consumer); sequence after the C15–C18 wave · [ADR 0008](decisions/0008-mythological-naming-aitna-akmon-kyklopes.md) · **propagation incomplete — priority raised ([review-20260705](reviews/review-20260705.md) finding 4):** mixed naming already breaks user-visible state — consumer memory index links `akmon-*` while files on disk are `keystone-*` (broken links `_aitna/memory/README.md:23,25`; 5 files missing from index per `verify --strict`), akmon README `develop/`→`meta/` links (fixed in the onboarding-docs pass — CHANGELOG Unreleased/Fixed), `ROADMAP.md:21` says "Two roles" vs five, `KEYSTONE_*` env prefixes (hook_core.py:467,478); 77 remnant lines across 26 files (exact count); every interim doc edit propagates remnants — fold these into the sweep checklist
- V2 · rename synthesizer → auditor · code+docs complete & D2-pending (D2-7) · architect+release · "synthesizer" doesn't convey the essence (a clean-context **audit** of a whole gate's material), and "review" is already the DEVELOP analysis role — owner picked "аудит". Name trio (owner-locked): agent `k-synthesizer`→`k-auditor`, task-kind `synthesis-verify`→`audit`, tier `synthesizer`→`auditor`. Scope: `registry.json` (agent def, `role_task_kinds` keys, tier→model binding, `gate_triggers`/policy refs), `routing.py`, `gate_pack.py` (ROLE + k-synthesizer refs), generated `agents/*.md`, design §9–11, `MODEL.md`, ADR 0004/0005, D2 ledger entries. Cheap now (alphavar sole consumer); sequence after the C16/C17/C20→C18 wave to avoid double doc-churn; fold in the C18 engineer-row fix
- V3 · restore the missing v0.3.0 changelog boundary · code-complete, awaiting owner review · release+engineer · tag `v0.3.0` exists at `69ba3b0`, but both its snapshot and the live CHANGELOG jumped from `## Unreleased` to `## v0.2.1`; consumers therefore could not compute the released v0.3 delta from headings. **Delivered:** `## v0.3.0` now contains byte-for-byte the tag snapshot's former Unreleased payload, cross-checked against `v0.2.1..v0.3.0`; live Unreleased contains only post-tag work, and the post-tag C9 line was removed from the historical v0.2.1 section. `1a9ab0f..` was not used as the release range: `1a9ab0f` is a child after the tag and already carries next-cycle C39/package fixes. This historical repair precedes C54. **Both open ends are now closed by owner choice F9:** the current-state choice is **`0.4.0.dev0` + `## Unreleased`**, executed inside C54 so the live pair stays P1.3's acceptance evidence; and the history-completeness check **is in scope** — every tag `vX.Y.Z` should have a `## vX.Y.Z` heading — as a **warn**, because tags are immutable and a hard error over a gap that can never be repaired would simply be waived. A check of that shape would have caught what V3 repaired by hand · CHANGELOG.md + git tag/history · provenance: release-hygiene review,

Role-triad + develop/use split ([ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)):

- C35 · USE-surface self-containment sweep (akmon `skills/`) · fixed · engineer · `skills/stats-digest/SKILL.md:11` cites `../../meta/design/model-routing.md` — a consumer-facing skill pointing into the dev tree, violating the ADR 0003 cordon (`verify --strict` errors on exactly this); `skills/release/SKILL.md` is the same surface and was checked by no audit zone. Replace dev-tree links with self-contained text or MODEL.md anchors across `skills/*`, re-run verify to green; also decide the cordon boundary for the new `examples/` dir (verify checks only `skills/` — `examples/gate-anatomy.md` carries one provenance link into `meta/reviews/`, same class as README's meta links: rule on whether that's in or out and teach verify accordingly) · [review](reviews/review-20260705.md) finding 7. **Landed (this session):** (a) rewrote stats-digest/SKILL.md:10-15 to drop the meta/design/ link, self-contained now; (b) `skills/release/SKILL.md` re-checked — already compliant, no change needed; (c) decided the `examples/` cordon boundary explicitly: same bridge class as README/CHANGELOG (maintainer-facing worked examples, not operative consumer guidance), documented in verify.py's USE-surface comment rather than left an accidental globs gap; (d) two new tests: one runs `check_use_surface_isolation()` against the live tree (the actual regression), one locks in the examples/ exemption. `verify --strict` still exits 1, but only on the separately-tracked V1 mixed keystone/akmon memory-index warning — unrelated to this item. 350 meta tests passed, ruff clean.
- C6 · UserPromptSubmit role-confirm for sub-agents · deferred · verify Claude Code hook behaviour first · [ADR 0003](decisions/0003-role-triad-and-develop-use-separation.md)
- C9 · release_check.py akmon-subject scoping · fixed · engineer · `--check --subject akmon` now runs upstream gates from the akmon source root (`meta/self_ci.py` plus meta tests) instead of applying consumer-only `bin/sync.py` and `bin/verify.py` to either a consumer or the upstream checkout; release-check runner resolution remains covered · tools/release/release_check.py + meta/tests/test_release_check.py
- C2 · release tool: pin-bump subject · deferred · the 3rd release subject (akmon pin bump recorded in a consuming project); only after akmon+package subjects settle · [design](design/release-versioning.md)
- A3 · cross-agent contract v2 · deferred · skill/role inventory beyond thin pointers · [design](design/cross-agent-contract-v2.md)
- C1 · harden git-commit-guard parsing · deferred · close regex bypasses only if a real one bites
- A1 · orchestration / role handoffs · deferred · until role routing causes real friction (4 roles now; release routes) · [ROADMAP O4](ROADMAP.md)
- A2 · OPERATE mode · deferred · keep as design note until a runtime actor exists · [ROADMAP O1](ROADMAP.md)

Distribution / adoption:

- C37 · `akmon` pip package: init/sync/verify console CLI · active (A10 locked) · engineer · pyproject + src/akmon thin CLI; `init` mechanizes BOOTSTRAP's mechanical steps (mount + layout + `.akmon.toml` + sync + routing init); launchers exec the mounted tree per the skew rule; **mode `package`**: mount-decoupled tree resolution, `<aitna>/.akmon/` hooks+guardrails materialization, `akmon path`, `mount` key in `.akmon.toml` — pilot consumer alphavar (git+https dev-group pin); self_ci package leg; README quick-start cut-over; publish step in the release pipeline (owner runs publish, D5). **Sequencing: first publish only after the V1 rename sweep** (the git+https pilot is not a publish) · [design](design/packaging-uvx-init.md) · [ADR 0009](decisions/0009-packaging-package-carrier-and-mount-modes.md)
- C39 · Codex runtime contract + package hardening for v0.4 · blocked (code complete; owner D2-13 verification pending) · architect→engineer · direct delegation anchor in the hand-owned AGENTS block + verifier gate; package-aware hook root/runtime dependencies; Codex SessionStart orchestration policy; self-hosted akmon AGENTS; honest capability matrix and package/release docs; installed-wheel/nested-cwd regression coverage; development version 0.4.0.dev0. Hard enforcement, named agents, and child-model routing stay unsupported until live Codex protocol probes prove them · [design](design/codex-runtime-contract.md) · D2-13
- N1 · vendor support matrix — verify & fill the Claude/Codex cells · active · every unknown Claude/Codex cell checked by experiment against the real harness, not vendor docs. **F4 timeout mechanics:** use disposable scratch wiring and a sentinel literal to probe every candidate Codex command entry; record exact build/version, invocation/config source, event/matcher/entry, literal/units, no-timeout control, before/after markers, expiry elapsed/tolerance, termination exit/signal, captured stdout/stderr and tool-call outcome. `Supported` requires observed timely termination, not parse acceptance; `unsupported` requires observed rejection or ignored field; `unmeasured` is not terminal. **F5 production budgets, after F6 bounds inputs:** map every generated Claude and F4-supported Codex command entry to exactly one dominant-workload class (payload/marker, project/config scan, git subprocess, transcript/routing I/O); run actual processes repeatedly on the largest F6-supported fixture corpus; record raw cold/warm timings, environment, fixture sizes, class rationale, formula/factor/rounding/floor, literal/units, false-timeout risk and hung-hook upper bound. Live-test individual entries and aggregate event groups at proposed literals. The F4 sentinel is not a production budget. Owner verifies the exact class/literal table before C52; later vendor versions or material runtime changes invalidate affected evidence · [hooks/README](../hooks/README.md) caveat inherited by Codex warning rows; stale evidence is an honesty bug
  - **F6 evidence gate (owner choice C):** inventory every consumer-growing axis per generated command entry — raw stdin bytes plus JSON depth/items/string bytes; command/description bytes; patch path count and per-path bytes/depth; transcript total/line bytes and matching records; D2 ledger bytes/rows; D2 config bytes and glob count/length/depth/`**` complexity; registry/overlay/local config/settings bytes/depth/items; briefs and generated output; agent roster/name/output; generated-agent directory/per-file/total bytes and rebind/prune population; marker/counter bytes. Propose exact cap/unit and combined supported corpus; owner verifies every cap and mapping before F5 timing or C52. The complete F4 → F6 → F5 evidence packet closes D2-23, which gates only C52. Over-cap is a separate measured `input envelope exceeded → fail-open` outcome, not crash or timeout. Normal deny claims are valid only inside the supported envelope; D2 owner verification explicitly accepts the payload-padding bypass
  - **Payload gap assigned at D2-18 verification:** the apply_patch **rename** form is unmeasured. `codex_adapter._PATCH_PATH_RE` reads `*** Add|Update|Delete File:` only, and `*** Move to:` occurs nowhere in this repository, so a renamed file would be classified under its source path and its destination not at all — C48's own silent shape, one form over. Probe codex 0.146.0 with a rename patch: capture the literal body, the `tool_input` keys, and whether the destination path arrives at all. Until it is measured no pattern is added, because guessing the form is precisely what D2-18(a) rejects. Gates nothing; feeds the C48 successor work and C52's payload envelope
- N4 · probe: is dispatch **completion** observable, per vendor · open · analyst · spawned by owner choice **F10** when C58 was narrowed to dispatch-only; the completion half is not deferred, it is unmeasured, and this is where it gets measured. Three unknowns, in order, because the second and third are meaningless without the first: (1) **is there a completion signal at all** on each vendor — an event, an exit payload, anything a hook can observe when a dispatched agent finishes, on the exact route (vendor/version/event/matcher) rather than in principle; on Codex the starting position is N2's finding that the SessionStart channel is parent-only, so `delivered: no` is a permitted and likely outcome; (2) **what a completion line may claim** — only fields the signal actually carries, no inferred success/failure; (3) **the correlation key** back to the dispatch line, and whether it survives a subagent that shares the parent `session_id` (the same ambiguity C28(d) hit). Output is a matrix cell per vendor plus, if and only if a signal exists, the line schema C58 would extend. **Not a design item:** nothing here is decided by choosing — it is measured or it is declared absent · provenance: F10, [§9](design/stage1-hardening-contracts.md)
- N3 · vendor support matrix — Gemini/Copilot cells · deferred · demand-driven: fill by experiment when a consumer asks; declared-only until then (gates nothing — A15 gates on the N1 Claude/Codex set)

Alternatives adoption ([plan](reviews/alternatives/plan-akmon-from-alternatives-20260718.md) —
weaknesses W1–W24 mined from ponytail, Superpowers, OpenSpec, wshobson/agents; reviews in
[`meta/reviews/alternatives/`](reviews/alternatives/)). Direction accepted: the A11 walkthrough
closed all 7 themes and decisions D1–D4 — [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md);
per-theme rationale, amendments and pilot acceptance criteria live in the
[walkthrough record](reviews/alternatives/walkthrough-a11-20260717.md). Process rule: **every
stage is design-first** — the A-task locks the contract, only then its C-task implements.
A-task design sessions run in the host's plan mode where available (Claude Code: the
owner-approved plan file is promoted into `meta/design/`, not left vendor-side); in agent-team
("tech lead") execution the lead's approval of each teammate's plan enforces the same rule per
dispatch (plan P2.8 / W24). The plan file stays the why-record (weakness→source mapping);
execution state lives here:

- N2 · stage 0 — vendor delivery probes & inventory (plan P0.1, P0.3 + the as-is half of P0.2/P0.4) · **done (both arms)** · prove real delivery paths (Claude → Codex, D4 built-ins arm); invariant + boundary/volume inventory; findings only · [rationale: themes 2, 6](reviews/alternatives/walkthrough-a11-20260717.md) · **findings: [n2-stage0-probes-inventory-20260807.md](reviews/alternatives/n2-stage0-probes-inventory-20260807.md)** — live probes on the attached consumer (alphavar, Claude Code 2.1.221, $0.688): guardrail delivery to built-ins is **type-dependent** (`Explore` gets zero akmon context, `general-purpose` gets the full chain); the `k-*` model pin holds against an opus session while built-ins carry **no pin** (they inherit the session model both directions) — so the tier floor is unenforceable on built-ins, which is the D4 economics answer; the delegation log **does** observe built-in dispatch but records no model for it (`bound_model_for` is registry-only), so coverage-map/stats undercount it; the SessionStart roster reaches **no** subagent, `k-explorer` included. P0.3: 5 invariant pairs (3 declared + 2 undeclared) + 1 orphan hook (`role_on_code_result`) — P1.2 must cover 5, not 3. P0.4: always-loaded chain measured at 433 lines / 23.6 KB (AGENTS.md 232 lines is hand-owned and the largest single item). **Spawns C46** (stale tool declarations). **Codex arm (codex-cli 0.146.0, [findings §1A](reviews/alternatives/n2-stage0-probes-inventory-20260807.md)):** Codex does **not** expand `@`-imports in `AGENTS.md` — the always-on guardrails (`_common.md`, `python.md`) never load in any Codex session, parent or child, so C39/D2-13 is now measured rather than assumed and the gap's shape is "the import mechanism is Claude-only", not "guardrails arrive weakened"; a delegate receives `AGENTS.md` through its own `world_state` but receives **nothing akmon injects at SessionStart** (`fork_turns="none"` → `[akmon]` ABSENT; the `fork_turns="all"` run only copied the parent's turns, same message ids), so akmon's dynamic channel is parent-only; the **model pin holds** (`thread_settings_applied {"model":"gpt-5.6-terra"}` + child `turn_context`, while the child self-reported "GPT-5" — enforced by the harness, invisible to the agent) and `agent_name` is a real addressable identity (`author:"/root"` → `recipient:"/root/k_explorer"`), which is what forced [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md); the advisory surface is inert for two independent reasons (**C48** payload key, **C47** relative paths); and a hard `deny` **is** enforced but only on the route its matcher names — the model re-issued the denied `apply_patch` through `exec_command` in the same turn, unprompted, and it went through, **spawning C49**. Method note carried into N1: agent self-reports about the harness are unreliable (two runs void on wrong workspace root, one wrong model self-report) — read codex's rollout files instead. Still unmeasured: `ask` as distinct from hard `deny`, TUI vs headless equivalence, and whether any subagent-start injection point exists on Codex (the Codex half of P4.1)

> **A12 gate override:** A12 is an awaiting-owner design draft, not a lock. D2-20 remains
> pending; C40, the remaining C46 tool-name work, and C51–C60 are not runnable. Inline
> `locked`, `runnable now`, and `Fork closed` wording below describes the proposal being
> walked through and has no gate effect until the owner choices are recorded and D2-20 is verified.
> The closure sequence is carried by **A17**.
- A17 · A12 pre-lock closure — finish the walkthrough, split D2-20, repair the draft's internal contradictions · **active** · architect · one carrier for everything that must land before any stage-1 shape becomes runnable; A12 stays awaiting-owner until every subitem closes. Ordered on purpose: (b) feeds (c), and (c) feeds (g); (f) runs before (g) too, because an audit over a document that contradicts itself in six places returns those six and proves nothing else. **Remaining: (g) alone** — the coherence audit, the two ADRs, then owner verification of D2-20. The audit wants a tree that cannot move, so the approved wave has to be committed first; that step is the owner's
 - **(a) current wave first** — owner-approve D2-15…D2-22 before the A12 coherence audit, then land the approved wave before the final clean-context audit. A pending point cannot carry a landing sha, and an audit run against a tree that can still move proves nothing. **D2-15 ✅ owner-verified** (clauses 1–3; sha pending on the wave commit) — clause 3 closed a fork the row had not stated: per-agent stats stay split at the rename boundary, and folding `-`/`_` in `stats.py::label_and_tier` the way `resolve_briefs` does is **rejected**, because a brief key addresses a live agent while a log label records what ran ([ADR 0011 Consequences](decisions/0011-agent-name-notation-k-underscore.md)). **D2-16 ✅ owner-verified** (clauses a–c) — clause (b) widened to the tolerance line the code already held: `agent_key` folds surrounding whitespace as well as case and `-`/`_`, an internal space stays significant, and the rule is now pinned by `test_overlay_brief_key_tolerance_covers_whitespace_but_not_spelling`. **D2-17 ✅ owner-verified** (clauses a–d) — two wordings tightened against what the code holds: the symlink boundary is recorded as an **advisory-grade classification boundary, not containment** (a link out of the repo stays lexically inside the root), explicitly not inheritable as a security property by deny-class work in F3/C52; and the "never against the hook process cwd" claim is narrowed to "once a root is known". **D2-18 ✅ owner-verified** (clauses a–c) — (a) extended per owner choice iii: the measured-only tolerance line now covers the nine guessed *path* keys too, which stay but no longer pass as measured fact (`unmeasured_path_source` + a stderr provenance line, advisories still run); the unmeasured `*** Move to:` rename form is recorded as an N1 probe item rather than guessed. **D2-19 ✅ owner-verified** (clauses a–e) — three statements the row was missing are now in it (matcher drift is silent and its fix belongs to C46; every unrecognized `apply_patch` form degrades to the *reported* shell route, never to silence; route-level diagnostics throttle by session while event-level ones throttle by event), and clause (e) was widened per owner choice ii: the unclassified-shell-route diagnostic is now vendor-neutral in `hook_core` and fires on **Claude** too, where a `Bash` write had been unreported entirely. **D2-21 ✅ owner-verified** (clauses a–c) — all three extended: (a) is now half of one stated policy with D2-19(c) and governs three diagnostics, not one; (b) narrowed per owner choice i to *this* marker, with the real purpose stated (a session id containing `/` must not escape the tempdir; secrecy is not the argument while three siblings carry literal ids) and the convention gap left with C36(a); (c) resurveyed C36(a) at the gate — the "lift the exemplar into a helper" half landed at D2-19(e), so migration, the `nosession` policy split and the age-out are what remain. **D2-22 ✅ owner-verified** (single clause, amended three ways) — the suppression window is now stated exactly (reused/surviving session id **and** same model **and** same warning text), the two loud channels are recorded as the *reason* fail-quiet is tolerable here rather than as a neighbouring fact, and the acceptance is given an expiry: it ends with C36(a). Task-status sweep (owner-approved): C47, C48, C50 and C49 first moved to **blocked** while their gates sat in Approved — a task whose gate has passed must not keep advertising itself as pending, or the audit in (g) reads the wave as unfinished — and moved again to **done** once the wave landed. **The wave landed at `f6d181c` and the ledger is closed behind it:** D2-16…D2-19, D2-21, D2-22 and D2-24 are Verified at that sha, D2-15 at `d4e5246` — where the rename it locks actually landed, not the wave commit that amended its ADR — and the three older Approved rows that had been carrying `landing sha pending` were closed at their own commits (D2-14 `da51329`, D2-12 `556927e`, D2-7 `7312972`). The `## Approved` section is now empty. C47, C48, C49 and C50 moved from `blocked` to `done` and are archived, their clause-level record kept in the Verified rows rather than duplicated in the task text. D2-20 is deliberately last: it remains a proposal rather than a lock; A17(c) has now separated its architecture boundary from D2-23. A17(b), A17(c), A17(d) and A17(f) are now closed; only A17(g) remains, and its audit needs this wave committed first
  - **(b) finish the register** ([stage1-hardening-contracts](design/stage1-hardening-contracts.md)) — **F8**: owner-lock the exhaustive seven-callable table (six exact policy IDs plus `session_start_result` as operational with a non-empty rationale) before C53. **F9**: version/changelog consistent-state repair, entangled with the live `pyproject 0.4.0` / `## Unreleased` / immutable `v0.3.0` state and therefore with V3. **F10+**: expand the placeholder row into named forks or declare it empty — a bucket whose contents cannot be counted cannot be closed. **F8 ✅ decided** ([§3](design/stage1-hardening-contracts.md)) — the seven-callable table is locked, and it forced four sub-decisions the fork had not named: `role.declaration` gets **new prose in `guardrails/_common.md`** rather than an operational exemption (the P0.3 orphan finally gets an owner, at an estimated +1…+2 always-loaded lines once three legacy `> **Enforced**` blocks are replaced by six markers — C56 confirms); `session_start_result` stays **operational**, but its rationale must record that on Codex it is the *only* delivery channel for the delegation rule, so the classification is not read as "incidental"; `verify.owner-verify-d2` **declares that its coverage is the consumer's** (`[d2_ledger] sensitive_paths`), with a future `init` default seeding it narrow, and erroring on the unconfigured state rejected against ADR 0007 §4's warn-first decision; and two mechanics are locked — the marker grammar (`Runtime check: <id>`, ID first token, ` — ` tail is human prose the parser ignores, needed because three of six markers claim a *subset*) and the statement that policy IDs and F1 finding codes are **separate namespaces** despite the identical shape. **F9 ✅ decided** ([§4](design/stage1-hardening-contracts.md)) — the packet found that the fork was mis-titled: not "pick a consistent pair" but "four version carriers with no join between any two of them", and that **the defect already shipped** — the tree at tag `v0.3.0` carries `version = "0.3.0.dev0"`, and `1a9ab0f` ("v0.3.0 package version") then wrote `0.4.0`, so the literal `0.3.0` never existed in the tree. Owner choices: **(1) `0.4.0.dev0` + `## Unreleased`** over cutting a `## v0.4.0` heading — a released heading with no tag names a release that does not exist, against the changelog's own "the git tag is the timeline" — **repaired inside C54, not before it**, because P1.3's acceptance is the live pair failing then passing and an early repair downgrades the tree to a fixture; **(2) two version literals joined by an equality check** (`pyproject.toml` ↔ `_STATIC_VERSION`) — a single literal is impossible, not merely unwanted, since `pyproject.toml` is excluded from the packaged tree, and `hatch-vcs` is rejected because a dev version legitimately runs ahead of the tag and mounted modes carry no metadata; **(3)** non-final is the full PEP 440 set, not `.devN` alone; **(4)** no version source → explicit skip finding, never silence; **(5)** tag-without-heading warns (tags are immutable — a hard error could never go green and would be waived), and `run_plan` gains the version-bump step whose absence produced the `v0.3.0` defect; **(6)** comparisons normalize the `v` prefix, and the `_skew_notice` defect this exposed is spun off as **C61** rather than voted on. **F10, F11, F12 ✅ decided** — the placeholder row was answered by first locking **what enters the register** (two or more defensible answers; the answer changes what a task implements or what the lock claims; it cannot be deferred into a task without the lock asserting something unverified), because a bucket without a counting rule cannot be shown to be empty. Sieving the draft with it leaves exactly three, and reclassifies the rest as repairs ((d), (f)), in-task decisions (C60 relocation-vs-deletion, C57's git trigger), parked items (D1, A16, N1, the public `akmon status` option name) and measurement gates (F4/F5/F6 literals — a number to obtain, not an answer to choose). **F10** — C58 is **dispatch-only**; the completion half becomes probe **N4**, with the drawback stated rather than hidden: the ledger cannot answer "what did I send that never came back". **F11** — the acceptance yardstick admits **no exemption class**; C59, the shape that adds no check of its own, is seeded against **fidelity to its sources**, at the cost of a test seam. **F12** — stage 1 produces **two ADRs**: contracts/vocabulary (F1, F2, F7, F8) apart from survivability/crash posture (F3–F6), so N1's measurements amend only the second. **(b) is closed — the register is walked in full**
  - **(c) split D2-20 — ✅ complete** ([ledger](D2_LEDGER.md)) — D2-20 owns the consolidated F1–F12 architecture and stable F4–F6 protocol; D2-23 owns the measured vendor results, caps, entry mappings, budgets and literals and blocks only C52. The F8 table now exists and is part of D2-20 verification; the obsolete non-existence premise has been removed
  - **(d) missing seeded-violation contracts — ✅ complete** — C51, C53, C55 and C56 already named checker, fixture mutation and expected failure; **C57** ([§7](design/stage1-hardening-contracts.md)) and the **C46 tool-name half** ([§8](design/stage1-hardening-contracts.md)) now do too. Neither was the mechanical work this item expected. **C57 had nothing to check in the required form** — every cell of the current matrix is a checkmark — which forced `CAPABILITIES.md` as the axis-complete home with README reduced to a claim-free summary, an explicit scope boundary (vendor claims only; akmon's own checkers out, guardrail prose to C53), and a **join from the runtime declaration to the generator**, without which half of §7 stays unverified prose. **C46's version stamp had nothing to compare against** — nothing in the tree reads a harness version — which forced detection-when-present, a warn-and-stamp answer for an unknown version instead of an error that would brick consumers on the vendor's release schedule, and a seed against the **root cause**: a second owner naming vendor tools outside the map. Both are observed **red before green**, so neither `README.md:200` nor `routing.py:364`/`:470`/`:503` is repaired ahead of its task. Carried in the same pass: §2's seeded paragraph said "every executable wrapper" after (f5) had rescoped the rest of §2 to the nine spawned entry points. **C59 left this item via F11**: its case was not a missing contract but the yardstick's own hard case — a shape that adds no check — and the answer is that **no exemption class exists**; §10 carries a fidelity contract (mutate the source finding set, the aggregate must follow; no dropped or reordered finding; text and JSON identical in content and order; strict/exit semantics preserved). No shape ships without one — this stage's own acceptance criterion, not an extra
  - **(e) C58 is underdefined** — **closed by F10: dispatch-only**, with the completion half spawned as **N4**. Completion event, ledger line schema and correlation key were all unknown on both vendors, and a ledger locked with half of it guessed is the failure class stage 1 exists to remove; a shape narrowed to what is measurable can be extended by evidence, while one locked on a guess must first be unlocked. Accepted drawback, recorded in §9: the stage-1 ledger cannot answer "what did I send that never came back"
  - **(f) dependency contradictions — ✅ complete** — five were listed, a sixth surfaced while repairing them, and none turned out to be purely editorial; each is recorded here with what it changed, because the repairs move the order graph and (g)'s audit reads that graph. **(1) F8 in the C53 row** ([order table](design/stage1-hardening-contracts.md)) — added, and marked as a *decision* C53 must hold before code rather than a task to wait for; the column already mixed tasks with evidence (`D2-23 evidence gate`, `P0.3 inventory`). **(2) C55's `P0.4 declaration (§6)`** — the row named a section belonging to C56, so a definition read as a task; it now names the always-loaded *definition* explicitly, and §6's `Declaration` block is annotated as the shared unit. Consequence, not cosmetics: **C55's only task dependency is C51** — it was never waiting for C56. Why C55 needs it at all: `AGENTS.md` is hand-owned by the consumer while carrying a generated akmon block, the single artifact with split ownership, and the boundary check cannot classify it without the definition. **(3) C60 in two lists** — the order-table row is **removed**, since the table's own caption limits it to dependencies internal to the proposed package and C60 is explicitly outside the lock; the `C56 → C60` edge moves into *Not in this proposed lock* so deleting the row loses nothing. Rejected: keeping the row under an "out of lock" marker, which would turn the table from the lock's contents into a mixed graph — and its completeness is what (g) audits. **(4) A16 `unblocked`** — now **`blocked (after D2-20)`**; the row's own qualifying sentence already said the schema cannot settle a vocabulary inheriting F3's axes before D2-20 locks F3, and its first job is consuming C57's cells, which sit behind the same gate, so the honest status costs nothing. Rejected: narrowing A16 to "schema from N2 data, no vocabulary commitment" to keep it running — it re-derives the vocabulary later and keeps a status arguing with its own text. Also rejected: a third status level for *decided but not locked*, which is what F3 is — that is D1, parked, and inventing it inside one task settles D1 by side effect. **(5) C52's "every executable wrapper"** — scoped to the **nine spawned entry points**, measured from the generator: eight Claude entries (`bin/sync.py:270`–`:306`) and one Codex entry, `codex-hook.py` with the route as an argument (`:203`, `:222`–`:244`); all nine carry a shebang and `__main__`, while `claude_adapter.py`, `codex_adapter.py` and `hook_core.py` carry neither. Adapters get **no guard of their own** — an imported module has no top level, and an exception inside one propagates into the wrapper's `main()`, which is also why F5 already excluded the adapter modules from timeout scopes; §2 and F5 now agree. The repair adds a contract rather than only deleting words: **an adapter writes its whole output in one operation**, because a guard cannot un-write truncated stdout and C52's seeded crash tests forbid partial output. Rejected: per-function `try/except` in the adapters — swallowing, not guarding. **(6) Found while repairing (1): C53 → C56 edge was missing** — C53 replaces three `> **Enforced**` blocks with six markers and writes the new `## Role declaration` section, all inside the surface C56 caps: +1…+2 lines against 13 lines of slack (~197 against the 210-line cap). They are compatible today, which is the reason to record the edge rather than to skip it — nothing else would show when they stop being
  - **(g) then close** — clean-context coherence audit over the resulting set, then **two ADRs, fixed by F12**: one for contracts and vocabulary (F1, F2, F7, F8 — the decisions other work cites) and one for survivability and crash posture (F3–F6 — the stable protocol that D2-23's measurements may populate without disturbing vocabulary), so a timing number cannot formally disturb a vocabulary already built on; the boundary between them is the maintenance cost, accepted. (`meta/decisions/` today holds only the 0011 agent-name slice.) Then owner-verify D2-20. **C51** and **C57** become runnable at that point; C52 remains behind D2-23, and N1's F4 probe can run in parallel throughout
- A12 · stage 1 design — deterministic hardening contracts (plan P1.1–P1.7 + P0.2/P0.4 decisions + C46 tool-name half) · active · the draft is being walked through fork by fork; no remaining implementation shape is runnable until the choices are recorded, consolidated into ADR(s), audited, and owner-verified. **A17 carries that closure sequence** — the remaining repairs, the D2-20 owner gate, and the draft’s internal contradictions. The separately accepted agent-name slice remains locked by [ADR 0011](decisions/0011-agent-name-notation-k-underscore.md) · [rationale: theme 6](reviews/alternatives/walkthrough-a11-20260717.md)
- C40 · stage 1 implementation · **blocked (after A12/D2-20)** · engineer · umbrella carrier only; the awaiting-owner draft proposes a split into C51–C59 plus the remaining C46 tool-name half, with C60 as a later cap-reduction carrier. Final task boundaries and order lock only after the A12 walkthrough, coherence audit, ADR(s), and owner verification
- A13 · stage 2 design — pipeline & delegation contracts (plan P2.1–P2.8) · blocked (after C40) · one lock per contract, amendments per rationale; a lock ready early may spawn its own C-id · [rationale: themes 3–6](reviews/alternatives/walkthrough-a11-20260717.md)
- C41 · stage 2 implementation · blocked (after A13; may land per lock) · guardrail/pipeline text + defer-marker harvest + routing policy fields + manifest tooling
- A14 · stage 3 design — behavioral evaluation contract (plan P3.1+P3.3) · blocked (after C41) · layered eval with a control arm; baseline-must-fail; evidence classes in PROMOTE · extends [ROADMAP O5](ROADMAP.md) · [rationale: theme 1](reviews/alternatives/walkthrough-a11-20260717.md)
- C42 · stage 3 implementation — harness + first behavior gates (plan P3.2) · blocked (after A14) · `meta/benchmarks/` harness; first two gates on the cheapest claims; D3 arm + W18 outcome data
- A16 · capability matrix as data — schema/ownership/generation contract (plan P3.4) · **blocked (after D2-20)** · lock the matrix data schema, ownership, README generation, `sync.py` consumption. The Codex cells N2 measured (no `@`-import expansion, no SessionStart injection into a delegate, model pin observed on its exact route, `deny` observed per route not per intended effect) are the first real data the schema has to express. Inherit A12/F3's independent axes: documented/delivered state, exact vendor/version/event/matcher route, observed normal-path effect, and crash posture; bare `enforced` and checkmarks are invalid. **Status repaired at A17(f):** the row read `unblocked — N2 landed` while its own text said the schema cannot settle a vocabulary inheriting F3's axes before D2-20 locks F3, and its first job — consuming C57's prose cells — sits behind that same gate. A status that contradicts its own qualifying sentence is the defect this stage exists to remove, and the honest status costs nothing here. N2's landed data is what A16 will express, not a reason to start. **Rejected: a third status level** for *decided but not locked*, which is what F3 actually is — more precise, but that is D1 (mutable status levels), parked, and inventing it inside one task would settle D1 by side effect · [plan P3.4](reviews/alternatives/plan-akmon-from-alternatives-20260718.md)
- C44 · matrix-as-data implementation · blocked (after A16) · data file + README generator + round-trip smoke checks; live cell evidence comes from N1
- A15 · stage 4 designs — new surface, one ADR each (plan P4.1–P4.3) · blocked (gated on C42 + N2 + the N1 Claude/Codex rerun, plan R2; ADRs may land one at a time, each spawning its C-id) · SubagentStart injection only if N2 reproduced the drift; plugin carrier; MCP transport; P4.4 rejected — D1, [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md)
- C43 · stage 4 implementation · blocked (after A15; may split per ADR) · per the accepted ADRs, effects verified by the C42 harness; stage 5 polish stays opportunistic (plan §6 Phase 5), no task yet

**Stage 1 proposed shapes** — the candidate C40 split in the
[awaiting-owner A12 draft](design/stage1-hardening-contracts.md). Each
entry carries a candidate decision and the fork it would close, so the alternative is retrievable
without re-reading the proposed lock. Acceptance is identical everywhere: **a seeded violation makes the check fail**,
with the test named in the task.

- C51 · shared finding envelope (plan P1.6) · blocked (after A12/D2-20) · engineer · one stdlib-only `bin/findings.py` with `severity · code · message · target · fix`, adopted by `verify.py`, `meta/bin/validate.py`, `sync --check` and `self_ci` (the last imports it directly rather than parsing subprocess output). `severity` carries `ok/warn/error` and preserves the current exit-code contract (error → 1; warn → 1 only under `--strict`); `code` is a stable dotted slug (`boundary.hand-edited`, `caps.always-loaded`) never reused after a check is removed; `fix` is a required imperative sentence — a finding the reader cannot act on is a defect of the *check*. **Owner choice F1: B — rename `level`→`severity`, with no property, constructor, or serialization alias.** This accepts a breaking Python API change and requires one-step migration of every in-repository constructor, attribute read, fixture, and test, including the independent Finding in `meta/bin/validate.py`; rendered values and exit semantics stay unchanged. A contract test asserts that `severity` exists, `level` does not, and `level=` is rejected. **Owner choice F2: C — C51 owns and exactly tests one pure serializer (`Finding.to_dict()` or one shared function, not both); C59 adds the first public JSON output.** Every adopter calls that serializer and carries no local field mapping. C51 adds no `--json` mode to its adopters. The JSON-safe schema contains only `severity · code · message · target · fix`, preserves Unicode, accepts an empty valid target, does not mutate the Finding, and passes `json.dumps`; the greppable text form remains `SEVERITY code target: message → fix`. **Out of scope: hooks** — they speak the harness JSON contract on stdout and prose on stderr, and a third output shape on a channel whose visibility is unverified helps nobody. **Known future adopter, recorded at D2-16 verification:** `routing.resolve_briefs` raises on the *first* bad overlay key, so an owner with two broken keys fixes one, re-runs and meets the next. Collecting every violation into one envelope is precisely what C51 buys, and the overlay path is its first non-check consumer. Out of C51's own scope — recorded so the one-error-at-a-time behaviour reads as a pending adoption rather than an oversight. Seeded violations: a check built without `fix`; a duplicate `code` across adopters; serialization missing a canonical field, adding `level`, changing a value, or being reimplemented locally
- C52 · hook survivability contract (plan P1.1) · blocked · engineer · **Gate:** D2-20 plus D2-23; no partial implementation, then parallel-safe with C51. Bounded stdin at the measured F6 literal (cap+1 detection before decode), `utf-8-sig`, a top-level exception guard in every **spawned entry point** — nine, enumerated at A17(f): the eight generated Claude entries (`bin/sync.py:270`–`:306`) plus `codex-hook.py`, which carries its route as an argument (`:203`, `:222`–`:244`) — measured timeout literals in generated Claude wiring and F4-supported Codex command entries, and `hooks/README.md` contract. **F3/C:** every unexpected hook crash is fail-open with exactly one safe hook-process stderr diagnostic, including deny-class hooks; healthy explicit deny remains deny; matrix separates exact-route normal effect from crash posture. Fail-closed remains a separate measured owner/D2 gate with tested recovery. **F4/C:** disposable sentinel probes establish Codex timeout mechanics per entry; support requires observed termination, unsupported pins omission, evidence is version-scoped, and timeout kills inherit no F3 labels. **F5/C:** after F6 bounds valid inputs, N1 derives budgets from actual-process measurements by dominant-workload class using an owner-verified formula and exact entry→class→literal table; include the git class's existing 5-second inner timeout and aggregate event-group latency. The sentinel is not the production budget. CI pins exhaustive class mapping, exact generated literals/units, omission on unsupported/unprobed Codex scopes, and failure for a new unmapped entry — not wall-clock timing. Evidence harness proves largest-supported fixtures finish below budget and controlled hangs expire; a regression in measured runtime re-blocks rather than silently raises a literal. **Adapters and `hook_core` get no guard of their own** — imported, never spawned, so they have no top level; an exception inside them propagates into the wrapper's `main()` and is caught there, which is the same reason F5 excludes the adapter modules from timeout scopes. That coverage carries a precondition, so C52 owns it as a contract: **an adapter assembles its whole output and writes it once**, or a crash mid-render leaves truncated stdout that the guard exits 0 behind. **Rejected: per-function `try/except` inside the adapters** — that is not a guard but swallowing; the function returns something plausible and the wrapper never learns its decision ran on empty data. Seeded crash tests cover all nine entry points, emit no blocking/partial stdout, and never disclose payload values, commands, content, session ids, or secrets
  - **F6/C oversize acceptance:** each declared growing axis has an exact owner-verified cap/unit and generated-entry mapping. Raw stdin uses cap+1 before decode; files use stat plus bounded read; collections enumerate cap+1 and discard on overflow; structural JSON caps are checked immediately after bounded parse. Preflight all applicable sources before stdout or any marker/log/config/artifact write, rebind, prune or delete. At cap+1 call no handler, retain no prefix/side effect, emit exactly one unthrottled safe stderr line per invocation with hook/dimension/cap/remediation, and exit 0 with empty stdout and no ask/deny/context/system message. Other processes on the event keep independent envelopes. Tests pin cap−1/cap/cap+1 for every source, exact declarations, handler-not-called and zero-side-effects at overflow, safe output, healthy deny at cap/fail-open above, and failure for an unmapped synchronous source or command entry. Raising a cap requires a separate owner/D2 gate with bounded streaming/index/cache equivalence, atomic update/invalidation/corruption recovery, live boundary probes and rederived F5 budgets; a numeric increase alone is insufficient
- C53 · invariant canary (plan P1.2) · blocked (after C51 and A12/D2-20; **the F8 table is now locked**) · engineer · **Owner choice F8 — the table C53 checks, and the tree changes C53 performs** ([§3](design/stage1-hardening-contracts.md)): six policy IDs — `privilege.no-escalation`, `commits.owner-owned`, `analysis.before-mutation`, `verify.owner-verify-d2`, `delegation.tier-floor`, `role.declaration` — plus `session_start_result` as operational. Three of the six claim a **subset**, not a section, and the marker wording must say so: the commit guard does not check "tests pass before a commit is offered" or "branch for non-trivial work"; the analysis guard cannot decide whether a turn was analysis-only, only that a planning doc is being edited first in a session; the D2 reminder sees only the consumer's configured globs. C53's one mechanical pass: convert the three live `> **Enforced** (not just documented) by …` blocks (Privilege escalation, Commits & ownership, Analysis before mutation) into `Runtime check:` markers, add the two missing markers, **write the new `## Role declaration` section** in `guardrails/_common.md` (the P0.3 orphan's prose owner), and annotate the seven docstrings. Deliberately **not** done at design time: prose asserting a join before the checker exists is the stage-1 defect itself. **Owner choice F7/B:** stable dotted policy IDs form a one-to-one join between exact runtime-checkable rule prose and callable-local AST metadata. Each runtime-backed guardrail subset owns one unique `Runtime check: <policy-id>` marker; never bare `Enforced by` or a claim over the whole surrounding section. Every public `hook_core.*_result` docstring declares exactly one of `Policy ID: <id>` or `Runtime classification: operational` plus non-empty `Rationale:`. Every policy ID occurs exactly once on each side; operational results have no prose marker. `self_ci` parses source/docs without importing hooks, reports both locations, and rejects malformed/duplicate/orphan IDs, missing or dual classifications, empty operational rationale and legacy live enforcement markers. No `INVARIANTS.md`, central mapping, decorator registry or generated annotations; function names/paths are not identity. F8 owner-locks the initial seven-function table before code: six scoped policies, `session_start_result` operational. `role_on_code_result` gains concise Role-declaration prose rather than an exemption. C56 remeasures actual annotation cost. **Edge added at A17(f):** C53's tree changes land *inside* the always-loaded surface C56 caps — an estimated +1…+2 lines against a measured slack of 13 (~197 shipped against the 210-line cap), so the two are compatible today; the edge is recorded precisely because nothing else would show when they stop being. Seeded tests cover every rejected relation and delete on either side
- C54 · version ↔ changelog cross-check (plan P1.3) · blocked (after C51) · engineer · **Owner choice F9** ([§4](design/stage1-hardening-contracts.md)). The shape is not "one mismatched pair" — the tree carries **four independent version carriers** (`pyproject.toml` `version`, `src/akmon/__init__.py::_STATIC_VERSION`, the topmost CHANGELOG heading, the git tag) and nothing relates any two; the first two already disagree today. **The defect has already shipped:** the tree at tag `v0.3.0` carries `0.3.0.dev0`, so a wheel built from the reviewed state is misnamed, and `1a9ab0f` then wrote `0.4.0` — the literal `0.3.0` never existed. It reproduces because `run_plan` stages only `CHANGELOG.md` and never a version literal, while `__init__.py` claims in a comment that the pipeline "cuts the real version from the git tag", which is false under a static hatchling version. Consumer cost: package mode stamps `akmon_version` PEP 440-shaped (`bin/sync.py::_package_mode_akmon_toml`), mounted mode stamps `git describe` (`v`-shaped), and the bump procedure computes its window from CHANGELOG headings, which `0.3.0.dev0` matches none of. **The check:** non-final version (full PEP 440 set — `.devN`, `aN`/`bN`/`rcN`, `.postN`, `+local`, not `.devN` alone) requires `## Unreleased`; final (exactly `X.Y.Z`) requires the topmost released heading to equal it; final with a matching tag is a **warn** (re-release); `pyproject` version and `_STATIC_VERSION` must be **literally equal**; a tag with no `## vX.Y.Z` heading **warns**; no version source at all emits an explicit **skip finding**, never silence; comparisons normalize the leading `v` and read a `describe` suffix as "ahead of that tag". **Same task, outside the checker:** `run_plan` prints the version bump and stages `pyproject.toml` + `src/akmon/__init__.py` before `git add`, and the false `__init__.py` comment is corrected. **Current-state repair: `0.4.0.dev0` + `## Unreleased`, executed inside this task, not before it** — P1.3's acceptance is the live pair observed failing and then passing; repairing early leaves the check with a fixture where it could have had the tree. **Forks closed:** the git tag is **not** authoritative (a dev version legitimately runs ahead, so tag-equality would fail continuously and get waived); a single version literal is **impossible**, not merely unwanted (`pyproject.toml` is excluded from the packaged tree), and `hatch-vcs` fails the same dev-ahead-of-tag case plus mounted modes with no metadata; a missing version source must **not** error (it fails every non-Python consumer) and must not pass quietly either; a tag with no heading must **not** error (tags are immutable, so the gap can never go green and the error would be waived). The `v`-prefix defect this exposed is **C61**, not part of this task
- C55 · source/generated boundary check (plan P1.7) · blocked (after C51) · engineer · **repaired at A17(f):** the gate read `after C51, C56's declaration`, which made a *definition* look like a task to wait for — C55 needs the always-loaded set definition ([§6](design/stage1-hardening-contracts.md), already written) because `AGENTS.md` is hand-owned by the consumer while carrying a generated akmon block, the one artifact with split ownership. It never depended on C56's cap check, so C51 is its only task dependency. **the generator is the declaration**: `sync.py`'s owned-file set *is* the boundary, so there is no hand-maintained second list to drift. Two-way check — a sync-owned file with no generated banner is an **error** (hand-created, or the banner was stripped); a banner on a file sync no longer owns is an **error, stale artifact** (a generator leftover that reads as authoritative to both a human and `sync --check`). **Fork closed: not content-hash drift detection** — `sync --check` already compares content; this shape checks *ownership declaration*, which nothing checks today, and the stale-artifact direction is reachable only from ownership. **Explicitly deferred, not dropped:** the plan folds doc gardening (dead links, staleness) in "as the same sweep" — it is not the same sweep (own traversal, own noise budget) and leaves stage 1, carried alongside C60. Seeded violations: strip a banner; rename a generated file so sync stops owning it while the old one survives
- C56 · always-loaded caps (plan P0.4) · blocked (after C51) · engineer · always-loaded = everything reaching the model without it asking (the akmon block in `AGENTS.md`, the guardrail chain it imports, the per-language guardrail). Measured baseline (N2): **433 lines / 23 613 B** total, of which `AGENTS.md` is 232 lines and hand-owned; akmon's own share is `_common.md` 133/7 689 + `python.md` 52/2 637 + a ~12-line pointer ≈ **197 lines / ~10.8 KB**. Caps: **akmon-shipped ≤210 lines / 12 KB, error, in `self_ci`**; **consumer total chain ≤460 lines / 25 KB, warn in `verify.py`, error under `--strict`**. The severities differ because the biggest single item is not akmon's to shrink — failing a consumer's build over their hand-owned `AGENTS.md` would be akmon dictating content it does not own, while its own share is entirely ours. **Fork closed: the borrowed ≤150-line reference is not adopted now** — a cap that fails the day it ships either blocks the stage or gets waived, and a waived cap teaches that caps are advisory. **Stated drawback, because it is the real one: a ratchet blesses the status quo** — which is why the reduction is **C60** with its own id rather than a "tighten later" sentence. Seeded violations: a padded fixture guardrail fails `self_ci`; a padded consumer fixture warns under `verify` and fails under `--strict`
- C57 · OS contract + capability-matrix vocabulary (plan P0.2) · blocked (after A12/D2-20) · engineer · declare the runtime: **POSIX shell + `python3` on PATH**, plus **git on PATH for the Codex route** — the generated Codex wiring resolves the root via `$(git rev-parse --show-toplevel)` (`bin/sync.py:203`) where Claude's uses `$CLAUDE_PROJECT_DIR` (`:252`) — and **Windows unsupported**, stated as unsupported rather than "untested", because the hedge is what keeps a broken path ambiguous. A capability records independent axes: `documented`, `delivered`, exact vendor/version/event/matcher route, observed normal-path effect (`none/advisory/ask/deny`), and crash posture (`fail-open/fail-closed/unmeasured`). Bare `enforced` and checkmarks are invalid; C57 adds a static check rejecting them and any ask/deny-like entry without exact route and crash posture. Under owner choice F3, current deny-class entries state `normal-path effect: deny observed on route R when the hook completes; crash posture: fail-open`. **Fork closed for now: the git dependency is declared, not removed** — resolving the root through `python3` (reusing package-mode discovery) is the right fix, but it rewrites generated wiring C52 is already touching in the same stage and no consumer has hit it; trigger to revisit: a consumer report, or P4.2's plugin carrier changing the wiring anyway. **Seam declared: matrix-as-data stays A16** — C57 writes prose cells, A16 owns schema/ownership/generation and consumes them first; the duplication window is bounded by A16 and recorded here so A16 does not rediscover it as a defect. **Seeded-violation contract added at A17(d)**, and writing it forced three additions, because §7 had nothing to check in the required form — every cell of today's matrix (`README.md:82`–`:92`) is a checkmark. (i) The axis-complete matrix moves to a shipped top-level **`CAPABILITIES.md`** beside `MODEL.md`; README keeps a delivery summary carrying **no enforcement claim**, and A16 later generates one file instead of a section inside another. Rejected: a compact notation plus a legend — a legend is a compression, and a compression is the checkmark under a new name. (ii) **Scope is claims about a vendor harness**: five mandatory axes inside `CAPABILITIES.md`'s marked regions, and a closed vocabulary (`enforced`, `enforces`, `✅`/`⚠️` next to a vendor or harness event) rejected in shipped docs outside them. Two boundaries stated so the audit does not read them as gaps — akmon's **own** checkers are out (`pipelines/tasks.md:101` is an accurate in-process claim where route and crash posture mean nothing), and guardrail prose belongs to **C53** (F7/F8 already own `_common.md`'s three `> **Enforced**` blocks). (iii) The **runtime declaration is joined to the generator**: C57 extracts external binary names from the literal command strings `sync.py` emits and compares them with the declared set — undeclared binary → error, declared-but-unused → warn, F9's asymmetry (an under-declaration breaks a consumer's path, an over-declaration is stale prose). `claude` and `codex` join the declaration as **optional** binaries, required by C46's detection. **Conversion happens inside C57 and the tree is the red state** (F9's rule for C54): the live defect to catch is `README.md:200`, which claims delegation is **enforced** without qualification while the matrix's own row records `❌ subagent hook payload unverified` for Codex — a global claim over one measured route. Checker: `self_ci`, through the C51 envelope. Seeded violations: bare `✅`/`enforced` in a marked region (`matrix.bare-claim`); a `deny`/`ask` cell missing exact route or crash posture (`matrix.unqualified-effect`); an `unmeasured` cell replaced by an assertion citing no evidence; `jq` added to a generated command without declaration (`runtime.undeclared-binary`); `git` removed from the declaration while the Codex wiring still calls it; a declared binary no command uses → warn, not error
- C58 · execution ledger — dispatch half (plan P1.5) · blocked (after C57) · engineer · **Owner choice F10: dispatch-only.** The completion half leaves this task for probe **N4** — which event marks a dispatch finished, what a completion line holds, and what key correlates it back are unknown on both vendors, and a ledger locked with half of it guessed is the failure class stage 1 exists to remove. Narrowed to the measurable, it can be extended by evidence; locked on a guess, it would have to be unlocked first. **Accepted drawback, recorded in [§9](design/stage1-hardening-contracts.md) rather than hidden:** a dispatch-only ledger cannot answer "what did I send that never came back", which is much of what "after compaction, trust the ledger" was meant to buy — stage 1 ships the smaller promise and says so. **one ledger, machine-written**: promote `delegation-log.py`'s TSV out of `.claude/` to a neutral `_aitna/` path (the ledger is not Claude's), add the clause **"after compaction, trust the ledger and `git log`"** to the orchestration guardrail. Migration is read-both/write-new; the logs are local and gitignored, so no migration command. **Fork closed: not a model-written ledger** (the plan's line reads as prose appended per dispatch) — a ledger the model maintains degrades exactly when the thing it guards against happens, under compaction and context pressure; if it is worth having *after* compaction it cannot depend on the compacted party to write it. **Codex gap declared, not papered over:** the delegation-log hook does not fire on Codex and whether Codex dispatch is observable at all is unmeasured (N2: the SessionStart channel is parent-only) → a matrix cell `delivered: no`, not a cross-vendor claim. Seeded violation: a dispatch with no ledger line
- C59 · `akmon status` (plan P1.4) · blocked (after C51–C58) · engineer · read-only aggregation of checks that **already exist** — verify findings, `sync --check` drift, D2 pending count, caps headroom — rendered through C51's envelope. Per owner choice F2, C59 exposes the first public JSON output by composing C51's already-tested serializer; it does not define a second Finding schema. Public framing and option name remain a later status fork; text and JSON modes must contain the same ordered findings and keep identical exit/strict semantics. No new check, no mutation, no new data source. Sequenced last on purpose: it reports a finished vocabulary instead of inventing one. **Owner choice F11 — this shape does *not* get an acceptance exemption.** C59 adds no check of its own, so it looked unseedable; the contract instead is **fidelity to its sources**, which is seedable: mutate the source finding set and the aggregate must change with it; a finding dropped, reordered, or present in one rendering but not the other fails; the exit code follows the same strict semantics as the underlying checks. Cost, accepted: a "pure aggregator" gains a test seam into its source set. Reason it was not waived: an exemption granted on the first shape that asks turns the stage's yardstick from a rule into a preference. **Fork closed elsewhere: mutable status levels stay parked** — D1, [ADR 0010](decisions/0010-alternatives-adoption-a11-verdicts.md)
- C60 · reduce the always-loaded chain toward a justified cap · blocked (after C56) · engineer · the work C56's ratchet deliberately did not do. Two obligations, in order: (1) **justify the target** — ≤150 lines is wshobson's number for a differently-shaped artifact, so akmon must argue its own budget (what a model must hold *before* it asks for anything) rather than inherit a borrowed one; (2) then move content out of the always-loaded chain and lower the caps in the same commit, so the ratchet actually moves down instead of standing as permanent permission for 197 lines. **Fork to decide in-task, not now: shrink by relocation or by deletion** — moving rules to on-demand includes keeps them at the cost of them not being read (which for a guardrail may equal deletion with extra steps), while deleting rules is honest but forfeits them; the measurement C56 ships is what makes either arm checkable. Carries the doc-gardening sweep (dead links, staleness) that C55 deliberately declined to fold in
- C61 · version-spelling normalization in the CLI skew notice · open · engineer · surfaced by **F9/6** and spun off deliberately: a defect with one right answer is not an owner fork. `src/akmon/cli.py::_skew_notice` compares the recorded pin to `__version__` with raw string equality and then renders `v{pinned}`, but the two sides use **different spellings by construction** — mounted mode records `git describe --tags` (`v0.3.0`, possibly with a `-N-gSHA` suffix, per BOOTSTRAP), package mode records installed metadata (`0.4.0.dev0`, PEP 440, no `v`, `bin/sync.py::_package_mode_akmon_toml`). So on any mounted consumer the notice fires on **every** command and prints "v v0.3.0". The existing test writes the pin as `f"{__version__}"` (`meta/tests/test_cli.py`), i.e. it only ever covers the exact-match case, which is why nothing caught this. Fix per F9's locked rule: normalize before comparing (strip a leading `v`; treat a `describe` suffix as "ahead of that tag", which is a *different* notice from a version mismatch, not the same one), and render the pin as recorded rather than re-prefixing it. Tests must cover both spellings and the describe-suffix form. Not part of C54 — C54 owns the release-time invariant, this owns the runtime notice · `src/akmon/cli.py` + `meta/tests/test_cli.py` · provenance: F9 packet,
- C62 · `validate.py` picks its pytest runner from a fact that no longer holds · open · engineer · `meta/bin/validate.py::_pytest_command` justifies its runner choice with "akmon is stdlib-only and **ships no pyproject**, so a bare `uv run pytest` cannot provision pytest". Since C37 akmon ships `pyproject.toml` with `[dependency-groups] dev = ["pytest>=8", "ruff>=0.8"]`, which `uv run` installs by default — the stated reason is dead. The behaviour built on it is worse than the comment: the fallback fires only when **both** `uv` *and* `pytest` are on PATH, so it refuses `uv run pytest` in exactly the case `uv` now handles (no pytest present, resolvable manifest with a dev group) and accepts it only when pytest is already installed. Fix the comment to the real distinction — akmon's own root now resolves a manifest, a consumer dev layer may not — and re-derive the guard from *manifest resolvability* rather than from `which("pytest")`, keeping the probe-before-claiming shape so "pytest absent" stays a skip-warning and never a spurious failure. Surfaced beside F9 as a packaging-era leftover, deliberately kept out of C54: same era, different contract · `meta/bin/validate.py` · provenance: F9 packet side finding,
- C63 · D2 approval state · blocked · implementation complete and D2-24 approved; awaiting owner landing, then `verify D2-24 --commit <sha>` in a follow-up closure commit · [design](design/d2-ledger.md)

Ids use the typed scheme ([ADR 0002](decisions/0002-task-id-convention.md)): **A** architecture/
design · **C** code · **L** learning · **V** release · **N** analysis/review; the role is derived. Archived `T#` are
grandfathered (frozen), so the historical references below keep their `T#`.

Trim rationale (A1/A2/C1/A3/C2 "deferred") is recorded in ROADMAP O# / the release design; do not
re-expand without a reason. The T3 release design (ADR 0001) is implemented (T10–T15); C2 is the
deferred pin-bump follow-up, A3 the deferred O3 v2 design, A4 surfaced while building the release
tool.

## Done

See [`TASKS_ARCHIVE.md`](TASKS_ARCHIVE.md).
